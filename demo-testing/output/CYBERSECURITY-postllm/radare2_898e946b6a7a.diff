@@ -6,61 +6,14 @@
 #include <r_io.h>
 #include <r_reg.h>
 
-#if __wasi__ || EMSCRIPTEN
-#define FE_OVERFLOW 0
-#define feclearexcept(x)
-#endif
-#include <math.h>
-#include <float.h>
-#include <fenv.h>
-
 // TODO: replace esil->verbose with R_LOG_DEBUG
 #define IFDBG if (esil->verbose > 1)
+R_IPI bool isregornum(REsil *esil, const char *str, ut64 *num);
 
-/* Returns the number that has bits + 1 least significant bits set. */
-static inline ut64 genmask(int bits) {
-	ut64 m = UT64_MAX;
-	if (bits > 0 && bits < 64) {
-		m = (ut64)(((ut64)(2) << bits) - 1);
-		if (!m) {
-			m = UT64_MAX;
-		}
-	}
-	return m;
-}
-
-static bool isnum(REsil *esil, const char *str, ut64 *num) {
-	R_RETURN_VAL_IF_FAIL (esil && str, false);
-	if (isdigit (*str)) {
-		if (num) {
-			*num = r_num_get (NULL, str);
-		}
-		return true;
-	}
-	if (num) {
-		*num = 0;
-	}
-	return false;
-}
-
-static bool isregornum(REsil *esil, const char *str, ut64 *num) {
-	if (!r_esil_reg_read (esil, str, num, NULL)) {
-		if (!isnum (esil, str, num)) {
-			return false;
-		}
-	}
-	return true;
-}
-
-/* pop Register or Number */
-static bool popRN(REsil *esil, ut64 *n) {
-	char *str = r_esil_pop (esil);
-	if (str) {
-		bool ret = isregornum (esil, str, n);
-		free (str);
-		return ret;
-	}
-	return false;
+R_IPI bool alignCheck(REsil *esil, ut64 addr) {
+	// r_anal_archinfo (esil->anal, R_ARCH_INFO_DATA_ALIGN);
+	const ut32 da = esil->data_align;
+	return !(da > 0 && addr % da);
 }
 
 static void esil_ops_free(HtPPKv *kv) {
@@ -91,7 +44,7 @@ R_API REsil *r_esil_new(int stacksize, int iotrap, unsigned int addrsize) {
 	esil->iotrap = iotrap;
 	r_esil_handlers_init (esil);
 	r_esil_plugins_init (esil);
-	esil->addrmask = genmask (addrsize - 1);
+	esil->addrmask = r_num_genmask (addrsize - 1);
 	esil->trace = r_esil_trace_new (esil);
 	int stats = 1;
 	r_esil_stats (esil, NULL, stats);
@@ -135,7 +88,7 @@ R_API bool r_esil_init(REsil *esil, int stacksize, bool iotrap,
 	esil->stacksize = stacksize;
 	esil->parse_goto_count = R_ESIL_GOTO_LIMIT;
 	esil->iotrap = iotrap;
-	esil->addrmask = genmask (addrsize - 1);
+	esil->addrmask = r_num_genmask (addrsize - 1);
 	esil->reg_if = *reg_if;
 	esil->mem_if = *mem_if;
 	return true;
@@ -164,7 +117,7 @@ R_API REsil *r_esil_new_ex(int stacksize, bool iotrap, ut32 addrsize,
 	return esil;
 }
 
-static bool default_is_reg (void *reg, const char *name) {
+static bool default_is_reg(void *reg, const char *name) {
 	RRegItem *ri = r_reg_get ((RReg *)reg, name, -1);
 	if (!ri) {
 		return false;
@@ -173,7 +126,7 @@ static bool default_is_reg (void *reg, const char *name) {
 	return true;
 }
 
-static bool default_reg_read (void *reg, const char *name, ut64 *val) {
+static bool default_reg_read(void *reg, const char *name, ut64 *val) {
 	RRegItem *ri = r_reg_get ((RReg *)reg, name, -1);
 	if (!ri) {
 		return false;
@@ -183,7 +136,7 @@ static bool default_reg_read (void *reg, const char *name, ut64 *val) {
 	return true;
 }
 
-static ut32 default_reg_size (void *reg, const char *name) {
+static ut32 default_reg_size(void *reg, const char *name) {
 	RRegItem *ri = r_reg_get ((RReg *)reg, name, -1);
 	if (!ri) {
 		return 0;
@@ -284,21 +237,6 @@ R_API void r_esil_del_op(REsil *esil, const char *op) {
 	ht_pp_delete (esil->ops, op);
 }
 
-static bool r_esil_fire_trap(REsil *esil, int trap_type, int trap_code) {
-	R_RETURN_VAL_IF_FAIL (esil, false);
-	if (esil->cmd && R_STR_ISNOTEMPTY (esil->cmd_trap)) {
-		if (esil->cmd (esil, esil->cmd_trap, trap_type, trap_code)) {
-			return true;
-		}
-	}
-#if 0
-	REsilTrapCB icb;
-	icb = (REsilTrapCB)sdb_ptr_get (esil->traps, i, 0);
-	return icb (esil, trap_type, trap_code);
-#endif
-	return false;
-}
-
 R_API void r_esil_set_pc(REsil *esil, ut64 addr) {
 	R_RETURN_IF_FAIL (esil);
 	// r_reg_set_value_by_role (esil->anal->reg, R_REG_NAME_PC, addr);
@@ -360,137 +298,6 @@ R_API void r_esil_free(REsil *esil) {
 	free (esil);
 }
 
-static ut8 esil_internal_sizeof_reg(REsil *esil, const char *r) {
-	R_RETURN_VAL_