@@ -29,7 +29,6 @@
 # POSSIBILITY OF SUCH DAMAGE.
 
 from winappdbg.debug import Debug
-from winappdbg.breakpoint import Breakpoint
 from winappdbg.event import EventHandler
 from winappdbg.win32 import PVOID
 




@@ -41,7 +41,6 @@ def print_state(process_name):
     # Find every process that matches the requested name.
     system = System()
     for process, filename in system.find_processes_by_filename(process_name):
-
         # Skip processes that don't match our architecture or bits.
         if process.get_arch() != arch or process.get_bits() != bits:
             print("Skipping: %s" % filename)




@@ -35,4 +35,12 @@ system = System()
 
 # Now we can enumerate the running processes.
 for process in system:
-    print("%d:\t%s\t%d\t%s" % (process.get_pid(), process.get_arch(), process.get_bits(), process.get_filename()))
+    print(
+        "%d:\t%s\t%d\t%s"
+        % (
+            process.get_pid(),
+            process.get_arch(),
+            process.get_bits(),
+            process.get_filename(),
+        )
+    )




@@ -12,6 +12,7 @@ sys.path.insert(0, os.path.dirname(__file__))
 from winappdbg.system import System
 from winappdbg.process import Process
 
+
 def test_arch_detection():
     """Test the improved architecture detection on Python processes."""
 
@@ -32,10 +33,10 @@ def test_arch_detection():
         print(f"Current process architecture: {arch}")
         print(f"Current process bits: {bits}")
         print(f"Current process is WOW64: {process.is_wow64()}")
-        print("✅ Successfully detected current process architecture!")
+        print("Successfully detected current process architecture!")
 
     except Exception as e:
-        print(f"❌ Error detecting current process architecture: {e}")
+        print(f"Error detecting current process architecture: {e}")
 
     print()
 
@@ -49,21 +50,25 @@ def test_arch_detection():
     for process in system.iter_processes():
         try:
             filename = process.get_filename()
-            if filename and ('python.exe' in filename.lower() or 'python' in filename.lower()):
+            if filename and (
+                "python.exe" in filename.lower() or "python" in filename.lower()
+            ):
                 python_processes.append(process)
         except Exception:
             # Skip processes we can't access
             continue
 
     if not python_processes:
-        print("❌ No Python processes found!")
+        print("No Python processes found!")
         return
 
     print(f"Found {len(python_processes)} Python process(es)")
     print()
     print("Testing architecture detection on all Python processes:")
     print("-" * 80)
-    print(f"{'PID':<8} {'Architecture':<12} {'Bits':<6} {'WOW64':<8} {'Executable Path'}")
+    print(
+        f"{'PID':<8} {'Architecture':<12} {'Bits':<6} {'WOW64':<8} {'Executable Path'}"
+    )
     print("-" * 80)
 
     success_count = 0
@@ -78,12 +83,14 @@ def test_arch_detection():
 
             # Clean up filename for display
             if filename:
-                filename = filename.replace('\\', '/')
+                filename = filename.replace("\\", "/")
                 # Show just the last part of the path for clarity
                 if len(filename) > 50:
                     filename = "..." + filename[-47:]
 
-            print(f"{process.get_pid():<8} {arch:<12} {bits:<6} {is_wow64:<8} {filename}")
+            print(
+                f"{process.get_pid():<8} {arch:<12} {bits:<6} {is_wow64:<8} {filename}"
+            )
 
             success_count += 1
             architectures_found.add(arch)
@@ -92,23 +99,28 @@ def test_arch_detection():
             print(f"{process.get_pid():<8} ERROR: {e}")
 
     print("-" * 80)
-    print(f"Successfully detected architecture for {success_count}/{len(python_processes)} Python processes")
+    print(
+        f"Successfully detected architecture for {success_count}/{len(python_processes)} Python processes"
+    )
     print(f"Architectu