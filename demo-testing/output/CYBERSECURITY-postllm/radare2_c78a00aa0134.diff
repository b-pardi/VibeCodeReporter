@@ -48500,7 +48500,8 @@ static JSValue js_date_constructor(JSContext *ctx, JSValueConst new_target,
     // Date(y, mon, d, h, m, s, ms)
     JSValue rv;
     int i, n;
-    double a, val;
+    double a = 0;
+    double val = 0;
 
     if (JS_IsUndefined(new_target)) {
         /* invoked as function */




@@ -671,7 +671,13 @@ static js_force_inline JSValue JS_NewFloat64(JSContext *ctx, double d)
         uint64_t u;
     } u, t;
     u.d = d;
-    val = (int32_t)d;
+    if (d < INT32_MIN) {
+	    d = INT32_MIN;
+    } else if (d > INT32_MAX) {
+	    d = INT32_MAX;
+    } else {
+	    val = (int32_t)d;
+    }
     t.d = val;
     /* -0 cannot be represented as integer, so we compare the bit
         representation */