@@ -1,9 +1,5 @@
-from routersploit.core.exploit.exploit import (
-    BaseExploit,
-)
-from routersploit.core.exploit.printer import (
-    print_error,
-)
+from routersploit.core.exploit.exploit import BaseExploit
+from routersploit.core.exploit.printer import print_error
 
 
 class BaseEncoder(BaseExploit):




@@ -101,7 +101,7 @@ class Exploit(BaseExploit):
                 target=target_function,
                 args=chain((threads_running,), args),
                 kwargs=kwargs,
-                name="thread-{}".format(thread_id),
+                name=f"thread-{thread_id}",
             )
             threads.append(thread)
 
@@ -189,7 +189,7 @@ def mute(fn):
     return wrapper
 
 
-class LockedIterator(object):
+class LockedIterator:
     def __init__(self, it):
         self.lock = threading.Lock()
         self.it = it.__iter__()
@@ -202,9 +202,9 @@ class LockedIterator(object):
         try:
             item = next(self.it)
 
-            if type(item) is tuple:
+            if isinstance(item, tuple):
                 return (item[0].strip(), item[1].strip())
-            elif type(item) is str:
+            elif isinstance(item, str):
                 return item.strip()
 
             return item




@@ -8,7 +8,8 @@ from routersploit.core.exploit.utils import (
 )
 
 
-class Option(object):
+# pylint: disable=no-member
+class Option:
     """ Exploit attribute that is set by the end user """
 
     def __init__(self, default, description="", advanced=False):




@@ -67,8 +67,8 @@ def shell(exploit, architecture="", method="", payloads=None, **params):
             print_status("Available payloads:")
             headers = ("Payload", "Name", "Description")
             data = []
-            for p in available_payloads.keys():
-                data.append((p, available_payloads[p]._Payload__info__["name"], available_payloads[p]._Payload__info__["description"]))
+            for p, v in available_payloads.items():
+                data.append((p, v._Payload__info__["name"], v._Payload__info__["description"]))
 
             print_table(headers, *data)
 
@@ -83,9 +83,9 @@ def shell(exploit, architecture="", method="", payloads=None, **params):
                 payload = available_payloads[c[2]]()
 
                 options = []
-                for option in payload.exploit_attributes.keys():
+                for option, attr in payload.exploit_attributes.items():
                     if option not in ["output", "filepath"]:
-                        options.append([option, getattr(payload, option), payload.exploit_attributes[option][1]])
+                        options.append([option, getattr(payload, option), attr[1]])
 
                 if payload.handler == "bind_tcp":
                     options.append(["rhost", exploit.target, "Target IP address"])
@@ -173,7 +173,7 @@ class HttpServer(HTTPServer):
             self.handle_request()
 
 
-class Communication(object):
+class Communication:
     def __init__(self, exploit, payload, options, location="", wget_options={}, echo_options={}, exec_binary=None):
         self.exploit = exploit
         self.payload = payload




@@ -237,7 +237,7 @@ def lookup_vendor(addr: str, resources_directory: str = RESOURCES_DIR) -> str:
     return None
 
 
-class Version(object):
+class Version:
     def __init__(self, value):
         self.value = str(value)
 
@@ -293,7 +293,7 @@ class Version(object):
 
         i = 0
 
-        while(i < len(arr1)):
+        while (i < len(arr1)):
             if int(arr2[i]) > int(arr1[i]):
                 return -1
 




@@ -11,7 +11,7 @@ from routersploit.core.exploit.printer import print_success
 FTP_TIMEOUT = 8.0
 
 
-class FTPCli(object):
+class FTPCli:
     """ FTP Client provides methods to handle communication with FTP server """
 
     def __init__(self, ftp_target: str, ftp_port: int, ssl: bool = False, verbosity: bool = False) -> None:
@@ -114,6 +114,7 @@ class FTPCli(object):
         return False
 
 
+# pylint: disable=no-me