@@ -277,6 +277,17 @@ class ExploitWorker:
                 self.log(f"Failed to send version: {e}")
             raise
 
+    def receive_ssh_version(self) -> bool:
+        """Receive SSH version from server"""
+        try:
+            version = self.recv_nonblocking(256, timeout=5)
+            if version:
+                self.log(f"Received version: {version.decode('utf-8', errors='ignore').strip()}")
+                return True
+        except Exception as e:
+            self.log(f"Version receive error: {e}")
+        return False
+
     def send_packet(self, packet_type: int, data: bytes):
         """Send SSH protocol packet - NO DELAYS like C code"""
         packet_len = len(data) + 1