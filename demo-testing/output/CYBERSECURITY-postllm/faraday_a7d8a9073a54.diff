@@ -1236,13 +1236,18 @@ class VulnerabilityView(PaginatedMixin,
         ) if not exclude_list else exclude_list}
         if 'group_by' not in filters:
             offset = None
-            limit = faraday_server.vulnerabilities_max_get_limit
             if 'offset' in filters:
                 offset = filters.pop('offset')
+
+            limit = faraday_server.vulnerabilities_max_get_limit
             if 'limit' in filters:
-                filter_limit = filters.pop('limit')
-                if limit > filter_limit > 0:
-                    limit = filter_limit
+                if limit:
+                    filter_limit = filters.pop('limit')
+                    if limit > filter_limit > 0:
+                        limit = filter_limit
+                else:
+                    limit = filters.pop('limit')
+
             try:
                 vulns = self._generate_filter_query(
                     VulnerabilityGeneric,




@@ -495,13 +495,17 @@ class VulnerabilityContextView(ContextMixin,
         )if not exclude_list else exclude_list}
         if 'group_by' not in filters:
             offset = None
-            limit = faraday_server.vulnerabilities_max_get_limit
             if 'offset' in filters:
                 offset = filters.pop('offset')
+
+            limit = faraday_server.vulnerabilities_max_get_limit
             if 'limit' in filters:
-                filter_limit = filters.pop('limit')
-                if limit > filter_limit > 0:
-                    limit = filter_limit
+                if limit:
+                    filter_limit = filters.pop('limit')
+                    if limit > filter_limit > 0:
+                        limit = filter_limit
+                else:
+                    limit = filters.pop('limit')
 
             try:
                 vulns = self._generate_filter_query(




@@ -154,7 +154,7 @@ class FaradayServerConfigObject(ConfigSection):
         self.socketio_ping_interval = 60
         self.socketio_ping_timeout = 220
         self.socketio_logger = False
-        self.vulnerabilities_max_get_limit = 4000
+        self.vulnerabilities_max_get_limit = 0  # No limit
 
 
 class StorageConfigObject(ConfigSection):