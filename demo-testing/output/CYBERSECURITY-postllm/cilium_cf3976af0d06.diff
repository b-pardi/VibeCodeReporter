@@ -2,13 +2,9 @@
 /* Copyright Authors of Cilium */
 
 static __always_inline void
-endpoint_v4_add_entry(__be32 addr, __u32 ifindex, __u16 lxc_id, __u32 flags, __u32 sec_id,
-		      __u32 parent_ifindex, const __u8 *ep_mac_addr, const __u8 *node_mac_addr)
+endpoint_add_entry(struct endpoint_key *key, __u32 ifindex, __u16 lxc_id, __u32 flags, __u32 sec_id,
+		   __u32 parent_ifindex, const __u8 *ep_mac_addr, const __u8 *node_mac_addr)
 {
-	struct endpoint_key key = {
-		.ip4 = addr,
-		.family = ENDPOINT_KEY_IPV4,
-	};
 	struct endpoint_info value = {
 		.ifindex = ifindex,
 		.lxc_id = lxc_id,
@@ -18,11 +14,24 @@ endpoint_v4_add_entry(__be32 addr, __u32 ifindex, __u16 lxc_id, __u32 flags, __u
 	};
 
 	if (ep_mac_addr)
-		memcpy(&value.mac, ep_mac_addr, ETH_ALEN);
+		__bpf_memcpy_builtin(&value.mac, ep_mac_addr, ETH_ALEN);
 	if (node_mac_addr)
-		memcpy(&value.node_mac, node_mac_addr, ETH_ALEN);
+		__bpf_memcpy_builtin(&value.node_mac, node_mac_addr, ETH_ALEN);
 
-	map_update_elem(&cilium_lxc, &key, &value, BPF_ANY);
+	map_update_elem(&cilium_lxc, key, &value, BPF_ANY);
+}
+
+static __always_inline void
+endpoint_v4_add_entry(__be32 addr, __u32 ifindex, __u16 lxc_id, __u32 flags, __u32 sec_id,
+		      __u32 parent_ifindex, const __u8 *ep_mac_addr, const __u8 *node_mac_addr)
+{
+	struct endpoint_key key = {
+		.ip4 = addr,
+		.family = ENDPOINT_KEY_IPV4,
+	};
+
+	endpoint_add_entry(&key, ifindex, lxc_id, flags, sec_id, parent_ifindex,
+			   ep_mac_addr, node_mac_addr);
 }
 
 static __always_inline void
@@ -45,19 +54,8 @@ endpoint_v6_add_entry(const union v6addr *addr, __u32 ifindex, __u16 lxc_id,
 		.family = ENDPOINT_KEY_IPV6,
 	};
 
-	struct endpoint_info value = {
-		.ifindex = ifindex,
-		.lxc_id = lxc_id,
-		.flags = flags,
-		.sec_id = sec_id,
-	};
-
 	memcpy(&key.ip6, addr, sizeof(*addr));
 
-	if (ep_mac_addr)
-		memcpy(&value.mac, ep_mac_addr, ETH_ALEN);
-	if (node_mac_addr)
-		memcpy(&value.node_mac, node_mac_addr, ETH_ALEN);
-
-	map_update_elem(&cilium_lxc, &key, &value, BPF_ANY);
+	endpoint_add_entry(&key, ifindex, lxc_id, flags, sec_id, 0,
+			   ep_mac_addr, node_mac_addr);
 }




@@ -116,7 +116,7 @@ int srv6_decap_to_pod_ipv4_pktgen(struct __ctx_buff *ctx)
 SETUP("tc", "tc_srv6_decap_to_pod_ipv4")
 int srv6_decap_to_pod_ipv4_setup(struct __ctx_buff *ctx __maybe_unused)
 {
-	union v6addr sid;
+	union v6addr sid __align_stack_8;
 	__u32 vrf_id = 1;
 
 	memcpy(sid.addr, (const void *)SID, sizeof(sid.addr));
@@ -393,7 +393,7 @@ int srv6_decap_to_service_ipv4_pktgen(struct __ctx_buff *ctx)
 SETUP("tc", "tc_srv6_decap_to_service_ipv4")
 int srv6_decap_to_service_ipv4_setup(struct __ctx_buff *ctx __maybe_unused)
 {
-	union v6addr sid;
+	union v6addr sid __align_stack_8;
 	__u32 vrf_id = 1;
 
 	memcpy(sid.addr, (const void *)SID, sizeof(sid.addr));