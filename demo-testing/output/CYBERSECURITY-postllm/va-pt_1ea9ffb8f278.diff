@@ -93,12 +93,26 @@ def ensure_ufw_port_allowed(port: int) -> bool:
         return False
 
 def cleanup_ufw_ports(ports: List[int]):
+    """Remove UFW rules for multiple ports - silent cleanup"""
+    removed = 0
+    failed = 0
     for port in ports:
         try:
-            subprocess.run(["sudo", "ufw", "delete", "allow", f"{port}/tcp"], check=True)
-            print(f"[+] Removed ufw rule for port {port}")
+            result = subprocess.run(
+                ["sudo", "ufw", "delete", "allow", f"{port}/tcp"],
+                capture_output=True,
+                text=True,
+                check=False
+            )
+            if result.returncode == 0:
+                removed += 1
+            else:
+                failed += 1
         except Exception:
-            pass
+            failed += 1
+
+    if removed > 0 or failed > 0:
+        print(f"[*] Cleaned up UFW rules: {removed} removed, {failed} already removed")
 
 def check_ufw_status() -> str:
     """Try to run 'sudo ufw status' to show firewall settings (requires sudo)."""
@@ -384,17 +398,16 @@ def ensure_ufw_dns_allowed() -> bool:
 
 
 def cleanup_ufw_dns() -> bool:
-    """Remove UFW rule for UDP/53"""
+    """Remove UFW rule for UDP/53 - silent"""
     try:
-        print("[*] Removing UFW rule for UDP/53...")
-        subprocess.run(["sudo", "ufw", "delete", "allow", "53/udp"],
-                      check=True,
-                      capture_output=True,
-                      text=True)
-        print("[+] UFW rule removed for UDP/53")
+        subprocess.run(
+            ["sudo", "ufw", "delete", "allow", "53/udp"],
+            capture_output=True,
+            text=True,
+            check=True
+        )
         return True
-    except Exception as e:
-        print(f"[!] Could not remove ufw rule for UDP/53: {e}")
+    except Exception:
         return False
 
 # ---- Active network functions ----
@@ -748,19 +761,18 @@ def main():
             if callbacks_received > 0:
                 print(f"\n[!!!] SUCCESS - {callbacks_received} TCP callback(s) received!")
 
-        print("\n=== Summary Report ===")
-        for item in final_report:
-            print(item)
-
         print("\nDone.")
 
     finally:
         # Cleanup
+        print("\n[*] Cleaning up firewall rules...")
         if dns_ufw_added:
             cleanup_ufw_dns()
 
         if open_tcp_ports:
             cleanup_ufw_ports(open_tcp_ports)
 
+        print("[*] Cleanup complete\n")
+
 if __name__ == "__main__":
     main()