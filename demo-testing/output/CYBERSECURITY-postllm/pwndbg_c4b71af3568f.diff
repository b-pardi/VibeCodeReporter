@@ -46,7 +46,7 @@ else:
 
     # Set virtualenv's bin path (needed for utility tools like ropper, pwntools etc)
     bin_path = os.path.join(venv_path, "bin")
-    os.environ["PATH"] = bin_path + os.pathsep + os.environ.get("PATH")
+    os.environ["PATH"] = bin_path + os.pathsep + os.environ.get("PATH", "")
 
     # Add pwndbg directory to sys.path so it can be imported
     sys.path.insert(0, directory)




@@ -14,7 +14,10 @@ import pwndbg.gdblib.memory
 import pwndbg.gdblib.qemu
 import pwndbg.gdblib.regs
 import pwndbg.gdblib.stack
+import pwndbg.gdblib.strings
 import pwndbg.gdblib.typeinfo
+import pwndbg.lib.cache
+import pwndbg.lib.memory
 
 example_info_auxv_linux = """
 33   AT_SYSINFO_EHDR      System-supplied DSO's ELF header 0x7ffff7ffa000
@@ -79,7 +82,7 @@ sys.modules[__name__].__dict__.update({v: k for k, v in AT_CONSTANTS.items()})
 
 
 class AUXV(dict):
-    def set(self, const, value) -> None:
+    def set(self, const: int, value) -> None:
         name = AT_CONSTANTS.get(const, "AT_UNKNOWN%i" % const)
 
         if name in ["AT_EXECFN", "AT_PLATFORM"]:
@@ -100,7 +103,7 @@ class AUXV(dict):
 
 
 @pwndbg.lib.cache.cache_until("objfile", "start")
-def get():
+def get() -> AUXV:
     return use_info_auxv() or walk_stack() or AUXV()
 
 
@@ -123,7 +126,7 @@ def use_info_auxv():
     return auxv
 
 
-def find_stack_boundary(addr):
+def find_stack_boundary(addr: gdb.Value | int) -> int:
     # For real binaries, we can just use pwndbg.gdblib.memory.find_upper_boundary
     # to search forward until we walk off the end of the stack.
     #
@@ -146,7 +149,7 @@ def find_stack_boundary(addr):
     return addr
 
 
-def walk_stack():
+def walk_stack() -> AUXV | None:
     if not pwndbg.gdblib.abi.linux:
         return None
     if pwndbg.gdblib.qemu.is_qemu_kernel():
@@ -168,7 +171,7 @@ def walk_stack():
     return auxv
 
 
-def walk_stack2(offset=0):
+def walk_stack2(offset: int = 0) -> AUXV:
     sp = pwndbg.gdblib.regs.sp
 
     if not sp:
@@ -248,10 +251,10 @@ def walk_stack2(offset=0):
         return AUXV()
 
 
-def _get_execfn():
+def _get_execfn() -> str | None:
     # If the stack is not sane, this won't work
     if not pwndbg.gdblib.memory.peek(pwndbg.gdblib.regs.sp):
-        return
+        return None
 
     # QEMU does not put AT_EXECFN in the Auxiliary Vector
     # on the stack.
@@ -274,3 +277,4 @@ def _get_execfn():
     v = pwndbg.gdblib.strings.get(addr, 1024)
     if v:
         return os.path.abspath(v)
+    return None




@@ -2,9 +2,10 @@ from __future__ import annotations
 
 import os
 import re
-from collections import namedtuple
-from typing import Any
-from typing import List
+from typing import Callable
+from typing import NamedTuple
+
+from pwndbg.lib.config import Parameter
 
 from . import theme
 
@@ -42,87 +43,87 @@ UNDERLINE = "\x1b[4m"
 none = str
 
 
-def normal(x):
+def normal(x: str) -> str:
     return colorize(x, NORMAL)
 
 
-def black(x):
+def black(x: str) -> str:
     return colorize(x, BLACK)
 
 
-def red(x):
+def red(x: str) -> str:
     return colorize(x, RED)
 
 
-def green(x):
+def green(x: str) -> str:
     return colorize(x, GREEN)
 
 
-def yellow(x):
+def yellow(x: str) -> str:
     return colorize(x, YELLOW)
 
 
-def blue(x):
+def blue(x: str) -> str:
     return colorize(x, BLUE)
 
 
-def purple(x):
+def purple(x: str) -> str:
     return colorize(x, PURPLE)
 
 
-def cyan(x):
+def cyan(x: str) -> str:
     return colorize(x, CYAN)
 
 
-def light_gray(x):
+def light_gray(x: str) -> str:
     return colorize(x, LIGHT_GRAY)
 
 
-def foreground(x):
+def foreground(x: str) -> str:
     return colorize(x, FOREGROUND)
 
 
-def gray(x):
+def gray(x: str) -> str:
     return colorize(x, GRAY)
 
 
-def light_red(x):
+def light_red(x: str) -> str:
     return colorize(x, LIGHT_RED)
 
 
-def light_green(x):
+def light_green(x: str) -> str:
     return colorize(x, LIGHT_GREEN)
 
 
-def light_yellow(x):
+def light_yellow(x: str) -> str:
     return colorize(x, LIGHT_YELLOW)
 
 
-def 