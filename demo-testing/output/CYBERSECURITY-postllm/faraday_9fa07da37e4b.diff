@@ -2102,7 +2102,9 @@ class ContextMixin(ReadOnlyView):
 
     count_extra_filters = []
 
-    def _get_base_query(self, operation="read", *args, **kwargs):
+    def _get_base_query(self, operation="", *args, **kwargs):
+        if not operation:
+            operation = "read" if flask.request.method in ['GET', 'HEAD', 'OPTIONS'] else "write"
         query = super()._get_base_query(*args, **kwargs)
         return self._apply_filter_context(query, operation)
 




@@ -327,7 +327,8 @@ class VulnerabilityContextView(ContextMixin,
             description: Ok
         """
         vuln_permission_check = self._apply_filter_context(
-            db.session.query(VulnerabilityGeneric).filter(VulnerabilityGeneric.id == vuln_id)
+            db.session.query(VulnerabilityGeneric).filter(VulnerabilityGeneric.id == vuln_id),
+            operation="write"
         ).first()
 
         if not vuln_permission_check:




@@ -805,7 +805,6 @@ class TestListVulnerabilityContextView(ReadOnlyAPITests, BulkUpdateTestsMixin, B
         res = test_client.get(urljoin(
             self.url(), f'?{param_name}=bbb'))
         assert res.status_code == 200
-        print(res.json)
 
         for vuln in res.json['data']:
             assert vuln['query'] == 'bbb'
@@ -1216,7 +1215,11 @@ class TestListVulnerabilityContextView(ReadOnlyAPITests, BulkUpdateTestsMixin, B
         data = {
             'q': '{"filters":[{"name":"cve_instances","op":"not_any","val":{"name":"name","op":"eq","val":"CVE-2014-0160"}}]}'
         }
-        res = test_client.get(f'/v3/ws/{self.workspace.name}/vulns/filter', query_string=data)
+        url = join(
+            self.url(),
+            'filter'
+        )
+        res = test_client.get(url, query_string=data)
 
         assert res.status_code == 200
         assert len(res.json['vulnerabilities']) == 2
@@ -1698,7 +1701,7 @@ class TestListVulnerabilityContextView(ReadOnlyAPITests, BulkUpdateTestsMixin, B
         res = test_client.post(
             join(self.url(vuln), 'attachment'),
             data=data, headers=headers, use_json_data=False)
-        assert res.status_code == 403
+        assert res.status_code == 404
         query_test = session.query(Vulnerability).filter_by(id=vuln.id).first().evidence
         assert query_test == []
 
@@ -1716,7 +1719,7 @@ class TestListVulnerabilityContextView(ReadOnlyAPITests, BulkUpdateTestsMixin, B
         data = {
             'q': '{"filters":[{"name":"severity","op":"eq","val":"medium"}]}'
         }
-        res = test_client.get(urljoin(self.url(), 'filter'), query_string=data)
+        res = test_client.get(join(self.url(), 'filter'), query_string=data)
         assert res.status_code == 200
         assert res.json['count'] == 1
 
@@ -1739,15 +1742,15 @@ class TestListVulnerabilityContextView(ReadOnlyAPITests, BulkUpdateTestsMixin, B
         data = {
             'q': '{"group_by":[{"field":"creator_id"}]}'
         }
-        res = test_client.get(urljoin(self.url(), 'filter'), query_string=data)
+        res = test_client.get(join(self.url(), 'filter'), query_string=data)
         assert res.status_code == 200
-        assert res.json['count'] == 1  # all vulns created by the same creator
         expected = [{'count': 2, 'creator_id': creator.id}]
-        assert [vuln['value'] for vuln in res.json['vulnerabilities']] == expected
+        assert [vuln['value'] for vuln in res.json['vulnerabilities'] if vuln['value']['creator_id'] == creator.id] == expected
 
     def test_vuln_group_by_severity_does_not_duplicate_groups(self, test_client, session):
         workspace = WorkspaceFactory.create()
         creator = UserFactory.create()
+        Vulnerability.query.delete()
         vuln = VulnerabilityFactory.create_batch(size=10,
                                                  workspace=workspace,
                                                  severity="critical",
@@ -1764,18 +1767,18 @@ class TestListVulnerabilityContextView(ReadOnlyAPITests, BulkUpd