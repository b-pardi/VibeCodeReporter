@@ -3624,6 +3624,8 @@ R_API int r_core_config_init(RCore *core) {
 	// maybe rename to asm.cmt.calls
 	SETBPREF ("asm.calls", "true", "show callee function related info as comments in disasm");
 	SETBPREF ("asm.comments", "true", "show comments in disassembly view");
+	SETBPREF ("asm.cmt.token", ";", "token to use before printing a comment");
+	// R2-590 - rename to asm.cmt.user
 	SETBPREF ("asm.usercomments", "false", "show user comments even if asm.comments is false");
 	SETBPREF ("asm.sub.jmp", "true", "always substitute jump, call and branch targets in disassembly");
 	SETBPREF ("asm.hints", "true", "disable all asm.hint* if false");




@@ -256,6 +256,7 @@ typedef struct r_disasm_state_t {
 	const char *color_func_var;
 	const char *color_func_var_type;
 	const char *color_func_var_addr;
+	const char *cmtoken; // ";"
 
 	RFlagItem *lastflag;
 	RAnalHint *hint;
@@ -542,7 +543,7 @@ static void ds_comment_(RDisasmState *ds, bool align, bool nl, const char *forma
 				ds_setup_print_pre (ds, false, false);
 				ds_print_lines_left (ds);
 				//core->print->resetbg = (ds->asm_highlight == UT64_MAX);
-				r_cons_print ("; ");
+				r_cons_printf ("%s ", ds->cmtoken);
 			}
 		}
 		r_cons_print (p);
@@ -623,6 +624,7 @@ static RDisasmState *ds_init(RCore *core) {
 	ds->core = core;
 	ds->addrbytes = core->io->addrbytes;
 	ds->strip = r_config_get (core->config, "asm.strip");
+	ds->cmtoken = r_config_get (core->config, "asm.cmt.token");
 	ds->pal_hint = core->cons->context->pal.jmp; // R2_590 pal.hint
 	ds->pal_comment = core->cons->context->pal.comment;
 	#define P(x) (core->cons && core->cons->context->pal.x)? core->cons->context->pal.x
@@ -1340,7 +1342,7 @@ static void ds_print_pins(RDisasmState *ds) {
 	const char *lepin = r_anal_pin_at (ds->core->anal, ds->at);
 	if (R_STR_ISNOTEMPTY (lepin)) {
 	 	ds_begin_comment (ds);
-		ds_comment (ds, true, "; [aep: %s]", lepin);
+		ds_comment (ds, true, "%s [aep: %s]", ds->cmtoken, lepin);
 	}
 }
 
@@ -1360,7 +1362,7 @@ static void ds_show_refs(RDisasmState *ds) {
 		RFlagItem *fis;
 		r_list_foreach (fls, iter2, fis) {
 			ds_begin_comment (ds);
-			ds_comment (ds, true, "; (%s)", fis->name);
+			ds_comment (ds, true, "%s (%s)", ds->cmtoken, fis->name);
 		}
 
 		// ds_align_comment (ds);
@@ -1369,7 +1371,7 @@ static void ds_show_refs(RDisasmState *ds) {
 		}
 		if (cmt) {
 			ds_begin_comment (ds);
-			ds_comment (ds, true, "; (%s)", cmt);
+			ds_comment (ds, true, "%s (%s)", ds->cmtoken, cmt);
 		}
 		if (ref->type & R_ANAL_REF_TYPE_CALL) {
 			RAnalOp aop;
@@ -1380,9 +1382,9 @@ static void ds_show_refs(RDisasmState *ds) {
 				RAnalFunction * fcn = r_anal_get_function_at (ds->core->anal, ref->addr);
 				ds_begin_comment (ds);
 				if (fcn) {
-					ds_comment (ds, true, "; %s", fcn->name);
+					ds_comment (ds, true, "%s %s", ds->cmtoken, fcn->name);
 				} else {
-					ds_comment (ds, true, "; 0x%" PFMT64x, ref->addr);
+					ds_comment (ds, true, "%s 0x%" PFMT64x, ds->cmtoken, ref->addr);
 				}
 			}
 			r_anal_op_fini (&aop);
@@ -1529,8 +1531,8 @@ static void ds_show_xrefs(RDisasmState *ds) {
 			ds_pre_xrefs (ds, fcnlines);
 			const char* plural = r_list_length (addrs) > 1 ? "S" : "";
 			const char* plus = fun ? "" : "+";
-			ds_comment (ds, false, "%s; %s XREF%s from %s @ ",
-				COLOR (ds, pal_comment), r_anal_ref_type_tostring (refi->type), plural,
+			ds_comment (ds, false, "%s%s %s XREF%s from %s @ ",
+				COLOR (ds, pal_comment), ds->cmtoken, r_anal_ref_type_tostring (refi->type), plural,
 				realname ? realname : name);
 			ut64 *addrptr;
 
@@ -1549,7 +1551,7 @@ static void ds_show_xrefs(RDisasmState *ds) {
 					ds_begin_line (ds);
 					ds_pre_xrefs (ds, fcnlines);
 				}
-				ds_comment (ds, false, " %s; %s", pad, name);
+				ds_comment (ds, false, " %s%s %s", pad, ds->cmtoken, name);
 			}
 			ds_comment (ds, false, "%s", COLOR_RESET (ds));
 			ds_newline (ds);
@@ -2080,7 +2082,7 @@ static void ds_show_functions(RDisasmState *ds) {
 					break;
 				}
 				if (var->