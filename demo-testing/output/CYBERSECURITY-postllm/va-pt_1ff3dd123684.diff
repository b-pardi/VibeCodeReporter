@@ -129,8 +129,8 @@ def ensure_ufw_port_allowed(port: int) -> bool:
     except Exception:
         return False
 
-def cleanup_ufw_ports(ports: List[int]):
-    """Remove UFW rules for multiple ports silently, with a single progress bar."""
+def cleanup_ufw_ports(ports: List[int], silent: bool = False):
+    """Remove UFW rules for multiple ports. If silent=True, suppress output."""
     import sys
 
     to_remove = [p for p in ports if p != 22]
@@ -139,7 +139,9 @@ def cleanup_ufw_ports(ports: List[int]):
     if total == 0:
         return
 
-    print("[*] Cleaning up UFW rules...")
+    if not silent:
+        print("[*] Cleaning up UFW rules...")
+
     for idx, port in enumerate(to_remove, 1):
         try:
             subprocess.run(
@@ -151,12 +153,13 @@ def cleanup_ufw_ports(ports: List[int]):
         except Exception:
             pass
 
-        # progress bar
-        percent = int((idx / total) * 100)
-        sys.stdout.write(f"\r[*] Cleanup progress: {percent}%")
-        sys.stdout.flush()
+        if not silent:
+            percent = int((idx / total) * 100)
+            sys.stdout.write(f"\r[*] Cleanup progress: {percent}%")
+            sys.stdout.flush()
 
-    sys.stdout.write("\n[*] UFW cleanup complete.\n")
+    if not silent:
+        sys.stdout.write("\n[*] UFW cleanup complete.\n")
 
 def check_ufw_status() -> str:
     """Check if UFW is active - returns simple status"""
@@ -612,7 +615,7 @@ def main():
     print(f"[+] Reconnaissance: parsing WSDL {wsdl}")
     recon = recon_wsdl(wsdl)
     services = recon["services"]
-    namespaces = recon.get("namespaces", {})
+    namespaces =  recon.get("namespaces", {})
 
     print(f"[+] Found services: {list(services.keys())}")
     for sname, ports in services.items():