@@ -50,6 +50,10 @@ TEST(ConfigParser, test_console_config)
 
   debug_level = 1000;
 
+#if HAVE_WIN32
+  WSA_Init();
+#endif
+
   std::string path_to_config_file
       = std::string("configs/bareos-configparser-tests");
 




@@ -220,6 +220,10 @@ void test_config_directive_type(
 
   OSDependentInit();
 
+#if HAVE_WIN32
+  WSA_Init();
+#endif
+
   std::string path_to_config_file
       = std::string("configs/bareos-configparser-tests/bareos-dir-") + test_name
         + std::string(".conf");




@@ -36,6 +36,10 @@ TEST(ConfigParser_SD, test_stored_config)
 {
   OSDependentInit();
 
+#if HAVE_WIN32
+  WSA_Init();
+#endif
+
   std::string path_to_config_file
       = std::string("configs/bareos-configparser-tests");
   my_config = InitSdConfig(path_to_config_file.c_str(), M_CONFIG_ERROR);
@@ -67,6 +71,10 @@ TEST(ConfigParser_SD, CFG_TYPE_STR_VECTOR_OF_DIRS)
 
   OSDependentInit();
 
+#if HAVE_WIN32
+  WSA_Init();
+#endif
+
   InitMsg(nullptr, nullptr);
 
   std::string path_to_config_file