@@ -582,6 +582,10 @@ R_API void r_bin_list(RBin *bin, PJ *pj, int format) {
 	RBinPlugin *bp;
 	RBinXtrPlugin *bx;
 	RBinLdrPlugin *ld;
+	bool local_pj = (format == 'j' && pj == NULL);
+	if (local_pj) {
+		pj = pj_new ();
+	}
 
 	if (format == 'q') {
 		r_list_foreach (bin->plugins, it, bp) {
@@ -638,6 +642,11 @@ R_API void r_bin_list(RBin *bin, PJ *pj, int format) {
 				ld->meta.desc, r_str_get_fail (ld->meta.license, "???"));
 		}
 	}
+	if (local_pj) {
+		char *s = pj_drain (pj);
+		bin->cb_printf ("%s\n", s);
+		free (s);
+	}
 }
 
 /* returns the base address of bin or UT64_MAX in case of errors */




@@ -401,7 +401,11 @@ static int cmd_plugins(void *data, const char *input) {
 		}
 		break;
 	case 'b': // "Lb"
-		r_bin_list (core->bin, NULL, 0);
+		if (input[1] == 'j') {
+			r_bin_list (core->bin, NULL, 'j');
+		} else {
+			r_bin_list (core->bin, NULL, 0);
+		}
 		break;
 	case 'm': // "Lm"
 		if (input[1] == 'j') {