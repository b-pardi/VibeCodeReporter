@@ -7,13 +7,7 @@
 // R2R db/perf/dex
 // R2R db/cmd/lea_intel
 
-R_API void r_bin_addrline_free(RBinAddrline *di) {
-	if (di) {
-		free ((char *)di->file);
-		free ((char *)di->path);
-		free (di);
-	}
-}
+// no longer needed since RBinAddrline items are owned by the store
 
 // must be tied to the rbinfile
 R_API void r_bin_addrline_reset(RBin *bin) {
@@ -51,7 +45,7 @@ R_API bool r_bin_addrline_foreach(RBin *bin, RBinDbgInfoCallback cb, void *user)
 	return false;
 }
 
-R_API RBinAddrline *r_bin_dbgitem_at(RBin *bin, ut64 addr) {
+R_API const RBinAddrline *r_bin_dbgitem_at(RBin *bin, ut64 addr) {
 	if (bin->cur && bin->cur->addrline.used) {
 		RBinAddrLineStore *als = &bin->cur->addrline;
 		if (als) {
@@ -61,7 +55,7 @@ R_API RBinAddrline *r_bin_dbgitem_at(RBin *bin, ut64 addr) {
 	return NULL;
 }
 
-R_API RBinAddrline *r_bin_addrline_get(RBin *bin, ut64 addr) {
+R_API const RBinAddrline *r_bin_addrline_get(RBin *bin, ut64 addr) {
 	if (bin->cur && bin->cur->addrline.used) {
 		RBinAddrLineStore *als = &bin->cur->addrline;
 		return als->al_get (als, addr);
@@ -69,11 +63,22 @@ R_API RBinAddrline *r_bin_addrline_get(RBin *bin, ut64 addr) {
 	return NULL;
 }
 
+R_API const char *r_bin_addrline_str(RBin *bin, ut32 idx) {
+	if (bin->cur && bin->cur->addrline.used) {
+		RBinAddrLineStore *als = &bin->cur->addrline;
+		if (als->al_str) {
+			return als->al_str (als, idx);
+		}
+	}
+	return NULL;
+}
+
 static char *addr2fileline(RBin *bin, ut64 addr) {
 	R_RETURN_VAL_IF_FAIL (bin, NULL);
-	RBinAddrline *al = r_bin_addrline_get (bin, addr);
+	const RBinAddrline *al = r_bin_addrline_get (bin, addr);
 	if (al) {
-		const char *file_nopath = r_file_basename (al->file);
+		const char *file = r_bin_addrline_str (bin, al->file);
+		const char *file_nopath = r_file_basename (file);
 		if (al->column > 0) {
 			return r_str_newf ("%s:%d:%d", file_nopath, al->line, al->column);
 		}
@@ -92,18 +97,22 @@ R_API char * R_NULLABLE r_bin_addrline_tostring(RBin *bin, ut64 addr, int origin
 	if (!bin->cur) {
 		return NULL;
 	}
-	RBinAddrline *di = r_bin_dbgitem_at (bin, addr);
+	const RBinAddrline *di = r_bin_dbgitem_at (bin, addr);
 	if (!di) {
 		return NULL;
 	}
+	const char *file = r_bin_addrline_str (bin, di->file);
+	if (!file) {
+		return NULL;
+	}
 	char *res = NULL;
-	char *filename = strdup (di->file);
+	char *filename = strdup (file);
 	if (R_STR_ISNOTEMPTY (bin->srcdir_base) && r_str_startswith (filename, bin->srcdir_base)) {
 		char *fn = strdup (filename + strlen (bin->srcdir_base));
 		free (filename);
 		filename = fn;
 	}
-	char *basename = strdup (r_file_basename (di->file));
+	char *basename = strdup (r_file_basename (file));
 #if __APPLE__
 	// early optimization because mac's home is slow
 	if (r_str_startswith (filename, "/home")) {
@@ -111,13 +120,6 @@ R_API char * R_NULLABLE r_bin_addrline_tostring(RBin *bin, ut64 addr, int origin
 		free (filename);
 		filename = strdup (basename);
 	}
-#endif
-#if 0
-	if (R_STR_ISEMPTY (bin->srcdir) && (di->file[0] == '/')) {
-		char *res = r_str_newf ("%s:%d:%d", basename, di->line, di->column);
-		r_bin_dbgitem_free (di);
-		return res;
-	}
 #endif
 	// check absolute path
 	if (!r_file_exists (filename)) {
@@ -141,10 +143,10 @@ R_API char * R_NULLABLE r_bin_addrline_tostring(RBin *bin, ut64 addr, int origin
 	if (origin) {
 		// filename + text or fullpath + text
 		if (di->column > 0) {
-			res = r_str_newf ("%s:%d:%d%s", (origin > 1)? di->file: basename,
+			res = r_str_newf ("%s:%d:%d%s", (origin > 1)? file: basename,
 				di->line, di->column, r_str_get (out));
 		} else {
-			res = r_str_newf ("%s:%d%s", (origin > 1)? di->file: basename,
+			res = r_str_newf ("%s:%d%s", (origin > 1)? file: basename,
 				di->line, r_str_get (out));
 		}
 		free (out);
@@ -155,6 +157,5 @@ R_API char * R_NULLABLE r_bin_addrline_tostring(RBin *bin, ut64 addr, int origin
 	}
 	free (filename);
 	free (basename);
-	r_bin_addrline_free (di);
 	return res;
 }




@@ -545,62 +545,37 @@ static void get_strin