@@ -95,12 +95,6 @@
 #  define NOMINMAX
 #endif
 
-#if defined(_MSC_VER)
-extern "C" {
-#  include "getopt.h"
-}
-#endif
-
 #include <sys/socket.h>
 #if defined(HAVE_WIN32) & !defined(HAVE_MINGW)
 #  include <winsock2.h>




@@ -63,11 +63,6 @@ namespace storagedaemon {
 extern bool ParseSdConfig(const char* configfile, int exit_code);
 }
 
-#ifdef _MSC_VER
-char* optarg{};
-#endif
-
-
 using namespace storagedaemon;
 
 static void DoExtract(char* devname,




@@ -60,10 +60,6 @@ namespace storagedaemon {
 extern bool ParseSdConfig(const char* configfile, int exit_code);
 }
 
-#ifdef _MSC_VER
-char* optarg{};
-#endif
-
 using namespace storagedaemon;
 
 static void DoBlocks(char* infname);




@@ -22,11 +22,6 @@
 #include "testing_dir_common.h"
 #include "dird/stats.h"
 
-#ifdef _MSC_VER
-char* optarg{};
-#endif
-
-
 class DirStatisticsThread : public ::testing::Test {
   void SetUp() override { InitDirGlobals(); }
 };




@@ -27,7 +27,9 @@
  * using a lowlevel SCSI interface.
  */
 
-#if !defined(_MSC_VER)
+#if defined(HAVE_MSVC)
+#  include "getopt.h"
+#else
 #  include <unistd.h>
 #endif
 #include "include/fcntl_def.h"




@@ -27,7 +27,9 @@
  * Kern Sibbald, MM
  */
 
-#if !defined(_MSC_VER)
+#if defined(HAVE_MSVC)
+#  include "getopt.h"
+#else
 #  include <unistd.h>
 #endif
 #include "include/bareos.h"




@@ -25,7 +25,9 @@
  * Program for determining drive type
  */
 
-#if !defined(_MSC_VER)
+#if defined(HAVE_MSVC)
+#  include "getopt.h"
+#else
 #  include <unistd.h>
 #endif
 #include "include/bareos.h"




@@ -25,7 +25,9 @@
  * Program for determining file system type
  */
 
-#if !defined(_MSC_VER)
+#if defined(HAVE_MSVC)
+#  include "getopt.h"
+#else
 #  include <unistd.h>
 #endif
 #include "include/bareos.h"




@@ -28,7 +28,9 @@
 #include <string.h>
 #include <fcntl.h>
 #include <time.h>
-#if !defined(_MSC_VER)
+#if defined(HAVE_MSVC)
+#  include "getopt.h"
+#else
 #  include <unistd.h>
 #endif
 #include <signal.h>