@@ -404,7 +404,7 @@ static void _print_strings(RCore *core, RList *list, PJ *pj, int mode, int va) {
 		type_string = r_bin_string_type (string->type);
 		if (b64str) {
 			ut8 *s = r_base64_decode_dyn (string->string, -1, NULL);
-			if (s && *s && IS_PRINTABLE (*s)) {
+			if (R_STR_ISNOTEMPTY (s) && IS_PRINTABLE (*s)) {
 				// TODO: add more checks
 				free (b64.string);
 				memcpy (&b64, string, sizeof (b64));
@@ -414,7 +414,7 @@ static void _print_strings(RCore *core, RList *list, PJ *pj, int mode, int va) {
 			}
 		}
 		if (IS_MODE_SET (mode)) {
-			if (r_cons_is_breaked ()) {
+			if (r_kons_is_breaked (core->cons)) {
 				break;
 			}
 			r_meta_set (core->anal, R_META_TYPE_STRING, vaddr, string->size, string->string);
@@ -436,7 +436,7 @@ static void _print_strings(RCore *core, RList *list, PJ *pj, int mode, int va) {
 			r_kons_printf (core->cons, "0x%"PFMT64x" %d %d %s\n", vaddr,
 				string->size, string->length, string->string);
 		} else if (IS_MODE_SIMPLEST (mode)) {
-			r_cons_println (string->string);
+			r_kons_println (core->cons, string->string);
 		} else if (IS_MODE_JSON (mode)) {
 			int *block_list;
 			pj_o (pj);
@@ -553,7 +553,7 @@ static void _print_strings(RCore *core, RList *list, PJ *pj, int mode, int va) {
 	if (IS_MODE_JSON (mode)) {
 		pj_end (pj);
 	} else if (IS_MODE_SET (mode)) {
-		r_cons_break_pop ();
+		r_kons_break_pop (core->cons);
 	} else if (IS_MODE_NORMAL (mode)) {
 		bool show_table = true;
 		if (core->table_query) {
@@ -562,7 +562,7 @@ static void _print_strings(RCore *core, RList *list, PJ *pj, int mode, int va) {
 		if (show_table) {
 			char *s = r_table_tostring (table);
 			if (s) {
-				r_cons_print (s);
+				r_kons_print (core->cons, s);
 				free (s);
 			}
 		}
@@ -1068,7 +1068,7 @@ static bool bin_info(RCore *core, PJ *pj, int mode, ut64 laddr) {
 				for (j = 0; j < h->len; j++) {
 					r_kons_printf (core->cons, "%02x", h->buf[j]);
 				}
-				r_cons_newline ();
+				r_kons_newline (core->cons);
 			}
 		}
 		if (IS_MODE_JSON (mode)) {
@@ -1180,14 +1180,14 @@ static bool bin_addrline(RCore *core, PJ *pj, int mode) {
 		return false;
 	}
 
-	r_cons_break_push (NULL, NULL);
+	r_kons_break_push (core->cons, NULL, NULL);
 	/* cache file:line contents */
 	HtPP* file_lines = ht_pp_new (NULL, file_lines_free_kv, NULL);
 
 	SetP *set = set_p_new ();
 	// XXX this leaks like there's no stopper
 	r_list_foreach (list, iter, row) {
-		if (r_cons_is_breaked ()) {
+		if (r_kons_is_breaked (core->cons)) {
 			break;
 		}
 		if (mode) {
@@ -1273,7 +1273,7 @@ static bool bin_addrline(RCore *core, PJ *pj, int mode) {
 		pj_end (pj);
 	}
 	set_p_free (set);
-	r_cons_break_pop ();
+	r_kons_break_pop (core->cons);
 	ht_pp_free (file_lines);
 	r_list_free (ownlist);
 	return true;
@@ -1368,7 +1368,7 @@ static bool bin_source(RCore *core, PJ *pj, int mode) {
 	RList *files = r_bin_addrline_files (core->bin);
 	if (files) {
 		char *s = r_str_list_join (files, "\n");
-		r_cons_println (s);
+		r_kons_println (core->cons, s);
 		free (s);
 	}
 #else
@@ -1467,7 +1467,7 @@ static bool bin_entry(RCore *core, PJ *pj, int mode, ut64 laddr, int va, bool in
 		if (r_list_empty (entries)) {
 			return true;
 		}
-		r_cons_println ("'fs+symbols");
+		r_kons_println (core->cons, "'fs+symbols");
 	} else if (IS_MODE_JSON (mode)) {
 		pj_a (pj);
 	}
@@ -1586,7 +1586,7 @@ static bool bin_entry(RCore *core, PJ *pj, int mode, ut64 laddr, int va, bool in
 			r_table_query (table, core->table_query);
 		}
 		char *s = r_table_tostring (table);
-		r_cons_print (s);
+		r_kons_print (core->cons, s);
 		free (s);
 		r_table_free (table);
 	} else if (IS_MODE_SET (mode)) {
@@ -1595,7 +1595,7 @@ static bool bin_entry(RCore *core, PJ *pj, int mode, ut64 laddr, int va, bool in
 			r_core_seek (core, at, false);
 		}
 	} else if (IS_MODE_RAD (mode)) {
-		r_cons_println ("'fs-");
+		r_kons_println (core->cons, "'fs-");
 	} else if (IS_MODE_JSON (mode)) {
 		pj_end (pj);
 	}
@@ -1944,7 +1944,7 @@ static bool bin_relocs