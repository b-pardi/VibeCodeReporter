@@ -57,10 +57,10 @@ int ntop_lua_check(lua_State *vm, const char *func, int pos,
                                  "%s : expected %s[@pos %d], got %s", func,
                                  lua_typename(vm, expected_type), pos,
                                  lua_typename(vm, lua_type(vm, pos)));
-    return (CONST_LUA_PARAM_ERROR);
+    return(CONST_LUA_PARAM_ERROR);
   }
 
-  return (CONST_LUA_OK);
+  return(CONST_LUA_OK);
 }
 
 /* ****************************************** */
@@ -94,9 +94,9 @@ static int ntop_dump_file(lua_State *vm) {
   ntop->getTrace()->traceEvent(TRACE_DEBUG, "%s() called", __FUNCTION__);
 
   if (ntop_lua_check(vm, __FUNCTION__, 1, LUA_TSTRING) != CONST_LUA_OK)
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
   if ((fname = (char *)lua_tostring(vm, 1)) == NULL)
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_PARAM_ERROR));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_PARAM_ERROR));
 
   if ((fd = fopen(fname, "r")) != NULL) {
     char tmp[1024];
@@ -116,10 +116,10 @@ static int ntop_dump_file(lua_State *vm) {
 
     fclose(fd);
     lua_pushnil(vm);
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_OK));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_OK));
   } else {
     ntop->getTrace()->traceEvent(TRACE_INFO, "Unable to read file %s", fname);
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
   }
 }
 
@@ -135,9 +135,9 @@ static int ntop_dump_binary_file(lua_State *vm) {
   ntop->getTrace()->traceEvent(TRACE_DEBUG, "%s() called", __FUNCTION__);
 
   if (ntop_lua_check(vm, __FUNCTION__, 1, LUA_TSTRING) != CONST_LUA_OK)
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
   if ((fname = (char *)lua_tostring(vm, 1)) == NULL)
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_PARAM_ERROR));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_PARAM_ERROR));
 
   ntop->fixPath(fname);
   if ((fd = fopen(fname, "rb")) != NULL) {
@@ -150,10 +150,10 @@ static int ntop_dump_binary_file(lua_State *vm) {
 
     fclose(fd);
     lua_pushnil(vm);
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_OK));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_OK));
   } else {
     ntop->getTrace()->traceEvent(TRACE_INFO, "Unable to read file %s", fname);
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
   }
 }
 
@@ -163,12 +163,12 @@ static int ntop_get_mac_manufacturer(lua_State *vm) {
   const char *mac = NULL;
 
   if (ntop_lua_check(vm, __FUNCTION__, 1, LUA_TSTRING) != CONST_LUA_OK)
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
+    return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
   mac = (char *)lua_tostring(vm, 1);
 
   ntop->getMacManufacturer(mac, vm);
 
-  return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_OK));
+  return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_OK));
 }
 
 /* ****************************************** */
@@ -182,7 +182,7 @@ static int ntop_get_host_information(lua_State *vm) {
   lua_push_str_table_entry(vm, "instance_name",
                            ntop->getPrefs()->get_instance_name());
 
-  return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_OK));
+  return(ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_OK));
 }
 
 /* ****************************************** */
@@ -194,10 +194,10 @@ static int ntop_set_bind_addr(lua_State *vm, bool http) {
   ntop->getTrace()->traceEvent(TRACE_DEBUG, "%s() called", __FUNCTION__);
 
   if (!ntop->isUserAdministrator(vm))
-    return (ntop_lua_return_value(vm, __FUNCTION__, CONST_LUA_ERROR));
+    return(ntop_lu