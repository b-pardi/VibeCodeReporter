@@ -3,67 +3,68 @@
 */
 const types = {
     no_formatting: {
-	id: "no_formatting",
-	um: null,
-	step: null,
-	decimal: null,
-	scale_values: null,
+        id: "no_formatting",
+        um: null,
+        step: null,
+        decimal: null,
+        scale_values: null,
     },
     number: {
-	id: "number",
-	um: ["", "K", "M", "G", "T"],
-	step: 1000,
-	decimal: null,
-	scale_values: null,
+        id: "number",
+        um: ["", "K", "M", "G", "T"],
+        step: 1000,
+        decimal: null,
+        scale_values: null,
+        thousands_sep: ",", /* Comment this to enable "um" scaled style */
     },
     bytes: {
-	id: "bytes",
-	um: ["B", "KB", "MB", "GB", "TB", "PB", "EB"],
-	step: 1024,
-	decimal: 2,
-	scale_values: null,
-	absolute_value: true,
+        id: "bytes",
+        um: ["B", "KB", "MB", "GB", "TB", "PB", "EB"],
+        step: 1024,
+        decimal: 2,
+        scale_values: null,
+        absolute_value: true,
     },
     bps: {
-	id: "bps",
-	um: ["bps", "Kbps", "Mbps", "Gbps", "Tbps", "Pbps"],
-	step: 1000,
-	decimal: 2,
-	scale_values: 8,	
-	absolute_value: true,
+        id: "bps",
+        um: ["bps", "Kbps", "Mbps", "Gbps", "Tbps", "Pbps"],
+        step: 1000,
+        decimal: 2,
+        scale_values: 8,        
+        absolute_value: true,
     },
     fps: {
-	id: "fps",
-	um: ["flows/s", "Kflows/s", "Mflows/s", "Gflows/s"],
-	step: 1000,
-	decimal: 2,
-	scale_values: null,	
-	absolute_value: true,
+        id: "fps",
+        um: ["flows/s", "Kflows/s", "Mflows/s", "Gflows/s"],
+        step: 1000,
+        decimal: 2,
+        scale_values: null,        
+        absolute_value: true,
     },
     pps: {
-	id: "pps",
-	um: ["pps", "Kpps", "Mpps", "Gpps", "Tpps"],
-	step: 1000,
-	decimal: 2,
-	scale_values: null,	
-	absolute_value: true,
+        id: "pps",
+        um: ["pps", "Kpps", "Mpps", "Gpps", "Tpps"],
+        step: 1000,
+        decimal: 2,
+        scale_values: null,        
+        absolute_value: true,
     },
     ms: {
-	id: "ms",
-	um: ["ms", "s"],
-	step: 1000,
-	decimal: 2,
-	scale_values: null,	
-	absolute_value: true,
+        id: "ms",
+        um: ["ms", "s"],
+        step: 1000,
+        decimal: 2,
+        scale_values: null,        
+        absolute_value: true,
     },
     percentage: {
-	id: "percentage",
-	um: ["%"],
-	step: 101,
-	decimal: 0,
-	scale_values: null,	
-	max_value: 100,
-	absolute_value: true,
+        id: "percentage",
+        um: ["%"],
+        step: 101,
+        decimal: 0,
+        scale_values: null,        
+        max_value: 100,
+        absolute_value: true,
     },
 };
 
@@ -71,23 +72,23 @@ function getUnitMeasureLen(type) {
     // 000.00
     let t = types[type];
     let spaceValue = 3;
-    if (t.decimal != null && t.decimal > 0) {	
-	spaceValue = 6;
+    if (t.decimal != null && t.decimal > 0) {        
+        spaceValue = 6;
     }
     let spaceUm = 0;
     if (t.um != null) {
-	spaceUm = Math.max(...t.um.map((um) => um.length));
+        spaceUm = Math.max(...t.um.map((um) => um.length));
     }
     return (spaceValue + 1 + spaceUm);
 }
 
 function getScaleFactorIndex(type, value) {
-   let typeOptions = types[type];
+    let typeOptions = types[type];
     if (type == types.no_formatting.id || value == null) {
-	return null;
+        return null;
     }
     if (typeOptions.scale_values != null) {
-	value *= typeOptions.scale_values;
+        value *= typeOptions.scale_values;
     }
     let step = typeOptions.step;
     let negativeValue = value < 0;
@@ -95,8 +96,8 @@ function getScaleFactorIndex(type, value) {
     let i = 0;
     let measures = typeOptions.um;
     while (value >= step && i < measures.length) {
-	value = value / step;
-	i += 1;
+        value = value / step;
+        i += 1;
     }
     return i;
 }
@@ -105,61 +106,74 @@ function getFormatter(type, absoluteValue, scaleFactorIndex) {
     let typeOptions = types[type];
     absoluteValue |= typeOptions.absolute_value; 
     let formatter = funct