@@ -104,6 +104,7 @@ def convert_to_markdown(filename: str, command: CommandObj) -> str:
     assert parser
     name = command.command_name
     description = command.description
+    assert description
 
     formatter = parser._get_formatter()
 
@@ -113,19 +114,18 @@ def convert_to_markdown(filename: str, command: CommandObj) -> str:
 
     mdFile = MdUtils(filename)
 
+    # title
     mdFile.new_header(level=1, title=name)
-    assert description
-    mdFile.new_header(level=2, title="Description")
-    mdFile.new_paragraph(description)
-
-    mdFile.new_header(level=2, title="Usage")
-    mdFile.insert_code(parser.format_usage(), language="bash")
+    # usage
+    mdFile.insert_code(parser.format_usage(), language="text")
+    # description
+    mdFile.new_paragraph(description + "\n")
 
     used_actions = {}
     positionals = ["Positional Argument", "Help"]
     optionals = ["Short", "Long", "Help"]
 
-    # Process positional arguments
+    # positional arguments
     if parser._positionals._group_actions:
         for action in parser._positionals._group_actions:
             # The formatter decides if the default should be shown.
@@ -138,7 +138,7 @@ def convert_to_markdown(filename: str, command: CommandObj) -> str:
 
             positionals.extend(list_of_str)
 
-        mdFile.new_header(level=2, title="Positional Arguments")
+        mdFile.write("### Positional arguments\n")
         positionals = [di if di is None else di.replace("\n", " ") for di in positionals]
         mdFile.new_table(
             columns=2,
@@ -146,8 +146,9 @@ def convert_to_markdown(filename: str, command: CommandObj) -> str:
             text=positionals,
             text_align="left",
         )
+        mdFile.write("\n")
 
-    # Process optional arguments
+    # optional arguments
     if parser._option_string_actions:
         for k in parser._option_string_actions:
             action = parser._option_string_actions[k]
@@ -169,7 +170,7 @@ def convert_to_markdown(filename: str, command: CommandObj) -> str:
 
             optionals.extend(list_of_str)
 
-        mdFile.new_header(level=2, title="Optional Arguments")
+        mdFile.write("### Optional arguments\n")
         optionals = [di if di is None else di.replace("\n", " ") for di in optionals]
         mdFile.new_table(
             columns=3,
@@ -181,14 +182,14 @@ def convert_to_markdown(filename: str, command: CommandObj) -> str:
 
     if command.examples:
         # Put the examples into a code block so they are formatted sensically.
-        mdFile.write("## Examples\n```text\n" + command.examples + "\n```\n")
+        mdFile.write("### Examples\n```text\n" + command.examples + "\n```\n")
 
     if command.notes:
         # The author of the note should make sure it make sense in markdown.
-        mdFile.write("## Notes\n" + command.notes + "\n")
+        mdFile.write("### Notes\n" + command.notes + "\n")
 
     if command.pure_epilog:
-        mdFile.write("## Extra\n" + command.pure_epilog + "\n")
+        mdFile.write("### Extra\n" + command.pure_epilog + "\n")
 
     autogen_warning = "<!-- THIS PART OF THIS FILE IS AUTOGENERATED. DO NOT MODIFY IT. See scripts/generate_docs.sh -->"
 




@@ -115,9 +115,7 @@ pwndbg> tele '$environ("LANG")'
             sys.exit(5)
 
         mdFile.new_paragraph(func_signature_code)
-        mdFile.new_paragraph(
-            "#### Description\n" + getdoc(func).replace("Example:", "#### Example")
-        )
+        mdFile.new_paragraph(getdoc(func).replace("Example:", "#### Example"))
         mdFile.new_paragraph("-" * 10)
 
     hide_nav = "---\nhide:\n  - navigation\n---\n"