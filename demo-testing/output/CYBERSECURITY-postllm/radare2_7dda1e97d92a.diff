@@ -102,6 +102,16 @@ int main(int argc, const char **argv) {
 	if (argc > 0 && strstr (argv[0], "r2p")) {
 		return r_main_r2pipe (argc, argv);
 	}
+	char *ea = r_sys_getenv ("R2_ARGS");
+	if (R_STR_ISNOTEMPTY (ea)) {
+		char **argv = r_str_argv (ea, &argc);
+		r_sys_setenv ("R2_ARGS", NULL);
+		int res = r_main_radare2 (argc, (const char **)argv);
+		free (ea);
+		free (argv);
+		return res;
+	}
+	free (ea);
 	return r_main_radare2 (argc, argv);
 }
 




@@ -4,7 +4,7 @@
 
 int main(int argc, const char **argv) {
 	char *ea = r_sys_getenv ("RARUN2_ARGS");
-	if (ea) {
+	if (R_STR_ISNOTEMPTY (ea)) {
 		char **argv = r_str_argv (ea, &argc);
 		r_sys_setenv ("RARUN2_ARGS", NULL);
 		int res = r_main_rarun2 (argc, (const char **)argv);
@@ -12,5 +12,6 @@ int main(int argc, const char **argv) {
 		free (argv);
 		return res;
 	}
+	free (ea);
 	return r_main_rarun2 (argc, argv);
 }




@@ -6,7 +6,7 @@ var freePtr = Module.findExportByName(null, "free");
 var freel = new NativeFunction(freePtr, 'void', ['pointer']);
 
 var reallocPtr = Module.findExportByName(null, "realloc");
-var reallocl = new NativeFunction(reallocPtr, 'pointer', ['pointer', 'int']);
+var reallocl = new NativeFunction(reallocPtr, 'pointer', ['pointer', 'long']);
 
 Interceptor.replace(mallocPtr, new NativeCallback(function (size) {
     var p = malloc(size);
@@ -16,11 +16,11 @@ Interceptor.replace(mallocPtr, new NativeCallback(function (size) {
 
 Interceptor.replace(freePtr, new NativeCallback(function (p) {
     freel(p);
-    if (p) console.error("mem::free " + p);
+    if (+p != 0) console.error("mem::free " + p);
 }, 'void', ['pointer']));
 
 Interceptor.replace(reallocPtr, new NativeCallback(function (p, size) {
-    var p_ret;
-    p_ret = reallocl(p, size);
+    var p_ret = reallocl(p, size);
     console.error("mem::realloc " + p + " " + size + " = " + p_ret);
-}, 'void', ['pointer', 'int']));
+    return p_ret;
+}, 'pointer', ['pointer', 'int']));