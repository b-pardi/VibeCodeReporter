similarity index 100%
rename from thug/DOM/scripts/date.js
rename to conf/scripts/date.js

similarity index 100%
rename from thug/DOM/scripts/eval.js
rename to conf/scripts/eval.js

similarity index 100%
rename from thug/DOM/scripts/storage.js
rename to conf/scripts/storage.js

similarity index 100%
rename from thug/DOM/scripts/thug.js
rename to conf/scripts/thug.js

similarity index 100%
rename from thug/DOM/scripts/write.js
rename to conf/scripts/write.js




@@ -1,8 +1,35 @@
 #!/usr/bin/env python
 
 import os
+import shutil
+
 from setuptools import setup
+from setuptools.command.install import install
+
+import appdirs
 
 os.environ['BUILD_LIB'] = '1'
 
-setup()
+THUG_GLOBAL_CONFIG_DIR = "/etc/thug"
+THUG_USER_CONFIG_DIR = f"{appdirs.user_config_dir()}/thug/"
+
+class thug_install(install):
+    def run(self):
+        if not os.path.exists(THUG_GLOBAL_CONFIG_DIR):
+            try:
+                shutil.copytree("conf",
+                                THUG_GLOBAL_CONFIG_DIR,
+                                dirs_exist_ok = True)
+            except PermissionError:
+                if not os.path.exists(THUG_USER_CONFIG_DIR):
+                    shutil.copytree("conf",
+                                     THUG_USER_CONFIG_DIR,
+                                     dirs_exist_ok = True)
+
+        install.run(self)
+
+setup(
+    cmdclass = dict(
+        install = thug_install
+    ),
+)




@@ -1,9 +1,16 @@
 import os
 
+import appdirs
+
 __version__            = "4.5"
 __jsengine__           = ""
 __jsengine_version__   = ""
-__configuration_path__ = "/etc/thug"
+
+__global_configuration_path__ = "/etc/thug"
+if os.path.exists(__global_configuration_path__):
+    __configuration_path__ = __global_configuration_path__
+else:
+    __configuration_path__ = f"{appdirs.user_config_dir()}/thug"
 
 __personalities_path__ = os.path.join(__configuration_path__, "personalities")
 __rules_path__         = os.path.join(__configuration_path__, "rules")