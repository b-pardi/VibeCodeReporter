@@ -182,12 +182,15 @@ def _process_field_data(obj, field):
 
     # special case for service_id
     if field[0] == "service_id":
-        return obj, "service_id"
+        return [obj], "service_id"
 
     # special case for host services
     if obj.__class__.__name__.lower() == "host" and field[0] == "service":
         field = ["services"] + field[1:]
 
+    if field[0] == "hostnames":
+        return [obj], "hostnames"
+
     for route in field:
 
         # break if previous iteration was a list
@@ -206,6 +209,9 @@ def _process_field_data(obj, field):
             field = route
             break
 
+    if isinstance(field, list):
+        field = field[-1]
+
     if not isinstance(obj, list):
         obj = [obj]
     return obj, field




@@ -418,6 +418,23 @@ class TestWorkflowMixinsView(ReadWriteAPITests):
         _process_entry(host.__class__.__name__, [host.id], host.workspace.id)
         assert host.description != "ActionExecuted"
 
+    def test_conditions_on_host_hostnames(self, test_client):
+        cond = [
+            {
+                "type": "leaf",
+                "field": "hostnames",
+                "operator": "in",
+                "data": "test"
+            }
+        ]
+        ws, action, workflow, pipeline = create_pipeline(test_client, cond=cond)
+        host = HostFactory.create(description="testing", workspace=ws)
+        host.set_hostnames(["test", "test2"])
+        db.session.add(host)
+        db.session.commit()
+        _process_entry(host.__class__.__name__, [host.id], host.workspace.id)
+        assert host.description == "ActionExecuted"
+
     def test_object_does_not_exist(self, test_client):
         action = ActionFactory.create()
         db.session.commit()