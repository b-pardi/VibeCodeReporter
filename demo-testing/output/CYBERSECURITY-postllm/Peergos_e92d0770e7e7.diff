@@ -18,6 +18,8 @@ public interface BlockMetadataStore {
 
     long size();
 
+    void applyToAll(Consumer<Cid> consumer);
+
     Stream<BlockVersion> list();
 
     void listCbor(Consumer<List<BlockVersion>> res);




@@ -38,6 +38,12 @@ public class DelegatingBlockMetadataStore implements BlockMetadataStore {
     public void remove(Cid block) {
     }
 
+    @Override
+    public boolean applyToAll(Consumer<Cid> action) {
+        store.applyToAll(c -> action.accept(Cid.cast(c.toBytes())));
+        return true;
+    }
+
     @Override
     public Stream<Cid> list() {
         return store.list().filter(bv -> bv.isLatest).map(bv2 -> io.ipfs.cid.Cid.cast(bv2.cid.toBytes()));




@@ -133,6 +133,20 @@ public class JdbcBlockMetadataStore implements BlockMetadataStore {
         }
     }
 
+    @Override
+    public void applyToAll(Consumer<Cid> action) {
+        try (Connection conn = getConnection();
+             PreparedStatement stmt = conn.prepareStatement(LIST)) {
+            ResultSet rs = stmt.executeQuery();
+            while (rs.next()) {
+                action.accept(Cid.cast(rs.getBytes("cid")));
+            }
+        } catch (SQLException sqe) {
+            LOG.log(Level.WARNING, sqe.getMessage(), sqe);
+            throw new RuntimeException(sqe);
+        }
+    }
+
     @Override
     public Stream<BlockVersion> list() {
         try (Connection conn = getConnection();




@@ -29,6 +29,11 @@ public class RamBlockMetadataStore implements BlockMetadataStore {
         store.remove(block);
     }
 
+    @Override
+    public void applyToAll(Consumer<Cid> action) {
+        store.keySet().stream().forEach(action);
+    }
+
     @Override
     public Stream<BlockVersion> list() {
         return store.keySet().stream().map(c -> new BlockVersion(c, null, true));