@@ -377,16 +377,35 @@ def main():
 
     if args.recon:
         print("\n=== Recon Summary ===")
+        flags = []
+
         if recon["encoded_style"]:
-            print("[!] Encoded RPC style detected — potentially vulnerable to deserialization-based attacks.")
+            print("[!] Detected RPC/encoded style — this is a key indicator of deserialization risk.")
+            flags.append("uses encoded style")
+
         if recon["suspicious_fields"]:
-            print(f"[!] Suspicious fields: {recon['suspicious_fields']}")
+            print(f"[!] Found suspicious fields that may accept serialized input: {recon['suspicious_fields']}")
+            flags.append("has suspicious input fields")
+
+        suspicious_ops = []
         for sname, ports in services.items():
             for pname, pinfo in ports.items():
                 interesting_ops = [op for op in pinfo.get("operations", []) if any(k in op.lower() for k in SUSPICIOUS_OP_KEYWORDS)]
                 if interesting_ops:
-                    print(f"[!] Suspicious operations in {sname}/{pname}: {interesting_ops}")
-        print("[+] Recon complete. Use --exploit to generate and test payloads.")
+                    print(f"[!] Found potentially dangerous operations in {sname}/{pname}: {interesting_ops}")
+                    suspicious_ops.extend(interesting_ops)
+
+        if "setJmsConfig" in suspicious_ops or "setFlatfileConfig" in suspicious_ops:
+            flags.append("exposes JMS/Flatfile setters")
+
+        if flags:
+            print("\n[!] This WSDL service appears potentially exploitable due to:")
+            for f in flags:
+                print(f"    - {f}")
+            print("[!] Proceed to active testing with --exploit ONLY if you have explicit authorization.")
+        else:
+            print("[+] No immediate signs of risky features found. Still proceed cautiously.")
+
         return
 
 if __name__ == "__main__":