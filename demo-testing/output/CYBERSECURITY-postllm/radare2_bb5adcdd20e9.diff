@@ -1058,15 +1058,13 @@ static int cmd_help(void *data, const char *input) {
 			r_core_cmd_help (core, help_msg_question_V);
 			break;
 		case 0: // "?V"
-#if R2_VERSION_COMMIT == 0
-			r_cons_printf ("%s release\n", R2_VERSION);
-#else
-			if (!strcmp (R2_VERSION, R2_GITTAP)) {
-				r_cons_printf ("%s %d\n", R2_VERSION, R2_VERSION_COMMIT);
-			} else {
-				r_cons_printf ("%s aka %s commit %d\n", R2_VERSION, R2_GITTAP, R2_VERSION_COMMIT);
+			{
+				char *v = r_str_version ("radare2");
+				if (v) {
+					r_cons_printf ("%s\n", v);
+				}
+				free (v);
 			}
-#endif
 			break;
 		case 'c': // "?Vc"
 			r_cons_printf ("%d\n", vernum (R2_VERSION));




@@ -4032,18 +4032,23 @@ R_API const char *r_str_str_xy(const char *s, const char *word, const char *prev
 #endif
 
 R_API char *r_str_version(const char *program) {
+	const char *release = "";
 	const char *asanstr = "";
 #if defined(__has_feature)
 #if __has_feature(address_sanitizer)
 	asanstr = " (asan)";
 #endif
+#endif
+#if R2_VERSION_COMMIT == 0
+	release " (release)";
+#else
 #endif
 	char *s = r_str_newf ("%s "R2_VERSION" %d @ "
 			R_SYS_OS"-"
-			R_SYS_ARCH"-%d git.%s%s\n",
+			R_SYS_ARCH"-%d git.%s%s%s\n",
 			program, R2_VERSION_COMMIT,
 			(R_SYS_BITS & 8)? 64: 32,
-			*R2_GITTAP ? R2_GITTAP: "", asanstr);
+			*R2_GITTAP ? R2_GITTAP: "", asanstr, release);
 	if (*R2_GITTIP) {
 		s = r_str_append (s, "commit: "R2_GITTIP" build: "R2_BIRTH);
 	}