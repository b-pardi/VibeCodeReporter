@@ -14,29 +14,28 @@ extern int memorystatus_control(uint32_t command, pid_t pid, uint32_t flags, voi
 #define USE_IOS_JETSAM 0
 #endif
 
-static int usage(int v) {
-	printf ("Usage: r2agent [-adhs] [-p port]\n"
-	"  -a        listen for everyone (localhost by default)\n"
-	"  -d        run in daemon mode (background)\n"
-	"  -h        show this help message\n"
-	"  -s        run in sandbox mode\n"
-	"  -u        enable http authorization access\n"
-	"  -t        user:password authentication file\n"
-	"  -p [port] specify listening port (defaults to 8080)\n");
+static int usage(bool v) {
+	printf("Usage: r2agent [-adhsLjv] [-p port]\n"
+	       "  -a        listen for everyone (localhost by default)\n"
+	       "  -d        run in daemon mode (background)\n"
+	       "  -h        show this help message\n"
+	       "  -s        run in sandbox mode\n"
+	       "  -u        enable http authorization access\n"
+	       "  -t        user:password authentication file\n"
+	       "  -p [port] specify listening port (defaults to 8080)\n"
+	       "  -L        list currently loaded r2 sessions and exit\n"
+	       "  -v        show r2 version information and exit\n"
+	       "  -j        output JSON when used with -v or -L\n");
 	return !v;
 }
 
-static int showversion(void) {
-	return r_main_version_print ("r2agent", 0);
-}
-
 R_API int r_main_r2agent(int argc, const char **argv) {
 	RSocket *s;
 	RCons *cons = NULL;
 	RSocketHTTPOptions so;
 	int c;
-	int dodaemon = 0;
-	int dosandbox = 0;
+	bool dodaemon = false;
+	bool dosandbox = false;
 	bool listenlocal = true;
 	const char *port = "8080";
 	const char *httpauthfile = NULL;
@@ -44,22 +43,26 @@ R_API int r_main_r2agent(int argc, const char **argv) {
 	memset (&so, 0, sizeof (so));
 
 	RGetopt opt;
-	r_getopt_init (&opt, argc, argv, "adhup:t:sv");
+	bool list_sessions = false;
+	bool list_json = false;
+	bool show_version = false;
+	r_getopt_init (&opt, argc, argv, "adhup:t:svLj");
 	while ((c = r_getopt_next (&opt)) != -1) {
 		switch (c) {
 		case 'a':
 			listenlocal = false;
 			break;
 		case 's':
-			dosandbox = 1;
+			dosandbox = true;
 			break;
 		case 'd':
-			dodaemon = 1;
+			dodaemon = true;
 			break;
 		case 'h':
-			return usage (1);
+			return usage (true);
 		case 'v':
-			return showversion ();
+			show_version = true;
+			break;
 		case 'u':
 			so.httpauth = true;
 			break;
@@ -69,12 +72,39 @@ R_API int r_main_r2agent(int argc, const char **argv) {
 		case 'p':
 			port = opt.arg;
 			break;
+		case 'L':
+			list_sessions = true;
+			break;
+		case 'j':
+			list_json = true;
+			break;
 		default:
-			return usage (0);
+			return usage (false);
 		}
 	}
 	if (opt.ind != argc) {
-		return usage (0);
+		return usage (false);
+	}
+
+	if (show_version) {
+		int mode = list_json ? 'j' : 0;
+		return r_main_version_print ("r2agent", mode);
+	}
+
+	if (list_sessions) {
+		RCore *core = r_core_new ();
+		if (!core) {
+			R_LOG_ERROR ("Unable to create RCore instance");
+			return 1;
+		}
+		const char *cmd = list_json ? "=lj" : "=l";
+		char *out = r_core_cmd_str (core, cmd);
+		if (out) {
+			printf ("%s\n", out);
+			free (out);
+		}
+		r_core_free (core);
+		return 0;
 	}
 
 	so.accept_timeout = 0;
@@ -83,7 +113,7 @@ R_API int r_main_r2agent(int argc, const char **argv) {
 	if (so.httpauth) {
 		if (!httpauthfile) {
 			R_LOG_ERROR ("No authentication user list set");
-			return usage (0);
+			return usage (false);
 		}
 
 		size_t sz;
@@ -92,11 +122,11 @@ R_API int r_main_r2agent(int argc, const char **argv) {
 			so.authtokens = r_str_split_list (pfile, "\n", 0);
 		} else {
 			R_LOG_ERROR ("Empty list of HTTP users");
-			return usage (0);
+			return usage (false);
 		}
 	}
 #if USE_IOS_JETSAM
-	memorystatus_control (MEMORYSTATUS_CMD_SET_JETSAM_TASK_LIMIT, r_sys_getpid (), 256, NULL, 0);
+	memorystatus_control (MEMORYSTATUS_CMD_SET_JETSAM_TASK_LIMIT, r_sys_getpid(), 256, NULL, 0);
 #endif
 	if (dodaemon) {
 #if LIBC_HAVE_FORK
@@ -107,6 +137,7 @@ R_API int r_mai