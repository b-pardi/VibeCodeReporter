@@ -20,7 +20,7 @@ if TYPE_CHECKING:
     from typing_extensions import Self
 
     from pcapkit.corekit.fields.field import NoValueType
-    from pcapkit.protocols.protocol import Protocol
+    from pcapkit.protocols.protocol import ProtocolBase as Protocol
     from pcapkit.protocols.schema.schema import Schema
 
 _TC = TypeVar('_TC')




@@ -20,7 +20,7 @@ if TYPE_CHECKING:
 
     from typing_extensions import Self
 
-    from pcapkit.protocols.protocol import Protocol
+    from pcapkit.protocols.protocol import ProtocolBase as Protocol
 
 __all__ = ['ProtoChain']
 
@@ -69,7 +69,7 @@ class ProtoChain(collections.abc.Sequence):
             data: Protocol chain list.
 
         """
-        from pcapkit.protocols.protocol import Protocol  # pylint: disable=import-outside-toplevel
+        from pcapkit.protocols.protocol import ProtocolBase as Protocol  # pylint: disable=import-outside-toplevel
 
         temp_data = []
         for proto in data:
@@ -105,7 +105,7 @@ class ProtoChain(collections.abc.Sequence):
             stop += len(self)
 
         # prepare comparison values
-        from pcapkit.protocols.protocol import Protocol  # pylint: disable=import-outside-toplevel
+        from pcapkit.protocols.protocol import ProtocolBase as Protocol  # pylint: disable=import-outside-toplevel
         comp = Protocol.expand_comp(value)
 
         pool = self.__data__[start:stop]
@@ -124,7 +124,7 @@ class ProtoChain(collections.abc.Sequence):
 
         """
         # prepare comparison values
-        from pcapkit.protocols.protocol import Protocol  # pylint: disable=import-outside-toplevel
+        from pcapkit.protocols.protocol import ProtocolBase as Protocol  # pylint: disable=import-outside-toplevel
         comp = Protocol.expand_comp(value)
 
         cnt = 0
@@ -150,7 +150,7 @@ class ProtoChain(collections.abc.Sequence):
             basis: Original protocol chain as base stacks.
 
         """
-        from pcapkit.protocols.protocol import Protocol  # pylint: disable=import-outside-toplevel
+        from pcapkit.protocols.protocol import ProtocolBase as Protocol  # pylint: disable=import-outside-toplevel
         if isinstance(proto, Protocol):
             if alias is None:
                 alias = proto.alias
@@ -196,7 +196,7 @@ class ProtoChain(collections.abc.Sequence):
             Whether ``name`` is in the chain.
 
         """
-        from pcapkit.protocols.protocol import Protocol  # pylint: disable=import-outside-toplevel
+        from pcapkit.protocols.protocol import ProtocolBase as Protocol  # pylint: disable=import-outside-toplevel
         comp = Protocol.expand_comp(name)
 
         for alias, proto in self.__data__:




@@ -56,7 +56,7 @@ if TYPE_CHECKING:
     from pcapkit.foundation.traceflow.tcp import TCP as TCP_TraceFlow
     from pcapkit.protocols.misc.pcap.frame import Frame
     from pcapkit.protocols.misc.pcapng import PCAPNG
-    from pcapkit.protocols.protocol import Protocol
+    from pcapkit.protocols.protocol import ProtocolBase as Protocol
 
     Formats = Literal['pcap', 'json', 'tree', 'plist']
     Engines = Literal['default', 'pcapkit', 'dpkt', 'scapy', 'pyshark']




@@ -17,7 +17,7 @@ if TYPE_CHECKING:
     from typing_extensions import Literal, TypeAlias
 
     from pcapkit.const.reg.transtype import TransType
-    from pcapkit.protocols.protocol import Protocol
+    from pcapkit.protocols.protocol import ProtocolBase as Protocol
 
 AT = TypeVar('AT', 'IPv4Address', 'IPv6Address')
 




@@ -17,7 +17,7 @@ if TYPE_CHECKING:
 
     from typing_extensions import Literal, TypeAlias
 
-    from pcapkit.protocols.protocol import Protocol
+    from pcapkit.protocols.protocol import ProtocolBase as Protocol
 
 IPAddress = TypeVar('IPAddress', 'IPv4Address', 'IPv6Address')
 




@@ -24,7 +24,7 @@ if TYPE_CHECKING:
     from typing_extensions import Self
 
     from pcapkit.corekit.infoclass import Info
-    from pcapkit.protocols.protocol import Protocol
+    from pcapkit.protocols.protocol i