@@ -165,7 +165,6 @@ static RVecAnalRef *_collect_all_refs(RefManager *rm, const AdjacencyList *adj_l
 				RVecAnalRef_free (result);
 				return false;
 			}
-
 			ref->at = entry->key;
 			ref->addr = edge_entry->key;
 			ref->type = edge_entry->val;




@@ -11,7 +11,6 @@ static bool stm8_op(RArchSession *as, RAnalOp *op, RAnalOpMask mask) {
 	op->type = R_ANAL_OP_TYPE_ILL;
 	// op->type = R_ANAL_OP_TYPE_NOP;
 	op->mnemonic = stm8_disasm (op->addr, op->bytes, op->size, &op->type, &jump, &len);
-	//if (op->type == R_ANAL_OP_TYPE_MOV) {
 	if (op->mnemonic) {
 		const char *ox = strstr (op->mnemonic, " 0x");
 		if (ox) {




@@ -20,11 +20,10 @@ static int replace(int argc, const char *argv[], char *newstr) {
 		{ 2, "ld", "# = #", { 1, 2 } },  // ld a, 0x86  | a = [0x86]
 		{ 3, "ld", "# = # + #", { 3, 2, 1 } },
 		{ 0, "decw", "# --", { 1 } },
-		// { 1, "clr 0x", "[0x#] = 0", { 1 } },
 		{ 0, "clrw", "# = 0", { 1 } },
 		{ 1, "clr", "# = 0", { 1 } },
-		{ 0, "dec", "[#] --", { 1 } },
-		{ 0, "ret", "return;", {}},
+		{ 0, "dec", "# --", { 1 } },
+		{ 0, "ret", "return a;", {}},
 		{ 0, "iret", "return;", {}},
 		{ 2, "mov", "# = #", { 1, 2 } }, // MOVS are stores
 		{ 2, "mul", "# *= #", { 1, 2 } },
@@ -35,26 +34,21 @@ static int replace(int argc, const char *argv[], char *newstr) {
 		{ 2, "bcp", "res = # & (1 << #)", { 1, 2 } },
 		{ 1, "cpl", "complement (#)", { 1, 1 } },
 		{ 2, "and", "# |= #", { 1, 2 } },
-		// { 2, "dec", "# -= #", { 1, 2 } },
 		{ 1, "popw", "# = [sp] , sp += 2", { 1 } },
 		{ 1, "pop", "# = [sp] , sp += 1", { 1 } },
-		{ 1, "pushw", "sp -= 2, # = [sp]", { 1 } },
-		{ 1, "push", "sp -= 1, # = [sp]", { 1 } },
+		{ 1, "pushw", "sp -= 2, [sp] = #", { 1 } },
+		{ 1, "push", "sp -= 1, [sp] = #", { 1 } },
 		{ 1, "incw", "# ++", { 1 } },
-		{ 1, "inc", "[#] ++", { 1 } },
+		{ 1, "inc", "# ++", { 1 } },
 		{ 0, "subw", "# -= #", { 1, 2 } },
 		{ 0, "addw", "# += #", { 1, 2 } },
 		{ 2, "bset", "# |= (1 << #)", { 1, 2 } },
 		{ 2, "bres", "# &= ~(1 << #)", { 1, 2 } },
-		{ 0, "jrne", "if (res != 0) goto #", { 1 } },
-		{ 0, "jreq", "if (res == 0) goto #", { 1 } },
-		{ 0, "jrnc", "if (!carry(res)) goto #", { 1 } },
-		{ 0, "jrc", "if (carry(res)) goto #", { 1 } },
-		{ 0, "jra", "goto #", { 1 } },
 		{ 0, "jp", "goto #", { 1 } },
 		{ 1, "rrwa", "a >>= #", { 1 } },
 		{ 1, "rrc", "# = rotate_right(#, 1)", { 1, 1 } },
-	 	{ 1, "rlc", "# = rotate_left(#, 1)", { 1, 1 } }, // set carry bit and use proper C expr
+		{ 1, "rlc", "# = rotate_left(#, 1)", { 1, 1 } },
+		{ 1, "srl", "# = shift_right(#, 1)", { 1, 1 } },
 		{ 2, "xor", "# ^= #", { 1, 2 } },
 		{ 1, "rlwa", "a <<= #", { 1 } },
 		{ 1, "sra", "a >>= #", { 1 } },
@@ -66,21 +60,29 @@ static int replace(int argc, const char *argv[], char *newstr) {
 		{ 1, "jrslt", "if (res < 0) goto #", { 1 } },
 		{ 1, "jrugt", "if (res > 0) goto #", { 1 } }, // TODO: support signed vs unsigned
 		{ 1, "jrult", "if (res < 0) goto #", { 1 } }, // TODO: support signed vs unsigned
+		{ 1, "jrule", "if (res <= 0) goto #", { 1 } },
+		{ 0, "jrne", "if (res != 0) goto #", { 1 } },
+		{ 0, "jreq", "if (res == 0) goto #", { 1 } },
+		{ 0, "jrnc", "if (!carry(res)) goto #", { 1 } },
+		{ 0, "jrc", "if (carry(res)) goto #", { 1 } },
+		{ 0, "jra", "goto #", { 1 } },
 		{ 3, "btjt", "if (# & (1 << #)) goto #", { 1, 2, 3 } },
 		{ 3, "btjf", "if (!(# & (1 << #))) goto #", { 1, 2, 3 } },
 		{ 0, "sllw", "# <<= 1", { 1 } },
+		{ 1, "sll", "# <<= 1", { 1, 1 } },
+		{ 2, "exgw", "# <=> #", {1, 2}},
+		{ 2, "exg", "# <=> #", {1, 2}},
 		{ 0, "slaw", "# <<= 1", { 1 } },
-		{ 1, "jrule", "if (res <= 0) goto #", { 1 } },
 		{ 0, "srlw", "# >>= 1", { 1 } },
+		{ 0, "srl", "# >>= 1", { 1 } },
 		{ 0, "callr", "# ()", { 1 } },
 		{ 0, "callf", "# ()", { 1 } },
-		{ 0, "rvf", "res.overflow = 0", { 1 } },
+		{ 0, "rvf", "res.overflow = 0", { } },
 		{ 0, "sraw", "# >>= 1", { 1 } },
 		{ 2, "add", "# += #", { 1, 2 } },
 		{ 2, "sub", "# -= #", { 1, 2 } },
 		{ 1, "int", "goto #", { 1 } }, // goto int