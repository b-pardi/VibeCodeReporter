@@ -1,5 +1,6 @@
 package org.batfish.representation.juniper;
 
+import com.google.common.base.MoreObjects;
 import java.util.List;
 import java.util.Objects;
 import java.util.Optional;
@@ -151,4 +152,13 @@ public class CommunityMemberParseResult {
   public int hashCode() {
     return Objects.hash(_member, _warning);
   }
+
+  @Override
+  public String toString() {
+    return MoreObjects.toStringHelper(this)
+        .omitNullValues()
+        .add("member", _member)
+        .add("warning", _warning)
+        .toString();
+  }
 }




@@ -1,5 +1,6 @@
 package org.batfish.representation.juniper;
 
+import com.google.common.base.MoreObjects;
 import javax.annotation.Nonnull;
 import javax.annotation.ParametersAreNonnullByDefault;
 import org.batfish.datamodel.bgp.community.Community;
@@ -39,4 +40,9 @@ public final class LiteralCommunityMember implements CommunityMember {
   public int hashCode() {
     return _community.hashCode();
   }
+
+  @Override
+  public String toString() {
+    return MoreObjects.toStringHelper(this).add("community", _community).toString();
+  }
 }




@@ -2,6 +2,7 @@ package org.batfish.representation.juniper;
 
 import com.github.benmanes.caffeine.cache.Caffeine;
 import com.github.benmanes.caffeine.cache.LoadingCache;
+import com.google.common.base.MoreObjects;
 import com.google.common.collect.ImmutableList;
 import dk.brics.automaton.Automaton;
 import dk.brics.automaton.RegExp;
@@ -151,4 +152,9 @@ public final class RegexCommunityMember implements CommunityMember {
   public int hashCode() {
     return _regex.hashCode();
   }
+
+  @Override
+  public String toString() {
+    return MoreObjects.toStringHelper(this).add("regex", _regex).toString();
+  }
 }