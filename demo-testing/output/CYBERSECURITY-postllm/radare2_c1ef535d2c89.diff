@@ -1,4 +1,4 @@
-/* radare - LGPL - Copyright 2009-2023 - pancake */
+/* radare - LGPL - Copyright 2009-2024 - pancake */
 
 #include <r_core.h>
 #include <r_types_base.h>
@@ -3613,9 +3613,6 @@ R_API int r_core_config_init(RCore *core) {
 	SETBPREF ("asm.midcursor", "false", "cursor in visual disasm mode breaks the instruction");
 	SETICB ("asm.codealign", 0, &cb_asm_codealign, "only recognize as valid instructions aligned to this value");
 	// maybe rename to asm.cmt.calls
-	SETBPREF ("asm.calls", "true", "show callee function related info as comments in disasm");
-	SETBPREF ("asm.comments", "true", "show comments in disassembly view");
-	// R2-590 - rename to asm.cmt.user
 	SETBPREF ("asm.sub.jmp", "true", "always substitute jump, call and branch targets in disassembly");
 	SETBPREF ("asm.hints", "true", "disable all asm.hint* if false");
 	SETBPREF ("asm.hint.jmp", "false", "show jump hints [numbers] in disasm");
@@ -3760,6 +3757,8 @@ R_API int r_core_config_init(RCore *core) {
 	SETBPREF ("asm.describe", "false", "show opcode description");
 	SETPREF ("asm.highlight", "", "highlight current line");
 	SETBPREF ("asm.marks", "true", "show marks before the disassembly");
+	SETBPREF ("asm.comments", "true", "show comments in disassembly view (see 'e asm.cmt.')");
+	SETBPREF ("asm.cmt.calls", "true", "show callee function related info as comments in disasm");
 	SETBPREF ("asm.cmt.user", "false", "show user comments even if asm.comments is false");
 	SETBPREF ("asm.cmt.refs", "false", "show flag and comments from refs in disasm");
 	SETBPREF ("asm.cmt.patch", "false", "show patch comments in disasm");




@@ -164,7 +164,7 @@ typedef struct r_disasm_state_t {
 	bool show_reloff;
 	ut32 show_reloff_to;
 	bool show_comments;
-	bool show_usercomments;
+	bool show_cmt_user;
 	bool asm_hints;
 	bool asm_hint_jmp;
 	bool asm_hint_cdiv;
@@ -193,11 +193,11 @@ typedef struct r_disasm_state_t {
 	bool show_asciidot;
 	RStrEnc strenc;
 	int cursor;
-	int show_comment_right_default;
+	int show_cmt_right_default;
 	bool show_flag_in_bytes;
 	bool show_flag_in_offset;
 	int lbytes;
-	int show_comment_right;
+	int show_cmt_right;
 	int pre;
 	char *ocomment;
 	int linesopts;
@@ -509,7 +509,7 @@ static const char *get_section_name(RDisasmState *ds) {
 }
 
 static void ds_comment_align(RDisasmState *ds) {
-	if (ds->show_comment_right) {
+	if (ds->show_cmt_right) {
 		if (ds->show_color) {
 			r_cons_print (ds->pal_comment);
 		}
@@ -540,7 +540,7 @@ static void ds_comment_(RDisasmState *ds, bool align, bool nl, const char *forma
 			*nl = 0;
 		}
 		if (ds->show_comments) {
-			if (ds->show_comment_right && align) {
+			if (ds->show_cmt_right && align) {
 				ds_align_comment (ds);
 			}
 		}
@@ -559,14 +559,14 @@ static void ds_comment_(RDisasmState *ds, bool align, bool nl, const char *forma
 		if (!nl) {
 			break;
 		}
-		if (!ds->show_comment_right && nl) {
+		if (!ds->show_cmt_right && nl) {
 			r_cons_newline ();
 		}
 		first = false;
 		p = nl + 1;
 	}
 	free (s);
-	if (!ds->show_comment_right && nl) {
+	if (!ds->show_cmt_right && nl) {
 		ds_newline (ds);
 	}
 }
@@ -596,12 +596,12 @@ static void ds_comment_esil(RDisasmState *ds, bool up, bool end, const char *for
 	va_start (ap, format);
 
 	if (ds->show_comments && up) {
-		ds->show_comment_right ? ds_align_comment (ds) : ds_comment_lineup (ds);
+		ds->show_cmt_right ? ds_align_comment (ds) : ds_comment_lineup (ds);
 	}
 	r_cons_printf_list (format, ap);
 	va_end (ap);
 
-	if (end && ds->show_comments && !ds->show_comment_right) {
+	if (end && ds->show_comments && !ds->show_cmt_right) {
 		ds_newline (ds);
 	}
 }
@@ -636,7 +636,7 @@ static RDisasmState *ds_init(RCore *core) {
 	ds->addrbytes = core->io->addrbytes;
 	ds->strip = r_config_get (core->config, "asm.strip");
 	ds->cmtoken = r_config_get (core->config, "asm.cmt.token");
-	ds->pal_hint = core->cons->context->pal.jmp; // R2_590 pal.hint
+	ds->pal_hint = core->cons->context->pal.jmp;
 	ds->pal_comment = core->cons->context->pal.c