@@ -130,28 +130,33 @@ def ensure_ufw_port_allowed(port: int) -> bool:
         return False
 
 def cleanup_ufw_ports(ports: List[int]):
-    """Remove UFW rules for multiple ports - silent cleanup"""
-    removed = 0
-    failed = 0
-    for port in ports:
-        if port == 22:
-            continue  # NEVER remove SSH access
+    """Remove UFW rules for multiple ports silently, with a single progress bar."""
+    import sys
+
+    to_remove = [p for p in ports if p != 22]
+    total = len(to_remove)
+
+    if total == 0:
+        return
+
+    print("[*] Cleaning up UFW rules...")
+    for idx, port in enumerate(to_remove, 1):
         try:
-            result = subprocess.run(
+            subprocess.run(
                 ["sudo", "ufw", "delete", "allow", f"{port}/tcp"],
                 capture_output=True,
                 text=True,
                 check=False
             )
-            if result.returncode == 0:
-                removed += 1
-            else:
-                failed += 1
         except Exception:
-            failed += 1
+            pass
+
+        # progress bar
+        percent = int((idx / total) * 100)
+        sys.stdout.write(f"\r[*] Cleanup progress: {percent}%")
+        sys.stdout.flush()
 
-    if removed > 0 or failed > 0:
-        print(f"[*] Cleaned up UFW rules: {removed} removed, {failed} already removed")
+    sys.stdout.write("\n[*] UFW cleanup complete.\n")
 
 def check_ufw_status() -> str:
     """Check if UFW is active - returns simple status"""