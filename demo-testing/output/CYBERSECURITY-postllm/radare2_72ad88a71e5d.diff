@@ -796,6 +796,17 @@ static int opcall(RArchSession *a, ut8 *data, const Opcode *op) {
 				mod = 2;
 			}
 		}
+		const int reg0 = op->operands[0].regs[0];
+		if (reg0 == 8) { // call [rip+x]
+			mod = 2;
+			data[l++] = 0x15;
+			data[l++] = offset;
+			data[l++] = offset >> 8;
+			data[l++] = offset >> 16;
+			data[l++] = offset >> 24;
+			return l;
+		}
+		data[l++] = mod << 6 | 2 << 3 | op->operands[0].regs[0];
 		data[l++] = mod << 6 | 2 << 3 | op->operands[0].regs[0];
 		if (mod) {
 			data[l++] = offset;




@@ -38,23 +38,23 @@ R_API char *r_bin_demangle_rust(RBinFile *binfile, const char *sym, ut64 vaddr)
 	}
 
 	while ((len = strlen (in)) > 0) {
-		if (!(*in == '$' && (RS("$SP$", '@')
-				|| RS("$BP$", '*')
-				|| RS("$RF$", '&')
-				|| RS("$LT$", '<')
-				|| RS("$GT$", '>')
-				|| RS("$LP$", '(')
-				|| RS("$RP$", ')')
-				|| RS("$C$", ',')
+		if (!(*in == '$' && (RS ("$SP$", '@')
+				|| RS ("$BP$", '*')
+				|| RS ("$RF$", '&')
+				|| RS ("$LT$", '<')
+				|| RS ("$GT$", '>')
+				|| RS ("$LP$", '(')
+				|| RS ("$RP$", ')')
+				|| RS ("$C$", ',')
 				// maybe a good idea to replace all utf-sequences by regexp \$u[0-9a-f]{2}\$ or so
-				|| RS("$u20$", ' ')
-				|| RS("$u22$", '\"')
-				|| RS("$u27$", '\'')
-				|| RS("$u2b$", '+')
-				|| RS("$u3b$", ';')
-				|| RS("$u5b$", '[')
-				|| RS("$u5d$", ']')
-				|| RS("$u7e$", '~')))) {
+				|| RS ("$u20$", ' ')
+				|| RS ("$u22$", '\"')
+				|| RS ("$u27$", '\'')
+				|| RS ("$u2b$", '+')
+				|| RS ("$u3b$", ';')
+				|| RS ("$u5b$", '[')
+				|| RS ("$u5d$", ']')
+				|| RS ("$u7e$", '~')))) {
 			if (*in == '.') {
 				if (len > 0 && in[1] == '.') {
 					in += 2;
@@ -71,6 +71,5 @@ R_API char *r_bin_demangle_rust(RBinFile *binfile, const char *sym, ut64 vaddr)
 		}
 	}
 	*out = '\0';
-
 	return str;
 }