@@ -1,4 +1,3 @@
-#!/usr/bin/env python3
 # =============================================================================
 # VAPT Toolkit - Vulnerability Assessment and Penetration Testing Toolkit
 # =============================================================================
@@ -34,11 +33,10 @@
 #         state, federal, and international laws.
 #
 # =============================================================================
-
 #!/usr/bin/env python3
-from datetime import datetime
 import argparse
 import struct
+from datetime import datetime
 
 from scapy.all import (
     sniff,
@@ -47,6 +45,7 @@ from scapy.all import (
     DHCP,
     BOOTP,
     IP,
+    UDP,
 )
 
 BROADCAST_MAC = "ff:ff:ff:ff:ff:ff"
@@ -58,10 +57,18 @@ MULTICAST_MAC_LABELS = {
     "33:33:00:00:00:01": "IPV6-MCAST",
 }
 
+CAPWAP_PORTS = {5246, 5247}  # control, data
+LLDP_ETHERTYPE = 0x88CC
+CDP_ETHERTYPE = 0x2000
+
+# Fortinet OUI used in org-specific LLDP TLVs
+FORTINET_OUI = b"\x00\x09\x0f"
+
 
 def parse_args():
     p = argparse.ArgumentParser(
-        description="Listen for L2 broadcast / discovery traffic (DHCP, CDP, ARP, LLDP, STP, etc.)"
+        description="Listen for L2 broadcast / discovery traffic "
+                    "(DHCP, ARP, CDP, LLDP/FDP, CAPWAP, etc.)"
     )
     p.add_argument(
         "-i",
@@ -72,14 +79,28 @@ def parse_args():
     return p.parse_args()
 
 
+def is_capwap(pkt):
+    return UDP in pkt and (
+        pkt[UDP].sport in CAPWAP_PORTS or pkt[UDP].dport in CAPWAP_PORTS
+    )
+
+
 def is_interesting_l2(pkt):
     if Ether not in pkt:
         return False
-    dst = pkt[Ether].dst.lower()
+
+    eth = pkt[Ether]
+    dst = eth.dst.lower()
+
     if dst == BROADCAST_MAC:
         return True
     if dst in MULTICAST_MAC_LABELS:
         return True
+
+    # Always show CAPWAP even if not broadcast at L2
+    if is_capwap(pkt):
+        return True
+
     return False
 
 
@@ -89,14 +110,17 @@ def get_proto_label(pkt):
             return "ARP"
         if DHCP in pkt or BOOTP in pkt:
             return "DHCP"
+        if is_capwap(pkt):
+            return "CAPWAP"
 
         eth = pkt[Ether]
         dst = eth.dst.lower()
 
-        if dst == "01:00:0c:cc:cc:cc":
+        if getattr(eth, "type", None) == CDP_ETHERTYPE or dst == "01:00:0c:cc:cc:cc":
             return "CDP"
-        if eth.type == 0x88CC:
+        if getattr(eth, "type", None) == LLDP_ETHERTYPE:
             return "LLDP"
+
         if dst == BROADCAST_MAC:
             return "BCAST"
         if dst in MULTICAST_MAC_LABELS:
@@ -153,7 +177,7 @@ def arp_info(pkt):
     return f"ARP[{op} {a.psrc} -> {a.pdst}]"
 
 
-# ---------- CDP DECODER (custom, no Scapy dependency) ----------
+# ---------------- CDP DECODER ----------------
 
 CDP_TLV_TYPES = {
     0x0001: "device_id",
@@ -162,7 +186,7 @@ CDP_TLV_TYPES = {
     0x0004: "capabilities",
     0x0005: "software_version",
     0x0006: "platform",
-    0x0007: "ip_prefix",      # rarely used
+    0x0007: "ip_prefix",
     0x0009: "vtp_domain",
     0x000a: "native_vlan",
     0x000b: "duplex",
@@ -170,7 +194,6 @@ CDP_TLV_TYPES = {
     0x0011: "untrusted_port_cos",
 }
 
-
 CDP_CAP_BITS = {
     0x00000001: "Router",
     0x00000002: "Transparent-Bridge",
@@ -191,10 +214,6 @@ def _decode_cdp_caps(val_bytes):
 
 
 def _decode_cdp_addresses(val_bytes):
-    """
-    Decode CDP Addresses TLV (type 0x0002).
-    Returns list of textual addresses (mostly IPv4).
-    """
     addrs = []
     if len(val_bytes) < 4:
         return addrs
@@ -220,7 +239,6 @@ def _decode_cdp_addresses(val_bytes):
             addr_bytes = val_bytes[idx:idx + addr_len]
             idx += addr_len
 
-            # Heuristic: IPv4 addresses are 4 bytes
             if addr_len == 4:
                 addrs.append(".".join(str(b) for b in addr_bytes))
             else:
@@ -231,36 +249,23 @@ def _decode_cdp_addresses(val_bytes):
 
 
 def _find_cdp_payload_bytes(pkt):
-    """
-    Try to locate the start of the CDP header in the 