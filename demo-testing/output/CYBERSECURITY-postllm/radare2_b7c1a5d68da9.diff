@@ -790,7 +790,6 @@ static int cmd_undo(void *data, const char *input) {
 			r_core_cmd_help (data, help_msg_uname);
 		}
 		return 1;
-	default:
 	case '?': // "u?"
 		if (*input && input[1] == 'j') {
 			r_cons_cmd_help_json (help_msg_u);
@@ -798,6 +797,9 @@ static int cmd_undo(void *data, const char *input) {
 			r_core_cmd_help (data, help_msg_u);
 		}
 		return 1;
+	default:
+		r_core_return_invalid_command (core, "u", *input);
+		return 1;
 	}
 	return 0;
 }
@@ -1401,9 +1403,12 @@ static int cmd_yank(void *data, const char *input) {
 	case '\0': // "y"
 		r_core_yank_dump (core, 0, input[0]);
 		break;
-	default:
+	case '?':
 		r_core_cmd_help (core, help_msg_y);
 		break;
+	default:
+		r_core_return_invalid_command (core, "y", *input);
+		break;
 	}
 	return true;
 }
@@ -2737,6 +2742,9 @@ static int cmd_kuery(void *data, const char *input) {
 	case '?':
 		r_core_cmd_help (core, help_msg_k);
 		break;
+	default:
+		r_core_return_invalid_command (core, "f", *input);
+		break;
 	}
 
 	if (input[0] == '\0') {




@@ -1633,7 +1633,7 @@ static int cmd_cmp(void *data, const char *input) {
 		}
 		break;
 	default:
-		r_core_cmd_help (core, help_msg_c);
+		r_core_return_invalid_command (core, "c", *input);
 		break;
 	}
 	if (val != UT64_MAX) {




@@ -6187,9 +6187,11 @@ static int cmd_debug(void *data, const char *input) {
 		}
 		break;
 	case '?': // "d?"
-	default:
 		r_core_cmd_help (core, help_msg_d);
 		break;
+	default:
+		r_core_return_invalid_command (core, "d", *input);
+		break;
 	}
 
 	/* RDebug, specifically RDebug.execute() may incorrectly




@@ -1,4 +1,4 @@
-/* radare - LGPL - Copyright 2009-2023 - pancake */
+/* radare - LGPL - Copyright 2009-2024 - pancake */
 
 #if R_INCLUDE_BEGIN
 
@@ -282,6 +282,9 @@ static int cmd_egg(void *data, const char *input) {
 	case '?':
 		r_core_cmd_help (core, help_msg_g);
 		break;
+	default:
+		r_core_return_invalid_command (core, "g", *input);
+		break;
 	}
 	return true;
 }




@@ -419,13 +419,15 @@ static int cmd_eval(void *data, const char *input) {
 		r_config_list (core->config, NULL, 0);
 		break;
 	case '?': // "e?"
-	default:
 		switch (input[1]) {
 		case '\0': r_core_cmd_help (core, help_msg_e); break;
 		case '?': r_config_list (core->config, input + 2, 2); break;
 		default: r_config_list (core->config, input + 1, 3); break;
 		}
 		break;
+	default:
+		r_core_return_invalid_command (core, "e", *input);
+		break;
 	case 't': // "et"
 		if (input[1] == 'a') {
 			r_cons_printf ("%s\n", (r_num_rand (10) % 2)? "wen": "son");




@@ -666,6 +666,9 @@ static int cmd_plugins(void *data, const char *input) {
 		}
 		}
 		break;
+	default:
+		r_core_return_invalid_command (core, "L", *input);
+		break;
 	}
 	return 0;
 }




@@ -1473,6 +1473,9 @@ static int cmd_meta(void *data, const char *input) {
 			break;
 		}
 		break;
+	default:
+		r_core_return_invalid_command (core, "C", *input);
+		break;
 	}
 	return true;
 }




@@ -946,6 +946,9 @@ static int cmd_mount(void *data, const char *_input) {
 	case '?':
 		r_core_cmd_help (core, help_msg_m);
 		break;
+	default:
+		r_core_return_invalid_command (core, "m", *input);
+		break;
 	}
 	free (oinput);
 	return 0;




@@ -8644,11 +8644,14 @@ static int cmd_print(void *data, const char *input) {
 			r_list_free (list);
 		}
 		break;
+	case '?':
+		r_core_cmd_help (core, help_msg_p);
+		break;
 	default:
 		if (*input && input[1] == 'j') {
 			r_cons_cmd_help_json (help_msg_p);
 		} else {
-			r_core_cmd_help (core, help_msg_p);
+			r_core_return_invalid_command (core, "p", *input);
 		}
 		break;
 	}




@@ -414,9 +414,12 @@ static int cmd_project(void *data, const char *input) {
 	case 'j': // "Pj"
 		r_core_project_list (core, input[0]);
 		break;
-	default:
+	case '?':
 		r_core_cmd_help (core, help_msg_P);
 		break;
+	default:
+		r_core_return_invalid_command (core, "P", *input);
+		break;
 	}
 	free (str);
 	return 0;




@@ -767,7 +767,7 @@ static bool print_link_readable_cb(void *p, const char *k, const char *v) {
 