@@ -650,17 +650,7 @@ lb6_to_lb4_service(const struct lb6_service *svc __maybe_unused)
 static __always_inline
 struct lb6_service *__lb6_lookup_service(struct lb6_key *key)
 {
-	struct lb6_service *svc;
-
-	svc = map_lookup_elem(&cilium_lb6_services_v2, key);
-
-	/* If there are no elements for a specific protocol, check for ANY entries. */
-	if (!svc && key->proto != IPPROTO_ANY) {
-		key->proto = IPPROTO_ANY;
-		svc = map_lookup_elem(&cilium_lb6_services_v2, key);
-	}
-
-	return svc;
+	return map_lookup_elem(&cilium_lb6_services_v2, key);
 }
 
 static __always_inline
@@ -1372,17 +1362,7 @@ lb4_to_lb6_service(const struct lb4_service *svc __maybe_unused)
 static __always_inline
 struct lb4_service *__lb4_lookup_service(struct lb4_key *key)
 {
-	struct lb4_service *svc;
-
-	svc = map_lookup_elem(&cilium_lb4_services_v2, key);
-
-	/* If there are no elements for a specific protocol, check for ANY entries. */
-	if (!svc && key->proto != IPPROTO_ANY) {
-		key->proto = IPPROTO_ANY;
-		svc = map_lookup_elem(&cilium_lb4_services_v2, key);
-	}
-
-	return svc;
+	return map_lookup_elem(&cilium_lb4_services_v2, key);
 }
 
 static __always_inline




@@ -11,6 +11,7 @@
 #undef ENABLE_HEALTH_CHECK
 #define ENABLE_SOCKET_LB_HOST_ONLY 1
 
+#define SERVICE_PROTO IPPROTO_TCP
 #define DST_PORT 6000
 #define DST_PORT_HOSTNS 6001
 #define BACKEND_PORT 7000
@@ -32,10 +33,11 @@ int my_get_netns_cookie(__maybe_unused const struct bpf_sock_addr *addr)
 #include "bpf_sock.c"
 #include "lib/common.h"
 
-#define SVC_KEY_VALUE(_port, _beslot, _beid, _scope) { \
+#define SVC_KEY_VALUE(_port, _proto, _beslot, _beid, _scope) { \
 	.key = { \
 		.address = v4_svc_one, \
 		.dport = (_port), \
+		.proto = (_proto), \
 		.scope = (_scope), \
 		.backend_slot = (_beslot) \
 	}, \
@@ -47,12 +49,12 @@ int my_get_netns_cookie(__maybe_unused const struct bpf_sock_addr *addr)
 	} \
 }
 
-#define BE_KEY_VALUE(_beid, _beip) { \
+#define BE_KEY_VALUE(_beid, _beip, _beproto) { \
 	.key = (_beid), \
 	.value = { \
 		.address = (_beip), \
 		.port = BACKEND_PORT, \
-		.proto = IPPROTO_TCP \
+		.proto = (_beproto), \
 	}, \
 }
 
@@ -64,16 +66,16 @@ int test1_check(__maybe_unused struct xdp_md *ctx)
 	struct bpf_sock_addr addr = {
 		.user_port = DST_PORT,
 		.user_ip4 = v4_svc_one,
-		.protocol = IPPROTO_TCP,
+		.protocol = SERVICE_PROTO,
 	};
 	struct { __u32 key; struct lb4_backend value; } backends[] = {
-		BE_KEY_VALUE(1, v4_pod_one),
+		BE_KEY_VALUE(1, v4_pod_one, SERVICE_PROTO),
 	};
 	struct { struct lb4_key key; struct lb4_service value; } services[] = {
-		SVC_KEY_VALUE(DST_PORT, 0, 0, LB_LOOKUP_SCOPE_EXT),
-		SVC_KEY_VALUE(DST_PORT, 1, 1, LB_LOOKUP_SCOPE_EXT),
-		SVC_KEY_VALUE(DST_PORT_HOSTNS, 0, 0, LB_LOOKUP_SCOPE_EXT),
-		SVC_KEY_VALUE(DST_PORT_HOSTNS, 1, 1, LB_LOOKUP_SCOPE_EXT),
+		SVC_KEY_VALUE(DST_PORT, SERVICE_PROTO, 0, 0, LB_LOOKUP_SCOPE_EXT),
+		SVC_KEY_VALUE(DST_PORT, SERVICE_PROTO, 1, 1, LB_LOOKUP_SCOPE_EXT),
+		SVC_KEY_VALUE(DST_PORT_HOSTNS, SERVICE_PROTO, 0, 0, LB_LOOKUP_SCOPE_EXT),
+		SVC_KEY_VALUE(DST_PORT_HOSTNS, SERVICE_PROTO, 1, 1, LB_LOOKUP_SCOPE_EXT),
 	};
 
 	/* Insert the service and backend map values */




@@ -19,7 +19,7 @@
 
 #include "lib/lb.h"
 
-/* Service was created with L4 proto differentiation: */
+/* TCP Service, single-scope */
 CHECK("tc", "lb4_tcp_single_scope")
 int test_lb4_tcp_single_scope(__maybe_unused struct __ctx_buff *ctx)
 {
@@ -44,7 +44,7 @@ int test_lb4_tcp_single_scope(__maybe_unused struct __ctx_buff *ctx)
 	test_finish();
 }
 
-/* Dual-scope Service was created with L4 proto differentiation: */
+/* TCP Service, dual-scope */
 CHECK("tc", "lb4_tcp_dual_scope")
 int test_lb4_tcp_dual_scope(__maybe_unused struct __ctx_buff *ctx)
 {
@@ -70,83 +70,103 @@ int test_lb4_tcp_dual_scope(__maybe_unused struct __ctx_buff *ctx)
 	test_finish();
 }
 
-/* Service was created without L4 proto differentiation: */
-CHECK("tc", "lb4_any_proto_single_scope")
-int test_lb4_any_proto_single_scope(__maybe_unused struct __ctx_buff *ctx)