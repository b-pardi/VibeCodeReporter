@@ -6,7 +6,7 @@
 its formatted output dumper.
 
     There is a project called
-    [`jspcapy](https://github.com/JarryShaw/jspcapy)
+    [`jspcapy`](https://github.com/JarryShaw/jspcapy)
     works on `pcapkit`, which is a command line tool for
     PCAP extraction.
 
@@ -46,6 +46,9 @@ different sections.
  - CoreKit (`pcapkit.corekit`)
     core utilities for `pcapkit` implementation
 
+ - ToolKit (`pcapkit.toolkit`)
+    utility tools for `pcapkit` implementation
+
  - DumpKit (`pcapkit.dumpkit`)
     dump utilities for `pcapkit` implementation
 
@@ -53,6 +56,9 @@ different sections.
 # Interface
 from pcapkit.interface import *
 
+# ToolKit
+from pcapkit.toolkit import *
+
 # Protocols
 from pcapkit.protocols.raw import Raw
 from pcapkit.protocols.link.arp import ARP




@@ -14,6 +14,7 @@ from pcapkit.interface import *
 from pcapkit.ipsuite import *
 from pcapkit.protocols import *
 from pcapkit.reassembly import *
+from pcapkit.toolkit import *
 from pcapkit.utilities import *
 
 
@@ -58,6 +59,17 @@ __all__ = [
     'IPv4_Reassembly', 'IPv6_Reassembly',                   # IP Reassembly
     'TCP_Reassembly',                                       # TCP Reassembly
 
+    # pcapkit.toolkit
+    'ipv4_reassembly', 'ipv6_reassembly', 'tcp_reassembly', 'tcp_traceflow',
+                                                            # default engine
+    'dpkt_ipv6_hdr_len', 'dpkt_packet2chain', 'dpkt_packet2dict',
+    'dpkt_ipv4_reassembly', 'dpkt_ipv6_reassembly', 'dpkt_tcp_reassembly', 'dpkt_tcp_traceflow',
+                                                            # DPKT engine
+    'pyshark_packet2dict', 'pyshark_tcp_traceflow',         # PyShark engine
+    'scapy_packet2chain', 'scapy_packet2dict',
+    'scapy_ipv4_reassembly', 'scapy_ipv6_reassembly', 'scapy_tcp_reassembly', 'scapy_tcp_traceflow',
+                                                            # Scapy engine
+
     # pcapkit.utilities
     'beholder_ng', 'seekset_ng',                            # Decorators
 ]




@@ -46,6 +46,13 @@ from pcapkit.utilities.warnings import FormatWarning, EngineWarning, \
 # from pcapkit.reassembly.ipv4 import IPv4_Reassembly
 # from pcapkit.reassembly.ipv6 import IPv6_Reassembly
 # from pcapkit.reassembly.tcp import TCP_Reassembly
+# from pcapkit.toolkit.default import ipv4_reassembly, ipv6_reassembly, \
+#         tcp_reassembly, tcp_traceflow
+# from pcapkit.toolkit.dpkt import packet2chain, packet2dict, \
+#         ipv4_reassembly, ipv6_reassembly, tcp_reassembly, tcp_traceflow
+# from pcapkit.toolkit.pyshark import packet2dict, tcp_traceflow
+# from pcapkit.toolkit.scapy import packet2chain, packet2dict, \
+#         ipv4_reassembly, ipv6_reassembly, tcp_reassembly, tcp_traceflow
 ###############################################################################
 
 
@@ -350,7 +357,7 @@ class Extractor:
         if self._flag_a:
             while True:
                 try:
-                    self._read_frame_hq()
+                    self._read_frame()
                 except (EOFError, StopIteration):
                     # quit when EOF
                     break
@@ -497,7 +504,7 @@ class Extractor:
 
     def __next__(self):
         try:
-            return self._read_frame_hq()
+            return self._read_frame()
         except (EOFError, StopIteration):
             self._cleanup()
             raise StopIteration
@@ -505,7 +512,7 @@ class Extractor:
     def __call__(self):
         if not self._flag_a:
             try:
-                return self._read_frame_hq()
+                return self._read_frame()
             except (EOFError, StopIteration) as error:
                 self._cleanup()
                 raise error from None
@@ -560,7 +567,7 @@ class Extractor:
         self._ifile.close()
         self._flag_e = True
 
-    def _read_frame_hq(self):
+    def _read_frame(self):
         """Headquarters for frame reader."""
         if self._exeng == 'scapy':
             return self._scapy_read_fram