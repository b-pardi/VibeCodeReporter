@@ -2996,6 +2996,35 @@ class TestCustomFieldVulnerability(ReadWriteAPITests):
         assert cmd_obj.object_id == res.json['_id']
         assert res.json['tool'] == command.tool
 
+    @pytest.mark.parametrize('refs', [
+        ('cve', 'CVE-2017-0002'),
+        ('owasp', 'https://www.owasp.org/index.php/XSS_%28Cross_Site_Scripting%29_Prevention_Cheat_Sheet'),
+        ('cwe', 'CWE-135'),
+        ('cvss', 'CVSS v2 Vector(AV:A/AC:M/Au:S/C:P/I:P/A:N)'),
+    ])
+    def test_vuln_with_refs(self, host_with_hostnames, test_client, session, refs):
+        ref_name, ref_example = refs
+        raw_data_vuln = _create_post_data_vulnerability(
+            name='New vuln 1',
+            vuln_type='Vulnerability',
+            parent_id=host_with_hostnames.id,
+            parent_type='Host',
+            refs=[ref_example],
+            policyviolations=[],
+            description='helloworld 1',
+            severity='low',
+        )
+        ws_name = host_with_hostnames.workspace.name
+
+        post_response = test_client.post(self.check_url(f'/v2/ws/{ws_name}/vulns/'), data=raw_data_vuln)
+        vuln_1_id = post_response.json['obj_id']
+        get_response = test_client.get(self.check_url(f'/v2/ws/{ws_name}/vulns/{vuln_1_id}/'))
+
+        assert get_response.status_code == 200
+        assert ref_name in get_response.json
+        assert 1 == len(get_response.json[ref_name])
+        assert ref_example == get_response.json[ref_name][0]
+
 
 class TestCustomFieldVulnerabilityV3(TestCustomFieldVulnerability, PatchableTestsMixin):
     view_class = VulnerabilityV3View