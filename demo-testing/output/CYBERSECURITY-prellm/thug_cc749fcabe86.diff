@@ -579,3 +579,13 @@ class TestMiscSamplesChrome(object):
                     'rows: 25']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLDocument(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLDocument.html")
+        expected = ['disabled: false',
+                    'head: [object HTMLHeadElement]',
+                    'referrer: ',
+                    'URL: about:blank',
+                    'Alert Text: Hello, world']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -544,3 +544,13 @@ class TestMiscSamplesFirefox(object):
                     'rows: 25']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLDocument(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLDocument.html")
+        expected = ['disabled: false',
+                    'head: [object HTMLHeadElement]',
+                    'referrer: ',
+                    'URL: about:blank',
+                    'Alert Text: Hello, world']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -718,3 +718,13 @@ class TestMiscSamplesIE(object):
                     'rows: 25']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLDocument(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLDocument.html")
+        expected = ['disabled: false',
+                    'head: [object HTMLHeadElement]',
+                    'referrer: ',
+                    'URL: about:blank',
+                    'Alert Text: Hello, world']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -686,3 +686,13 @@ class TestMiscSamplesIE(object):
                     'rows: 25']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLDocument(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLDocument.html")
+        expected = ['disabled: false',
+                    'head: [object HTMLHeadElement]',
+                    'referrer: ',
+                    'URL: about:blank',
+                    'Alert Text: Hello, world']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -720,3 +720,13 @@ class TestMiscSamplesIE(object):
                     'rows: 25']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLDocument(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLDocument.html")
+        expected = ['disabled: false',
+                    'head: [object HTMLHeadElement]',
+                    'referrer: ',
+                    'URL: about:blank',
+                    'Alert Text: Hello, world']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -651,3 +651,13 @@ class TestMiscSamplesIE(object):
                     'rows: 25']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLDocument(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLDocument.html")
+        expected = ['disabled: false',
+                    'head: [object HTMLHeadElement]',
+                    'referrer: ',
+                    'URL: about:blank',
+                    'Alert Text: Hello, world']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -833,3 +833,13 @@ class TestMiscSamplesIE(object):
                     'rows: 25']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLDocument(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLDocument.html")
+        expected = ['disabled: false',
+                    'head: [object HTMLHeadElement]',
+                    'referrer: ',
+                    'URL: about:blank',
+                    'Alert Text: Hello, world']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -560,3 +560,13 @@ class TestMiscSamplesChrome(object):
                     'rows: 25']
 
         self.do_perform_test(caplog, sample, expected)