@@ -26,6 +26,7 @@ public final class BgpActivePeerConfig extends BgpPeerConfig {
       @JsonProperty(PROP_AUTHENTICATION_SETTINGS) @Nullable
           BgpAuthenticationSettings authenticationSettings,
       @JsonProperty(PROP_CLUSTER_ID) @Nullable Long clusterId,
+      @JsonProperty(PROP_CONFEDERATION_AS) @Nullable Long confederation,
       @JsonProperty(PROP_DEFAULT_METRIC) int defaultMetric,
       @JsonProperty(PROP_DESCRIPTION) @Nullable String description,
       @JsonProperty(PROP_EBGP_MULTIHOP) boolean ebgpMultihop,
@@ -43,6 +44,7 @@ public final class BgpActivePeerConfig extends BgpPeerConfig {
         appliedRibGroup,
         authenticationSettings,
         clusterId,
+        confederation,
         defaultMetric,
         description,
         ebgpMultihop,
@@ -61,6 +63,7 @@ public final class BgpActivePeerConfig extends BgpPeerConfig {
       @Nullable RibGroup appliedRibGroup,
       @Nullable BgpAuthenticationSettings authenticationSettings,
       @Nullable Long clusterId,
+      @Nullable Long confederation,
       int defaultMetric,
       @Nullable String description,
       boolean ebgpMultihop,
@@ -77,6 +80,7 @@ public final class BgpActivePeerConfig extends BgpPeerConfig {
         appliedRibGroup,
         authenticationSettings,
         clusterId,
+        confederation,
         defaultMetric,
         description,
         ebgpMultihop,
@@ -146,6 +150,7 @@ public final class BgpActivePeerConfig extends BgpPeerConfig {
               _appliedRibGroup,
               _authenticationSettings,
               _clusterId,
+              _confederation,
               _defaultMetric,
               _description,
               _ebgpMultihop,




@@ -28,6 +28,7 @@ public final class BgpPassivePeerConfig extends BgpPeerConfig {
       @JsonProperty(PROP_AUTHENTICATION_SETTINGS) @Nullable
           BgpAuthenticationSettings authenticationSettings,
       @JsonProperty(PROP_CLUSTER_ID) @Nullable Long clusterId,
+      @Nullable @JsonProperty(PROP_CONFEDERATION_AS) Long confederation,
       @JsonProperty(PROP_DEFAULT_METRIC) int defaultMetric,
       @JsonProperty(PROP_DESCRIPTION) @Nullable String description,
       @JsonProperty(PROP_EBGP_MULTIHOP) boolean ebgpMultihop,
@@ -45,6 +46,7 @@ public final class BgpPassivePeerConfig extends BgpPeerConfig {
         appliedRibGroup,
         authenticationSettings,
         clusterId,
+        confederation,
         defaultMetric,
         description,
         ebgpMultihop,
@@ -63,6 +65,7 @@ public final class BgpPassivePeerConfig extends BgpPeerConfig {
       @Nullable RibGroup appliedRibGroup,
       @Nullable BgpAuthenticationSettings authenticationSettings,
       @Nullable Long clusterId,
+      @Nullable Long confederation,
       int defaultMetric,
       @Nullable String description,
       boolean ebgpMultihop,
@@ -79,6 +82,7 @@ public final class BgpPassivePeerConfig extends BgpPeerConfig {
         appliedRibGroup,
         authenticationSettings,
         clusterId,
+        confederation,
         defaultMetric,
         description,
         ebgpMultihop,
@@ -138,6 +142,7 @@ public final class BgpPassivePeerConfig extends BgpPeerConfig {
               _appliedRibGroup,
               _authenticationSettings,
               _clusterId,
+              _confederation,
               _defaultMetric,
               _description,
               _ebgpMultihop,




@@ -31,6 +31,7 @@ public abstract class BgpPeerConfig implements Serializable {
   static final String PROP_APPLIED_RIB_GROUP = "appliedRibGroup";
   static final String PROP_AUTHENTICATION_SETTINGS = "authenticationSettings";
   static final String PROP_CLUSTER_ID = "clusterId";
+  static final String PROP_CONFEDERATION_AS = "confederationAs";
   static final String PROP_DEFAULT_METRIC = "defaultMetric";
   static final String PROP_DESCRIPTION = "description";
   static final String PROP_EBGP_MULTIHOP = "ebgpMultihop";
@@ -47,6 +48,8 @@ public abstract class BgpPeerConfig implements Se