@@ -54,6 +54,8 @@ def generate_regex_from_human(*values):
     values = [re.escape(value) for value in values]
     newvalues = []
     for value in values:
+        if not value:
+            continue
         match = REGEX_FROM_HUMAN.search(value)
         i = 0
         while match:
@@ -66,11 +68,11 @@ def generate_regex_from_human(*values):
             match = REGEX_FROM_HUMAN.search(value)
             i += 1
         newvalues.append(value.lower())
-    retval = "|".join(newvalues)
-    if len(values) > 1:
-        return "(%s)" % retval
-    else:
-        return retval
+
+    if not newvalues:
+        raise ValueError("No or only empty strings given. Can't generate a regular expression.")
+
+    return "^(%s)$" % "|".join(newvalues)
 
 
 def download(url: str=URL, add_default=False, params=None, include_malpedia=False,
@@ -91,11 +93,14 @@ def download(url: str=URL, add_default=False, params=None, include_malpedia=Fals
         download.raise_for_status()
         for actor in download.json()['values']:
             names = [actor["value"]] + actor.get("meta", {}).get("synonyms", [])
-            identifier = "%s-generic" % IDENTIFIER_FROM_HUMAN.sub("-",
-                                                                  actor["value"].lower())
+            identifier = ("%s-generic"
+                          "" % IDENTIFIER_FROM_HUMAN.sub("-",
+                                                        actor["value"].lower()))
+            rule_name = "misp-threat-actors-%s" % identifier
 
             rules.append(generate_rule(generate_regex_from_human(*names),
-                                       identifier))
+                                       identifier,
+                                       name=rule_name))
 
     if add_default:
         rules.append(generate_rule(".*", "{msg[malware.name]}", name="default"))