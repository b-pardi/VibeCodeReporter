@@ -18,6 +18,7 @@
 #  Author: Mauro Soria
 
 import sys
+
 if sys.version_info < (3, 0):
     sys.stdout.write("Sorry, dirsearch requires Python 3.x\n")
     sys.exit(1)
@@ -26,6 +27,7 @@ from lib.core import ArgumentParser
 from lib.controller import *
 from lib.output import *
 
+
 class Program(object):
     def __init__(self):
         self.script_path = (os.path.dirname(os.path.realpath(__file__)))




@@ -16,5 +16,6 @@
 #
 #  Author: Mauro Soria
 
+
 class RequestException(Exception):
     pass




@@ -16,22 +16,21 @@
 #
 #  Author: Mauro Soria
 
+import http.client
 import random
-import urllib.parse
 import socket
-import http.client
-import urllib.request
-import urllib.parse
-import urllib.error
 import time
+import urllib.error
+import urllib.parse
+import urllib.parse
+import urllib.request
 
 import thirdparty.requests as requests
-from .Response import *
 from .RequestException import *
+from .Response import *
 
 
 class Requester(object):
-
     headers = {
         'User-agent': 'Mozilla/5.0 (Windows NT 6.1) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/28.0.1468.0 Safari/537.36',
         'Accept-Language': 'en-us',
@@ -39,13 +38,16 @@ class Requester(object):
         'Keep-Alive': '300',
         'Connection': 'keep-alive',
         'Cache-Control': 'max-age=0',
-        }
+    }
+
+    def __init__(self, url, cookie=None, useragent=None,
+                 maxPool=1, maxRetries=5, delay=0, timeout=30,
+                 ip=None, proxy=None, redirect=False, requestByHostname=False):
 
-    def __init__(self, url, cookie=None, useragent=None, maxPool=1, maxRetries=5, delay=0, timeout=30, ip=None, proxy=None,
-                 redirect=False, requestByHostname=False):
         # if no backslash, append one
         if not url.endswith('/'):
             url = url + '/'
+
         parsed = urllib.parse.urlparse(url)
         self.basePath = parsed.path
 
@@ -53,9 +55,12 @@ class Requester(object):
         if parsed.scheme != 'http' and parsed.scheme != 'https':
             parsed = urllib.parse.urlparse('http://' + url)
             self.basePath = parsed.path
+
         self.protocol = parsed.scheme
+
         if self.protocol != 'http' and self.protocol != 'https':
             self.protocol = 'http'
+
         self.host = parsed.netloc.split(':')[0]
 
         # resolve DNS to decrease overhead
@@ -66,6 +71,7 @@ class Requester(object):
                 self.ip = socket.gethostbyname(self.host)
             except socket.gaierror:
                 raise RequestException({'message': "Couldn't resolve DNS"})
+
         self.headers['Host'] = self.host
 
         # If no port specified, set default (80, 443)
@@ -77,8 +83,10 @@ class Requester(object):
         # Set cookie and user-agent headers
         if cookie is not None:
             self.setHeader('Cookie', cookie)
+
         if useragent is not None:
             self.setHeader('User-agent', useragent)
+
         self.maxRetries = maxRetries
         self.maxPool = maxPool
         self.delay = delay
@@ -103,52 +111,80 @@ class Requester(object):
         i = 0
         proxy = None
         result = None
+
         while i <= self.maxRetries:
+
             try:
                 if self.proxy is not None:
-                    proxy = {"https" : self.proxy, "http" : self.proxy}
+                    proxy = {"https": self.proxy, "http": self.proxy}
+
                 if self.requestByHostname:
                     url = "{0}://{1}:{2}".format(self.protocol, self.host, self.port)
+
                 else:
                     url = "{0}://{1}:{2}".format(self.protocol, self.ip, self.port)
+
                 url = urllib.parse.urljoin(url, self.basePath)
 
                 # Joining with concatenation because a urljoin bug with "::"
                 if not url.endswith('/'):
                     url += "/"
+
                 if path.startswith('/'):
                     path = path[1:]
+
                 url += path
 
                 headers = dict(self.h