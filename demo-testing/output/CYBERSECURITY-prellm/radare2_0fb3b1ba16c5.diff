@@ -4,6 +4,10 @@
 
 #include <r_util/r_strbuf.h>
 
+#ifdef __cplusplus
+extern "C" {
+#endif
+
 typedef struct pj_t {
 	RStrBuf sb;
 	bool is_first;
@@ -49,5 +53,10 @@ R_API PJ *pj_d(PJ *j, double d);
 R_API PJ *pj_f(PJ *j, float d);
 R_API PJ *pj_i(PJ *j, int d);
 R_API PJ *pj_j(PJ *j, const char *k);
+
+#ifdef __cplusplus
+}
+#endif
+
 #endif
 




@@ -3,6 +3,10 @@
 
 #include <r_util.h>
 
+#ifdef __cplusplus
+extern "C" {
+#endif
+
 typedef struct {
 	const char *name;
 	RListComparator cmp;
@@ -86,4 +90,8 @@ R_API void r_table_format(RTable *t, int nth, RTableColumnType *type);
 R_API ut64 r_table_reduce(RTable *t, int nth);
 R_API void r_table_columns(RTable *t, RList *cols); // const char *name, ...);
 
+#ifdef __cplusplus
+}
+#endif
+
 #endif