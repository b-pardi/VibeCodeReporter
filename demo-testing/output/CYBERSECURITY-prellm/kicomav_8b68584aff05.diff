@@ -3,6 +3,7 @@
 
 
 import hashlib
+import zlib
 from ctypes import c_ushort
 
 
@@ -16,6 +17,16 @@ def md5(data):
     return hashlib.md5(data).hexdigest()
 
 
+# -------------------------------------------------------------------------
+# crc32(data)
+# 주어진 데이터에 대해 CRC32 해시를 구한다.
+# 입력값 : data - 데이터
+# 리턴값 : CRC32 해시 문자열
+# -------------------------------------------------------------------------
+def crc32(data):
+    return '%08x' % (zlib.crc32(data) & 0xffffffff)
+
+
 # -------------------------------------------------------------------------
 # sha256(data)
 # 주어진 데이터에 대해 SHA256 해시를 구한다.