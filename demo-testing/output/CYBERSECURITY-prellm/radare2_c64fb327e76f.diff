@@ -140,6 +140,8 @@ struct MACH0_(obj_t) {
 	Sdb *kv;
 	int has_crypto;
 	int has_canary;
+	int has_retguard;
+	int has_sanitizers;
 	int dbg_info;
 	const char *lang;
 	int uuidn;




@@ -398,6 +398,8 @@ static RList* imports(RBinFile *bf) {
 		return ret;
 	}
 	bin->has_canary = false;
+	bin->has_retguard = -1;
+	bin->has_sanitizers = false;
 	for (i = 0; !imports[i].last; i++) {
 		if (!(ptr = R_NEW0 (RBinImport))) {
 			break;
@@ -427,6 +429,10 @@ static RList* imports(RBinFile *bf) {
 		if (!strcmp (name, "__stack_chk_fail") ) {
 			bin->has_canary = true;
 		}
+		if (!strcmp (name, "__asan_init") ||
+                   !strcmp (name, "__tsan_init")) {
+			bin->has_sanitizers = true;
+		}
 		r_list_append (ret, ptr);
 	}
 	free (imports);
@@ -518,6 +524,8 @@ static RBinInfo* info(RBinFile *bf) {
 	}
 	if (bin) {
 		ret->has_canary = bin->has_canary;
+		ret->has_retguard = -1;
+		ret->has_sanitizers = bin->has_sanitizers;
 		ret->dbg_info = bin->dbg_info;
 		ret->lang = bin->lang;
 	}




@@ -728,6 +728,7 @@ static int bin_info(RCore *r, int mode) {
 		if (info->has_retguard != -1) {
 			pair_bool ("retguard", info->has_retguard, mode, false);
 		}
+		pair_bool ("sanitiz", info->has_sanitizers, mode, false);
 		pair_str ("class", info->bclass, mode, false);
 		if (info->actual_checksum) {
 			/* computed checksum */




@@ -204,6 +204,7 @@ typedef struct r_bin_info_t {
 	int has_pi; // pic/pie
 	int has_canary;
 	int has_retguard;
+	int has_sanitizers;
 	int has_crypto;
 	int has_nx;
 	int big_endian;