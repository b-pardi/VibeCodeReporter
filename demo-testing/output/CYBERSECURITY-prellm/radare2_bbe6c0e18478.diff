@@ -40,10 +40,11 @@ R_API int r_cons_pipe_open(const char *file, int fdn, int append) {
 	}
 	if (backup_fd != -1) {
 		close (backup_fd);
+		// already set in __dupDescriptor // backup_fd = -1;
 	}
 	backup_fdn = fdn;
 	if (!__dupDescriptor (fd, fdn)) {
-		eprintf ("Cannot dup stdout to %d\n", backup_fd);
+		eprintf ("Cannot dup stdout to %d\n", fdn);
 		free (targetFile);
 		return -1;
 	}




@@ -4553,39 +4553,43 @@ R_API int r_core_flush(RCore *core, const char *cmd) {
 }
 
 R_API char *r_core_cmd_str_pipe(RCore *core, const char *cmd) {
-	char *s, *tmp = NULL;
-	if (r_sandbox_enable (0)) {
-		char *p = (*cmd != '"')? strchr (cmd, '|'): NULL;
-		if (p) {
-			// This code works but its pretty ugly as its a workaround to
-			// make the webserver work as expected, this was broken some
-			// weeks. let's use this hackaround for now
-			char *c = strdup (cmd);
-			c[p - cmd] = 0;
-			if (!strcmp (p + 1, "H")) {
-				char *res = r_core_cmd_str (core, c);
-				free (c);
-				char *hres = r_cons_html_filter (res, NULL);
-				free (res);
-				return hres;
-			} else {
-				int sh = r_config_get_i (core->config, "scr.color");
-				r_config_set_i (core->config, "scr.color", 0);
-				char *ret = r_core_cmd_str (core, c);
-				r_config_set_i (core->config, "scr.color", sh);
-				free (c);
-				return ret;
-			}
-		}
+	char *tmp = NULL;
+	char *p = (*cmd != '"')? strchr (cmd, '|'): NULL;
+	if (!p && *cmd != '!' && *cmd != '.') {
 		return r_core_cmd_str (core, cmd);
 	}
+#if 0
+	char *p = (*cmd != '"')? strchr (cmd, '|'): NULL;
+	if (p) {
+		// This code works but its pretty ugly as its a workaround to
+		// make the webserver work as expected, this was broken some
+		// weeks. let's use this hackaround for now
+		char *c = strdup (cmd);
+		c[p - cmd] = 0;
+		if (!strcmp (p + 1, "H")) {
+			char *res = r_core_cmd_str (core, c);
+			free (c);
+			char *hres = r_cons_html_filter (res, NULL);
+			free (res);
+			return hres;
+		} else {
+			int sh = r_config_get_i (core->config, "scr.color");
+			r_config_set_i (core->config, "scr.color", 0);
+			char *ret = r_core_cmd_str (core, c);
+			r_config_set_i (core->config, "scr.color", sh);
+			free (c);
+			return ret;
+		}
+	}
+	return r_core_cmd_str (core, cmd);
+#else
 	r_cons_reset ();
-	r_sandbox_disable (1);
+	r_sandbox_disable (true);
 	if (r_file_mkstemp ("cmd", &tmp) != -1) {
 		int pipefd = r_cons_pipe_open (tmp, 1, 0);
 		if (pipefd == -1) {
 			r_file_rm (tmp);
-			r_sandbox_disable (0);
+			r_sandbox_disable (false);
 			free (tmp);
 			return r_core_cmd_str (core, cmd);
 		}
@@ -4593,23 +4597,24 @@ R_API char *r_core_cmd_str_pipe(RCore *core, const char *cmd) {
 		r_core_cmd_subst (core, _cmd);
 		r_cons_flush ();
 		r_cons_pipe_close (pipefd);
-		s = r_file_slurp (tmp, NULL);
-		if (s) {
+		if (r_file_exists (tmp)) {
+			char *s = r_file_slurp (tmp, NULL);
 			r_file_rm (tmp);
-			r_sandbox_disable (0);
+			r_sandbox_disable (false);
 			free (tmp);
 			free (_cmd);
-			return s;
+			return s? s: strdup ("");
 		}
 		eprintf ("slurp %s fails\n", tmp);
 		r_file_rm (tmp);
 		free (tmp);
 		free (_cmd);
-		r_sandbox_disable (0);
+		r_sandbox_disable (false);
 		return r_core_cmd_str (core, cmd);
 	}
 	r_sandbox_disable (0);
 	return NULL;
+#endif
 }
 
 R_API char *r_core_cmd_strf(RCore *core, const char *fmt, ...) {




@@ -342,6 +342,9 @@ R_API char *r_file_slurp(const char *str, int *usz) {
 	char *ret;
 	FILE *fd;
 	long sz;
+	if (usz) {
+		*usz = 0;
+	}
 	if (!r_file_exists (str)) {
 		return NULL;
 	}




@@ -107,6 +107,7 @@ R_API bool r_sandbox_disable (bool e) {
 		enabled = false;
 	} else {
 		enabled = disabled;
+		disabled = false;
 	}
 	return enabled;
 }