@@ -353,7 +353,7 @@ static int string_scan_range(RList *list, RBinFile *bf, int min,
 		pj_end (pj);
 		RIO *io = bin->iob.io;
 		if (io) {
-			io->cb_printf ("%s\n", pj_string (pj));
+			io->cb_printf ("%s", pj_string (pj));
 		}
 		pj_free (pj);
 	}




@@ -1108,7 +1108,6 @@ static void print_types_json(const RPdb *pdb, PJ *pj, const RList *types) {
 
 	RListIter *it = r_list_iterator (types);
 
-	pj_o (pj);
 	pj_ka (pj, "types");
 
 	while (r_list_iter_next (it)) {
@@ -1215,7 +1214,6 @@ static void print_types_json(const RPdb *pdb, PJ *pj, const RList *types) {
 		}
 	}
 	pj_end (pj);
-	pj_end (pj);
 }
 
 /**
@@ -1366,7 +1364,6 @@ static void print_gvars(RPdb *pdb, ut64 img_base, PJ *pj, int format) {
 	}
 
 	if (format == 'j') {
-		pj_o (pj);
 		pj_ka (pj, "gvars");
 	}
 	gsym_data_stream = (SGDATAStream *)gsym->stream;
@@ -1424,7 +1421,6 @@ static void print_gvars(RPdb *pdb, ut64 img_base, PJ *pj, int format) {
 	}
 	if (format == 'j') {
 		pj_end (pj);
-		pj_end (pj);
 	}
 }
 




@@ -376,12 +376,15 @@ static void _print_strings(RCore *r, RList *list, int mode, int va) {
 	RListIter *iter;
 	RBinString *string;
 	RBinSection *section;
-	PJ *pj = NULL;
+	PJ *pj;
 
 	bin->minstrlen = minstr;
 	bin->maxstrlen = maxstr;
 	if (IS_MODE_JSON (mode)) {
 		pj = r_core_pj_new (r);
+		if (!pj) {
+			return;
+		}
 		pj_a (pj);
 	} else if (IS_MODE_RAD (mode)) {
 		r_cons_println ("fs strings");
@@ -389,7 +392,7 @@ static void _print_strings(RCore *r, RList *list, int mode, int va) {
 		r_flag_space_set (r->flags, R_FLAGS_FS_STRINGS);
 		r_cons_break_push (NULL, NULL);
 	} else if (IS_MODE_NORMAL (mode)) {
-		r_cons_printf ("[Strings]\n");
+		r_cons_println ("[Strings]");
 		r_table_set_columnsf (table, "nXXnnsss", "nth", "paddr", "vaddr", "len", "size", "section", "type", "string");
 	}
 	RBinString b64 = { 0 };
@@ -555,9 +558,8 @@ static void _print_strings(RCore *r, RList *list, int mode, int va) {
 	R_FREE (b64.string);
 	if (IS_MODE_JSON (mode)) {
 		pj_end (pj);
-		r_cons_printf ("%s\n", pj_string (pj));
+		r_cons_print (pj_string (pj));
 		pj_free (pj);
-		pj = NULL;
 	} else if (IS_MODE_SET (mode)) {
 		r_cons_break_pop ();
 	} else if (IS_MODE_NORMAL (mode)) {
@@ -566,10 +568,9 @@ static void _print_strings(RCore *r, RList *list, int mode, int va) {
 		}
 		char *s = r_table_tostring (table);
 		if (s) {
-			r_cons_printf ("%s", s);
+			r_cons_print (s);
 			free (s);
 		}
-
 	}
 	r_table_free (table);
 }
@@ -642,7 +643,7 @@ static bool bin_strings(RCore *r, int mode, int va) {
 	if (plugin->info && plugin->name) {
 		if (strcmp (plugin->name, "any") == 0 && !rawstr) {
 			if (IS_MODE_JSON (mode)) {
-				r_cons_print ("[]\n");
+				r_cons_print ("[]");
 				return true;
 			}
 			return false;
@@ -786,7 +787,7 @@ static int bin_info(RCore *r, int mode, ut64 laddr) {
 	RBinFile *bf = r_bin_cur (r->bin);
 	if (!bf) {
 		if (IS_MODE_JSON (mode)) {
-			r_cons_printf ("{}\n");
+			r_cons_print ("{}");
 		}
 		return false;
 	}
@@ -796,7 +797,7 @@ static int bin_info(RCore *r, int mode, ut64 laddr) {
 
 	if (!info || !obj) {
 		if (IS_MODE_JSON (mode)) {
-			r_cons_printf ("{}\n");
+			r_cons_print ("{}");
 			return true;
 		}
 		return false;
@@ -1032,7 +1033,7 @@ static int bin_info(RCore *r, int mode, ut64 laddr) {
 		if (IS_MODE_JSON (mode)) {
 			pj_end (pj);
 			pj_end (pj);
-			r_cons_println (pj_string (pj));
+			r_cons_print (pj_string (pj));
 			pj_free (pj);
 		}
 	}
@@ -1269,8 +1270,14 @@ R_API bool r_core_pdb_info(RCore *core, const char *file, int mode) {
 		mode = 'd'; // default
 		break;
 	}
-	PJ *pj = r_core_pj_new (core);
-	pj_a (pj);
+	PJ *pj = NULL;
+	if (mode == 'j') {
+		pj = r_core_pj_new (core);
+		if (!pj) {
+			return false;
+		}
+		pj_o (pj);
+	}
 
 	pdb.print_types (&pdb, pj, mode);
 	pdb.print_gvars (&pdb, baddr, pj, mode);
@@ -1278,12 +1285,11 @@ R_API bool r_core_pdb_info(RCore *core, const char *file, int mode) {
 	r_parse_pdb_types (core->anal, &pdb);
 	pdb.finish_pdb_parse (&pdb);
 
-	pj_end (pj