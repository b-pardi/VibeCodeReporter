new file mode 100644



@@ -0,0 +1,32 @@
+import serial
+import time
+
+def read_reply(s):
+  while True:
+    if s.inWaiting() > 0:
+      time.sleep(0.1)
+      buf = s.read(s.inWaiting())
+      if len(buf) == 4:
+        print("Current misc settings: " + str(buf[3]))
+        break
+
+# CONFIG
+serial_port =  "/dev/ttyUSB0"
+#iscs = 18
+# CONFIG END
+
+print("!!!!! MAKE SURE TO SET THE BOARD TO CONFIGURATION MODE. M0 = short, M1 = open. Timeout? Possibly an incorrect serial baud rate.")
+
+s = serial.Serial(serial_port, 9600)
+s.flushInput()
+time.sleep(0.1)
+
+print("Connected to serial port.")
+print("Reading current misc configuration...")
+
+s.write(b'\xC1\x06\x01')
+read_reply(s)
+
+print("Complete.")
+
+s.close()

new file mode 100644



@@ -0,0 +1,39 @@
+import serial
+import time
+
+def read_reply(s):
+  while True:
+    if s.inWaiting() > 0:
+      time.sleep(0.1)
+      buf = s.read(s.inWaiting())
+      if len(buf) == 4:
+        print("Current misc configuration: " + str(buf[3]))
+        break
+
+# CONFIG
+serial_port =  "/dev/ttyUSB0"
+value = 144
+# CONFIG END
+
+print("!!!!! MAKE SURE TO SET THE BOARD TO CONFIGURATION MODE. M0 = short, M1 = open. Timeout? Possibly an incorrect serial baud rate.")
+
+s = serial.Serial(serial_port, 9600)
+s.flushInput()
+time.sleep(0.1)
+
+print("Connected to serial port.")
+print("Reading current misc configuration...")
+
+s.write(b'\xC1\x06\x01')
+read_reply(s)
+
+print("Setting misc configuration...")
+
+command = bytearray(b'\xC0\x06\x01')
+command.append(value)
+s.write(command)
+read_reply(s)
+
+print("Complete.")
+
+s.close()




@@ -12,7 +12,7 @@ def read_reply(s):
 
 # CONFIG
 serial_port = "/dev/ttyUSB0"
-speeds = 98 # Decimal representation of the configuration bitmask.
+speeds = 101 # Decimal representation of the configuration bitmask.
 # CONFIG END
 
 print("!!!!! MAKE SURE TO SET THE BOARD TO CONFIGURATION MODE. M0 = short, M1 = open. Timeout? Possibly an incorrect serial baud rate.")




@@ -103,7 +103,7 @@ public class Main {
                     System.exit(FAILURE);
                 }
 
-                Nzyme nzyme = new NzymeImpl(leaderConfiguration, database);
+                NzymeLeader nzyme = new NzymeLeaderImpl(leaderConfiguration, database);
                 nzyme.initialize();
 
                 Runtime.getRuntime().addShutdownHook(new Thread(() -> {




@@ -23,6 +23,7 @@ import com.google.common.collect.Lists;
 import horse.wtf.nzyme.alerts.Alert;
 import horse.wtf.nzyme.alerts.service.AlertsService;
 import horse.wtf.nzyme.bandits.engine.ContactIdentifier;
+import horse.wtf.nzyme.bandits.trackers.GroundStation;
 import horse.wtf.nzyme.bandits.trackers.TrackerManager;
 import horse.wtf.nzyme.configuration.IncompleteConfigurationException;
 import horse.wtf.nzyme.configuration.InvalidConfigurationException;
@@ -49,7 +50,7 @@ import java.security.Key;
 import java.util.Collections;
 import java.util.List;
 
-public class MockNzyme implements Nzyme {
+public class MockNzyme implements NzymeLeader {
 
     private File loadFromResourceFile(String name) {
         URL resource = getClass().getClassLoader().getResource(name);
@@ -189,6 +190,11 @@ public class MockNzyme implements Nzyme {
         return null;
     }
 
+    @Override
+    public GroundStation getGroundStation() {
+        return null;
+    }
+
     @Override
     public SystemStatus getSystemStatus() {
         return systemStatus;

similarity index 94%
rename from src/main/java/horse/wtf/nzyme/Nzyme.java
rename to src/main/java/horse/wtf/nzyme/NzymeLeader.java



@@ -22,6 +22,7 @@ import com.fasterxml.jackson.databind.ObjectMapper;
 import horse.wtf.nzyme.alerts.Alert;
 import horse.wtf.nzyme.alerts.service.AlertsService;
 import horse.wtf.nzyme.bandits.engine.ContactIdentifier;
+import horse.wtf.nzyme.bandits.trackers.GroundStation;
 import horse.wtf.nzyme.bandits.trackers.TrackerManager;
 import horse.wtf.nzyme.configuration.leader.LeaderConfiguration;
 import horse.wtf.nzyme.database