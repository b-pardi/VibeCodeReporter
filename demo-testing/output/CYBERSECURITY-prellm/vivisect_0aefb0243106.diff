@@ -277,7 +277,7 @@ def branch_misc(va, val, val2): # bl and misc control
                     return COND_AL, None, 'sub', opers, IF_PSR_S, 0
 
                 return COND_AL, None, 'eret', tuple(), envi.IF_RET | envi.IF_NOFALL, 0
-            print "TEST ME: branch_misc subsection 3"
+            print("TEST ME: branch_misc subsection 3")
 ##### FIXME!  THIS NEEDS TO ALSO HIT MSR BELOW....
             #raise InvalidInstruction(
             #    mesg="branch_misc subsection 3",
@@ -285,7 +285,6 @@ def branch_misc(va, val, val2): # bl and misc control
 
             # xx0xxxxx and others
             if op == 0b0111000:
-                print "HIT"
                 tmp = op2 & 3
 
                 Rn = val & 0xf
@@ -296,8 +295,8 @@ def branch_misc(va, val, val2): # bl and misc control
 
                 else:
                     R = (val >> 4) & 1
-
                     #raise Exception("FIXME:  MSR(register) p B9-1968")
+
                 opers = (
                         ArmPgmStatRegOper(R, mask),
                         ArmRegOper(Rn)
@@ -309,6 +308,7 @@ def branch_misc(va, val, val2): # bl and misc control
                 # coalesce with previous
                 raise Exception("FIXME:  MSR(register) p B9-1968")
 
+
             elif op == 0b0111010:
                 flags = 0
 




@@ -49,9 +49,7 @@ class MemoryCanvas(object):
     of memory will be displayed. The methods implemented here show
     how a memory canvas which simply prints would be implemented.
     """
-    def __init__(self, mem=None, syms=None):
-        if mem == None:
-            raise Exception("MemoryCanvas must include mem args")
+    def __init__(self, mem, syms=None):
         if syms == None:
             syms = e_resolv.SymbolResolver()
         self.mem = mem




@@ -1,12 +1,7 @@
 '''
 A widget for editing EnviConfig options.
 '''
-try:
-    from PyQt4 import QtCore, QtGui
-    from PyQt4.QtGui import *
-except:
-    from PyQt5 import QtCore, QtGui, QtWidgets
-    from PyQt5.QtWidgets import *
+from PyQt4 import QtCore, QtGui
 
 class EnviConfigOption:
 
@@ -19,10 +14,10 @@ class EnviConfigOption:
         self.evalue = evalue
         self.econfig[self.ename] = evalue
 
-class EnviConfigBool(EnviConfigOption,QCheckBox):
+class EnviConfigBool(EnviConfigOption,QtGui.QCheckBox):
 
     def __init__(self, config, name, value, parent=None):
-        QCheckBox.__init__(self, parent=parent)
+        QtGui.QCheckBox.__init__(self, parent=parent)
         EnviConfigOption.__init__(self, config, name, value)
         self.toggled.connect( self.setEnviValue )
         self.setChecked(value)
@@ -30,10 +25,10 @@ class EnviConfigBool(EnviConfigOption,QCheckBox):
     def parseEnviValue(self):
         self.setEnviValue( self.isChecked() )
 
-class EnviConfigInt(EnviConfigOption,QLineEdit):
+class EnviConfigInt(EnviConfigOption,QtGui.QLineEdit):
 
     def __init__(self, config, name, value, parent=None):
-        QLineEdit.__init__(self, parent=parent)
+        QtGui.QLineEdit.__init__(self, parent=parent)
         EnviConfigOption.__init__(self, config, name, value)
         self.editingFinished.connect( self.parseEnviValue )
 
@@ -45,9 +40,9 @@ class EnviConfigInt(EnviConfigOption,QLineEdit):
     def parseEnviValue(self):
         self.setEnviValue(int(str(self.text()),0))
 
-class EnviConfigString(EnviConfigOption,QLineEdit):
+class EnviConfigString(EnviConfigOption,QtGui.QLineEdit):
     def __init__(self, config, name, value, parent=None):
-        QLineEdit.__init__(self, parent=parent)
+        QtGui.QLineEdit.__init__(self, parent=parent)
         EnviConfigOption.__init__(self, config, name, value)
         self.editingFinished.connect( self.parseEnviValue )
         self.setText(value)
@@ -63,13 +58,13 @@ cfgtypes = {
     bool:EnviConfigBool,
 }
 
-class EnviConfigEditor(QWidget):
+class EnviConfigEditor(QtGui.QWidget):
 
     def __init__(self, config, parent=None):
-        QWidget.__init__(self, parent=parent)
+        QtGui.QWidget