@@ -4,6 +4,7 @@ import static com.google.common.base.Preconditions.checkArgument;
 
 import com.fasterxml.jackson.annotation.JsonCreator;
 import com.fasterxml.jackson.annotation.JsonProperty;
+import com.google.common.base.MoreObjects;
 import java.util.Comparator;
 import java.util.Objects;
 import javax.annotation.Nonnull;
@@ -153,6 +154,15 @@ public final class BgpPeerConfigId implements Comparable<BgpPeerConfigId> {
     return hashCode;
   }
 
+  @Override
+  public String toString() {
+    return MoreObjects.toStringHelper(this)
+        .add(PROP_HOSTNAME, _hostname)
+        .add(PROP_INTERFACE, _peerInterface)
+        .add(PROP_PREFIX, _remotePeerPrefix)
+        .toString();
+  }
+
   /** Types of BGP peers */
   public enum BgpPeerConfigType {
     /** Type for {@link BgpActivePeerConfig}s */




@@ -3,6 +3,7 @@ package org.batfish.datamodel.bgp;
 import com.fasterxml.jackson.annotation.JsonCreator;
 import com.fasterxml.jackson.annotation.JsonIgnore;
 import com.fasterxml.jackson.annotation.JsonProperty;
+import com.google.common.base.MoreObjects;
 import com.google.common.collect.ImmutableList;
 import com.google.common.graph.ImmutableValueGraph;
 import com.google.common.graph.MutableValueGraph;
@@ -137,5 +138,10 @@ public final class BgpTopology {
     public int compareTo(EdgeId o) {
       return COMPARATOR.compare(this, o);
     }
+
+    @Override
+    public String toString() {
+      return MoreObjects.toStringHelper(this).add("tail", _tail).add("head", _head).toString();
+    }
   }
 }