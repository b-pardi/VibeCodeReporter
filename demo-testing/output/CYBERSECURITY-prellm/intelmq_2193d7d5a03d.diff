@@ -156,19 +156,20 @@ def convert_http_host_and_url(value, row):
         if row['http_host']:
             hostname = row['http_host']
 
-    path = ""
     if "url" in row:
-        if row['url']:
-            path = row['url']
+        path = row.get('url', '')
     elif "http_url" in row:
-        if row['http_url']:
-            path = row['http_url']
+        path = row.get('http_url', '')
+    else:
+        path = ''
+    # remove potential leading/trailing HTTP request information
+    path = re.sub(r'^[^/]*', '', path)
+    path = re.sub(r'\s.*$', '', path)
+    # If field was empty:
+    if not path.startswith('/'):
+        path = '/' + path
 
     if hostname and path:
-        # remove potential leading/trailing HTTP request information
-        path = re.sub(r'^[^/]*', '', path)
-        path = re.sub(r'\s.*$', '', path)
-
         application = "http"
         if "application" in row:
             if row['application'] in ['http', 'https']:




@@ -30,7 +30,7 @@ EVENTS = [{'__type': 'Event',
            'destination.geolocation.cc': 'NL',
            'destination.ip': '2a02:1668:1034::2',
            'destination.port': 80,
-           'extra.http_url': 'POST /okla/api/1 HTTP/1.1',
+           'destination.url': 'http://198-51-100-38.example.net/okla/api/1',
            'feed.name': 'ShadowServer IPv6 Sinkhole HTTP Drone',
            'malware.name': 'ghost-push',
            'protocol.transport': 'tcp',
@@ -52,7 +52,7 @@ EVENTS = [{'__type': 'Event',
            'destination.port': 80,
            'extra.http_agent': 'Mozilla/5.0 (Windows NT 6.1; WOW64; rv:40.0) '
                                'Gecko/20100101 Firefox/40.1',
-           'extra.http_url': 'GET / HTTP/1.1',
+           'destination.url': 'http://198-51-100-38.example.net/',
            'feed.name': 'ShadowServer IPv6 Sinkhole HTTP Drone',
            'protocol.transport': 'tcp',
            'raw': utils.base64_encode('\n'.join([RECONSTRUCTED_LINES[0],
@@ -72,7 +72,7 @@ EVENTS = [{'__type': 'Event',
            'destination.ip': '2a02:1668:1034::2',
            'destination.port': 80,
            'extra.http_agent': 'Mercury/907 CFNetwork/758.5.3 Darwin/15.6.0',
-           'extra.http_url': 'POST / HTTP/1.1',
+           'destination.url': 'http://198-51-100-38.example.net/',
            'feed.name': 'ShadowServer IPv6 Sinkhole HTTP Drone',
            'malware.name': 'xcodeghost',
            'protocol.transport': 'tcp',




@@ -31,6 +31,7 @@ EVENTS = [{'__type': 'Event',
            'destination.fqdn': '198-51-100-55.example.net',
            'destination.ip': '198.51.100.248',
            'destination.port': 80,
+           'destination.url': 'http://198-51-100-55.example.net/',
            'malware.name': 'avalanche-goznym',
            'protocol.transport': 'tcp',
            'raw': utils.base64_encode('\n'.join([RECONSTRUCTED_LINES[0],
@@ -56,7 +57,7 @@ EVENTS = [{'__type': 'Event',
            'destination.geolocation.cc': 'US',
            'destination.ip': '198.51.100.22',
            'destination.port': 80,
-           'extra.url': 'GET /search?q=1 HTTP/1.0',
+           'destination.url': 'http://198.51.100.90/search?q=1',
            'extra.user_agent': 'Mozilla/4.0 (compatible; MSIE 6.0; Windows NT 5.1; '
                                'SV1; .NET CLR 1.1.4322; .NET CLR 2.0.50727; .NET CLR '
                                '3.0.4506.2152; .NET CLR 3.5.30729; .NET4.0C; .NET4.0E)',