@@ -5,6 +5,7 @@ Copyright (C) 2013  Infobyte LLC (http://www.infobytesec.com/)
 See the file 'doc/LICENSE' for the license information
 
 '''
+import json
 from tempfile import NamedTemporaryFile
 
 import os
@@ -2159,6 +2160,29 @@ class TestVulnerabilityCustomFields(ReadOnlyAPITests):
         session.commit()
 
 
+@pytest.mark.usefixtures('logged_user')
+class TestVulnerabilitySearch():
+
+    def test_search_by_filter_web_vulns(self, test_client, session):
+        workspace = WorkspaceFactory.create()
+        host = HostFactory.create(workspace=workspace)
+        host.hostnames.append(HostnameFactory.create(workspace=workspace))
+        vuln = VulnerabilityFactory.create(host=host, workspace=workspace)
+        session.add(vuln)
+        session.add(host)
+        session.commit()
+
+        query_filter = {"filters":
+            [{"name": "hostname", "op": "eq", "val": "test"}]
+        }
+        res = test_client.get(
+            '/v2/ws/{}/vulns/filter?q={}'.format(workspace.name, json.dumps(query_filter)))
+        assert res.status_code == 200
+        assert res.json['count'] == 1
+        assert res.json['count']['vulnerabilities']['_id'] == vuln.id
+
+
+
 def test_type_filter(workspace, session,
                      vulnerability_factory,
                      vulnerability_web_factory):