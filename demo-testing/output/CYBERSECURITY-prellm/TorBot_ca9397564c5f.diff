similarity index 63%
rename from modules/getLinks.py
rename to modules/go_linker.py



@@ -2,20 +2,20 @@ import sys
 
 from ctypes import cdll, c_char_p, c_longlong, c_int, Structure
 
-getLinks = cdll.LoadLibrary("modules/lib/getLinks.so")
+go_linker = cdll.LoadLibrary("modules/lib/go_get_urls.so")
 
 class GoString(Structure):
     _fields_ = [("p", c_char_p), ("n", c_longlong)]
 
-getLinks.GetLinks.argtypes = [GoString, GoString, GoString, c_int]
+go_linker.go_get_urls.argtypes = [GoString, GoString, GoString, c_int]
 
-def GetLinks(url, addr, port, timeout):
+def go_get_urls(url, addr, port, timeout):
 
     url = url.encode('utf-8')
     addr = addr.encode('utf-8')
     port = str(port).encode('utf-8')
 
-    getLinks.GetLinks(GoString(url, len(url)),
+    go_linker.go_get_urls(GoString(url, len(url)),
                       GoString(addr, len(addr)),
                       GoString(port, len(port)),
                       timeout)

similarity index 100%
rename from modules/lib/getLinks.h
rename to modules/lib/go_get_urls.h




@@ -7,21 +7,17 @@ SCRIPT_DIR = os.path.dirname(os.path.realpath(
              os.path.join(os.getcwd(), os.path.expanduser(__file__))))
 
 sys.path.append(os.path.normpath(os.path.join(SCRIPT_DIR, PACKAGE_PARENT)))
-from modules import getLinks, pagereader
+from modules import getweblinks, pagereader
 
 
 def test_get_links_successful():
-    link = 'http://www.whatsmyip.net'
-    soup = pagereader.read_first_page(link)[0]
+    soup = pagereader.read_first_page('http://www.whatsmyip.net/')[0]
     data = ['http://aff.ironsocket.com/SH7L',
             'http://aff.ironsocket.com/SH7L',
             'http://wsrs.net/',
             'http://cmsgear.com/']
 
-    LOCALHOST = '127.0.0.1'
-    PORT = 9050
-    result = getLinks.GetLinks(link, LOCALHOST, PORT, 15)
-    
+    result = getweblinks.get_links(soup, ext=['.com', '.net'])
     assert result == data
 
 




@@ -2,7 +2,7 @@ import argparse
 import socket
 import socks
 from modules import (bcolors, getemails, pagereader, getweblinks, updater,
-                     info, savefile, getLinks)
+                     info, savefile, go_linker)
 
 
 
@@ -170,7 +170,7 @@ def main(conn=False):
             if args.save:
                 print('Nothing to save.\n')
         else:
-            links = getLinks.GetLinks(link, LOCALHOST, PORT, 15)
+            links = go_linker.go_get_urls(link, LOCALHOST, PORT, 15)
             #links = getweblinks.get_links(soup=html_content, ext=args.extension, live=args.live)
             if args.save:
                 savefile.saveJson("Links", links)