@@ -280,3 +280,9 @@ class TestMiscSamplesChrome(object):
                     'childNodes length: 0']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild.html")
+        expected = ["Alert Text: New child", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -268,3 +268,9 @@ class TestMiscSamplesFirefox(object):
                     'childNodes length: 0']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild.html")
+        expected = ["Alert Text: New child", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -359,3 +359,9 @@ class TestMiscSamplesIE(object):
                     'childNodes length: 0']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild.html")
+        expected = ["Alert Text: New child", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -339,3 +339,9 @@ class TestMiscSamplesIE(object):
                     'childNodes length: 0']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild.html")
+        expected = ["Alert Text: New child", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -314,3 +314,9 @@ class TestMiscSamplesIE(object):
                     'childNodes length: 0']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild.html")
+        expected = ["Alert Text: New child", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -314,3 +314,9 @@ class TestMiscSamplesIE(object):
                     'childNodes length: 0']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild.html")
+        expected = ["Alert Text: New child", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -364,3 +364,9 @@ class TestMiscSamplesIE(object):
                     'childNodes length: 0']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild.html")
+        expected = ["Alert Text: New child", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -270,3 +270,9 @@ class TestMiscSamplesChrome(object):
                     'childNodes length: 0']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild.html")
+        expected = ["Alert Text: New child", ]
+
+        self.do_perform_test(caplog, sample, expected)