@@ -286,6 +286,11 @@ static void resolve_pct_format(double * value, const char ** format)
 
 static void resolve_qty_format(double * value, const char ** format)
 {
+    int neg = *value < 0;
+    if (neg) {
+        *value *= -1;
+    }
+
     if (*value >= 1000 * 1000 * 1000) {
         *format = "%7.1fG";
         *value /= 1000 * 1000 * 1000;
@@ -298,4 +303,8 @@ static void resolve_qty_format(double * value, const char ** format)
     } else {
         *format = "%8.f";
     }
+
+    if (neg) {
+        *value *= -1;
+    }
 }