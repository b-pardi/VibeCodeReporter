@@ -1,25 +1,36 @@
-# Faraday Penetration Test IDE
-# Copyright (C) 2016  Infobyte LLC (http://www.infobytesec.com/)
-# See the file 'doc/LICENSE' for the license information
-from io import TextIOWrapper
+"""
+Faraday Penetration Test IDE
+Copyright (C) 2016  Infobyte LLC (https://faradaysec.com/)
+See the file 'doc/LICENSE' for the license information
+"""
 
+# Standard library imports
 import json
 import threading
 import logging
 import csv
+from io import TextIOWrapper
 
-from werkzeug.exceptions import Conflict
-from flask import Blueprint, request, abort, jsonify, make_response
-from flask_classful import route
+# Related third party imports
+import wtforms
 from filteralchemy import (
     FilterSet,
     operators,
 )
+from flask import (
+    Blueprint,
+    request,
+    abort,
+    jsonify,
+    make_response,
+)
+from flask_classful import route
 from flask_wtf.csrf import validate_csrf
 from marshmallow import fields, ValidationError, Schema, post_load
 from marshmallow.validate import OneOf
-import wtforms
+from werkzeug.exceptions import Conflict
 
+# Local application imports
 from faraday.server.api.base import (
     AutoSchema,
     FilterAlchemyMixin,
@@ -28,16 +39,14 @@ from faraday.server.api.base import (
     ReadWriteView,
     FilterMixin,
     BulkDeleteMixin,
-    BulkUpdateMixin
+    BulkUpdateMixin,
 )
-
 from faraday.server.schemas import (
     PrimaryKeyRelatedField,
     SeverityField,
     SelfNestedField,
     FaradayCustomField,
 )
-
 from faraday.server.models import (
     db,
     CustomFieldsSchema,
@@ -93,10 +102,12 @@ class VulnerabilityTemplateSchema(AutoSchema):
                   'external_id', 'creator', 'create_at', 'creator_id',
                   'customfields')
 
-    def get_references(self, obj):
+    @staticmethod
+    def get_references(obj):
         return ', '.join(map(lambda ref_tmpl: ref_tmpl.name, obj.reference_template_instances))
 
-    def load_references(self, value):
+    @staticmethod
+    def load_references(value):
         if isinstance(value, bytes):
             value = value.decode('utf-8')
         if isinstance(value, list):
@@ -213,6 +224,7 @@ class VulnerabilityTemplateView(PaginatedMixin,
           200:
             description: Ok
         """
+        vulns_to_create, status_code = None, None
         csrf_token = request.form.get('csrf_token', '')
         if not csrf_token:
             csrf_token = request.json.get('csrf_token', '')
@@ -286,7 +298,8 @@ class VulnerabilityTemplateView(PaginatedMixin,
             status_code
         )
 
-    def _parse_vuln_from_file(self, vulns_reader):
+    @staticmethod
+    def _parse_vuln_from_file(vulns_reader):
         custom_fields = {cf_schema.field_name: cf_schema for cf_schema in db.session.query(CustomFieldsSchema).all()}
         vulns_list = []
         for index, vuln_dict in enumerate(vulns_reader):