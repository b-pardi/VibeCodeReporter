@@ -5155,7 +5155,7 @@ static bool stringAt(RCore *core, ut64 addr) {
 }
 
 R_API int r_core_search_value_in_range(RCore *core, RInterval search_itv, ut64 vmin,
-				     ut64 vmax, int vsize, bool asterisk, inRangeCb cb) {
+				     ut64 vmax, int vsize, inRangeCb cb, void *cb_user) {
 	int i, align = core->search->align, hitctr = 0;
 	bool vinfun = r_config_get_i (core->config, "anal.vinfun");
 	bool vinfunr = r_config_get_i (core->config, "anal.vinfunrange");
@@ -5247,7 +5247,7 @@ R_API int r_core_search_value_in_range(RCore *core, RInterval search_itv, ut64 v
 					}
 				}
 				if (isValidMatch) {
-					cb (core, addr, value, vsize, asterisk, hitctr);
+					cb (core, addr, value, vsize, hitctr, cb_user);
 					if (analStrings && stringAt (core, addr)) {
 						add_string_ref (mycore, addr, value);
 					}




@@ -8410,7 +8410,8 @@ static bool archIsThumbable(RCore *core) {
 	return false;
 }
 
-static void _CbInRangeAav(RCore *core, ut64 from, ut64 to, int vsize, bool asterisk, int count) {
+static void _CbInRangeAav(RCore *core, ut64 from, ut64 to, int vsize, int count, void *user) {
+	bool asterisk = user != NULL;
 	int arch_align = r_anal_archinfo (core->anal, R_ANAL_ARCHINFO_ALIGN);
 	bool vinfun = r_config_get_i (core->config, "anal.vinfun");
 	int searchAlign = r_config_get_i (core->config, "search.align");
@@ -8491,7 +8492,7 @@ static void cmd_anal_aav(RCore *core, const char *input) {
 			oldstr = r_print_rowlog (core->print, sdb_fmt ("from 0x%"PFMT64x" to 0x%"PFMT64x" (aav)", map->itv.addr, r_itv_end (map->itv)));
 			r_print_rowlog_done (core->print, oldstr);
 			(void)r_core_search_value_in_range (core, map->itv,
-				map->itv.addr, r_itv_end (map->itv), vsize, asterisk, _CbInRangeAav);
+				map->itv.addr, r_itv_end (map->itv), vsize, _CbInRangeAav, (void *)asterisk);
 		}
 		r_list_free (list);
 	} else {
@@ -8526,7 +8527,7 @@ static void cmd_anal_aav(RCore *core, const char *input) {
 				}
 				oldstr = r_print_rowlog (core->print, sdb_fmt ("0x%08"PFMT64x"-0x%08"PFMT64x" in 0x%"PFMT64x"-0x%"PFMT64x" (aav)", from, to, begin, end));
 				r_print_rowlog_done (core->print, oldstr);
-				(void)r_core_search_value_in_range (core, map->itv, from, to, vsize, asterisk, _CbInRangeAav);
+				(void)r_core_search_value_in_range (core, map->itv, from, to, vsize, _CbInRangeAav, (void *)asterisk);
 				}
 		}
 		r_list_free (list);




@@ -141,8 +141,6 @@ static const char *help_msg_slash_x[] = {
 static int preludecnt = 0;
 static int searchflags = 0;
 static int searchshow = 0;
-static bool json = false;
-static const char *cmdhit = NULL;
 static const char *searchprefix = NULL;
 
 struct search_parameters {
@@ -150,6 +148,7 @@ struct search_parameters {
 	RList *boundaries;
 	const char *mode;
 	const char *cmd_hit;
+	int outmode; // 0 or R_MODE_RADARE or R_MODE_JSON
 	bool inverse;
 	bool crypto_search;
 	bool aes_search;
@@ -465,7 +464,7 @@ static int _cb_hit(RSearchKeyword *kw, void *user, ut64 addr) {
 	if (searchshow && kw && kw->keyword_length > 0) {
 		int len, i, extra, mallocsize;
 		char *s = NULL, *str = NULL, *p = NULL;
-		extra = (json)? 3: 1;
+		extra = (param->outmode == R_MODE_JSON)? 3: 1;
 		const char *type = "hexpair";
 		bool escaped = false;
 		ut8 *buf = malloc (keyword_len);
@@ -489,7 +488,7 @@ static int _cb_hit(RSearchKeyword *kw, void *user, ut64 addr) {
 				pos = strdup ("");
 			}
 			free (buf);
-			if (json) {
+			if (param->outmode == R_MODE_JSON) {
 				char *pre_esc = r_str_escape (pre);
 				char *pos_esc = r_str_escape (pos);
 				s = r_str_newf ("%s%s%s", pre_esc, wrd, pos_esc);
@@ -516,7 +515,7 @@ static int _cb_hit(RSearchKeyword *kw, void *user, ut64 addr) {
 				p = str;
 				memset (str, 0, len);
 				r_io_read_at (core->io, base_addr + addr, buf, keyword_len);
-				if (json) {
+				if (param->outmode == R_MODE_JSON) {
 					p = str;
 				}
 				const int bytes = (len > 40)? 40: len;
@@ -537,7 +536,7 @@ static int _cb_hit(RSearchKeyword *kw, void *user, ut64 addr) {
 			break;
 		