@@ -254,6 +254,15 @@ typedef struct {
 	uint64_t value_add;
 } cache_slide2_t;
 
+typedef struct {
+	uint32_t version;
+	uint32_t toc_offset;
+	uint32_t toc_count;
+	uint32_t entries_offset;
+	uint32_t entries_count;
+	uint32_t entries_size;
+} cache_slide1_t;
+
 typedef struct
 {
 	uint32_t version;




@@ -12,6 +12,18 @@
 #include "objc/mach0_classes.h"
 
 typedef struct {
+	ut8 version;
+	ut64 slide;
+	ut8 *one_page_buf;
+	ut32 page_size;
+	ut64 start_of_data;
+} RDyldRebaseInfo;
+
+typedef struct {
+	ut8 version;
+	ut64 slide;
+	ut8 *one_page_buf;
+	ut32 page_size;
 	ut64 start_of_data;
 	ut16 *page_starts;
 	ut32 page_starts_count;
@@ -21,10 +33,19 @@ typedef struct {
 	ut64 value_mask;
 	ut32 delta_shift;
 	ut64 value_add;
-	ut32 page_size;
+} RDyldRebaseInfo2;
+
+typedef struct {
+	ut8 version;
 	ut64 slide;
 	ut8 *one_page_buf;
-} RDyldRebaseInfo;
+	ut32 page_size;
+	ut64 start_of_data;
+	ut16 *toc;
+	ut32 toc_count;
+	ut8 *entries;
+	ut32 entries_size;
+} RDyldRebaseInfo1;
 
 typedef struct _r_dyldcache {
 	ut8 magic[8];
@@ -47,31 +68,46 @@ static void free_bin(RDyldBinImage *bin) {
 		return;
 	}
 
-	if (bin->file) {
-		R_FREE (bin->file);
-	}
-
+	R_FREE (bin->file);
 	R_FREE (bin);
 }
 
-static void rebase_info_free(RDyldRebaseInfo *rebase_info) {
+static void rebase_info2_free(RDyldRebaseInfo2 *rebase_info) {
 	if (!rebase_info) {
 		return;
 	}
 
-	if (rebase_info->page_starts) {
-		R_FREE (rebase_info->page_starts);
-	}
+	R_FREE (rebase_info->page_starts);
+	R_FREE (rebase_info->page_extras);
+	R_FREE (rebase_info);
+}
 
-	if (rebase_info->page_extras) {
-		R_FREE (rebase_info->page_extras);
+static void rebase_info1_free(RDyldRebaseInfo1 *rebase_info) {
+	if (!rebase_info) {
+		return;
 	}
 
-	if (rebase_info->one_page_buf) {
-		R_FREE (rebase_info->one_page_buf);
+	R_FREE (rebase_info->toc);
+	R_FREE (rebase_info->entries);
+	R_FREE (rebase_info);
+}
+
+static void rebase_info_free(RDyldRebaseInfo *rebase_info) {
+	if (!rebase_info) {
+		return;
 	}
 
-	R_FREE (rebase_info);
+	R_FREE (rebase_info->one_page_buf);
+
+	ut8 version = rebase_info->version;
+
+	if (version == 1) {
+		rebase_info1_free ((RDyldRebaseInfo1*) rebase_info);
+	} else if (version == 2) {
+		rebase_info2_free ((RDyldRebaseInfo2*) rebase_info);
+	} else {
+		R_FREE (rebase_info);
+	}
 }
 
 static void r_dyldcache_free(RDyldCache *cache) {
@@ -94,18 +130,9 @@ static void r_dyldcache_free(RDyldCache *cache) {
 		cache->rebase_info = NULL;
 	}
 
-	if (cache->hdr) {
-		R_FREE (cache->hdr);
-	}
-
-	if (cache->maps) {
-		R_FREE (cache->maps);
-	}
-
-	if (cache->accel) {
-		R_FREE (cache->accel);
-	}
-
+	R_FREE (cache->hdr);
+	R_FREE (cache->maps);
+	R_FREE (cache->accel);
 	R_FREE (cache);
 }
 
@@ -283,8 +310,8 @@ beach:
 }
 
 static RDyldRebaseInfo *get_rebase_info(RBinFile *bf, RDyldCache *cache) {
-	ut16 *page_starts = NULL;
-	ut16 *page_extras = NULL;
+	ut8 *tmp_buf_1 = NULL;
+	ut8 *tmp_buf_2 = NULL;
 	ut8 *one_page_buf = NULL;
 	RBuffer *cache_buf = cache->buf;
 
@@ -303,83 +330,159 @@ static RDyldRebaseInfo *get_rebase_info(RBinFile *bf, RDyldCache *cache) {
 		return NULL;
 	}
 
-	cache_slide2_t slide_info;
 	ut64 offset = cache->hdr->slideInfoOffset;
-	ut64 size = sizeof (cache_slide2_t);
-	if (r_buf_fread_at (cache_buf, offset, (ut8*) &slide_info, "6i2l", 1) != size) {
+	ut32 slide_info_version = 0;
+	if (r_buf_read_at (cache_buf, offset, (ut8*) &slide_info_version, 4) != 4) {
 		return NULL;
 	}
 
-	if (slide_info.version != 2) {
-		return NULL;
-	}
+	if (slide_info_version == 2) {
+		cache_slide2_t slide_info;
+		ut64 size = sizeof (cache_slide2_t);
+		if (r_buf_fread_at (cache_buf, offset, (ut8*) &slide_info, "6i2l", 1) != size) {
+			return NULL;
+		}
 
-	if (slide_info.page_starts_offset == 0 ||
-		slide_info.page_starts_offset > cache->hdr->slideInfoSize ||
-		slide_info.page_starts_offset + slide_info.page_starts_count * 2 > cache->hdr->slideInfoSize) {
-		return NULL;
-	}
+		if (slide_info.page_starts_offset == 0 ||
+			slide_info.