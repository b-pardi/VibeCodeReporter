@@ -537,7 +537,7 @@ struct lladdr_info {
 
 extern void aarp_print(netdissect_options *, const u_char *, u_int);
 extern int ah_print(netdissect_options *, const u_char *);
-extern void ahcp_print(netdissect_options *, const u_char *, const u_int);
+extern void ahcp_print(netdissect_options *, const u_char *, u_int);
 extern void aodv_print(netdissect_options *, const u_char *, u_int, int);
 extern void aoe_print(netdissect_options *, const u_char *, const u_int);
 extern int  arista_ethertype_print(netdissect_options *,const u_char *, u_int);




@@ -96,14 +96,15 @@ static const struct tok ahcp1_opt_str[] = {
 	{ 0, NULL }
 };
 
-static int
-ahcp_time_print(netdissect_options *ndo, const u_char *cp, const u_char *ep)
+static void
+ahcp_time_print(netdissect_options *ndo,
+                const u_char *cp, uint8_t len)
 {
 	time_t t;
 	struct tm *tm;
 	char buf[BUFSIZE];
 
-	if (cp + 4 != ep)
+	if (len != 4)
 		goto invalid;
 	t = GET_BE_U_4(cp);
 	if (NULL == (tm = gmtime(&t)))
@@ -112,133 +113,131 @@ ahcp_time_print(netdissect_options *ndo, const u_char *cp, const u_char *ep)
 		ND_PRINT(": strftime() error");
 	else
 		ND_PRINT(": %s UTC", buf);
-	return 0;
+	return;
 
 invalid:
 	nd_print_invalid(ndo);
-	ND_TCHECK_LEN(cp, ep - cp);
-	return 0;
+	ND_TCHECK_LEN(cp, len);
+	return;
 trunc:
-	nd_print_trunc(ndo);
-	return -1;
+	nd_trunc(ndo);
 }
 
-static int
-ahcp_seconds_print(netdissect_options *ndo, const u_char *cp, const u_char *ep)
+static void
+ahcp_seconds_print(netdissect_options *ndo,
+                   const u_char *cp, uint8_t len)
 {
-	if (cp + 4 != ep)
+	if (len != 4)
 		goto invalid;
 	ND_PRINT(": %us", GET_BE_U_4(cp));
-	return 0;
+	return;
 
 invalid:
 	nd_print_invalid(ndo);
-	ND_TCHECK_LEN(cp, ep - cp);
-	return 0;
+	ND_TCHECK_LEN(cp, len);
+	return;
 trunc:
-	nd_print_trunc(ndo);
-	return -1;
+	nd_trunc(ndo);
 }
 
-static int
-ahcp_ipv6_addresses_print(netdissect_options *ndo, const u_char *cp, const u_char *ep)
+static void
+ahcp_ipv6_addresses_print(netdissect_options *ndo,
+                          const u_char *cp, uint8_t len)
 {
 	const char *sep = ": ";
 
-	while (cp < ep) {
-		if (cp + 16 > ep)
+	while (len) {
+		if (len < 16)
 			goto invalid;
 		ND_PRINT("%s%s", sep, GET_IP6ADDR_STRING(cp));
 		cp += 16;
+		len -= 16;
 		sep = ", ";
 	}
-	return 0;
+	return;
 
 invalid:
 	nd_print_invalid(ndo);
-	ND_TCHECK_LEN(cp, ep - cp);
-	return 0;
+	ND_TCHECK_LEN(cp, len);
+	return;
 trunc:
-	nd_print_trunc(ndo);
-	return -1;
+	nd_trunc(ndo);
 }
 
-static int
-ahcp_ipv4_addresses_print(netdissect_options *ndo, const u_char *cp, const u_char *ep)
+static void
+ahcp_ipv4_addresses_print(netdissect_options *ndo,
+                          const u_char *cp, uint8_t len)
 {
 	const char *sep = ": ";
 
-	while (cp < ep) {
-		if (cp + 4 > ep)
+	while (len) {
+		if (len < 4)
 			goto invalid;
 		ND_PRINT("%s%s", sep, GET_IPADDR_STRING(cp));
 		cp += 4;
+		len -= 4;
 		sep = ", ";
 	}
-	return 0;
+	return;
 
 invalid:
 	nd_print_invalid(ndo);
-	ND_TCHECK_LEN(cp, ep - cp);
-	return 0;
+	ND_TCHECK_LEN(cp, len);
+	return;
 trunc:
-	nd_print_trunc(ndo);
-	return -1;
+	nd_trunc(ndo);
 }
 
-static int
-ahcp_ipv6_prefixes_print(netdissect_options *ndo, const u_char *cp, const u_char *ep)
+static void
+ahcp_ipv6_prefixes_print(netdissect_options *ndo,
+                         const u_char *cp, uint8_t len)
 {
 	const char *sep = ": ";
 
-	while (cp < ep) {
-		if (cp + 17 > ep)
+	while (len) {
+		if (len < 17)
 			goto invalid;
-		ND_TCHECK_LEN(cp, 17);
-		ND_PRINT("%s%s/%u", sep, GET_IP6ADDR_STRING(cp),
-			 GET_U_1(cp + 16));
+		ND_PRINT("%s%s/%u", sep, GET_IP6ADDR_STRING(cp), GET_U_1(cp + 16));
 		cp += 17;
+		len -= 17;
 		sep = ", ";
 	}
-	return 0;
+	return;
 
 invalid:
 	nd_print_invalid(ndo);
-	ND_TCHECK_LEN(cp, ep - cp);
-	return 0;
+	ND_TCHECK_LEN(cp, len);
+	return;
 trunc:
-	nd_print_trunc(ndo);
-	return -1;
+	nd_trunc(ndo);
 }
 
-static int
-ahcp_ipv4_prefixes_print(netdissect_options *ndo, const u_char *cp, c