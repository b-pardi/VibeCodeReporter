@@ -7,47 +7,52 @@
 // Map: NAT level one.  Dest IP and port -> ID and num backends.
 
 struct calico_nat_v4_key {
-    uint32_t addr; // NBO
-    uint16_t port; // HBO
-    uint8_t protocol;
-} __attribute__((packed));
+	uint32_t addr; // NBO
+	uint16_t port; // HBO
+	uint8_t protocol;
+	uint8_t pad;
+};
 
 struct calico_nat_v4_value {
-    uint32_t id;
-    uint32_t count;
+	uint32_t id;
+	uint32_t count;
 };
 
 struct bpf_map_def_extended __attribute__((section("maps"))) calico_nat_map_v4 = {
-    .type           = BPF_MAP_TYPE_HASH,
-    .key_size       = sizeof(struct calico_nat_v4_key),
-    .value_size     = sizeof(struct calico_nat_v4_value),
-    .map_flags          = BPF_F_NO_PREALLOC,
-    .max_entries       = 511000, // arbitrary
+	.type = BPF_MAP_TYPE_HASH,
+	.key_size = sizeof(struct calico_nat_v4_key),
+	.value_size = sizeof(struct calico_nat_v4_value),
+	.map_flags = BPF_F_NO_PREALLOC,
+	.max_entries = 511000, // arbitrary
+	.pinning_strategy = 2 /* global namespace */,
 };
 
 // Map: NAT level two.  ID and ordinal -> new dest and port.
 
 struct calico_nat_secondary_v4_key {
-    uint32_t id;
-    uint32_t ordinal;
+	uint32_t id;
+	uint32_t ordinal;
 };
 
 struct calico_nat_dest {
-    uint32_t addr;
-    uint16_t port;
+	uint32_t addr;
+	uint16_t port;
+	uint8_t pad[2];
 };
 
 struct bpf_map_def_extended __attribute__((section("maps"))) calico_nat_secondary_map_v4 = {
-    .type           = BPF_MAP_TYPE_HASH,
-    .key_size       = sizeof(struct calico_nat_secondary_v4_key),
-    .value_size     = sizeof(struct calico_nat_dest),
-    .map_flags          = BPF_F_NO_PREALLOC,
-    .max_entries       = 510000, // arbitrary
+	.type = BPF_MAP_TYPE_HASH,
+	.key_size = sizeof(struct calico_nat_secondary_v4_key),
+	.value_size = sizeof(struct calico_nat_dest),
+	.map_flags = BPF_F_NO_PREALLOC,
+	.max_entries = 510000, // arbitrary
+	.pinning_strategy = 2 /* global namespace */,
 };
 
-struct calico_nat_dest *calico_v4_nat_lookup(__u8 ip_proto, __be32 ip_dst, __u16 dport, enum calico_tc_flags flags) {
+struct calico_nat_dest* calico_v4_nat_lookup(__u8 ip_proto, __be32 ip_dst, __u16 dport,
+	enum calico_tc_flags flags) {
 	if (((flags & CALI_TC_HOST_EP) && !(flags & CALI_TC_INGRESS)) ||
-				(!(flags & CALI_TC_HOST_EP) && (flags & CALI_TC_INGRESS))) {
+		(!(flags & CALI_TC_HOST_EP) && (flags & CALI_TC_INGRESS))) {
 		// Skip NAT lookup for traffic leaving the host namespace.
 		return NULL;
 	}
@@ -60,7 +65,8 @@ struct calico_nat_dest *calico_v4_nat_lookup(__u8 ip_proto, __be32 ip_dst, __u16
 
 	struct calico_nat_v4_value *nat_lv1_val = bpf_map_lookup_elem(&calico_nat_map_v4, &nat_key);
 	CALI_DEBUG("NAT: 1st level lookup addr=%x port=%x protocol=%x.\n",
-			(int)be32_to_host(nat_key.addr), (int)be16_to_host(nat_key.port), (int)(nat_key.protocol));
+		(int)be32_to_host(nat_key.addr), (int)be16_to_host(nat_key.port),
+		(int)(nat_key.protocol));
 	if (!nat_lv1_val) {
 		return NULL;
 	}
@@ -70,10 +76,9 @@ struct calico_nat_dest *calico_v4_nat_lookup(__u8 ip_proto, __be32 ip_dst, __u16
 		.ordinal = bpf_get_prandom_u32() % nat_lv1_val->count,
 	};
 	CALI_DEBUG("NAT: 1st level hit; id=%d ordinal=%d\n", nat_lv2_key.id, nat_lv2_key.ordinal);
-	struct calico_nat_dest *nat_lv2_val = bpf_map_lookup_elem(&calico_nat_secondary_map_v4, &nat_lv2_key);
+	struct calico_nat_dest *nat_lv2_val = bpf_map_lookup_elem(&calico_nat_secondary_map_v4,
+		&nat_lv2_key);
 	return nat_lv2_val;
 }
 
-
-
 #endif /* __CALI_NAT_H__ */