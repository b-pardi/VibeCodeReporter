@@ -41,7 +41,7 @@ static CALI_BPF_INLINE void do_nat_common(struct bpf_sock_addr *ctx, uint8_t pro
 	 * XXX same affinity, which (a) is sub-optimal and (b) leaks info between
 	 * XXX workloads.
 	 */
-	uint8_t nat_lvl1_drop = 0;
+	bool nat_lvl1_drop = 0;
 	uint16_t dport_he = (uint16_t)(be32_to_host(ctx->user_port)>>16);
 	struct calico_nat_dest *nat_dest;
 	nat_dest = calico_v4_nat_lookup(0, ctx->user_ip4, proto, dport_he, &nat_lvl1_drop);




@@ -134,7 +134,7 @@ static CALI_BPF_INLINE struct calico_nat_dest* calico_v4_nat_lookup2(__be32 ip_s
 								     __u8 ip_proto,
 								     __u16 dport,
 								     bool from_tun,
-								     __u8 *drop)
+								     bool *drop)
 {
 	struct calico_nat_v4_key nat_key = {
 		.prefixlen = 88,
@@ -143,11 +143,6 @@ static CALI_BPF_INLINE struct calico_nat_dest* calico_v4_nat_lookup2(__be32 ip_s
 		.protocol = ip_proto,
 		.saddr = ip_src,
 	};
-	struct calico_nat_v4 nat_data = {
-		.addr = ip_dst,
-		.port = dport,
-		.protocol = ip_proto,
-	};
 	struct calico_nat_v4_value *nat_lv1_val;
 	struct calico_nat_secondary_v4_key nat_lv2_key;
 	struct calico_nat_dest *nat_lv2_val;
@@ -211,6 +206,10 @@ static CALI_BPF_INLINE struct calico_nat_dest* calico_v4_nat_lookup2(__be32 ip_s
 		}
 		CALI_DEBUG("NAT: nodeport hit\n");
 	}
+	/* With LB source range, we install a drop entry in the NAT FE map
+	 * with a special ID 0xffffffff and count 0. If we hit this entry,
+	 * packet is dropped.
+	 */
 	if (nat_lv1_val->id == 0xffffffff && nat_lv1_val->count == 0) {
 		*drop = 1;
 		return NULL;
@@ -228,6 +227,11 @@ static CALI_BPF_INLINE struct calico_nat_dest* calico_v4_nat_lookup2(__be32 ip_s
 		goto skip_affinity;
 	}
 
+        struct calico_nat_v4 nat_data = {
+                .addr = ip_dst,
+                .port = dport,
+                .protocol = ip_proto,
+        };
 	affkey.nat_key =  nat_data;
 	affkey.client_ip = ip_src;
 
@@ -293,7 +297,7 @@ skip_affinity:
 }
 
 static CALI_BPF_INLINE struct calico_nat_dest* calico_v4_nat_lookup(__be32 ip_src, __be32 ip_dst,
-								    __u8 ip_proto, __u16 dport, __u8 *drop)
+								    __u8 ip_proto, __u16 dport, bool *drop)
 {
 	return calico_v4_nat_lookup2(ip_src, ip_dst, ip_proto, dport, false, drop);
 }




@@ -35,7 +35,7 @@ enum calico_reason {
 	CALI_REASON_ICMP_DF = 0x1c,
 	CALI_REASON_IP_OPTIONS = 0xeb,
 	CALI_REASON_IP_MALFORMED = 0xec,
-	CALI_REASON_UNKNOWN_SOURCE = 0xed,
+	CALI_REASON_UNAUTH_SOURCE = 0xed,
 	CALI_REASON_RT_UNKNOWN = 0xdead,
 };
 




@@ -336,7 +336,7 @@ static CALI_BPF_INLINE int calico_tc(struct __sk_buff *skb)
 		.reason = CALI_REASON_UNKNOWN,
 	};
 	struct calico_nat_dest *nat_dest = NULL;
-	__u8 nat_lvl1_drop = 0;
+	bool nat_lvl1_drop = 0;
 
 	/* we assume we do FIB and from this point on, we only set it to false
 	 * if we decide not to do it.
@@ -600,7 +600,7 @@ static CALI_BPF_INLINE int calico_tc(struct __sk_buff *skb)
 
 	if (nat_lvl1_drop) {
 		CALI_DEBUG("Packet is from an unauthorised source: DROP\n");
-		fwd.reason = CALI_REASON_UNKNOWN_SOURCE;
+		fwd.reason = CALI_REASON_UNAUTH_SOURCE;
 		goto deny;
 	}
 	if (nat_dest != NULL) {