@@ -57,6 +57,9 @@ class VulnerabilityTemplateSchema(AutoSchema):
         allow_none=True)
     policyviolations = fields.List(fields.String,
                                    attribute='policy_violations')
+    creator = PrimaryKeyRelatedField('username', dump_only=True, attribute='creator')
+    create_at = fields.DateTime(attribute='create_date',
+                        dump_only=True)
 
     # Here we use vulnerability instead of vulnerability_template to avoid duplicate row
     # in the custom_fields_schema table.
@@ -68,7 +71,7 @@ class VulnerabilityTemplateSchema(AutoSchema):
         fields = ('id', '_id', '_rev', 'cwe', 'description', 'desc',
                   'exploitation', 'name', 'references', 'refs', 'resolution',
                   'impact', 'easeofresolution', 'policyviolations', 'data',
-                  'customfields')
+                  'customfields', 'creator', 'create_at')
 
     def get_references(self, obj):
         return ', '.join(map(lambda ref_tmpl: ref_tmpl.name, obj.reference_template_instances))




@@ -28,6 +28,8 @@ angular.module('faradayApp').
                 this.policyviolations = [];
                 this.customfields = [];
                 this.easeofresolution = "";
+                this.creator = "";
+                this.create_at = "";
                 if (data) {
                     if(data.name === undefined || data.name === "") {
                         throw new Error("Unable to create a Vulnerability Model whithout a name");
@@ -39,7 +41,8 @@ angular.module('faradayApp').
             VulnModel.prototype = {
 
                 public_properties: ['exploitation', 'references', 'name', 'resolution', 'cwe', 'description',
-                                    'desc', 'data', 'id', 'refs', 'impact', 'easeofresolution', 'policyviolations', 'customfields'],
+                                    'desc', 'data', 'id', 'refs', 'impact', 'easeofresolution', 'policyviolations', 'customfields',
+                                    'creator', 'create_at'],
 
                 set: function(data) {
                     var self = this;