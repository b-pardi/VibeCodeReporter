@@ -24,6 +24,8 @@
 
 #include "ntop_includes.h"
 
+#ifdef HAVE_MYSQL
+
 class MySQLDB : public DB {
  protected:
   MYSQL mysql;
@@ -64,4 +66,6 @@ class MySQLDB : public DB {
   int exec_single_query(lua_State *vm, char *sql);
 };
 
+#endif
+
 #endif /* _MYSQL_DB_CLASS_H_ */




@@ -21,6 +21,8 @@
 
 #include "ntop_includes.h"
 
+#ifndef HAVE_NEDGE
+
 /* ************************************************ */
 
 void ListeningPorts::parsePortInfo(json_object *z, std::map <u_int16_t, ListeningPortInfo> *info) {
@@ -70,3 +72,5 @@ void ListeningPorts::parsePorts(json_object *z) {
       parsePortInfo(p, &udp6);
   }
 }
+
+#endif