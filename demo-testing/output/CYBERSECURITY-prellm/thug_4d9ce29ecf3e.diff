@@ -330,3 +330,16 @@ class TestMiscSamplesChrome(object):
         expected = ['[After remove and add] <div class="anotherclass"></div>', ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentType(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentType.html")
+        expected = ['Doctype: [object DocumentType]',
+                    'Doctype name: html',
+                    'Doctype nodeName: html',
+                    'Doctype nodeType: 10',
+                    'Doctype nodeValue: null',
+                    'Doctype publicId: ',
+                    'Doctype systemId: ',
+                    'Doctype textContent: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -318,3 +318,16 @@ class TestMiscSamplesFirefox(object):
         expected = ['[After remove and add] <div class="anotherclass"></div>', ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentType(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentType.html")
+        expected = ['Doctype: [object DocumentType]',
+                    'Doctype name: html',
+                    'Doctype nodeName: html',
+                    'Doctype nodeType: 10',
+                    'Doctype nodeValue: null',
+                    'Doctype publicId: ',
+                    'Doctype systemId: ',
+                    'Doctype textContent: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -406,3 +406,16 @@ class TestMiscSamplesIE(object):
         expected = ['[After remove and add] <div class="anotherclass"></div>', ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentType(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentType.html")
+        expected = ['Doctype: [object DocumentType]',
+                    'Doctype name: html',
+                    'Doctype nodeName: html',
+                    'Doctype nodeType: 10',
+                    'Doctype nodeValue: null',
+                    'Doctype publicId: ',
+                    'Doctype systemId: ',
+                    'Doctype textContent: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -386,3 +386,16 @@ class TestMiscSamplesIE(object):
         expected = ['[After remove and add] <div class="anotherclass"></div>', ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentType(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentType.html")
+        expected = ['Doctype: [object DocumentType]',
+                    'Doctype name: html',
+                    'Doctype nodeName: html',
+                    'Doctype nodeType: 10',
+                    'Doctype nodeValue: null',
+                    'Doctype publicId: ',
+                    'Doctype systemId: ',
+                    'Doctype textContent: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -355,3 +355,16 @@ class TestMiscSamplesIE(object):
         expected = ['[After remove and add] <div class="anotherclass"></div>', ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentType(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentType.html")
+        expected = ['Doctype: [object DocumentType]',
+                    'Doctype name: html',
+                    'Doctype nodeName: html',
+                    'Doctype nodeType: 10',
+                    'Doctype nodeValue: null',
+                    'Doctype publicId: ',
+                    'Doctype systemId: ',
+                    'Doctype textContent: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -355,3 +355,16 @@ class TestMiscSamplesIE(object):
         expected = ['[After remove and add] <div class="anotherclass"></div>', ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentType(self