@@ -571,3 +571,11 @@ class TestMiscSamplesChrome(object):
                     'row.deleteCell(20) failed']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTextArea(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTextArea.html")
+        expected = ['type: textarea',
+                    'cols: 100',
+                    'rows: 25']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -536,3 +536,11 @@ class TestMiscSamplesFirefox(object):
                     'c.port: ']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTextArea(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTextArea.html")
+        expected = ['type: textarea',
+                    'cols: 100',
+                    'rows: 25']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -710,3 +710,11 @@ class TestMiscSamplesIE(object):
                     'row.deleteCell(20) failed']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTextArea(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTextArea.html")
+        expected = ['type: textarea',
+                    'cols: 100',
+                    'rows: 25']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -678,3 +678,11 @@ class TestMiscSamplesIE(object):
                     'row.deleteCell(20) failed']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTextArea(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTextArea.html")
+        expected = ['type: textarea',
+                    'cols: 100',
+                    'rows: 25']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -712,3 +712,11 @@ class TestMiscSamplesIE(object):
                     'row.deleteCell(20) failed']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTextArea(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTextArea.html")
+        expected = ['type: textarea',
+                    'cols: 100',
+                    'rows: 25']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -643,3 +643,11 @@ class TestMiscSamplesIE(object):
                     'row.deleteCell(20) failed']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTextArea(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTextArea.html")
+        expected = ['type: textarea',
+                    'cols: 100',
+                    'rows: 25']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -825,3 +825,11 @@ class TestMiscSamplesIE(object):
                     'row.deleteCell(20) failed']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTextArea(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTextArea.html")
+        expected = ['type: textarea',
+                    'cols: 100',
+                    'rows: 25']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -552,3 +552,11 @@ class TestMiscSamplesChrome(object):
                     'row.deleteCell(20) failed']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTextArea(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTextArea.html")
+        expected = ['type: textarea',
+                    'cols: 100',
+                    'rows: 25']
+
+        self.do_perform_test(caplog, sample, expected)