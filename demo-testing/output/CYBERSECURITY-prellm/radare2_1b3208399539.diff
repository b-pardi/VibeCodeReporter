@@ -930,13 +930,8 @@ static void anop_esil(RAnal *a, RAnalOp *op, ut64 addr, const ut8 *buf, int len,
 	case X86_INS_PUSH:
 		{
 			dst = getarg (&gop, 0, 0, NULL, DST_AR);
-			if (!strcmp (dst, sp)) {
-				esilprintf (op, "%s,%s,&,%d,%s,-=,1,PICK,%s,=[%d]",
-					sp, sp, rs, sp, sp, rs);				
-			} else {
-				esilprintf (op, "%d,%s,-=,%s,%s,=[%d]",
-					rs, sp, dst?dst:"eax", sp, rs);				
-			}
+			esilprintf (op, "%s,%d,%s,-,=[%d],%d,%s,-=",
+				dst ? dst : "eax", rs, sp, rs, rs, sp);
 		}
 		break;
 	case X86_INS_PUSHF:




@@ -319,10 +319,10 @@ static const ut8 *r_bin_dwarf_parse_lnp_header (
 			if (buf>=buf_end) { buf = NULL; goto beach; }
 
 			if (i) {
-				char *include_dir = NULL, *comp_dir = NULL, *pinclude_dir = NULL;
+				char *include_dir = NULL, *comp_dir = NULL;
 				char *allocated_id = NULL;
 				if (id_idx > 0) {
-					include_dir = pinclude_dir = sdb_array_get (s, "includedirs", id_idx - 1, 0);
+					include_dir = sdb_array_get (s, "includedirs", id_idx - 1, 0);
 					if (include_dir && include_dir[0] != '/') {
 						comp_dir = sdb_get (bf->sdb_addrinfo, "DW_AT_comp_dir", 0);
 						if (comp_dir) {
@@ -334,7 +334,7 @@ static const ut8 *r_bin_dwarf_parse_lnp_header (
 						}
 					}
 				} else {
-					include_dir = pinclude_dir = sdb_get (bf->sdb_addrinfo, "DW_AT_comp_dir", 0);
+					include_dir = sdb_get (bf->sdb_addrinfo, "DW_AT_comp_dir", 0);
 					if (!include_dir)
 						include_dir = "./";
 				}
@@ -351,8 +351,6 @@ static const ut8 *r_bin_dwarf_parse_lnp_header (
 					hdr->file_names[count].mod_time = mod_time;
 					hdr->file_names[count].file_len = file_len;
 				}
-				free (comp_dir);
-				free (pinclude_dir);
 			}
 			count++;
 			if (f && i) {
@@ -732,18 +730,15 @@ R_API int r_bin_dwarf_parse_line_raw2(const RBin *a, const ut8 *obuf,
 		buf_tmp = buf;
 		buf = r_bin_dwarf_parse_lnp_header (a->cur, buf, buf_end, &hdr, f, mode);
 		if (!buf) {
-			r_bin_dwarf_header_fini (&hdr);
 			return false;
 		}
 		r_bin_dwarf_set_regs_default (&hdr, &regs);
 		tmplen = (int)(buf_end - buf);
 		tmplen = R_MIN (tmplen, 4 + hdr.unit_length.part1);
 		if (tmplen < 1) {
-			r_bin_dwarf_header_fini (&hdr);
 			break;
 		}
 		if (!r_bin_dwarf_parse_opcodes (a, buf, tmplen, &hdr, &regs, f, mode)) {
-			r_bin_dwarf_header_fini (&hdr);
 			break;
 		}
 




@@ -694,7 +694,12 @@ static int cmd_yank(void *data, const char *input) {
 	return true;
 }
 
-R_API int r_core_run_script (RCore *core, const char *file) {
+static int lang_run_file(RCore *core, RLang *lang, const char *file) {
+	r_core_sysenv_begin (core, NULL);
+	return r_lang_run_file (core->lang, file);
+}
+
+R_API int r_core_run_script(RCore *core, const char *file) {
 	int ret = false;
 	RListIter *iter;
 	RLangPlugin *p;
@@ -724,10 +729,9 @@ R_API int r_core_run_script (RCore *core, const char *file) {
 		ret = out? true: false;
 	} else {
 		p = r_lang_get_by_extension (core->lang, file);
-		r_core_sysenv_begin (core, NULL);
 		if (p) {
 			r_lang_use (core->lang, p->name);
-			ret = r_lang_run_file (core->lang, file);
+			ret = lang_run_file (core, core->lang, file);
 		} else {
 #if __WINDOWS__
 #define cmdstr(x) r_str_newf (x" %s", file);
@@ -741,7 +745,7 @@ R_API int r_core_run_script (RCore *core, const char *file) {
 				if (!strcmp (ext, "js")) {
 					char *cmd = cmdstr ("node");
 					r_lang_use (core->lang, "pipe");
-					r_lang_run_file (core->lang, cmd);
+					lang_run_file (core, core->lang, cmd);
 					free (cmd);
 					ret = 1;
 				} else if (!strcmp (ext, "exe")) {
@@ -751,59 +755,59 @@ R_API int r_core_run_script (RCore *core, const char *file) {
 					char *cmd = cmdstr ("wine");
 #endif
 					r_lang_use (core->lang, "pipe");
-					r_lang_run_file (core->lang, cmd);
+					lang_run_file (core, core->lang, cmd);
 					free (cmd);
 					ret = 1;
 				} else if (!strcmp (ext, "zig")) {
 					char *cmd = cmdstr ("zig run");
 					r_lang_use (core->lang, "pipe");
-					r_lang_run_file (core->lang, cmd);
+					lang_run_file (core, core-