@@ -279,7 +279,7 @@ static void setASLR(RRunProfile *r, int enabled) {
 	// for osxver>=10.7
 	// "unset the MH_PIE bit in an already linked executable" with --no-pie flag of the script
 	// the right way is to disable the aslr bit in the spawn call
-#elif __FreeBSD__ || __NetBSD__
+#elif __FreeBSD__ || __NetBSD__ || __DragonFly__
 	r_sys_aslr (enabled);
 #else
 	// not supported for this platform




@@ -17,6 +17,10 @@
 #  define FREEBSD_WITH_BACKTRACE
 # endif
 #endif
+#if defined(__DragonFly__)
+# include <sys/param.h>
+# include <sys/sysctl.h>
+#endif
 #if defined(__HAIKU__)
 # include <kernel/image.h>
 # include <sys/param.h>
@@ -558,6 +562,13 @@ R_API bool r_sys_aslr(int val) {
 		eprintf ("Failed to set RVA\n");
 		ret = false;
 	}
+#elif __DragonFly__
+	size_t vlen = sizeof (val);
+	if (sysctlbyname ("vm.randomize_mmap", NULL, 0, &val, vlen) == -1) {
+		eprintf ("Failed to set RVA\n");
+		ret = false;
+	}
+#elif __DragonFly__
 #endif
 	return ret;
 }