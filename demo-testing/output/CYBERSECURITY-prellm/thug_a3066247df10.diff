@@ -286,3 +286,9 @@ class TestMiscSamplesChrome(object):
         expected = ["Alert Text: New child", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testCookie(self, caplog):
+        sample   = os.path.join(self.misc_path, "testCookie.html")
+        expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -274,3 +274,9 @@ class TestMiscSamplesFirefox(object):
         expected = ["Alert Text: New child", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testCookie(self, caplog):
+        sample   = os.path.join(self.misc_path, "testCookie.html")
+        expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -365,3 +365,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: New child", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testCookie(self, caplog):
+        sample   = os.path.join(self.misc_path, "testCookie.html")
+        expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -345,3 +345,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: New child", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testCookie(self, caplog):
+        sample   = os.path.join(self.misc_path, "testCookie.html")
+        expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -320,3 +320,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: New child", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testCookie(self, caplog):
+        sample   = os.path.join(self.misc_path, "testCookie.html")
+        expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -320,3 +320,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: New child", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testCookie(self, caplog):
+        sample   = os.path.join(self.misc_path, "testCookie.html")
+        expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -370,3 +370,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: New child", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testCookie(self, caplog):
+        sample   = os.path.join(self.misc_path, "testCookie.html")
+        expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -276,3 +276,9 @@ class TestMiscSamplesChrome(object):
         expected = ["Alert Text: New child", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testCookie(self, caplog):
+        sample   = os.path.join(self.misc_path, "testCookie.html")
+        expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
+
+        self.do_perform_test(caplog, sample, expected)