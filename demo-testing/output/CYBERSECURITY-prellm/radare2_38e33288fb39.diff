@@ -1225,9 +1225,9 @@ static bool cb_cfgcharset(void *user, void *data) {
 	} else {
 		char *syscs = r_str_newf ("%s%s.sdb", cs, cf);
 		if (r_file_exists (syscs)) {
-			rc = r_charset_open (core->charset, syscs);
+			rc = r_charset_open (core->print->charset, syscs);
 		} else {
-			rc = r_charset_open (core->charset, cf);
+			rc = r_charset_open (core->print->charset, cf);
 		}
 		if (!rc) {
 			eprintf ("Warning: Cannot load charset file '%s' '%s'.\n", syscs, cf);




@@ -5846,10 +5846,10 @@ l = use_blocksize;
 				size_t out_len = len * 10;
 				ut8 *out = calloc (len, 10);
 				if (out) {
-					ut8 *data = calloc (1, len);
+					ut8 *data = malloc (len);
 					if (data) {
 						r_io_read_at (core->io, core->offset, data, len);
-						r_charset_encode_str (core->charset, out, out_len, data, len);
+						r_charset_encode_str (core->print->charset, out, out_len, data, len);
 						r_print_string (core->print, core->offset,
 							out, len, R_PRINT_STRING_ZEROEND);
 						free (data);




@@ -2713,8 +2713,7 @@ R_API bool r_core_init(RCore *core) {
 	core->printidx = 0;
 	core->lastcmd = NULL;
 	core->cmdlog = NULL;
-	core->charset = r_charset_new ();
-	core->charset->db = sdb_ns (core->sdb, "charset", 1);
+	core->print->charset->db = sdb_ns (core->sdb, "charset", 1);
 	core->stkcmd = NULL;
 	core->cmdqueue = NULL;
 	core->cmdrepeat = true;
@@ -2902,7 +2901,6 @@ R_API void r_core_fini(RCore *c) {
 	if (!c) {
 		return;
 	}
-	r_charset_free (c->charset); // TODO. maybe move into RPrint?
 	r_core_task_break_all (&c->tasks);
 	r_core_task_join (&c->tasks, NULL, -1);
 	r_core_wait (c);




@@ -331,7 +331,6 @@ struct r_core_t {
 	bool log_events; // core.c:cb_event_handler : log actions from events if cfg.log.events is set
 	RList *ropchain;
 	bool use_tree_sitter_r2cmd;
-	RCharset *charset;
 
 	bool marks_init;
 	ut64 marks[UT8_MAX + 1];




@@ -125,6 +125,7 @@ typedef struct r_print_t {
 	// represents the first not-visible offset on the screen
 	// (only when in visual disasm mode)
 	ut64 screen_bounds;
+	RCharset *charset;
 } RPrint;
 
 #ifdef R_API




@@ -342,6 +342,7 @@ R_API RPrint* r_print_new(void) {
 	p->strconv_mode = NULL;
 	memset (&p->consbind, 0, sizeof (p->consbind));
 	p->io_unalloc_ch = '.';
+	p->charset = r_charset_new ();
 	return p;
 }
 
@@ -359,6 +360,7 @@ R_API RPrint* r_print_free(RPrint *p) {
 	}
 	R_FREE (p->lines_cache);
 	R_FREE (p->row_offsets);
+	r_charset_free (p->charset);
 	free (p);
 	return NULL;
 }