@@ -7,16 +7,17 @@ import time
 import jspcap
 
 
-lid = list()
-for index in range(1, 101):
-    now = time.time()
+for engine in {'default', 'pyshark', 'scapy', 'dpkt'}:
+    lid = list()
+    for index in range(1, 101):
+        now = time.time()
 
-    extraction = jspcap.extract(fin='../sample/in.pcap', tcp=True, store=False, nofile=True, strict=True)
+        extraction = jspcap.extract(fin='../sample/in.pcap', store=False, nofile=True, strict=True, engine=engine)
 
-    delta = time.time() - now
-    print(f'No. {index:>3d}: {extraction.length} packets extracted and reassembled in {delta} seconds.')
-    lid.append(float(delta))
+        delta = time.time() - now
+        # print(f'[{engine}] No. {index:>3d}: {extraction.length} packets extracted in {delta} seconds.')
+        lid.append(float(delta))
 
-avetime = statistics.mean(lid)
-average = avetime / extraction.length
-print(f'Report: {average} seconds per packet.')
+    avetime = statistics.mean(lid)
+    average = avetime / extraction.length
+    print(f'Report: [{engine}] {average} seconds per packet.')