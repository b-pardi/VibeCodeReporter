@@ -626,3 +626,11 @@ class TestMiscSamplesChrome(object):
                     'height: 4']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLImageElement(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLImageElement.html")
+        expected = ['src (before changes): test.jpg',
+                    'src (after first change): test2.jpg',
+                    'onerror handler fired']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -591,3 +591,11 @@ class TestMiscSamplesFirefox(object):
                     'height: 4']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLImageElement(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLImageElement.html")
+        expected = ['src (before changes): test.jpg',
+                    'src (after first change): test2.jpg',
+                    'onerror handler fired']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -808,3 +808,11 @@ class TestMiscSamplesIE(object):
                     'height: 4']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLImageElement(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLImageElement.html")
+        expected = ['src (before changes): test.jpg',
+                    'src (after first change): test2.jpg',
+                    'onerror handler fired']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -776,3 +776,11 @@ class TestMiscSamplesIE(object):
                     'height: 4']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLImageElement(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLImageElement.html")
+        expected = ['src (before changes): test.jpg',
+                    'src (after first change): test2.jpg',
+                    'onerror handler fired']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -810,3 +810,11 @@ class TestMiscSamplesIE(object):
                     'height: 4']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLImageElement(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLImageElement.html")
+        expected = ['src (before changes): test.jpg',
+                    'src (after first change): test2.jpg',
+                    'onerror handler fired']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -741,3 +741,11 @@ class TestMiscSamplesIE(object):
                     'height: 4']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLImageElement(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLImageElement.html")
+        expected = ['src (before changes): test.jpg',
+                    'src (after first change): test2.jpg',
+                    'onerror handler fired']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -949,3 +949,11 @@ class TestMiscSamplesIE(object):
                     'height: 4']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLImageElement(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLImageElement.html")
+        expected = ['src (before changes): test.jpg',
+                    'src (after first change): test2.jpg',
+                    'onerror handler fired']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -607,3 +607,11 @@ class TestMiscSamplesChrome(object):
                     'height: 4']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLImageElement(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLImageElement.html")
+        expected = ['src (before changes): test.jpg',
+                    'src (after first change): test2.jpg',
+                    'onerror handler fired']
+
+        self.do_perform_test(caplog, sample, expected)