@@ -613,6 +613,8 @@ class TestMiscSamplesChrome(object):
 
     def test_testFrame(self, caplog):
         sample   = os.path.join(self.misc_path, "testFrame.html")
-        expected = ['[frame redirection]']
+        expected = ['[frame redirection]',
+                    'Alert Text: https://buffer.github.io/thug/'
+                    'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)




@@ -578,6 +578,8 @@ class TestMiscSamplesFirefox(object):
 
     def test_testFrame(self, caplog):
         sample   = os.path.join(self.misc_path, "testFrame.html")
-        expected = ['[frame redirection]']
+        expected = ['[frame redirection]',
+                    'Alert Text: https://buffer.github.io/thug/',
+                    'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)




@@ -795,6 +795,8 @@ class TestMiscSamplesIE(object):
 
     def test_testFrame(self, caplog):
         sample   = os.path.join(self.misc_path, "testFrame.html")
-        expected = ['[frame redirection]']
+        expected = ['[frame redirection]',
+                    'Alert Text: https://buffer.github.io/thug/',
+                    'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)




@@ -763,6 +763,8 @@ class TestMiscSamplesIE(object):
 
     def test_testFrame(self, caplog):
         sample   = os.path.join(self.misc_path, "testFrame.html")
-        expected = ['[frame redirection]']
+        expected = ['[frame redirection]',
+                    'Alert Text: https://buffer.github.io/thug/',
+                    'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)




@@ -797,6 +797,8 @@ class TestMiscSamplesIE(object):
 
     def test_testFrame(self, caplog):
         sample   = os.path.join(self.misc_path, "testFrame.html")
-        expected = ['[frame redirection]']
+        expected = ['[frame redirection]',
+                    'Alert Text: https://buffer.github.io/thug/',
+                    'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)




@@ -728,6 +728,8 @@ class TestMiscSamplesIE(object):
 
     def test_testFrame(self, caplog):
         sample   = os.path.join(self.misc_path, "testFrame.html")
-        expected = ['[frame redirection]']
+        expected = ['[frame redirection]',
+                    'Alert Text: https://buffer.github.io/thug/',
+                    'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)




@@ -910,7 +910,9 @@ class TestMiscSamplesIE(object):
 
     def test_testFrame(self, caplog):
         sample   = os.path.join(self.misc_path, "testFrame.html")
-        expected = ['[frame redirection]']
+        expected = ['[frame redirection]',
+                    'Alert Text: https://buffer.github.io/thug/',
+                    'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)
 




@@ -594,6 +594,8 @@ class TestMiscSamplesChrome(object):
 
     def test_testFrame(self, caplog):
         sample   = os.path.join(self.misc_path, "testFrame.html")
-        expected = ['[frame redirection]']
+        expected = ['[frame redirection]',
+                    'Alert Text: https://buffer.github.io/thug/',
+                    'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)




@@ -27,10 +27,7 @@ class HTMLFrameElement(HTMLElement):
 
     @property
     def contentWindow(self):
-        if self.id in log.ThugLogging.windows:
-            return log.ThugLogging.windows[self.id]
+        # if self.id 