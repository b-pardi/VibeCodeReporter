@@ -115,6 +115,28 @@ def raw_ability(deploy_ability, test_executor):
     return raw_ability
 
 
+@pytest.fixture
+def app_config():
+    return {
+        'app.contact.dns.domain': 'mycaldera.caldera',
+        'app.contact.dns.socket': '0.0.0.0:8853',
+        'app.contact.ftp.host': '0.0.0.0',
+        'app.contact.http': 'http://0.0.0.0:8888',
+        'app.contact.tcp': '0.0.0.0:7010'
+    }
+
+
+@pytest.fixture
+def agent_config():
+    return {
+        'deadman_abilities': [],
+        'implant_name': 'test',
+        'sleep_max': 6,
+        'sleep_min': 3,
+        'watchdog': 0
+    }
+
+
 @pytest.fixture
 def combined_config(agent_config, app_config):
     app_config.update({f'agents.{k}': v for k, v in agent_config.items()})




@@ -260,28 +260,6 @@ def agent_profile():
     return _agent_profile
 
 
-@pytest.fixture
-def app_config():
-    return {
-        'app.contact.dns.domain': 'mycaldera.caldera',
-        'app.contact.dns.socket': '0.0.0.0:8853',
-        'app.contact.ftp.host': '0.0.0.0',
-        'app.contact.http': 'http://0.0.0.0:8888',
-        'app.contact.tcp': '0.0.0.0:7010'
-    }
-
-
-@pytest.fixture
-def agent_config():
-    return {
-        'deadman_abilities': [],
-        'implant_name': 'test',
-        'sleep_max': 6,
-        'sleep_min': 3,
-        'watchdog': 0
-    }
-
-
 @pytest.fixture
 def api_v2_client(loop, aiohttp_client, contact_svc):
     def make_app(svcs):