@@ -188,11 +188,13 @@ mapping.
     try:
         return (b'\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\xff\xff' +
                 socket.inet_aton(ipval))
-    except socket.error:
+    except (socket.error, ValueError, TypeError):
+        # Value and Type Errors when correct unicode but
+        # already a binary representation
         pass
     try:
         return socket.inet_pton(socket.AF_INET6, ipval)
-    except socket.error:
+    except (socket.error, ValueError, TypeError):
         pass
     # Probably already a binary representation
     if len(ipval) == 16:




@@ -2521,6 +2521,11 @@ which `predicate()` is True, given `webflt`.
         )
         self.assertEqual(ivre.utils.ip2bin(b'T3STTESTTESTTEST'),
                          b'T3STTESTTESTTEST')
+        self.assertEqual(
+            ivre.utils.ip2bin(
+                b' \x01H`\x00\x00 \x01\x00\x00\x00\x00\x00\x00\x00h'),
+            b' \x01H`\x00\x00 \x01\x00\x00\x00\x00\x00\x00\x00h'
+        )
         # str2pyval
         self.assertEqual(ivre.utils.str2pyval("{'test': 0}"), {'test': 0})
         self.assertEqual(ivre.utils.str2pyval("{'test: 0}"), "{'test: 0}")