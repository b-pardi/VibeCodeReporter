@@ -1375,7 +1375,7 @@ public class F5BigipStructuredConfigurationBuilder extends F5BigipStructuredPars
 
   @Override
   public void exitSgs_hostname(Sgs_hostnameContext ctx) {
-    String hostname = unquote(ctx.hostname.getText());
+    String hostname = unquote(ctx.hostname.getText()).toLowerCase();
     _c.setHostname(hostname);
   }
 




@@ -647,6 +647,15 @@ public final class F5BigipStructuredGrammarTest {
     assertThat(configurations, hasKey(hostname));
   }
 
+  @Test
+  public void testHostnameCapital() throws IOException {
+    String filename = "f5_bigip_structured_hostname_capital";
+    String hostname = "myhostname";
+    Map<String, Configuration> configurations = parseTextConfigs(filename);
+
+    assertThat(configurations, hasKey(hostname));
+  }
+
   @Test
   public void testImish() {
     assertTrue(