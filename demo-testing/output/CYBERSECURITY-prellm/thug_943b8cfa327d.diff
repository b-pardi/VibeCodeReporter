@@ -502,3 +502,9 @@ class TestMiscSamplesChrome(object):
         expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild2.html")
+        expected = ['<div id="foobar"><div id="test"></div></div>']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -481,3 +481,9 @@ class TestMiscSamplesFirefox(object):
         expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild2.html")
+        expected = ['<div id="foobar"><div id="test"></div></div>']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -574,3 +574,9 @@ class TestMiscSamplesIE(object):
         expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild2.html")
+        expected = ['<div id="foobar"><div id="test"></div></div>']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -543,3 +543,9 @@ class TestMiscSamplesIE(object):
         expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild2.html")
+        expected = ['<div id="foobar"><div id="test"></div></div>']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -577,3 +577,9 @@ class TestMiscSamplesIE(object):
         expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild2.html")
+        expected = ['<div id="foobar"><div id="test"></div></div>']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -508,3 +508,9 @@ class TestMiscSamplesIE(object):
         expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild2.html")
+        expected = ['<div id="foobar"><div id="test"></div></div>']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -655,3 +655,9 @@ class TestMiscSamplesIE(object):
         expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild2.html")
+        expected = ['<div id="foobar"><div id="test"></div></div>']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -483,3 +483,9 @@ class TestMiscSamplesChrome(object):
         expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testReplaceChild2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testReplaceChild2.html")
+        expected = ['<div id="foobar"><div id="test"></div></div>']
+
+        self.do_perform_test(caplog, s