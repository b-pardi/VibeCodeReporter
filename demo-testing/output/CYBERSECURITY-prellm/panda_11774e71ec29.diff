similarity index 98%
rename from panda/pypanda/utils/create_panda_datatypes.py
rename to panda/python/core/create_panda_datatypes.py



@@ -17,9 +17,9 @@ if sys.version_info[0] < 3:
 #
 
 #XXX: When trying to install via pip these files get copied to /tmp and then our paths are all bad
-OUTPUT_DIR = os.path.abspath(os.path.join(*[os.path.dirname(__file__), "..", "panda", "autogen"]))                       # panda-git/panda/pypanda/panda/autogen
+OUTPUT_DIR = os.path.abspath(os.path.join(*[os.path.dirname(__file__), "..", "core", "panda", "autogen"]))               # panda-git/panda/python/core/panda/autogen
 PLUGINS_DIR = os.path.abspath(os.path.join(*[os.path.dirname(__file__), "..", "..", "plugins"]))                         # panda-git/panda/plugins
-INCLUDE_DIR_PYP = os.path.abspath(os.path.join(*[os.path.dirname(__file__), "..", "..", "pypanda", "panda", "include"])) # panda-git/panda/pypanda/panda/include
+INCLUDE_DIR_PYP = os.path.abspath(os.path.join(*[os.path.dirname(__file__), "..", "core", "panda", "include"]))          # panda-git/panda/python/core/panda/include
 INCLUDE_DIR_PAN = os.path.abspath(os.path.join(*[os.path.dirname(__file__), "..", "..", "include", "panda"]))            # panda-git/panda/include/panda
 
 

similarity index 70%
rename from panda/pypanda/panda/__init__.py
rename to panda/python/core/panda/__init__.py



@@ -1,3 +1,3 @@
-from .pypanda import Panda
+from .main import Panda
 from .decorators import blocking
 from .ffi_importer import ffi

similarity index 100%
rename from panda/pypanda/panda/arm/__init__.py
rename to panda/python/core/panda/arm/__init__.py

similarity index 100%
rename from panda/pypanda/panda/arm/helper.py
rename to panda/python/core/panda/arm/helper.py

similarity index 100%
rename from panda/pypanda/panda/asyncthread.py
rename to panda/python/core/panda/asyncthread.py

similarity index 81%
rename from panda/pypanda/panda/blocking_mixins.py
rename to panda/python/core/panda/blocking_mixins.py



@@ -130,3 +130,31 @@ class blocking_mixins():
             except:
                 traceback.print_exc()
             os._exit(1) # Force process to exit now
+
+    @blocking
+    def interact(self, confirm_quit=True):
+        '''
+        Expose console interactively until user types pandaquit
+        Must be run in blocking thread.
+
+        TODO: This should probably repace self.serial_console with something
+        that directly renders output to the user. Then we don't have to handle
+        buffering and other problems. But we will need to re-enable the serial_console
+        interface after this returns
+        '''
+        print("PANDA: entering interactive mode. Type pandaquit to exit")
+        prompt = self.expect_prompt.decode("utf8") if self.expect_prompt  else "$ "
+        if not prompt.endswith(" "): prompt += " "
+        while True:
+            cmd = input(prompt) # TODO: Strip all control characters - Ctrl-L breaks things
+            if cmd.strip() == 'pandaquit':
+                if confirm_quit:
+                    q = input("PANDA: Quitting interactive mode. Are you sure? (y/n) ")
+                    if len(q) and q.lower()[0] == 'y':
+                        break
+                    else:
+                        continue
+                else: # No confirm - just break
+                    break
+            r = self.run_serial_cmd(cmd) # XXX: may timeout
+            print(r)

similarity index 100%
rename from panda/pypanda/panda/callback_mixins.py
rename to panda/python/core/panda/callback_mixins.py

similarity index 100%
rename from panda/pypanda/panda/decorators.py
rename to panda/python/core/panda/decorators.py

similarity index 100%
rename from panda/pypanda/panda/ffi_importer.py
rename to panda/python/core/panda/ffi_importer.py

similarity index 100%
rename from panda/pypanda/panda/hooking_mixins.py
rename to panda/python/core/panda/hooking_mixins.py

similarity index 100%
rename from panda/pypanda/panda/images/__init__.py
rename to pand