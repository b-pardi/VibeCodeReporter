@@ -52,7 +52,9 @@ from faraday.server.models import (
     Reference,
     PolicyViolation,
     CommandObject,
-    File
+    File,
+    Host,
+    Service
 )
 from tests.factories import (
     ServiceFactory,
@@ -1985,17 +1987,28 @@ class TestListVulnerabilityView(ReadOnlyAPITests):  # TODO migration: use read w
         assert res.status_code == 404
 
     def test_export_vuln_csv_empty_workspace(self, test_client):
+        Host.query.delete()
+        Service.query.delete()
         Vulnerability.query.delete()
         res = test_client.get(self.url() + 'export_csv/')
         expected_headers = [
-        "confirmed", "id", "date", "name", "severity", "service",
-        "target", "desc", "status", "hostnames", "comments", "owner", "os", "resolution", "easeofresolution", "web_vulnerability",
-        "data", "website", "path", "status_code", "request", "method", "params", "pname", "query",
-        "policyviolations", "external_id", "impact_confidentiality", "impact_integrity", "impact_availability",
-        "impact_accountability", "update_date"
+            'host_id', 'ip', 'hostnames', 'host_description', 'os',
+            'mac', 'host_owned', 'host_creator_id', 'obj_type',
+            'service_id', 'service_name', 'service_description', 'service_owned',
+            'port', 'protocol', 'summary', 'version', 'service_status',
+            'service_creator_id', 'obj_type', 'parent_id',
+            'confirmed', 'vuln_id', 'date', 'update_date', 'vuln_name', 'severity', 'service',
+            'target', 'vuln_desc', 'vuln_status', 'hostnames', 'comments', 'vuln_owner',
+            'os', 'resolution', 'refs', 'easeofresolution', 'web_vulnerability', 'data',
+            'website', 'path', 'status_code', 'request', 'response', 'method', 'params',
+            'pname', 'query', 'policyviolations', 'external_id', 'impact_confidentiality',
+            'impact_integrity', 'impact_availability', 'impact_accountability', 'vuln_creator',
+            'obj_type', 'parent_id', 'parent_type'
         ]
         assert res.status_code == 200
-        assert res.data.decode('utf-8').strip('\r\n').split(',') == expected_headers
+        response_headers = res.data.decode('utf-8').strip('\r\n').split(',')
+        filtered_response_headers = [field.strip() for field in response_headers if field]
+        assert filtered_response_headers == expected_headers
 
     @pytest.mark.usefixtures('ignore_nplusone')
     def test_export_vuln_csv_filters_confirmed_using_filters_query(self, test_client, session):
@@ -2047,13 +2060,18 @@ class TestListVulnerabilityView(ReadOnlyAPITests):  # TODO migration: use read w
         session.commit()
         res = test_client.get(self.url() + 'export_csv/')
         assert res.status_code == 200
-        csv_data = csv.DictReader(StringIO(res.data.decode('utf-8')), delimiter=',')
+        csv_data = csv.reader(StringIO(res.data.decode('utf-8')), delimiter=',')
+        check_vulns_fields = False # Avoid to check hosts' and services' fields
         for index, line in enumerate(csv_data):
-            if self.first_object.id == int(line['id']):
-                create_date = parser.parse(line['date'])
-                update_date = parser.parse(line['update_date'])
-                delta = update_date - create_date
-                assert create_date < update_date
+            if line[1] == 'vuln_id':
+                check_vulns_fields = True
+                continue
+            if check_vulns_fields:
+                if self.first_object.id == int(line[1]):
+                    create_date = parser.parse(line[2])
+                    update_date = parser.parse(line[3])
+                    delta = update_date - create_date
+                    assert create_date < update_date
 
 
     @pytest.mark.usefixtures('ignore_nplusone')
@@ -2084,32 +2102,66 @@ class TestListVulnerabilityView(ReadOnlyAPITests):  # TODO migration: use read w
 
     def _verify_csv(self, raw_csv_data, confirmed=False, sever