@@ -792,7 +792,7 @@ static int cmd_type(void *data, const char *input) {
 			// TODO #7967 help refactor: move to detail
 			r_core_cmd_help (core, help_msg_td);
 			r_cons_printf ("Note: The td command should be put between double quotes\n"
-				"Example: \" td struct foo {int bar;int cow};\""
+				"Example: \"td struct foo {int bar;int cow;};\""
 				"\nt");
 
 		} else if (input[1] == ' ') {




@@ -93,7 +93,10 @@ R_API char *r_parse_c_string(RAnal *anal, const char *code) {
 	if (!T) return NULL;
 	tcc_set_callback (T, &appendstring, &str);
 	sdb_foreach (anal->sdb_types, typeload, NULL);
-	tcc_compile_string (T, code);
+	if (tcc_compile_string (T, code) != 0) {
+		free (str);
+		str = NULL;
+	}
 	tcc_delete (T);
 	return str;
 }




@@ -846,6 +846,11 @@ ST_DATA int tcc_ext;
 /* XXX: get rid of this ASAP */
 ST_DATA struct TCCState *tcc_state;
 
+static inline int tcc_nerr() {
+	return tcc_state->nb_errors;
+}
+
+
 #ifdef MEM_DEBUG
 ST_DATA int mem_cur_size;
 ST_DATA int mem_max_size;




@@ -20,6 +20,10 @@
 
 #include "tcc.h"
 
+#define TCC_ERR(...) do {				\
+	tcc_error (__VA_ARGS__);			\
+	return;								\
+} while (0)
 /* callback pointer */
 ST_DATA char **tcc_cb_ptr;
 
@@ -151,6 +155,7 @@ ST_FUNC Sym *sym_push2(Sym **ps, int v, int t, long long c)
 			if (!(v & SYM_FIELD) && (v & ~SYM_STRUCT) < SYM_FIRST_ANOM && s->v == v) {
 				tcc_error ("incompatible types for redefinition of '%s'",
 					get_tok_str (v, NULL));
+				return NULL;
 			}
 		}
 	}
@@ -216,7 +221,10 @@ int tcc_sym_push(char *typename, int typesize, int meta)
 	new_type->ref = sym_malloc ();
 	new_type->t = meta;
 
-	sym_push (0, new_type, 0, 0);
+	if (!sym_push (0, new_type, 0, 0)) {
+		return 0;
+	}
+
 	free (new_type);
 	return 1;
 }
@@ -277,6 +285,9 @@ ST_FUNC Sym *sym_push(int v, CType *type, int r, long long c)
 	}
 	// dump_type(type, 5);
 	s = sym_push2 (ps, v, type->t, c);
+	if (!s) {
+		return NULL;
+	}
 	s->type.ref = type->ref;
 	s->r = r;
 	/* don't record fields or anonymous symbols */
@@ -301,7 +312,7 @@ ST_FUNC Sym *global_identifier_push(int v, int t, long long c)
 	Sym *s, **ps;
 	s = sym_push2 (&global_stack, v, t, c);
 	/* don't record anonymous symbol */
-	if (v < SYM_FIRST_ANOM) {
+	if (s && v < SYM_FIRST_ANOM) {
 		ps = &table_ident[v - TOK_IDENT]->sym_identifier;
 		/* modify the top most local identifier, so that
 		   sym_identifier will point to 's' when popped */
@@ -359,7 +370,7 @@ ST_FUNC void swap(int *p, int *q)
 static void vsetc(CType *type, int r, CValue *vc)
 {
 	if (vtop >= vstack + (VSTACK_SIZE - 1)) {
-		tcc_error ("memory full");
+		TCC_ERR ("memory full");
 	}
 	vtop++;
 	vtop->type = *type;
@@ -451,7 +462,7 @@ ST_FUNC void vswap(void)
 ST_FUNC void vpushv(SValue *v)
 {
 	if (vtop >= vstack + (VSTACK_SIZE - 1)) {
-		tcc_error ("memory full");
+		TCC_ERR ("memory full");
 	}
 	vtop++;
 	*vtop = *v;
@@ -568,6 +579,9 @@ ST_FUNC void mk_pointer(CType *type)
 {
 	Sym *s;
 	s = sym_push (SYM_FIELD, type, 0, -1);
+	if (!s) {
+		return;
+	}
 	type->t = VT_PTR | (type->t & ~VT_TYPE);
 	type->ref = s;
 }
@@ -828,7 +842,7 @@ static void parse_attribute(AttributeDef *ad)
 					next ();
 					n = expr_const ();
 					if (n <= 0 || (n & (n - 1)) != 0) {
-						tcc_error ("alignment must be a positive power of two");
+						TCC_ERR ("alignment must be a positive power of two");
 					}
 					skip (')');
 				} else {
@@ -963,7 +977,7 @@ static void struct_decl(CType *type, int u)
 		s = struct_find (v);
 		if (s) {
 			if (s->type.t != a) {
-				tcc_error ("invalid type");
+				TCC_ERR ("invalid type");
 			}
 			goto do_decl;
 		}
@@ -973,6 +987,9 @@ static void struct_decl(CType *type, int u)
 	type1.t = a;
 	/* we put an undefined size for struct/union */
 	s = sym_push (v | SYM_STRUCT, &type1, 0, -1);
+	if (!s) {
+		return;
+	}
 	s->r = 0;	/* default alignment is zero as gcc */
 	/* put struct/union/enum name in type */
 do_decl:
@@ -982,7 +999,7 @@ do_decl:
 	if (tok == '{') {
 		next ();
 		if (s->c != -1) {
-			tcc_err