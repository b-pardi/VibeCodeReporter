@@ -9,6 +9,7 @@ import re
 from base64 import b64encode, b64decode
 from json.decoder import JSONDecodeError
 from pathlib import Path
+from copy import deepcopy
 
 import flask
 from filteralchemy import Filter, FilterSet, operators
@@ -1170,6 +1171,7 @@ class VulnerabilityView(PaginatedMixin,
         data.pop('tool', '')
         data.pop('service_id', '')
         data.pop('host_id', '')
+
         # TODO For now, we don't want to accept multiples attachments; moreover, attachments have its own endpoint
         data.pop('_attachments', [])
         super()._pre_bulk_update(data, **kwargs)
@@ -1181,6 +1183,15 @@ class VulnerabilityView(PaginatedMixin,
             field_name = getattr(parent, "target_collection", None)
             if field_name and field_name in model_association_proxy_fields:
                 association_proxy_fields[key] = data.pop(key)
+
+        if 'cvssv2' in data:
+            cvssv2 = data.pop('cvssv2', None)
+            association_proxy_fields['cvssv2'] = cvssv2
+
+        if 'cvssv3' in data:
+            cvssv3 = data.pop('cvssv3', None)
+            association_proxy_fields['cvssv3'] = cvssv3
+
         return association_proxy_fields
 
     def _post_bulk_update(self, ids, extracted_data, workspace_name, **kwargs):
@@ -1191,6 +1202,14 @@ class VulnerabilityView(PaginatedMixin,
                                                **kwargs)
             for obj in queryset.all():
                 for (key, value) in extracted_data.items():
+                    if key == 'cvssv2':
+                        if obj.cvssv2:
+                            db.session.delete(obj.cvssv2)
+                            value = deepcopy(value)
+                    if key == 'cvssv3':
+                        if obj.cvssv3:
+                            db.session.delete(obj.cvssv3)
+                            value = deepcopy(value)
                     setattr(obj, key, value)
                     db.session.add(obj)
 




@@ -3593,6 +3593,37 @@ class TestListVulnerabilityView(ReadWriteAPITests, BulkUpdateTestsMixin, BulkDel
         assert res.status_code == 200
         assert res.json['updated'] == 0
 
+    @pytest.mark.usefixtures('ignore_nplusone')
+    def test_bulk_update_vuln_cvss(self, test_client, session):
+        host = HostFactory.create(workspace=self.workspace)
+        vuln = VulnerabilityFactory.create(workspace=self.workspace, host_id=host.id)
+        vuln.cvssv2 = CVSSV2(base_score=5.9)
+        vuln.cvssv3 = CVSSV3(vector_string='AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:L')
+
+        vuln2 = VulnerabilityFactory.create(workspace=self.workspace, host_id=host.id)
+        vuln2.cvssv2 = CVSSV2(base_score=6.0)
+        vuln2.cvssv3 = CVSSV3(vector_string='AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:N')
+
+        session.add_all([vuln, vuln2])
+        session.commit()
+
+        cvssv2 = {'base_score': 5.8}
+        cvssv3 = {'vector_string': 'AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:H'}
+
+        raw_data = {'ids': [vuln.id, vuln2.id], 'cvssv2': cvssv2, 'cvssv3': cvssv3}
+        res = test_client.patch(self.url(), data=raw_data)
+        assert res.status_code == 200
+        assert res.json['updated'] == 2
+
+        cvssv2_objs = session.query(CVSSV2).all()
+        cvssv3_objs = session.query(CVSSV3).all()
+
+        assert len(cvssv2_objs) == 2
+        assert len(cvssv3_objs) == 2
+
+        assert {cvssv2_objs.base_score for cvssv2_objs in cvssv2_objs} == {5.8}
+        assert {cvssv3_objs.vector_string for cvssv3_objs in cvssv3_objs} == {'AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:H'}
+
 
 @pytest.mark.usefixtures('logged_user')
 class TestCustomFieldVulnerability(ReadWriteAPITests):