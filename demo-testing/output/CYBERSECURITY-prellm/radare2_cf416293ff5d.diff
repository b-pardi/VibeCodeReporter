similarity index 100%
rename from libr/asm/arch/lm32/lm32_isa.h
rename to libr/anal/arch/lm32/lm32_isa.h




@@ -149,7 +149,7 @@ static int bf_op(RAnal *anal, RAnalOp *op, ut64 addr, const ut8 *buf, int len, R
 	switch (buf[0]) {
 	case '[':
 		op->type = R_ANAL_OP_TYPE_CJMP;
-		op->fail = addr+1;
+		op->fail = addr + 1;
 		buf = r_mem_dup ((void *)buf, len);
 		if (!buf) {
 			break;

similarity index 64%
rename from libr/asm/p/asm_lm32.c
rename to libr/anal/p/anal_lm32.c



@@ -9,7 +9,7 @@
 #define LM32_UNUSED 0
 
 //str has to be at least 8 chars elements long
-static int reg_number_to_string(ut8 reg, char *str) {
+static bool reg_number_to_string(ut8 reg, char *str) {
 	ut8 match_idx = 0xff;
 	int i;
 	for (i = 0; i < RAsmLm32RegNumber; i++) {
@@ -20,10 +20,10 @@ static int reg_number_to_string(ut8 reg, char *str) {
 	}
 	//register number not found in array. this shouldn't happen
 	if (match_idx == 0xff) {
-		return -1;
+		return false;
 	}
 	strcpy (str, RAsmLm32Regs[match_idx].name);
-	return 0;
+	return true;
 }
 
 #if LM32_UNUSED
@@ -79,7 +79,7 @@ static int string_to_opcode(const char *str, ut8 *num) {
 #endif
 
 //str has to be at least 8 chars elements long
-static int csr_number_to_string(ut8 csr, char *str) {
+static bool csr_number_to_string(ut8 csr, char *str) {
 	ut8 match_idx = 0xff;
 	int i;
 	for (i = 0; i < RAsmLm32CsrNumber; i++) {
@@ -90,10 +90,10 @@ static int csr_number_to_string(ut8 csr, char *str) {
 	}
 	//csr number not found in array
 	if (match_idx == 0xff) {
-		return -1;
+		return false;
 	}
 	strcpy (str, RAsmLm32Csrs[match_idx].name);
-	return 0;
+	return true;
 }
 
 //sign_loc is the location of the sign bit before the shift
@@ -159,10 +159,10 @@ static bool is_pseudo_instr_raise(RAsmLm32Instruction *instr) {
 	return instr->op == raise_instr;
 }
 
-static int r_asm_lm32_decode(RAsmLm32Instruction *instr) {
+static bool r_asm_lm32_decode(RAsmLm32Instruction *instr) {
 	instr->op = extract_opcode (instr->value);
 	if (instr->op >= RAsmLm32OpcodeNumber) {
-		return -1;
+		return false;
 	}
 	instr->op_decode = RAsmLm32OpcodeList[instr->op];
 
@@ -186,7 +186,7 @@ static int r_asm_lm32_decode(RAsmLm32Instruction *instr) {
 		break;
 	case reg_imm5:
 		if (is_invalid_imm5_instr (instr)) {
-			return -1;
+			return false;
 		}
 		instr->dest_reg = extract_reg_v (instr->value);
 		instr->src0_reg = extract_reg_u (instr->value);
@@ -194,7 +194,7 @@ static int r_asm_lm32_decode(RAsmLm32Instruction *instr) {
 		break;
 	case raise_instr:
 		if (is_invalid_imm5_instr (instr)) {
-			return -1;
+			return false;
 		}
 		//might be less bits used, but this shouldn't hurt
 		//invalid parameters are caught in print_pseudo_instruction anyway
@@ -202,13 +202,13 @@ static int r_asm_lm32_decode(RAsmLm32Instruction *instr) {
 		break;
 	case one_reg:
 		if (is_invalid_one_reg_instr (instr)) {
-			return -1;
+			return false;
 		}
 		instr->src0_reg = extract_reg_u (instr->value);
 		break;
 	case two_regs:
 		if (is_invalid_two_reg_instr (instr)) {
-			return -1;
+			return false;
 		}
 		instr->dest_reg = extract_reg_w (instr->value);
 		instr->src0_reg = extract_reg_u (instr->value);
@@ -220,7 +220,7 @@ static int r_asm_lm32_decode(RAsmLm32Instruction *instr) {
 		break;
 	case reg_csr: //wcsr
 		if (is_invalid_wcsr_instr (instr)) {
-			return -1;
+			return false;
 		}
 		instr->src0_reg = extract_reg_v (instr->value);
 		instr->csr = extract_reg_u (instr->value);
@@ -228,7 +228,7 @@ static int r_asm_lm32_decode(RAsmLm32Instruction *instr) {
 	case csr_reg: //rcsr
 		//bitmask is the same as the two register one
 		if (is_invalid_two_reg_instr (instr)) {
-			return -1;
+			return false;
 		}
 		instr->dest_reg = extract_reg_w (instr->value);
 		instr->csr = extract_reg_u (instr->value);
@@ -239,75 +239,74 @@ static int r_asm_lm32_decode(RAsmLm32Instruction *instr) {
 		break;
 	case reserved:
 	default:
-		return -1;
+		return false;
 	}
 
 	//see if the instruction corresponds to a pseudo-instruction
 	inst