@@ -298,3 +298,9 @@ class TestMiscSamplesChrome(object):
         expected = ["<div><p>Test</p></div>", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment2.html")
+        expected = ["<div id=\"foobar\"><b>This is B</b></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -286,3 +286,9 @@ class TestMiscSamplesFirefox(object):
         expected = ["<div><p>Test</p></div>", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment2.html")
+        expected = ["<div id=\"foobar\"><b>This is B</b></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -377,3 +377,9 @@ class TestMiscSamplesIE(object):
         expected = ["<div><p>Test</p></div>", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment2.html")
+        expected = ["<div id=\"foobar\"><b>This is B</b></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -357,3 +357,9 @@ class TestMiscSamplesIE(object):
         expected = ["<div><p>Test</p></div>", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment2.html")
+        expected = ["<div id=\"foobar\"><b>This is B</b></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -332,3 +332,9 @@ class TestMiscSamplesIE(object):
         expected = ["<div><p>Test</p></div>", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment2.html")
+        expected = ["<div id=\"foobar\"><b>This is B</b></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -332,3 +332,9 @@ class TestMiscSamplesIE(object):
         expected = ["<div><p>Test</p></div>", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment2.html")
+        expected = ["<div id=\"foobar\"><b>This is B</b></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -382,3 +382,9 @@ class TestMiscSamplesIE(object):
         expected = ["<div><p>Test</p></div>", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment2.html")
+        expected = ["<div id=\"foobar\"><b>This is B</b></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -288,3 +288,9 @@ class TestMiscSamplesChrome(object):
         expected = ["<div><p>Test</p></div>", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment2(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment2.html")
+        expected = ["<div id=\"foobar\"><b>This is B</b></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)