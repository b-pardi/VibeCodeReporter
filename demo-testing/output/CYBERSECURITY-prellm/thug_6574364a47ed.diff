@@ -520,3 +520,12 @@ class TestMiscSamplesChrome(object):
                     'platform: Win32']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLOptionsCollection(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLOptionsCollection.html")
+        expected = ['length: 4',
+                    'item(0): Volvo',
+                    'namedItem(\'audi\'): Audi',
+                    'namedItem(\'mercedes\').value: mercedes']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -499,3 +499,12 @@ class TestMiscSamplesFirefox(object):
                     'platform: Linux x86_64']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLOptionsCollection(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLOptionsCollection.html")
+        expected = ['length: 4',
+                    'item(0): Volvo',
+                    'namedItem(\'audi\'): Audi',
+                    'namedItem(\'mercedes\').value: mercedes']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -629,3 +629,12 @@ class TestMiscSamplesIE(object):
                     '[Window] Alert Text: [After MoveFile] FileExists(\'C:\\Windows\\System32\\test3.txt\'): true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLOptionsCollection(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLOptionsCollection.html")
+        expected = ['length: 4',
+                    'item(0): Volvo',
+                    'namedItem(\'audi\'): Audi',
+                    'namedItem(\'mercedes\').value: mercedes']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -598,3 +598,12 @@ class TestMiscSamplesIE(object):
                     '[Window] Alert Text: [After MoveFile] FileExists(\'C:\\Windows\\System32\\test3.txt\'): true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLOptionsCollection(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLOptionsCollection.html")
+        expected = ['length: 4',
+                    'item(0): Volvo',
+                    'namedItem(\'audi\'): Audi',
+                    'namedItem(\'mercedes\').value: mercedes']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -632,3 +632,12 @@ class TestMiscSamplesIE(object):
                     '[Window] Alert Text: [After MoveFile] FileExists(\'C:\\Windows\\System32\\test3.txt\'): true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLOptionsCollection(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLOptionsCollection.html")
+        expected = ['length: 4',
+                    'item(0): Volvo',
+                    'namedItem(\'audi\'): Audi',
+                    'namedItem(\'mercedes\').value: mercedes']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -563,3 +563,12 @@ class TestMiscSamplesIE(object):
                     '[Window] Alert Text: [After MoveFile] FileExists(\'C:\\Windows\\System32\\test3.txt\'): true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLOptionsCollection(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLOptionsCollection.html")
+        expected = ['length: 4',
+                    'item(0): Volvo',
+                    'namedItem(\'audi\'): Audi',
+                    'namedItem(\'mercedes\').value: mercedes']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -745,3 +745,12 @@ class TestMiscSamplesIE(object):
                     '[Window] Alert Text: [After MoveFile] FileExists(\'C:\\Windows\\System32\\test3.txt\'): true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testHTMLOptionsCollection(self, caplog):
+        sample   = os.path.join(self.misc_path, "testHTMLOptionsCollection.html")
+        expected = ['length: 4',
+                    'item(