@@ -88,6 +88,7 @@ class RTCollectorBot(CollectorBot):
                 if urlmatch:
                     content = 'url'
                     url = urlmatch.group(0)
+                    self.logger.info('Matching URL found %r.', url)
                 else:
                     self.logger.debug('No matching attachment or URL found.')
                     continue




@@ -9,7 +9,8 @@ from intelmq.bots.collectors.rt.collector_rt import RTCollectorBot
 REPORT = {'__type': 'Report',
           'feed.accuracy': 100.0,
           'feed.name': 'Request Tracker',
-          'rtir_id': 6,
+          'rtir_id': 1,
+          'raw': 'bm90IGNvbXBsZXRlbHkgZW1wdHk=',
           }
 
 
@@ -19,14 +20,14 @@ class TestRTCollectorBot(test.BotTestCase, unittest.TestCase):
     """
     A TestCase for RtCollectorBot.
 
-    Search should result in ticket #6
-    http://demo.request-tracker.fr/Ticket/Display.html?id=6
+    Search should result in ticket #1
+    http://demo.request-tracker.fr/Ticket/Display.html?id=1
     """
 
     @classmethod
     def set_bot(cls):
         cls.bot_reference = RTCollectorBot
-        cls.sysconfig = {"attachment_regex": "\\.csv$",
+        cls.sysconfig = {"attachment_regex": "^test\\.csv$",
                          "feed": "Request Tracker",
                          "password": "administrateur",
                          "search_not_older_than": None,
@@ -38,14 +39,14 @@ class TestRTCollectorBot(test.BotTestCase, unittest.TestCase):
                          "take_ticket": False,
                          "unzip_attachment": False,
                          "uri": "http://demo.request-tracker.fr/REST/1.0",
-                         "url_regex": "http://",
+                         "url_regex": "http://[^ \n]+",
                          "user": "administrateur",
                          }
 
     def test_events(self):
         """ Test if correct Events have been produced. """
         self.run_bot()
-        self.assertMessageEqual(0, REPORT, compare_raw=False)
+        self.assertMessageEqual(0, REPORT)
 
 
 if __name__ == '__main__':  # pragma: no cover