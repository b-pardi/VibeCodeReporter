@@ -686,12 +686,12 @@ static int http_ui_handler_data(const char * data_dir, const char *relative_path
 
             spx_php_output_direct_print("{");
 
-            spx_php_output_direct_printf("\"key\": \"%s\",", spx_metrics_info[i].key);
-            spx_php_output_direct_printf("\"short_name\": \"%s\",", spx_metrics_info[i].short_name);
-            spx_php_output_direct_printf("\"name\": \"%s\",", spx_metrics_info[i].name);
+            spx_php_output_direct_printf("\"key\": \"%s\",", spx_metric_info[i].key);
+            spx_php_output_direct_printf("\"short_name\": \"%s\",", spx_metric_info[i].short_name);
+            spx_php_output_direct_printf("\"name\": \"%s\",", spx_metric_info[i].name);
 
             spx_php_output_direct_print("\"type\": \"");
-            switch (spx_metrics_info[i].type) {
+            switch (spx_metric_info[i].type) {
                 case SPX_FMT_TIME:
                     spx_php_output_direct_print("time");
                     break;
@@ -710,7 +710,7 @@ static int http_ui_handler_data(const char * data_dir, const char *relative_path
 
             spx_php_output_direct_print("\",");
 
-            spx_php_output_direct_printf("\"releasable\": %d", spx_metrics_info[i].releasable);
+            spx_php_output_direct_printf("\"releasable\": %d", spx_metric_info[i].releasable);
 
             spx_php_output_direct_print("}\n");
         });




@@ -3,6 +3,7 @@
 
 #include "spx_metric.h"
 #include "spx_resource_stats.h"
+#include "spx_thread.h"
 #include "spx_php.h"
 
 #ifdef __GNUC__
@@ -11,13 +12,33 @@
 #   error "Please open an issue"
 #endif
 
-const spx_metric_info_t spx_metrics_info[SPX_METRIC_COUNT] = {
+
+struct spx_metric_collector_t {
+    int enabled_metrics[SPX_METRIC_COUNT];
+    double ref_values[SPX_METRIC_COUNT];
+    double last_values[SPX_METRIC_COUNT];
+    double current_fixed_noise[SPX_METRIC_COUNT];
+};
+
+
+static size_t metric_handler_idle_time(void);
+static size_t metric_handler_io_bytes(void);
+static size_t metric_handler_io_r_bytes(void);
+static size_t metric_handler_io_w_bytes(void);
+
+static void memoize_io_stats(void);
+static size_t memoized_metric_value(spx_metric_t metric);
+
+static void collect_raw_values(const int * enabled_metrics, double * current_values);
+
+const spx_metric_info_t spx_metric_info[SPX_METRIC_COUNT] = {
     ARRAY_INIT_INDEX(SPX_METRIC_WALL_TIME) {
         "wt",
         "Wall time",
         "Wall time",
         SPX_FMT_TIME,
         0,
+        spx_resource_stats_wall_time,
     },
     ARRAY_INIT_INDEX(SPX_METRIC_CPU_TIME) {
         "ct",
@@ -25,6 +46,7 @@ const spx_metric_info_t spx_metrics_info[SPX_METRIC_COUNT] = {
         "CPU time",
         SPX_FMT_TIME,
         0,
+        spx_resource_stats_cpu_time,
     },
     ARRAY_INIT_INDEX(SPX_METRIC_IDLE_TIME) {
         "it",
@@ -32,6 +54,7 @@ const spx_metric_info_t spx_metrics_info[SPX_METRIC_COUNT] = {
         "Idle time",
         SPX_FMT_TIME,
         0,
+        metric_handler_idle_time,
     },
     ARRAY_INIT_INDEX(SPX_METRIC_ZE_MEMORY_USAGE) {
         "zm",
@@ -39,6 +62,7 @@ const spx_metric_info_t spx_metrics_info[SPX_METRIC_COUNT] = {
         "Zend Engine memory usage",
         SPX_FMT_MEMORY,
         1,
+        spx_php_zend_memory_usage,
     },
     ARRAY_INIT_INDEX(SPX_METRIC_ZE_MEMORY_ALLOC_COUNT) {
         "zmac",
@@ -46,6 +70,7 @@ const spx_metric_info_t spx_metrics_info[SPX_METRIC_COUNT] = {
         "Zend Engine allocation count",
         SPX_FMT_QUANTITY,
         0,
+        spx_php_zend_memory_alloc_count,
     },
     ARRAY_INIT_INDEX(SPX_METRIC_ZE_MEMORY_ALLOC_BYTES) {
         "zmab",
@@ -53,6 +78,7 @@ const spx_metric_info_t spx_metrics_info[SPX_METRIC_COUNT] = {
         "Zend Engine allocated bytes",
         SPX_FMT_MEMORY,
         0,
+        spx_php_zend_memory_alloc_bytes,
     },
     ARRAY_INIT_INDEX(SPX_METRIC_ZE_MEMORY_FREE_COUNT) {
         "zmfc",
@@ -60,6 +86,7 @@ const spx_metric_info_t spx_metrics_info[SPX_METRIC_COUNT] = {