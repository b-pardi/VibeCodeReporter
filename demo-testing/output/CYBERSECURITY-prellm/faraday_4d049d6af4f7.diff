@@ -601,10 +601,14 @@ angular.module("faradayApp")
                 }
             }
 
-            ServerAPI.exportCsv = function (wsName) {
+            ServerAPI.exportCsv = function (wsName, confirmed) {
+
                 let url = APIURL + "ws/" + wsName + "/vulns/export_csv/";
-                return get(url);
 
+                if(confirmed)
+                    return get(url, {confirmed: confirmed});
+                else
+                    return get(url);
             }
 
             ServerAPI.deleteVuln = function (wsName, vulnId, rev) {




@@ -807,7 +807,10 @@ angular.module("faradayApp")
 
         $scope.csv = function() {
            deferred = $q.defer();
-           vulnsManager.exportCsv($scope.workspace).then(function(result){
+
+           let confirmed = $scope.propertyFilterConfirmed === "Confirmed" ? true : false;
+
+           vulnsManager.exportCsv($scope.workspace, confirmed).then(function(result){
                 var title = "";
 
                 if ($scope.workspace === null) {




@@ -35,9 +35,9 @@ angular.module('faradayApp')
             return vuln.remove();
         };
 
-        vulnsManager.exportCsv = function(ws){
+        vulnsManager.exportCsv = function(ws, confirmed){
             let deferred = $q.defer();
-            ServerAPI.exportCsv(ws)
+            ServerAPI.exportCsv(ws, confirmed)
                 .then(function(response) {
                     deferred.resolve(response);
                 }, function(response) {