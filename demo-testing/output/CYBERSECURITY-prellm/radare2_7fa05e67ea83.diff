@@ -4058,20 +4058,21 @@ static void ds_print_ptr(RDisasmState *ds, int len, int idx) {
 		if (ds->immstr) {
 			char *str = r_str_from_ut64 (r_read_ble64 (&v, core->print->big_endian));
 			if (str && *str) {
-				const char *ptr = str;
 				bool printable = true;
-				for (; *ptr; ptr++) {
-					if (!IS_PRINTABLE (*ptr)) {
+				const char *ptr = str;
+				for (; *ptr ; ptr++) {
+					if (*ptr < 10) {
 						printable = false;
-						break;
 					}
 				}
-				if (r_flag_get_i (core->flags, v)) {
+				if (printable && r_flag_get_i (core->flags, v)) {
 					printable = false;
 				}
 				if (canHaveChar && printable) {
+					char *s = r_str_escape (str);
 					ds_begin_comment (ds);
-					ds_comment (ds, true, "; '%s'", str);
+					ds_comment (ds, true, "; '%s'", s);
+					free (s);
 				}
 			}
 			free (str);




@@ -3652,6 +3652,10 @@ R_API char *r_str_from_ut64(ut64 val) {
 	if (!str) {
 		return NULL;
 	}
+	while (!*v && i < 8) {
+		v++;
+		i++;
+	}
 	while (i < 8 && *v) {
 		str[i++] = *v++;
 	}