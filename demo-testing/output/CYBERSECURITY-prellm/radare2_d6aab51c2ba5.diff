@@ -2802,9 +2802,9 @@ static int runword(RAnalEsil *esil, const char *word) {
 	}
 
 	if (esil->Reil && esil->Reil->skip) {
-		int tmp_len = strlen (esil->Reil->if_buf);
-		strncat (esil->Reil->if_buf, word, sizeof (esil->Reil->if_buf) - tmp_len - 2);
-		strncat (esil->Reil->if_buf, ",", 1);
+		char *if_buf = esil->Reil->if_buf;
+		strncat (if_buf, word, sizeof (esil->Reil->if_buf) - strlen (if_buf) - 1);
+		strncat (if_buf, ",", sizeof (esil->Reil->if_buf) - strlen (if_buf) - 1);
 		if (!strcmp (word, "}")) {
 			r_anal_esil_pushnum (esil, esil->Reil->addr + esil->Reil->cmd_count + 1);
 			r_anal_esil_parse (esil, esil->Reil->if_buf);




@@ -1,7 +1,14 @@
 /* radare - LGPL - 2013-2015 - Fedor Sakharov <fedor.sakharov@gmail.com> */
 
-#include <r_types.h>
 #include "ebc_disas.h"
+#include <r_types.h>
+
+#define CHK_SNPRINTF(dst, sz, fmt, ...) do { \
+		int len_chk_snprintf_ = snprintf (dst, sz, fmt, __VA_ARGS__); \
+		if (len_chk_snprintf_ >= sz) { \
+			return -1; \
+		}\
+	} while (0)
 
 static const char *instr_names[] = {
 	"break",
@@ -148,21 +155,21 @@ static int decode_jmp(const ut8 *bytes, ebc_command_t *cmd) {
 			}
 		}
 		if (TEST_BIT (bytes[0], 7)) {
-			if (TEST_BIT(bytes[1], 3)) {
-				decode_index32(bytes + 2, &idx32);
+			if (TEST_BIT (bytes[1], 3)) {
+				decode_index32 (bytes + 2, &idx32);
 				sign = idx32.sign ? '+' : '-';
 
-				snprintf(cmd->operands, EBC_OPERANDS_MAXLEN,
-						"%s(%c%u, %c%u)",
-						op1, sign, idx32.n, sign, idx32.c);
+				CHK_SNPRINTF (cmd->operands, EBC_OPERANDS_MAXLEN,
+					"%s(%c%u, %c%u)",
+					op1, sign, idx32.n, sign, idx32.c);
 			} else {
-				immed32 = *(int32_t*)(bytes + 2);
-				snprintf(cmd->operands, EBC_OPERANDS_MAXLEN,
-						"%s0x%x", op1, immed32);
+				immed32 = *(int32_t *)(bytes + 2);
+				CHK_SNPRINTF (cmd->operands, EBC_OPERANDS_MAXLEN,
+					"%s0x%x", op1, immed32);
 			}
 			ret = 6;
 		} else {
-			snprintf(cmd->operands, EBC_OPERANDS_MAXLEN, "%s", op1);
+			snprintf (cmd->operands, EBC_OPERANDS_MAXLEN, "%s", op1);
 			ret = 2;
 		}
 	}
@@ -199,42 +206,41 @@ static int decode_call(const ut8 *bytes, ebc_command_t *cmd) {
 			//operand 1 indirect
 			if (TEST_BIT (bytes[0], 7)) {
 				// immediate data is present
-				decode_index32(bytes + 2, &idx32);
+				decode_index32 (bytes + 2, &idx32);
 				sign = idx32.sign ? '+' : '-';
 
-				snprintf (cmd->operands, EBC_OPERANDS_MAXLEN,
-						"@r%d(%c%u, %c%u)",
-						op1, sign, idx32.n, sign, idx32.c);
+				CHK_SNPRINTF (cmd->operands, EBC_OPERANDS_MAXLEN,
+					"@r%d(%c%u, %c%u)",
+					op1, sign, idx32.n, sign, idx32.c);
 				ret = 6;
 			} else {
 				snprintf (cmd->operands, EBC_OPERANDS_MAXLEN,
-						"@r%d", op1);
+					"@r%d", op1);
 			}
 		} else {
 			//operand 1 direct
 			if (TEST_BIT (bytes[0], 7)) {
 				// immediate data present
-				i1 = *(ut32*)(bytes + 2);
-				snprintf (cmd->operands, EBC_OPERANDS_MAXLEN,
-						"r%d(0x%x)", op1, i1);
+				i1 = *(ut32 *)(bytes + 2);
+				CHK_SNPRINTF (cmd->operands, EBC_OPERANDS_MAXLEN,
+					"r%d(0x%x)", op1, i1);
 				ret = 6;
 			} else {
 				// no immediate data present
-				snprintf(cmd->operands, EBC_OPERANDS_MAXLEN,
-						"r%d", op1);
+				CHK_SNPRINTF (cmd->operands, EBC_OPERANDS_MAXLEN,
+					"r%d", op1);
 			}
 		}
 	} else {
 		bits = 64;
 		ret = 10;
-		i2 = *(ut64*)&bytes[2];
-		snprintf (cmd->operands, EBC_OPERANDS_MAXLEN,
-				"0x%lx", i2);
+		i2 = *(ut64 *)&bytes[2];
+		CHK_SNPRINTF (cmd->operands, EBC_OPERANDS_MAXLEN, "0x%lx", i2);
 	}
-	snprintf (cmd->instr, EBC_INSTR_MAXLEN, "%s%d%s%s",
-			instr_names[EBC_CALL], bits,
-			TEST_BIT(bytes[1], 5) ? "ex" : "",
-			TEST_BIT(bytes[1], 4) ? "" : "a");
+	CHK_SNPRINTF (cmd->instr, EBC_INSTR_MAXLEN, "%s%d%s%s",
+		instr_names[EBC_CALL], bits,
+		TEST_BIT (bytes[1], 5) ? "ex" : "",
+		TEST_BIT (bytes[1], 4) ? "" : "a");
 	return ret;
 }
 
@@ -255,22 +261,22 @@ static int decode_cmp(const ut8 *bytes, ebc_command_t *cmd) {
 	op1 = bytes[1] & 0x07;
 	op2 = (bytes[1] >> 4) & 0x07;
 
-	if (TEST_BIT(bytes[