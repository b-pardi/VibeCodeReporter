@@ -5,17 +5,15 @@ Copyright (C) 2013  Infobyte LLC (http://www.infobytesec.com/)
 See the file 'doc/LICENSE' for the license information
 
 '''
-import json
 from tempfile import NamedTemporaryFile
 
 import os
+import csv
 from base64 import b64encode
 from io import BytesIO
 
 
 import pytz
-from hypothesis import given
-from hypothesis.strategies import text, lists, integers, one_of, none
 from depot.manager import DepotManager
 
 from faraday.server.fields import FaradayUploadedFile
@@ -26,11 +24,9 @@ except ImportError:
     from urllib.parse import urlencode
 
 import pytest
-import time
 from hypothesis import given, strategies as st
 
 from faraday.server.api.modules.vulns import (
-    TypeFilter,
     VulnerabilityFilterSet,
     VulnerabilitySchema,
     VulnerabilityView
@@ -45,7 +41,12 @@ from faraday.server.models import (
     VulnerabilityGeneric,
     Vulnerability,
     VulnerabilityWeb,
-    Reference, PolicyViolation, CommandObject, File)
+    CustomFieldsSchema,
+    Reference,
+    PolicyViolation,
+    CommandObject,
+    File
+)
 from tests.factories import (
     ServiceFactory,
     CommandFactory,
@@ -1883,6 +1884,71 @@ class TestListVulnerabilityView(ReadOnlyAPITests):  # TODO migration: use read w
             .format(ws_name=workspace.name,vuln_id=vuln.id, name="random_name"))
         assert res.status_code == 404
 
+    def test_export_vuln_csv_empty_workspace(self, test_client):
+        Vulnerability.query.delete()
+        res = test_client.get(self.url() + 'export_csv/')
+        expected_headers = "confirmed,id,date,name,severity,service,target,desc,status,hostnames".split(',')
+        assert res.status_code == 200
+        assert res.data.strip('\r\n').split(',') == expected_headers
+
+    def test_export_vulns_confirmed(self, session, test_client):
+        self.first_object.confirmed = True
+        session.add(self.first_object)
+        session.commit()
+
+        res = test_client.get(self.url() + 'export_csv/?confirmed=true')
+        expected_headers = "confirmed,id,date,name,severity,service,target,desc,status,hostnames".split(',')
+        assert res.status_code == 200
+        self._verify_csv(res.data, confirmed=True)
+
+    def test_export_vulns_csv_with_custom_fields(self, session, test_client):
+
+        custom_field_schema = CustomFieldsSchemaFactory(
+            field_name='cvss',
+            field_type='str',
+            field_display_name='CVSS',
+            table_name='vulnerability'
+        )
+        session.add(custom_field_schema)
+        session.commit()
+        for vuln in self.objects:
+            vuln.custom_fields = {"cvss": "9"}
+
+        # lets add a non "schema" custom field called invalid, this should not be shown on the csv
+        self.first_object.custom_fields = {"cvss": "9", "invalid": "not shown"}
+        # another case witt custom fields as None
+        vuln = VulnerabilityFactory.create()
+        vuln.custom_fields=None
+        session.add(vuln)
+        session.commit()
+
+        res = test_client.get(self.url() + 'export_csv/')
+
+        assert self._verify_csv(res.data)
+
+    def _verify_csv(self, raw_csv_data, confirmed=False):
+        custom_fields = [custom_field.field_name for custom_field in CustomFieldsSchema.query.all()]
+        expected_headers = "confirmed,id,date,name,severity,service,target,desc,status,hostnames".split(',')
+        final_expected_headers = expected_headers + custom_fields
+        csv_data = csv.reader(BytesIO(raw_csv_data), delimiter=',')
+        for index, line in enumerate(csv_data):
+
+            if index == 0:
+                if line != final_expected_headers:
+                    return False
+            else:
+                vuln = Vulnerability.query.filter_by(id=line[1], confirmed=confirmed).first()
+                if vuln.name != line[3]:
+                    return False
+                # test custom fields
+                for c_index, custom_field in enumerate(custom_fields):
+ 