@@ -480,3 +480,19 @@ class TestMiscSamplesChrome(object):
                     'target: xml-stylesheet']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testWindow(self, caplog):
+        sample   = os.path.join(self.misc_path, "testWindow.html")
+        expected = ['window: [object Window]',
+                    'self: [object Window]',
+                    'top: [object Window]',
+                    'length: 0',
+                    'history: [object History]',
+                    'pageXOffset: 0',
+                    'pageYOffset: 0',
+                    'screen: [object Screen]',
+                    'screenLeft: 0',
+                    'screenX: 0',
+                    'confirm: true']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -459,3 +459,19 @@ class TestMiscSamplesFirefox(object):
                     'target: xml-stylesheet']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testWindow(self, caplog):
+        sample   = os.path.join(self.misc_path, "testWindow.html")
+        expected = ['window: [object Window]',
+                    'self: [object Window]',
+                    'top: [object Window]',
+                    'length: 0',
+                    'history: [object History]',
+                    'pageXOffset: 0',
+                    'pageYOffset: 0',
+                    'screen: [object Screen]',
+                    'screenLeft: 0',
+                    'screenX: 0',
+                    'confirm: true']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -552,3 +552,19 @@ class TestMiscSamplesIE(object):
                     'target: xml-stylesheet']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testWindow(self, caplog):
+        sample   = os.path.join(self.misc_path, "testWindow.html")
+        expected = ['window: [object Window]',
+                    'self: [object Window]',
+                    'top: [object Window]',
+                    'length: 0',
+                    'history: [object History]',
+                    'pageXOffset: 0',
+                    'pageYOffset: 0',
+                    'screen: [object Screen]',
+                    'screenLeft: 0',
+                    'screenX: 0',
+                    'confirm: true']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -521,3 +521,19 @@ class TestMiscSamplesIE(object):
                     'target: xml-stylesheet']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testWindow(self, caplog):
+        sample   = os.path.join(self.misc_path, "testWindow.html")
+        expected = ['window: [object Window]',
+                    'self: [object Window]',
+                    'top: [object Window]',
+                    'length: 0',
+                    'history: [object History]',
+                    'pageXOffset: 0',
+                    'pageYOffset: 0',
+                    'screen: [object Screen]',
+                    'screenLeft: 0',
+                    'screenX: 0',
+                    'confirm: true']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -555,3 +555,19 @@ class TestMiscSamplesIE(object):
                     '<div id="outer"><div>Just a div<div id="test"><div>Just a sample</div></div></div></div>']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testWindow(self, caplog):
+        sample   = os.path.join(self.misc_path, "testWindow.html")
+        expected = ['window: [object Window]',
+                    'self: [object Window]',
+                    'top: [object Window]',
+                    'length: 0',
+                    'history: [object History]',
+                    'pageXOffset: 0',
+                    'pageYOffset: 0',
+                    'screen: [object Screen]',
+                    'screenLeft: 0',
+                    'screenX: 0',
+                    'confirm: true']
+
+        self.do_perform_test(