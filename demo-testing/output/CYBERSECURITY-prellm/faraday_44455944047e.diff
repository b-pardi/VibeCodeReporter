@@ -506,6 +506,7 @@ class VulnerabilityView(PaginatedMixin,
 
     def _update_object(self, obj, data):
         data.pop('type') # It's forbidden to change vuln type!
+        data.pop('run_date', None)
         return super(VulnerabilityView, self)._update_object(obj, data)
 
     def _perform_update(self, object_id, obj, data, workspace_name):




@@ -7,6 +7,7 @@ See the file 'doc/LICENSE' for the license information
 '''
 from __future__ import absolute_import
 from builtins import str
+from datetime import datetime, timedelta, timezone
 
 import json
 from tempfile import NamedTemporaryFile
@@ -1438,6 +1439,40 @@ class TestListVulnerabilityView(ReadOnlyAPITests):  # TODO migration: use read w
                               data=raw_data)
         assert res.status_code == 200
 
+    def test_create_and_update_webvuln_with_run_date(self, host_with_hostnames, test_client, session):
+        run_date = datetime.now(timezone.utc) - timedelta(days=30)
+        command = CommandFactory.create(workspace=self.workspace)
+        service = ServiceFactory.create(workspace=self.workspace)
+        session.commit()  # flush host_with_hostnames
+        raw_data = _create_post_data_vulnerability(
+            name='New vulnsweb',
+            vuln_type='VulnerabilityWeb',
+            parent_id=service.id,
+            parent_type='Service',
+            refs=[],
+            policyviolations=[],
+            description='helloworld',
+            severity='low',
+        )
+        ws_name = host_with_hostnames.workspace.name
+        res = test_client.post('/v2/ws/{0}/vulns/?command_id={1}'.format(ws_name, command.id), data=raw_data)
+        assert res.status_code == 201
+        raw_data = _create_post_data_vulnerability(
+            name='Update vulnsweb',
+            vuln_type='VulnerabilityWeb',
+            parent_id=service.id,
+            parent_type='Service',
+            refs=[],
+            policyviolations=[],
+            description='Update helloworld',
+            severity='high',
+        )
+        raw_data['run_date'] = run_date.timestamp()
+        res = test_client.put('/v2/ws/{0}/vulns/{1}/'.format(ws_name, res.json['_id']),
+                              data=raw_data)
+        assert res.status_code == 200
+        assert res.json.get('run_date', None) == None
+
     def test_create_vuln_from_command(self, test_client, session):
         command = EmptyCommandFactory.create(workspace=self.workspace)
         service = ServiceFactory.create(workspace=self.workspace)