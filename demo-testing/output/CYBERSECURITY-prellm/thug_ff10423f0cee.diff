@@ -610,3 +610,9 @@ class TestMiscSamplesChrome(object):
         expected = ['[applet redirection]']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFrame.html")
+        expected = ['[frame redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -575,3 +575,9 @@ class TestMiscSamplesFirefox(object):
         expected = ['[applet redirection]']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFrame.html")
+        expected = ['[frame redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -792,3 +792,9 @@ class TestMiscSamplesIE(object):
         expected = ['[applet redirection]']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFrame.html")
+        expected = ['[frame redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -760,3 +760,9 @@ class TestMiscSamplesIE(object):
         expected = ['[applet redirection]']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFrame.html")
+        expected = ['[frame redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -794,3 +794,9 @@ class TestMiscSamplesIE(object):
         expected = ['[applet redirection]']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFrame.html")
+        expected = ['[frame redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -725,3 +725,9 @@ class TestMiscSamplesIE(object):
         expected = ['[applet redirection]']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFrame.html")
+        expected = ['[frame redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -907,3 +907,9 @@ class TestMiscSamplesIE(object):
         expected = ['[applet redirection]']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFrame.html")
+        expected = ['[frame redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -591,3 +591,9 @@ class TestMiscSamplesChrome(object):
         expected = ['[applet redirection]']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFrame.html")
+        expected = ['[frame redirection]']
+
+        self.do_perform_test(caplog, sample, expected)