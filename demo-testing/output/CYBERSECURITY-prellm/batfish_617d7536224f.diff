@@ -4,6 +4,7 @@ import com.google.common.collect.ImmutableList;
 import org.batfish.common.Warnings;
 import org.batfish.datamodel.AclIpSpace;
 import org.batfish.datamodel.Configuration;
+import org.batfish.datamodel.EmptyIpSpace;
 import org.batfish.datamodel.HeaderSpace;
 import org.batfish.datamodel.IpWildcard;
 import org.batfish.datamodel.RouteFilterList;
@@ -33,6 +34,12 @@ public final class FwFromDestinationPrefixList extends FwFrom {
       }
       RouteFilterList destinationPrefixList = c.getRouteFilterLists().get(_name);
 
+      // if referenced prefix list is empty, it should not match anything
+      if (destinationPrefixList.getLines().isEmpty()) {
+        headerSpaceBuilder.addDstIp(EmptyIpSpace.INSTANCE);
+        return;
+      }
+
       headerSpaceBuilder.addDstIp(
           AclIpSpace.union(
               destinationPrefixList




@@ -4,6 +4,7 @@ import com.google.common.collect.ImmutableList;
 import org.batfish.common.Warnings;
 import org.batfish.datamodel.AclIpSpace;
 import org.batfish.datamodel.Configuration;
+import org.batfish.datamodel.EmptyIpSpace;
 import org.batfish.datamodel.HeaderSpace;
 import org.batfish.datamodel.IpWildcard;
 import org.batfish.datamodel.RouteFilterList;
@@ -33,6 +34,12 @@ public final class FwFromDestinationPrefixListExcept extends FwFrom {
       }
       RouteFilterList destinationPrefixList = c.getRouteFilterLists().get(_name);
 
+      // if referenced prefix list is empty, it should not match anything
+      if (destinationPrefixList.getLines().isEmpty()) {
+        headerSpaceBuilder.addNotDstIp(EmptyIpSpace.INSTANCE);
+        return;
+      }
+
       headerSpaceBuilder.addNotDstIp(
           AclIpSpace.union(
               destinationPrefixList




@@ -4,6 +4,7 @@ import com.google.common.collect.ImmutableList;
 import org.batfish.common.Warnings;
 import org.batfish.datamodel.AclIpSpace;
 import org.batfish.datamodel.Configuration;
+import org.batfish.datamodel.EmptyIpSpace;
 import org.batfish.datamodel.HeaderSpace;
 import org.batfish.datamodel.IpWildcard;
 import org.batfish.datamodel.RouteFilterList;
@@ -33,6 +34,12 @@ public final class FwFromPrefixList extends FwFrom {
       }
       RouteFilterList prefixList = c.getRouteFilterLists().get(_name);
 
+      // if referenced prefix list is empty, it should not match anything
+      if (prefixList.getLines().isEmpty()) {
+        headerSpaceBuilder.addSrcOrDstIp(EmptyIpSpace.INSTANCE);
+        return;
+      }
+
       headerSpaceBuilder.addSrcOrDstIp(
           AclIpSpace.union(
               prefixList




@@ -4,6 +4,7 @@ import com.google.common.collect.ImmutableList;
 import org.batfish.common.Warnings;
 import org.batfish.datamodel.AclIpSpace;
 import org.batfish.datamodel.Configuration;
+import org.batfish.datamodel.EmptyIpSpace;
 import org.batfish.datamodel.HeaderSpace;
 import org.batfish.datamodel.IpWildcard;
 import org.batfish.datamodel.RouteFilterList;
@@ -33,6 +34,12 @@ public final class FwFromSourcePrefixList extends FwFrom {
       }
       RouteFilterList sourcePrefixList = c.getRouteFilterLists().get(_name);
 
+      // if referenced prefix list is empty, it should not match anything
+      if (sourcePrefixList.getLines().isEmpty()) {
+        headerSpaceBuilder.addSrcIp(EmptyIpSpace.INSTANCE);
+        return;
+      }
+
       headerSpaceBuilder.addSrcIp(
           AclIpSpace.union(
               sourcePrefixList




@@ -4,6 +4,7 @@ import com.google.common.collect.ImmutableList;
 import org.batfish.common.Warnings;
 import org.batfish.datamodel.AclIpSpace;
 import org.batfish.datamodel.Configuration;
+import org.batfish.datamodel.EmptyIpSpace;
 import org.batfish.datamodel.HeaderSpace;
 import org.batfish.datamodel.IpWildcard;
 import org.batfish.datamodel.RouteFilterList;
@@ -33,6 +34,12 @@ public class FwFromSourcePrefixListExcept extends FwFrom {
       }
       RouteFilterList sourcePrefixList = c.getRouteFilterLists().get(_name);
 
+      // if referenced prefix list is 