@@ -953,6 +953,45 @@ static bool parseOpcode(const char *str, ArmOp *op) {
 	return parseOperands (space, op);
 }
 
+static bool handlePAC(ut32 *op, const char *str) {
+	if (!strcmp (str, "autiasp")) {
+		*op = 0xbf2303d5;
+		return *op != -1;
+	}
+	if (!strcmp (str, "autiaz")) {
+		*op = 0x9f2303d5;
+		return *op != -1;
+	}
+	if (!strcmp (str, "autibsp")) {
+		*op = 0xff2303d5;
+		return *op != -1;
+	}
+	if (!strcmp (str, "autibz")) {
+		*op = 0xdf2303d5;
+		return *op != -1;
+	}
+	if (!strcmp (str, "paciaz")) {
+		*op = 0x1f2303d5;
+		return *op != -1;
+	}
+	if (!strcmp (str, "pacibz")) {
+		*op = 0x5f2303d5;
+		return *op != -1;
+	}
+	if (!strcmp (str, "paciasp")) {
+		*op = 0x3f2303d5;
+		return *op != -1;
+	}
+	if (!strcmp (str, "pacibsp")) {
+		*op = 0x7f2303d5;
+		return *op != -1;
+	}
+	if (!strcmp (str, "retab")) {
+		*op = 0xff0f5fd6;
+		return *op != -1;
+	}
+}
+
 bool arm64ass(const char *str, ut64 addr, ut32 *op) {
 	ArmOp ops = {0};
 	if (!parseOpcode (str, &ops)) {
@@ -1043,6 +1082,10 @@ bool arm64ass(const char *str, ut64 addr, ut32 *op) {
 		*op = 0xdf3f03d5;
 		return *op != -1;
 	}
+	// PAC
+	if (handlePAC (op, str)) {
+		return true;
+	}
 	if (!strcmp (str, "nop")) {
 		*op = 0x1f2003d5;
 		return *op != -1;