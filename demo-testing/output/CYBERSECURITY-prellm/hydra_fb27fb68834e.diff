@@ -1,3 +1,5 @@
 from hydra.errors import MissingConfigException
 from hydra.hydra import main, Hydra
+from hydra.launcher import Launcher
 from hydra import utils
+

new file mode 100644



@@ -0,0 +1,2 @@
+from hydra.launchers.fairtask_launcher import FAIRTaskLauncher
+from hydra.launchers.submitit_launcher import SubmititLauncher
\ No newline at end of file

similarity index 99%
rename from hydra/fairtask_launcher.py
rename to hydra/launchers/fairtask_launcher.py



@@ -4,7 +4,7 @@ import os
 from fairtask import TaskQueues, gatherl
 
 from hydra import utils
-from .launcher import Launcher
+from hydra import Launcher
 
 
 class FAIRTaskLauncher(Launcher):

similarity index 87%
rename from hydra/submitit_launcher.py
rename to hydra/launchers/submitit_launcher.py



@@ -2,14 +2,15 @@ import os
 
 import submitit
 
+from hydra import Launcher
 from hydra import utils
-from .launcher import Launcher
 
 
 class SubmititLauncher(Launcher):
 
-    def __init__(self, queue, folder):
+    def __init__(self, queue, folder, queue_parameters):
         self.queue = queue
+        self.queue_parameters = queue_parameters
         self.folder = folder
         self.cfg_dir = None
         self.cfg_filename = None
@@ -48,7 +49,9 @@ class SubmititLauncher(Launcher):
                              job_subdir_key='hydra.sweep.subdir')
 
     def launch(self):
-        if self.queue == 'slurm':
+        if self.queue == 'auto':
+            executor = submitit.AutoExecutor(folder=self.folder)
+        elif self.queue == 'slurm':
             executor = submitit.SlurmExecutor(folder=self.folder)
         elif self.queue == 'chronos':
             executor = submitit.ChronosExecutor(folder=self.folder)
@@ -57,6 +60,8 @@ class SubmititLauncher(Launcher):
         else:
             raise RuntimeError('Unsupported queue type {}'.format(self.queue))
 
+        executor.update_parameters(**self.queue_parameters[self.queue])
+
         # TODO: use logger
         print("Sweep output dir : {}".format(self.hydra_cfg.hydra.sweep.dir))
         os.makedirs(self.hydra_cfg.hydra.sweep.dir, exist_ok=True)
@@ -71,5 +76,4 @@ class SubmititLauncher(Launcher):
                                   utils.JobRuntime().get('name'))
             jobs.append(job)
 
-        outputs = [job.result() for job in jobs]
-
+        return [job.results() for job in jobs]




@@ -147,13 +147,18 @@ def test_demos_defaults__override_all_configs_and_overrides(task_runner):
         )
 
 
-def test_demos_sweep_1_job(sweep_runner):
+@pytest.mark.parametrize(
+    'overrides',
+    [
+        ['launcher=fairtask', 'hydra.launcher.queue=local', 'hydra.no_workers=true'],
+        # submitit local queue is broken. re-enable once fixed. https://github.com/fairinternal/submitit/issues/121
+        # ['launcher=submitit', 'hydra.launcher.queue=local'],
+    ]
+)
+def test_demos_sweep_1_job(sweep_runner, overrides):
     sweep = sweep_runner(conf_dir='demos/6_sweep/conf/',
                          conf_filename='config.yaml',
-                         overrides=[
-                             'hydra.launcher.queue=local',
-                             'hydra.no_workers=true'
-                         ])
+                         overrides=overrides)
     with sweep:
         assert len(sweep.returns) == 1
         assert sweep.returns[0].overrides == []