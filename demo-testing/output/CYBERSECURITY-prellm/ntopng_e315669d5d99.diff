@@ -35,7 +35,7 @@ void DNSTraffic::periodicUpdate(Host *h, HostAlert *engaged_alert) {
   u_int64_t delta;
 
   if((delta = h->cb_status_delta_dns_bytes(h->get_ndpi_stats()->getProtoBytes(NDPI_PROTOCOL_DNS))) > dns_bytes_threshold) {
-    if (!alert) alert = allocAlert(this, h, 50, 0, delta, dns_bytes_threshold);
+    if (!alert) alert = allocAlert(this, h, SCORE_LEVEL_ERROR, 0, delta, dns_bytes_threshold);
     if (alert) h->triggerAlert(alert);
   }
 }




@@ -32,7 +32,7 @@ FlowAnomaly::FlowAnomaly() : HostCallback(ntopng_edition_community) {
 void FlowAnomaly::periodicUpdate(Host *h, HostAlert *engaged_alert) {
   HostAlert *alert = engaged_alert;
   u_int8_t cli_score = 0, srv_score = 0;
-  const u_int8_t score_value = 50;
+  const u_int8_t score_value = SCORE_LEVEL_WARNING;
   u_int32_t value = 0, lower_bound = 0, upper_bound = 0;  
 
   if(h->has_flows_anomaly(true)) {




@@ -28,9 +28,9 @@ void FlowFlood::periodicUpdate(Host *h, HostAlert *engaged_alert) {
   u_int16_t flows = 0;
 
   if((flows = h->flow_flood_attacker_hits()) > threshold)
-    triggerFlowHitsAlert(h, engaged_alert, true, flows, threshold, 100, 0);
+    triggerFlowHitsAlert(h, engaged_alert, true, flows, threshold, SCORE_LEVEL_ERROR, 0);
   else if((flows = h->flow_flood_victim_hits()) > threshold) 
-    triggerFlowHitsAlert(h, engaged_alert, false, flows, threshold, 0, 20);
+    triggerFlowHitsAlert(h, engaged_alert, false, flows, threshold, 0, SCORE_LEVEL_NOTICE);
 
   /* Reset counters once done */
   h->reset_flow_flood_hits();




@@ -35,7 +35,7 @@ void P2PTraffic::periodicUpdate(Host *h, HostAlert *engaged_alert) {
   u_int64_t delta;
 
   if((delta = h->cb_status_delta_p2p_bytes(h->get_ndpi_stats()->getCategoryBytes(NDPI_PROTOCOL_CATEGORY_FILE_SHARING))) > p2p_bytes_threshold) {
-    if (!alert) alert = allocAlert(this, h, 50, 0, delta, p2p_bytes_threshold);
+    if (!alert) alert = allocAlert(this, h, SCORE_LEVEL_ERROR, 0, delta, p2p_bytes_threshold);
     if (alert) h->triggerAlert(alert);
   }
 }




@@ -30,7 +30,7 @@ RemoteConnection::RemoteConnection() : HostCallback(ntopng_edition_community) {}
 
 void RemoteConnection::periodicUpdate(Host *h, HostAlert *engaged_alert) {
   HostAlert *alert = engaged_alert;
-  u_int8_t cli_score = 5, srv_score = 5, num_remote_access = 0;
+  u_int8_t cli_score = SCORE_LEVEL_NOTICE, srv_score = SCORE_LEVEL_NOTICE, num_remote_access = 0;
 
   if(h) num_remote_access = h->getRemoteAccess();
 




@@ -28,9 +28,9 @@ void SYNFlood::periodicUpdate(Host *h, HostAlert *engaged_alert) {
   u_int16_t hits = 0;
 
   if((hits = h->syn_flood_attacker_hits()) > threshold) 
-    triggerFlowHitsAlert(h, engaged_alert, true, hits, threshold, 100, 0);
+    triggerFlowHitsAlert(h, engaged_alert, true, hits, threshold, SCORE_LEVEL_ERROR, 0);
   else if((hits = h->syn_flood_victim_hits()) > threshold) 
-     triggerFlowHitsAlert(h, engaged_alert, false, hits, threshold, 0, 20);
+     triggerFlowHitsAlert(h, engaged_alert, false, hits, threshold, 0, SCORE_LEVEL_NOTICE);
 
   /* Reset counters once done */
   h->reset_syn_flood_hits();  




@@ -28,9 +28,9 @@ void SYNScan::periodicUpdate(Host *h, HostAlert *engaged_alert) {
   u_int16_t hits = 0;
 
   if((hits = h->syn_scan_attacker_hits()) > threshold)
-    triggerFlowHitsAlert(h, engaged_alert, true, hits, threshold, 100, 0);
+    triggerFlowHitsAlert(h, engaged_alert, true, hits, threshold, SCORE_LEVEL_ERROR, 0);
   else if((hits = h->syn_scan_victim_hits()) > threshold) 
-    triggerFlowHitsAlert(h, engaged_alert, false, hits, threshold, 0, 20);
+    triggerFlowHitsAlert(h, engaged_alert, false, hits, threshold, 0, SCORE_LEVEL_NOTICE);
 
   /* Reset counters once done */
   h->reset_syn_scan_hits();




@@ -35,7 +35,7 @@ void ServerContacts::periodicUpdate(Host *h, HostAlert *engaged_alert) {
   u_int32_t contacted_servers = 0;
 
   if((contacted_servers = getContactedServers(h)) >= contacts_threshold) {
-    if (!alert) alert = allocAlert(this, h, 50, 0, contacted_servers, con