@@ -292,3 +292,9 @@ class TestMiscSamplesChrome(object):
         expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment1.html")
+        expected = ["<div><p>Test</p></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -280,3 +280,9 @@ class TestMiscSamplesFirefox(object):
         expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment1.html")
+        expected = ["<div><p>Test</p></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -371,3 +371,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment1.html")
+        expected = ["<div><p>Test</p></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -351,3 +351,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment1.html")
+        expected = ["<div><p>Test</p></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -326,3 +326,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment1.html")
+        expected = ["<div><p>Test</p></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -326,3 +326,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment1.html")
+        expected = ["<div><p>Test</p></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -376,3 +376,9 @@ class TestMiscSamplesIE(object):
         expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment1.html")
+        expected = ["<div><p>Test</p></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)




@@ -282,3 +282,9 @@ class TestMiscSamplesChrome(object):
         expected = ["Alert Text: favorite_food=tripe; name=oeschger", ]
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testDocumentFragment1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testDocumentFragment1.html")
+        expected = ["<div><p>Test</p></div>", ]
+
+        self.do_perform_test(caplog, sample, expected)