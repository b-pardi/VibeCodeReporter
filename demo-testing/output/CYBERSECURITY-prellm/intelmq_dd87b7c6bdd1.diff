@@ -82,7 +82,7 @@ def download(url: str=URL, add_default=False, params=None, include_malpedia=Fals
     rules = [generate_rule(*line[:2])
              for line in csv.reader(io.StringIO(download.text))
              if not line[0].startswith('#')
-             if (not mwnmp_ignore_adware or not any(['adware' in field for field in line]))]
+             if (not mwnmp_ignore_adware or not any(['adware' == field for field in line]))]
     if include_malpedia:
         download = requests.get(URL_MALPEDIA)
         download.raise_for_status()
@@ -156,7 +156,8 @@ and multiple ones:
     parser.add_argument('--mwnmp-ignore-adware',
                         action='store_true',
                         help='Ignore adware in the malware name mapping data. '
-                             'for rules.')
+                             'for rules. Ignores all lines with an explicit '
+                             "'adware' field.")
     return parser
 
 




@@ -27,11 +27,20 @@ class TestDownloadMapping(unittest.TestCase):
         parser = create_parser()
         args = parser.parse_args(["--mwnmp-ignore-adware"])
         with requests_mock.Mocker() as mocker:
-            mocker.get(URL, text='"^foobar$",foobar,adware')
+            mocker.get(URL, text='"^foobar$",foobar,adware,barfoo')
             result = json.loads(main(args))
         self.assertEqual(len(result), 0)
         self.assertIsInstance(result, list)
 
+    def test_download_not_ignore_not_adware(self):
+        parser = create_parser()
+        args = parser.parse_args(["--mwnmp-ignore-adware"])
+        with requests_mock.Mocker() as mocker:
+            mocker.get(URL, text='"^foobar$",foobar,not adware,barfoo')
+            result = json.loads(main(args))
+        self.assertEqual(len(result), 1)
+        self.assertIsInstance(result, list)
+
     def test_download_add_default(self):
         " Test --add-default parameter. "
         parser = create_parser()