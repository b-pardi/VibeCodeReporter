@@ -404,7 +404,7 @@ int cmd_debug_timing(t_hydra_console *con, t_tokenline_parsed *p)
 
 	while (1) {
 		/* Exit if User Button is pressed */
-		if (USER_BUTTON) {
+		if (hydrabus_ubtn()) {
 			break;
 		}
 
@@ -532,7 +532,7 @@ int cmd_debug_test_rx(t_hydra_console *con, t_tokenline_parsed *p)
 		chnRead(chp, (uint8_t *)g_sbuf, sizeof(g_sbuf) - 1);
 
 		/* Exit if User Button is pressed */
-		if (USER_BUTTON) {
+		if (hydrabus_ubtn()) {
 			break;
 		}
 		//get_char(con);
@@ -643,3 +643,8 @@ uint32_t reverse_u32(uint32_t value)
 	value = (value & 0x00FF00FF) << 8 | (value & 0xFF00FF00) >> 8;
 	return value << 16 | value >> 16;
 }
+
+uint8_t hydrabus_ubtn(void)
+{
+	return bsp_gpio_pin_read(BSP_GPIO_PORTA, 0);
+}




@@ -34,12 +34,6 @@
 #undef NS2RTT
 #define NS2RTT(nsec) ( (US2ST(nsec)/1000) )
 
-/* UBTN PA0 Configured as Input */
-#undef USER_BUTTON
-#define USER_BUTTON (palReadPad(GPIOA, 0))
-#undef ABORT_BUTTON
-#define ABORT_BUTTON() ((palReadPad(GPIOA, 0)))
-
 /* ULED PA4 Configured as Output for Test */
 #undef ULED_ON
 #define ULED_ON  (palSetPad(GPIOA, 4))
@@ -179,4 +173,6 @@ uint8_t reverse_u8(uint8_t value);
 uint16_t reverse_u16(uint16_t value);
 uint32_t reverse_u32(uint32_t value);
 
+uint8_t hydrabus_ubtn(void);
+
 #endif /* _COMMON_H_ */




@@ -121,7 +121,7 @@ void bsp_enter_usb_dfu(void)
 	__GPIOA_CLK_ENABLE();
 	bsp_gpio_init(BSP_GPIO_PORTA, 0, MODE_CONFIG_DEV_GPIO_IN, MODE_CONFIG_DEV_GPIO_NOPULL);
 
-	if(USER_BUTTON) {
+	if(bsp_gpio_pin_read(BSP_GPIO_PORTA, 0)) {
 		reboot_usb_dfu();
 	}
 }




@@ -17,10 +17,6 @@ limitations under the License.
 
 #include <stdint.h>
 
-/* UBTN PA0 Configured as Input */
-#undef USER_BUTTON
-#define USER_BUTTON (palReadPad(GPIOA, 0))
-
 /* Macro for fast read, set & clear GPIO pin */
 #define gpio_get_pin(GPIOx, GPIO_Pin) (GPIOx->IDR & GPIO_Pin)
 #define gpio_set_pin(GPIOx, GPIO_Pin) (GPIOx->BSRR = GPIO_Pin<<16)




@@ -48,7 +48,7 @@ static void read_continuous(t_hydra_console *con, uint16_t *gpio, int period)
 	}
 	cprint(con, "\r\n", 2);
 
-	while (!USER_BUTTON) {
+	while (!hydrabus_ubtn()) {
 		for (port = 0; port < 3; port++) {
 			for (pin = 0; pin < 16; pin++) {
 				if (!(gpio[port] & (1 << pin)))




@@ -145,7 +145,7 @@ int cmd_adc(t_hydra_console *con, t_tokenline_parsed *p)
 		cprintf(con, "%s\t", adc_channel_names[sources[i]]);
 	cprintf(con, "\r\n");
 
-	while (!USER_BUTTON) {
+	while (!hydrabus_ubtn()) {
 		if (!adc_read(con, num_sources))
 			break;
 		if (!continuous && --count == 0)




@@ -48,7 +48,7 @@ int cmd_bbio(t_hydra_console *con)
 
 	cprint(con, "BBIO1", 5);
 
-	while (!palReadPad(GPIOA, 0)) {
+	while (!hydrabus_ubtn()) {
 		if(chnRead(con->sdu, &bbio_mode, 1) == 1) {
 			switch(bbio_mode) {
 			case BBIO_SPI:




@@ -76,7 +76,7 @@ void bbio_mode_can(t_hydra_console *con)
 
 	bbio_mode_id(con);
 
-	while(!USER_BUTTON) {
+	while(!hydrabus_ubtn()) {
 		if(chnRead(con->sdu, &bbio_subcommand, 1) == 1) {
 			switch(bbio_subcommand) {
 			case BBIO_RESET:




@@ -48,7 +48,7 @@ void bbio_mode_flash(t_hydra_console *con)
 
 	bbio_mode_id(con);
 
-	while (!USER_BUTTON) {
+	while (!hydrabus_ubtn()) {
 		if(chnRead(con->sdu, &bbio_subcommand, 1) == 1) {
 			switch(bbio_subcommand) {
 			case BBIO_RESET:




@@ -68,7 +68,7 @@ void bbio_mode_i2c(t_hydra_console *con)
 
 	bbio_mode_id(con);
 
-	while (!USER_BUTTON) {
+	while (!hydrabus_ubtn()) {
 		if(chnRead(con->sdu, &bbio_subcommand, 1) == 1) {
 			switch(bbio_subcommand) {
 			case BBIO_RESET:




@@ -47,7 +47,7 @@ void bbio_mode_onewire(t_hydra_console *con)
 
 	bbio_mode_id(con);
 
-	while (!USER_BUTTON) {
+	while (!hydrabus_ubtn()) {
 		if(chnRead(con->sdu, &bbio_subcommand, 1) == 1) {
 			switch(bbio_subcommand) {
 			case BBIO_RESET:




@@ -85,7 +85,7 @@ void bbio_mode_rawwire(t_hydra_console *con)
 
 	bbio_mode_id(con);
 
-	while (!USER_BUTTON) {
+	while (!hydrabus_ubtn()) {
 		if(chnRead(con->sdu, &bbio_subcommand, 1) == 1) {
 			switch(bbio_subcommand) {
 			case BBIO_RESET:




@@ -69,7 +69,7 @@ void 