@@ -1107,6 +1107,8 @@ static void print_types_json(const RPdb *pdb, PJ *pj, const RList *types) {
 	r_return_if_fail (pdb && types && pj);
 
 	RListIter *it = r_list_iterator (types);
+
+	pj_o (pj);
 	pj_ka (pj, "types");
 
 	while (r_list_iter_next (it)) {
@@ -1213,6 +1215,7 @@ static void print_types_json(const RPdb *pdb, PJ *pj, const RList *types) {
 		}
 	}
 	pj_end (pj);
+	pj_end (pj);
 }
 
 /**
@@ -1363,6 +1366,7 @@ static void print_gvars(RPdb *pdb, ut64 img_base, PJ *pj, int format) {
 	}
 
 	if (format == 'j') {
+		pj_o (pj);
 		pj_ka (pj, "gvars");
 	}
 	gsym_data_stream = (SGDATAStream *)gsym->stream;
@@ -1420,6 +1424,7 @@ static void print_gvars(RPdb *pdb, ut64 img_base, PJ *pj, int format) {
 	}
 	if (format == 'j') {
 		pj_end (pj);
+		pj_end (pj);
 	}
 }
 




@@ -1270,6 +1270,7 @@ R_API bool r_core_pdb_info(RCore *core, const char *file, int mode) {
 		break;
 	}
 	PJ *pj = r_core_pj_new (core);
+	pj_a (pj);
 
 	pdb.print_types (&pdb, pj, mode);
 	pdb.print_gvars (&pdb, baddr, pj, mode);
@@ -1277,6 +1278,8 @@ R_API bool r_core_pdb_info(RCore *core, const char *file, int mode) {
 	r_parse_pdb_types (core->anal, &pdb);
 	pdb.finish_pdb_parse (&pdb);
 
+	pj_end (pj);
+
 	if (mode == 'j') {
 		r_cons_printf ("%s\n", pj_string (pj));
 	}