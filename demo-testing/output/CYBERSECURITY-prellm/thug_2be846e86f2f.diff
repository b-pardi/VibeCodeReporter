@@ -25,11 +25,12 @@ import datetime
 log = logging.getLogger("Thug")
 
 
-class BaseLogging(object):
+class BaseLogging:
     def __init__(self):
         self.baseDir = None
 
-    def check_module(self, module, config):
+    @staticmethod
+    def check_module(module, config):
         if not getattr(log.ThugOpts, "%s_logging" % (module, ), True):
             return False
 




@@ -21,7 +21,7 @@ import logging
 log = logging.getLogger("Thug")
 
 
-class Features(object):
+class Features:
     counters = (
         'activex_count',
         'addeventlistener_count',




@@ -31,7 +31,7 @@ from thug.Magic.Magic import Magic
 log = logging.getLogger("Thug")
 
 
-class SampleLogging(object):
+class SampleLogging:
     doc_mime_types = (
         'application/msword',
         'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
@@ -50,7 +50,8 @@ class SampleLogging(object):
                       'DOC',
                       'RTF', )
 
-    def is_pe(self, data):
+    @staticmethod
+    def is_pe(data):
         try:
             pefile.PE(data = data, fast_load = True)
         except Exception:
@@ -58,7 +59,8 @@ class SampleLogging(object):
 
         return True
 
-    def get_imphash(self, data):
+    @staticmethod
+    def get_imphash(data):
         try:
             pe = pefile.PE(data = data)
         except Exception:
@@ -66,11 +68,13 @@ class SampleLogging(object):
 
         return pe.get_imphash()
 
-    def is_pdf(self, data):
+    @staticmethod
+    def is_pdf(data):
         data = data.encode() if isinstance(data, str) else data
-        return (data[:1024].find(b'%PDF') != -1)
+        return data[:1024].find(b'%PDF') != -1
 
-    def is_jar(self, data):
+    @staticmethod
+    def is_jar(data):
         data = data.encode() if isinstance(data, str) else data
 
         fd, jar = tempfile.mkstemp()
@@ -88,7 +92,8 @@ class SampleLogging(object):
         os.remove(jar)
         return result
 
-    def is_swf(self, data):
+    @staticmethod
+    def is_swf(data):
         data = data.encode() if isinstance(data, str) else data
         return data.startswith(b'CWS') or data.startswith(b'FWS')
 




@@ -24,8 +24,8 @@ import string
 import errno
 import hashlib
 import logging
-import six
 import six.moves.configparser as ConfigParser
+import six
 
 from thug.Analysis.shellcode.Shellcode import Shellcode
 from thug.Analysis.virustotal.VirusTotal import VirusTotal
@@ -72,12 +72,15 @@ class ThugLogging(BaseLogging, SampleLogging):
         self.__init_pyhooks()
         self.__init_config()
 
-    def get_random_name(self):
+    @staticmethod
+    def get_random_name():
         return ''.join(random.choice(string.ascii_lowercase) for _ in range(random.randint(10, 32)))
 
     def __init_hook_symbols(self):
         for name in ('eval', 'write', ):
-            setattr(self, '{}_symbol'.format(name), (self.get_random_name(), self.get_random_name(), ))
+            setattr(self,
+                    '{}_symbol'.format(name),
+                    (self.get_random_name(), self.get_random_name(), ))
 
     def __init_pyhooks(self):
         hooks = log.PyHooks.get('ThugLogging', None)
@@ -93,7 +96,7 @@ class ThugLogging(BaseLogging, SampleLogging):
     def __init_config(self):
         conf_file = os.path.join(log.configuration_path, 'thug.conf')
         if not os.path.exists(conf_file): # pragma: no cover
-            log.warning("[CRITICAL] Logging subsystem not initialized (configuration file not found)")
+            log.critical("Logging subsystem not initialized (configuration file not found)")
             return
 
         self.modules = dict()
@@ -279,7 +282,10 @@ class ThugLogging(BaseLogging, SampleLogging):
         @meta           Rule meta
         @tags           Rule tags
         """
-        self.add_behavior_warn("[%s Classifier] URL: %s (Rule: %s, Classification: %s)" % (classifier.upper(), url, rule, tags, ))
+        self.add_behavior_warn("[%s Classifier] URL: %s (Rule: %s,