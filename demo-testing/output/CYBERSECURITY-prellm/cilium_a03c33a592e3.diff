@@ -44,8 +44,12 @@ static inline int handle_ipv6(struct __sk_buff *skb)
 	bool decrypted;
 
 	decrypted = ((skb->mark & MARK_MAGIC_HOST_MASK) == MARK_MAGIC_DECRYPT);
-	if (!revalidate_data(skb, &data, &data_end, &ip6))
-		return DROP_INVALID;
+	if (!revalidate_data(skb, &data, &data_end, &ip6)) {
+		int err = skb_pull_data(skb, ETH_HLEN + sizeof(struct ipv6hdr));
+
+		if (err || !revalidate_data(skb, &data, &data_end, &ip6))
+			return DROP_INVALID;
+	}
 
 	if (!decrypted) {
 		/* IPSec is not currently enforce (feature coming soon)
@@ -82,8 +86,12 @@ static inline int handle_ipv4(struct __sk_buff *skb)
 	bool decrypted;
 
 	decrypted = ((skb->mark & MARK_MAGIC_HOST_MASK) == MARK_MAGIC_DECRYPT);
-	if (!revalidate_data(skb, &data, &data_end, &ip4))
-		return DROP_INVALID;
+	if (!revalidate_data(skb, &data, &data_end, &ip4)) {
+		int err = skb_pull_data(skb, ETH_HLEN + sizeof(struct iphdr));
+
+		if (err || !revalidate_data(skb, &data, &data_end, &ip4))
+			return DROP_INVALID;
+	}
 
 	if (!decrypted) {
 		/* IPSec is not currently enforce (feature coming soon)




@@ -175,6 +175,7 @@ static int BPF_FUNC(skb_change_proto, struct __sk_buff *skb, uint32_t proto,
 		    uint32_t flags);
 static int BPF_FUNC(skb_change_tail, struct __sk_buff *skb, uint32_t nlen,
 		    uint32_t flags);
+static int BPF_FUNC(skb_pull_data, struct __sk_buff *skb, uint32_t len);
 
 /* Packet vlan encap/decap */
 static int BPF_FUNC(skb_vlan_push, struct __sk_buff *skb, uint16_t proto,