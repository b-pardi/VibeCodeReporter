@@ -2172,7 +2172,9 @@ static void anop(RAnal *a, RAnalOp *op, ut64 addr, const ut8 *buf, int len, csh
 				op->stackptr = regsz;
 				op->type |= R_ANAL_OP_TYPE_REG;
 			}
-			op->ptr = INSOP(1).imm;
+			if (INSOP(1).type == X86_OP_IMM) {
+				op->val = INSOP(1).imm;
+			}
 			break;
 		default:
 			switch (INSOP(1).type) {
@@ -2186,6 +2188,9 @@ static void anop(RAnal *a, RAnalOp *op, ut64 addr, const ut8 *buf, int len, csh
 					op->stackop = R_ANAL_STACK_SET;
 					op->stackptr = regsz;
 				}
+				if (INSOP(0).type == X86_OP_IMM) {
+					op->val = INSOP(0).imm;
+				}
 				break;
 			case X86_OP_IMM:
 				op->val = op->ptr = INSOP(1).imm;




@@ -618,13 +618,14 @@ R_API void r_core_anal_type_match(RCore *core, RAnalFunction *fcn) {
 						if ((jmp_op->type == R_ANAL_OP_TYPE_RET && r_anal_bb_is_in_offset (jmpbb, jmp_addr))
 								|| jmp_op->type == R_ANAL_OP_TYPE_CJMP) {
 							jmp = true;
+							r_anal_op_free (jmp_op);
 							break;
 						}
 						jmp_addr += jmp_op->size;
 						r_anal_op_free (jmp_op);
 					}
 					int cond = jmp? cond_invert (next_op->cond): next_op->cond;
-					var_add_range (anal, var, cond, aop.ptr);
+					var_add_range (anal, var, cond, aop.val);
 				}
 			}
 			prev_var = (var && aop.direction == R_ANAL_OP_DIR_READ)? true: false;