@@ -21,7 +21,6 @@ public final class CpmiClusterMember extends GatewayOrServer {
       @JsonProperty(PROP_POLICY) @Nullable GatewayOrServerPolicy policy,
       @JsonProperty(PROP_UID) @Nullable Uid uid) {
     checkArgument(interfaces != null, "Missing %s", PROP_INTERFACES);
-    checkArgument(ipv4Address != null, "Missing %s", PROP_IPV4_ADDRESS);
     checkArgument(name != null, "Missing %s", PROP_NAME);
     checkArgument(policy != null, "Missing %s", PROP_POLICY);
     checkArgument(uid != null, "Missing %s", PROP_UID);
@@ -30,7 +29,7 @@ public final class CpmiClusterMember extends GatewayOrServer {
 
   @VisibleForTesting
   CpmiClusterMember(
-      Ip ipv4Address,
+      @Nullable Ip ipv4Address,
       String name,
       List<Interface> interfaces,
       GatewayOrServerPolicy policy,




@@ -24,7 +24,6 @@ public final class CpmiGatewayCluster extends Cluster {
       @JsonProperty(PROP_POLICY) @Nullable GatewayOrServerPolicy policy,
       @JsonProperty(PROP_UID) @Nullable Uid uid) {
     checkArgument(interfaces != null, "Missing %s", PROP_INTERFACES);
-    checkArgument(ipv4Address != null, "Missing %s", PROP_IPV4_ADDRESS);
     checkArgument(name != null, "Missing %s", PROP_NAME);
     checkArgument(policy != null, "Missing %s", PROP_POLICY);
     checkArgument(uid != null, "Missing %s", PROP_UID);
@@ -40,7 +39,7 @@ public final class CpmiGatewayCluster extends Cluster {
   @VisibleForTesting
   CpmiGatewayCluster(
       List<String> clusterMemberNames,
-      Ip ipv4Address,
+      @Nullable Ip ipv4Address,
       String name,
       List<Interface> interfaces,
       GatewayOrServerPolicy policy,




@@ -21,7 +21,6 @@ public final class CpmiHostCkp extends GatewayOrServer {
       @JsonProperty(PROP_NAME) @Nullable String name,
       @JsonProperty(PROP_POLICY) @Nullable GatewayOrServerPolicy policy,
       @JsonProperty(PROP_UID) @Nullable Uid uid) {
-    checkArgument(ipv4Address != null, "Missing %s", PROP_IPV4_ADDRESS);
     checkArgument(name != null, "Missing %s", PROP_NAME);
     checkArgument(policy != null, "Missing %s", PROP_POLICY);
     checkArgument(uid != null, "Missing %s", PROP_UID);
@@ -31,7 +30,7 @@ public final class CpmiHostCkp extends GatewayOrServer {
 
   @VisibleForTesting
   CpmiHostCkp(
-      Ip ipv4Address,
+      @Nullable Ip ipv4Address,
       List<Interface> interfaces,
       String name,
       GatewayOrServerPolicy policy,




@@ -23,7 +23,6 @@ public final class CpmiVsClusterNetobj extends Cluster {
       @JsonProperty(PROP_NAME) @Nullable String name,
       @JsonProperty(PROP_POLICY) @Nullable GatewayOrServerPolicy policy,
       @JsonProperty(PROP_UID) @Nullable Uid uid) {
-    checkArgument(interfaces != null, "Missing %s", PROP_INTERFACES);
     checkArgument(name != null, "Missing %s", PROP_NAME);
     checkArgument(policy != null, "Missing %s", PROP_POLICY);
     checkArgument(uid != null, "Missing %s", PROP_UID);
@@ -31,7 +30,7 @@ public final class CpmiVsClusterNetobj extends Cluster {
         ImmutableList.copyOf(firstNonNull(clusterMemberNames, ImmutableList.of())),
         ipv4Address,
         name,
-        interfaces,
+        ImmutableList.copyOf(firstNonNull(interfaces, ImmutableList.of())),
         policy,
         uid);
   }




@@ -1,10 +1,12 @@
 package org.batfish.vendor.check_point_management;
 
+import static com.google.common.base.MoreObjects.firstNonNull;
 import static com.google.common.base.Preconditions.checkArgument;
 
 import com.fasterxml.jackson.annotation.JsonCreator;
 import com.fasterxml.jackson.annotation.JsonProperty;
 import com.google.common.annotations.VisibleForTesting;
+import com.google.common.collect.ImmutableList;
 import java.util.List;
 import javax.annotation.Nonnull;
 import javax.annotation.Nullable;
@@ -20,17 +22,20 @@ public final class CpmiVsNetobj extends GatewayOrServer {
       @JsonProperty(PROP_NAME) @Nullable String name,
       @JsonProperty(PROP_POLICY) @Nullable GatewayOrServerPolicy policy,
       @JsonPrope