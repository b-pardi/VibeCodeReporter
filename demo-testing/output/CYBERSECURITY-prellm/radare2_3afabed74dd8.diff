@@ -3903,6 +3903,11 @@ static void getpcfromstack(RCore *core, RAnalEsil *esil) {
 	free (buf);
 }
 
+static inline bool canal_isThumb(RCore *core) {
+	const char *asmarch = r_config_get (core->config, "asm.arch");
+	return (!strcmp (asmarch, "arm") && core->anal->bits == 16);
+}
+
 R_API void r_core_anal_esil(RCore *core, const char *str, const char *target) {
 	bool cfg_anal_strings = r_config_get_i (core->config, "anal.strings");
 	RAnalEsil *ESIL = core->anal->esil;
@@ -4005,7 +4010,6 @@ R_API void r_core_anal_esil(RCore *core, const char *str, const char *target) {
 	}
 
 	int opalign = r_anal_archinfo (core->anal, R_ANAL_ARCHINFO_ALIGN);
-	int in = r_syscall_get_swi (core->anal->syscall);
 	const char *sn = r_reg_get_name (core->anal->reg, R_REG_NAME_SN);
 	r_reg_arena_push (core->anal->reg);
 	for (i = 0; i < iend; i++) {
@@ -4036,24 +4040,20 @@ R_API void r_core_anal_esil(RCore *core, const char *str, const char *target) {
 			i += minopsize - 1;
 			continue;
 		}
-		switch (op.type) {
-		case R_ANAL_OP_TYPE_SWI:
-			if (!refptr && (in == -1 || op.val == in)) {
-				r_flag_space_set (core->flags, "syscalls");
-				int snv = (int)r_reg_getv (core->anal->reg, sn);
-				if (snv > 0) {
-					RSyscallItem *si = r_syscall_get (core->anal->syscall, snv, in);
-					if (si) {
-					//	eprintf ("0x%08"PFMT64x" SYSCALL %-4d %s\n", cur, snv, si->name);
-						r_flag_set_next (core->flags, sdb_fmt ("syscall.%s", si->name), cur, 1);
-					} else {
-					//	eprintf ("0x%08"PFMT64x" SYSCALL %d\n", cur, snv);
-						r_flag_set_next (core->flags, sdb_fmt ("syscall.%d", snv), cur, 1);
-					}
-					r_flag_space_set (core->flags, NULL);
-				}
+		if (op.type == R_ANAL_OP_TYPE_SWI) {
+			r_flag_space_set (core->flags, "syscalls");
+			int snv = canal_isThumb (core)? op.val: (int)r_reg_getv (core->anal->reg, sn);
+			RSyscallItem *si = r_syscall_get (core->anal->syscall, snv, -1);
+			if (si) {
+			//	eprintf ("0x%08"PFMT64x" SYSCALL %-4d %s\n", cur, snv, si->name);
+				r_flag_set_next (core->flags, sdb_fmt ("syscall.%s", si->name), cur, 1);
+			} else {
+				//todo were doing less filtering up top because we cant match against 80 on all platforms
+				// might get too many of this path now.. 
+			//	eprintf ("0x%08"PFMT64x" SYSCALL %d\n", cur, snv);
+				r_flag_set_next (core->flags, sdb_fmt ("syscall.%d", snv), cur, 1);
 			}
-			break;
+			r_flag_space_set (core->flags, NULL);
 		}
 		if (1) {
 			const char *esilstr = R_STRBUF_SAFEGET (&op.esil);




@@ -1221,15 +1221,25 @@ static const char *syscallNumber(int n) {
 	return sdb_fmt (n > 1000 ? "0x%x" : "%d", n);
 }
 
-R_API char *cmd_syscall_dostr(RCore *core, int n) {
+const bool archIsThumb(RCore *core) {
+	return (!strcmp (core->anal->cur->arch, "arm") && core->anal->bits == 16);
+}
+
+R_API char *cmd_syscall_dostr(RCore *core, int n, ut64 addr) {
 	char *res = NULL;
 	int i;
 	char str[64];
 	if (n == -1) {
-		n = (int)r_debug_reg_get (core->dbg, "oeax");
-		if (!n || n == -1) {
-			const char *a0 = r_reg_get_name (core->anal->reg, R_REG_NAME_SN);
-			n = (int)r_debug_reg_get (core->dbg, a0);
+		if (addr && archIsThumb (core)) {
+			RAnalOp *aop = r_core_anal_op (core, addr, R_ANAL_OP_MASK_BASIC);
+			n = aop->val;
+			r_anal_op_free (aop);
+		} else {
+			n = (int)r_debug_reg_get (core->dbg, "oeax");
+			if (!n || n == -1) {
+				const char *a0 = r_reg_get_name (core->anal->reg, R_REG_NAME_SN);
+				n = (a0 == NULL)? -1: (int)r_debug_reg_get (core->dbg, a0);
+			}
 		}
 	}
 	RSyscallItem *item = r_syscall_get (core->anal->syscall, n, -1);
@@ -1292,8 +1302,8 @@ R_API char *cmd_syscall_dostr(RCore *core, int n) {
 	return res;
 }
 
-static void cmd_syscall_do(RCore *core, int n) {
-	char *msg = cmd_syscall_dostr (core, n);
+static void cmd_syscall_do(RCore *core, int n, ut64 addr) {
+	char *msg = cmd_syscall_dostr (core, n, addr);
 	if (msg) {
 		r_cons_println (msg);
 		free (msg);
@@ -5244,10 +5254,10 @@ static void cmd_anal_syscall(RCore *core, const char *input) {
 		// JSON su