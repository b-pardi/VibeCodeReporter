@@ -70,8 +70,8 @@ BareosSocket* ConnectToDirector(JobControlRecord& jcr,
     local_tls_resource = director_resource;
   }
 
-  UA_sock->SetNwdump(
-      BnetDump::Create(own_resource, director_resource));
+  UA_sock->FillBSockWithConnectedDaemonInformation(own_resource,
+                                                   director_resource);
 
   if (local_tls_resource->IsTlsConfigured()) {
     std::string qualified_resource_name;




@@ -268,8 +268,8 @@ bool ConnectToFileDaemon(JobControlRecord* jcr,
 
     if (jcr->file_bsock) {
       jcr->setJobStatus(JS_Running);
-      jcr->file_bsock->SetNwdump(
-          BnetDump::Create(me, jcr->res.client));
+      jcr->file_bsock->FillBSockWithConnectedDaemonInformation(me,
+                                                               jcr->res.client);
       if (AuthenticateWithFileDaemon(jcr)) {
         success = true;
         SendInfoSuccess(jcr, ua);




@@ -143,7 +143,7 @@ bool ConnectToStorageDaemon(JobControlRecord* jcr,
     }
   }
 
-  sd->SetNwdump(BnetDump::Create(me, store));
+  sd->FillBSockWithConnectedDaemonInformation(me, store);
 
   if (!AuthenticateWithStorageDaemon(sd.get(), jcr, store)) {
     sd->close();




@@ -36,6 +36,7 @@
 #include "lib/berrno.h"
 #include "lib/bnet_server_tcp.h"
 #include "lib/try_tls_handshake_as_a_server.h"
+#include "lib/parse_conf.h"
 #include "lib/util.h"
 
 #include <atomic>
@@ -112,8 +113,6 @@ static void* HandleConnectionRequest(ConfigurationParser* config, void* arg)
 
   Dmsg1(110, "Conn: %s", bs->msg);
 
-  FillBSockWithConnectedDaemonInformation(*my_config, bs);
-
   /*
    * See if this is a File daemon connection. If so call FD handler.
    */
@@ -122,10 +121,11 @@ static void* HandleConnectionRequest(ConfigurationParser* config, void* arg)
       (sscanf(bs->msg, hello_client, name) == 1)) {
     Dmsg1(110, "Got a FD connection at %s\n",
           bstrftimes(tbuf, sizeof(tbuf), (utime_t)time(NULL)));
+    bs->FillBSockWithConnectedDaemonInformation(*my_config, R_CLIENT);
     return HandleFiledConnection(client_connections, bs, name,
                                  fd_protocol_version);
   }
-
+  bs->FillBSockWithConnectedDaemonInformation(*my_config, R_CONSOLE);
   return HandleUserAgentClientRequest(bs);
 }
 




@@ -1694,8 +1694,8 @@ static bool StorageCmd(JobControlRecord* jcr)
     }
   }
 
-  storage_daemon_socket->SetNwdump(BnetDump::Create(
-      me, R_STORAGE, *my_config->GetQualifiedResourceNameTypeConverter()));
+  storage_daemon_socket->FillBSockWithConnectedDaemonInformation(*my_config,
+                                                                 R_STORAGE);
 
   storage_daemon_socket->fsend("Hello Start Job %s\n", jcr->Job);
   if (!AuthenticateWithStoragedaemon(jcr)) {
@@ -2321,7 +2321,7 @@ static BareosSocket* connect_to_director(JobControlRecord* jcr,
     }
   }
 
-  director_socket->SetNwdump(BnetDump::Create(me, dir_res));
+  director_socket->FillBSockWithConnectedDaemonInformation(me, dir_res);
 
   Dmsg1(10, "Opened connection with Director %s\n", dir_res->resource_name_);
   jcr->dir_bsock = director_socket.get();




@@ -81,8 +81,6 @@ static void* HandleConnectionRequest(ConfigurationParser* config, void* arg)
 
   Dmsg1(110, "Conn: %s\n", bs->msg);
 
-  FillBSockWithConnectedDaemonInformation(*my_config, bs);
-
   /*
    * See if its a director making a connection.
    */
@@ -90,6 +88,7 @@ static void* HandleConnectionRequest(ConfigurationParser* config, void* arg)
   if (bstrncmp(bs->msg, "Hello Director", 14)) {
     Dmsg1(110, "Got a DIR connection at %s\n",
           bstrftimes(tbuf, sizeof(tbuf), (utime_t)time(NULL)));
+    bs->FillBSockWithConnectedDaemonInformation(*my_config, R_DIRECTOR);
     return handle_director_connection(bs);
   }
 
@@ -99,6 +98,7 @@ static void* HandleConnectionRequest(ConfigurationParser* config, void* arg)
   if (bstrncmp(bs->msg, "Hello Storage", 13)) {
     Dmsg1(110, "Got a SD connection at %s\n",
           bstrftimes(tbuf, sizeof(tbuf), (utime_