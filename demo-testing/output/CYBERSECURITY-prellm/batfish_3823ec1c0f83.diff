@@ -718,6 +718,12 @@ public enum JunosApplication implements Application {
           break;
         }
 
+      case JUNOS_PING:
+        {
+          ipProtocol = IpProtocol.ICMP;
+          break;
+        }
+
       case JUNOS_PC_ANYWHERE:
         {
           portRangeStart = NamedPort.PCANYWHERE_STATUS.number();
@@ -740,6 +746,13 @@ public enum JunosApplication implements Application {
           break;
         }
 
+      case JUNOS_RADIUS:
+        {
+          portRangeStart = NamedPort.RADIUS_JUNIPER.number();
+          ipProtocol = IpProtocol.UDP;
+          break;
+        }
+
       case JUNOS_RIP:
         {
           portRangeStart = NamedPort.RIP.number();
@@ -788,6 +801,13 @@ public enum JunosApplication implements Application {
           break;
         }
 
+      case JUNOS_SYSLOG:
+        {
+          portRangeStart = NamedPort.CMDtcp_OR_SYSLOGudp.number();
+          ipProtocol = IpProtocol.UDP;
+          break;
+        }
+
       case JUNOS_SUN_RPC_ANY_TCP:
         {
           ipProtocol = IpProtocol.TCP;