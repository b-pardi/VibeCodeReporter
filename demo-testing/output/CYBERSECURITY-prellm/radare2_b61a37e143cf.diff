@@ -2727,6 +2727,11 @@ static int bin_fields(RCore *r, int mode, int va) {
 				// TODO: filter comment before json
 				r_cons_printf (",\"format\":\"%s\"", field->format);
 			}
+			char *o = r_core_cmd_strf (r, "pfj.%s@0x%"PFMT64x, field->format, field->vaddr);
+			if (o && *o) {
+				r_cons_printf (",\"pf\":%s", o);
+			}
+			free (o);
 			r_cons_printf ("}");
 		} else if (IS_MODE_NORMAL (mode)) {
 			const bool haveComment = (field->comment && *field->comment);




@@ -185,16 +185,15 @@ static void r_print_format_byte(const RPrint* p, int endian, int mode,
 			p->cb_printf ("%d", buf[i]);
 		} else {
 			p->cb_printf ("[ ");
+			const char *comma = "";
 			while (size--) {
 				if (elem == -1 || elem == 0) {
-					p->cb_printf (", %d", buf[i]);
+					p->cb_printf ("%s%d", comma, buf[i]);
+					comma = ",";
 					if (elem == 0) {
 						elem = -2;
 					}
 				}
-				if (size != 0 && elem == -1) {
-					p->cb_printf (", ");
-				}
 				if (elem > -1) {
 					elem--;
 				}