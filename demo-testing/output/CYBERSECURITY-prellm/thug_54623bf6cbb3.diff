@@ -43,8 +43,9 @@ class CookieClassifier(BaseClassifier):
             self.handle_match_etags(match)
 
             rule = match.rule
+            meta = match.meta
             tags = ",".join([" ".join(t.split('_')) for t in match.tags])
-            log.ThugLogging.log_classifier("cookie", url, rule, tags)
+            log.ThugLogging.log_classifier("cookie", url, rule, tags, meta)
 
         for c in self.custom_classifiers:
             self.custom_classifiers[c](url, cookie)
@@ -54,8 +55,9 @@ class CookieClassifier(BaseClassifier):
 
         for match in self.filters.match(data = cookie):
             rule = match.rule
+            meta = match.meta
             tags = ", ".join([" ".join(t.split('_')) for t in match.tags])
-            log.ThugLogging.log_classifier("cookiefilter", url, rule, tags)
+            log.ThugLogging.log_classifier("cookiefilter", url, rule, tags, meta)
             ret = True
 
         return ret




@@ -43,8 +43,9 @@ class HTMLClassifier(BaseClassifier):
             self.handle_match_etags(match)
 
             rule = match.rule
+            meta = match.meta
             tags = ",".join([" ".join(t.split('_')) for t in match.tags])
-            log.ThugLogging.log_classifier("html", url, rule, tags)
+            log.ThugLogging.log_classifier("html", url, rule, tags, meta)
 
         for c in self.custom_classifiers:
             self.custom_classifiers[c](url, html)
@@ -54,8 +55,9 @@ class HTMLClassifier(BaseClassifier):
 
         for match in self.filters.match(data = html):
             rule = match.rule
+            meta = match.meta
             tags = ", ".join([" ".join(t.split('_')) for t in match.tags])
-            log.ThugLogging.log_classifier("htmlfilter", url, rule, tags)
+            log.ThugLogging.log_classifier("htmlfilter", url, rule, tags, meta)
             ret = True
 
         return ret




@@ -40,9 +40,10 @@ class JSClassifier(BaseClassifier):
             self.handle_match_etags(match)
 
             rule = match.rule
+            meta = match.meta
             tags = ",".join([" ".join(t.split('_')) for t in match.tags])
 
-            log.ThugLogging.log_classifier("js", url, rule, tags)
+            log.ThugLogging.log_classifier("js", url, rule, tags, meta)
 
         for c in self.custom_classifiers:
             self.custom_classifiers[c](url, script)
@@ -52,8 +53,9 @@ class JSClassifier(BaseClassifier):
 
         for match in self.filters.match(data = script):
             rule = match.rule
+            meta = match.meta
             tags = ", ".join([" ".join(t.split('_')) for t in match.tags])
-            log.ThugLogging.log_classifier("jsfilter", url, rule, tags)
+            log.ThugLogging.log_classifier("jsfilter", url, rule, tags, meta)
             ret = True
 
         return ret




@@ -45,8 +45,9 @@ class SampleClassifier(BaseClassifier):
             self.handle_match_etags(match)
 
             rule = match.rule
+            meta = match.meta
             tags = ", ".join([" ".join(t.split('_')) for t in match.tags])
-            log.ThugLogging.log_classifier("sample", md5, rule, tags)
+            log.ThugLogging.log_classifier("sample", md5, rule, tags, meta)
 
         for c in self.custom_classifiers:
             self.custom_classifiers[c](sample, md5)
@@ -56,8 +57,9 @@ class SampleClassifier(BaseClassifier):
 
         for match in self.filters.match(data = sample):
             rule = match.rule
+            meta = match.meta
             tags = ", ".join([" ".join(t.split('_')) for t in match.tags])
-            log.ThugLogging.log_classifier("samplefilter", md5, rule, tags)
+            log.ThugLogging.log_classifier("samplefilter", md5, rule, tags, meta)
             ret = True
 
         return ret




@@ -43,8 +43,9 @@ class TextClassifier(BaseClassifier):
             self.handle_match_etags(match)
 
             rule = match.rule
+            meta = match.meta
             tags = ",".join([" ".join(t.split('_')) for t in ma