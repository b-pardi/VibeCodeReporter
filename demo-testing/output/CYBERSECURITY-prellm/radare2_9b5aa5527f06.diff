@@ -30,7 +30,7 @@ static ut64 binobj_a2b(RBinObject *o, ut64 addr) {
 	return o ? addr + o->baddr_shift : addr;
 }
 
-static void print_string(RBinFile *bf, RBinString *string, int raw, bool first) {
+static void print_string(RBinFile *bf, RBinString *string, int raw, PJ *pj) {
 	r_return_if_fail (bf && string);
 
 	int mode = bf->strmode;
@@ -57,7 +57,6 @@ static void print_string(RBinFile *bf, RBinString *string, int raw, bool first)
 	switch (mode) {
 	case R_MODE_JSON:
 		{
-			PJ *pj = pj_new ();
 			if (pj) {
 				pj_o (pj);
 				pj_kn (pj, "vaddr", vaddr);
@@ -69,8 +68,6 @@ static void print_string(RBinFile *bf, RBinString *string, int raw, bool first)
 				pj_ks (pj, "type", type_string);
 				pj_ks (pj, "string", string->string);
 				pj_end (pj);
-				io->cb_printf ("%s%s\n", first ? "" : ",", pj_string (pj));
-				pj_free (pj);
 			}
 		}
 		break;
@@ -145,7 +142,13 @@ static int string_scan_range(RList *list, RBinFile *bf, int min,
 	st64 vdelta = 0, pdelta = 0;
 	RBinSection *s = NULL;
 	bool ascii_only = false;
-	bool first = true;
+	PJ *pj = NULL;
+	if (bf->strmode == R_MODE_JSON && !list) {
+		pj = pj_new ();
+		if (pj) {
+			pj_a (pj);
+		}
+	}
 	r_buf_read_at (bf->buf, from, buf, len);
 	// may oobread
 	while (needle < to) {
@@ -337,11 +340,8 @@ static int string_scan_range(RList *list, RBinFile *bf, int min,
 					ht_up_insert (bf->o->strings_db, bs->vaddr, bs);
 				}
 			} else {
-				print_string (bf, bs, raw, first);
+				print_string (bf, bs, raw, pj);
 				r_bin_string_free (bs);
-				if (first) {
-					first = false;
-				}
 			}
 			if (from == 0 && to == bf->size) {
 				/* force lookup section at the next one */
@@ -351,6 +351,14 @@ static int string_scan_range(RList *list, RBinFile *bf, int min,
 		ascii_only = false;
 	}
 	free (buf);
+	if (pj) {
+		pj_end (pj);
+		RIO *io = bin->iob.io;
+		if (io) {
+			io->cb_printf ("%s", pj_string (pj));
+		}
+		pj_free (pj);
+	}
 	return count;
 }
 




@@ -448,7 +448,7 @@ static int cmd_info(void *data, const char *input) {
 	int mode = 0; //R_MODE_SIMPLE;
 	bool rdump = false;
 	int is_array = 0;
-	bool is_zz = false;
+	bool is_izzzj = false;
 	Sdb *db;
 
 	for (i = 0; input[i] && input[i] != ' '; i++)
@@ -463,15 +463,18 @@ static int cmd_info(void *data, const char *input) {
 	if (mode == R_MODE_JSON) {
 		int suffix_shift = 0;
 		if (!strncmp (input, "SS", 2) || !strncmp (input, "ee", 2)
-		    || (is_zz = !strncmp (input, "zz", 2))) {
+		    || !strncmp (input, "zz", 2)) {
 			suffix_shift = 1;
 		}
 		if (strlen (input + 1 + suffix_shift) > 1) {
 			is_array = 1;
 		}
+		if (!strncmp (input, "zzz", 2)) {
+			is_izzzj = true;
+		}
 	}
-	if (is_array) {
-		r_cons_printf (is_zz ? "[" : "{");
+	if (is_array && !is_izzzj) {
+		r_cons_printf ("{");
 	}
 	if (!*input) {
 		cmd_info_bin (core, va, mode);
@@ -1234,8 +1237,8 @@ static int cmd_info(void *data, const char *input) {
 		}
 	}
 done:
-	if (is_array) {
-		r_cons_printf ("%s\n", is_zz ? "]" : "}");
+	if (is_array && !is_izzzj) {
+		r_cons_printf ("}\n");
 	}
 	if (newline) {
 		r_cons_newline ();