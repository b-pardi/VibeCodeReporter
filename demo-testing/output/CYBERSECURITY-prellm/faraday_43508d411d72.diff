@@ -846,7 +846,7 @@ class VulnerabilityView(PaginatedMixin,
             flask.abort(400, "Invalid filters")
 
         workspace = self._get_workspace(workspace_name)
-        marshmallow_params = {'many': True, 'context': {}}
+        marshmallow_params = {'many': True, 'context': {}, 'exclude': ('_attachments', )}
         if 'group_by' not in filters:
             offset = None
             limit = None




@@ -2214,6 +2214,11 @@ class TestListVulnerabilityView(ReadWriteAPITests):
         new_host = HostFactory.create(workspace=workspace)
         session.commit()
 
+        attachments = [
+            (TEST_DATA_PATH / 'faraday.png').open('rb'),
+            (TEST_DATA_PATH / 'test.html').open('rb')
+        ]
+
         raw_data = _create_post_data_vulnerability(
             name='vuln_1',
             vuln_type='Vulnerability',
@@ -2223,6 +2228,7 @@ class TestListVulnerabilityView(ReadWriteAPITests):
             policyviolations=[],
             description='helloworld',
             severity='medium',
+            attachments=attachments,
         )
         res = test_client.post(f'/v3/ws/{workspace.name}/vulns', data=raw_data)
 
@@ -2234,6 +2240,8 @@ class TestListVulnerabilityView(ReadWriteAPITests):
         assert res.status_code == 200
         value = res.json['vulnerabilities'][0]['value']
         assert value['name'] == 'vuln_1'
+        assert '_attachments' not in value
+        assert value['attachments_count'] == 2
 
     def test_invalid_vuln_filters(self, test_client, session, workspace):
         data = {