@@ -991,3 +991,11 @@ class TestExploitSamples(object):
         expected = ['[VsmIDE.DTE ActiveX] CreateObject (WScript.Network)']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_AolAttack(self, caplog):
+        sample   = os.path.join(self.exploits_path, "AolAttack.html")
+        expected = ['ActiveX object: 189504B8-50D1-4AA8-B4D6-95C8F58A6414',
+                    '[AOL ActiveX] Attack in LinkSBIcons function',
+                    'CVE-2006-5820']
+
+        self.do_perform_test(caplog, sample, expected)