new file mode 100644



@@ -0,0 +1,45 @@
+'''
+Faraday Penetration Test IDE
+Copyright (C) 2013  Infobyte LLC (http://www.infobytesec.com/)
+See the file 'doc/LICENSE' for the license information
+
+'''
+from __future__ import absolute_import
+from __future__ import print_function
+
+import flask_security.views
+import flask
+from flask_security.recoverable import \
+    send_reset_password_instructions, generate_reset_password_token, _security
+
+from faraday.server.config import faraday_server
+from flask import request, session, Blueprint, redirect
+from flask_security.views import anonymous_user_required, login_user, after_this_request, get_post_login_redirect, _commit, _render_json
+from flask_security.utils import config_value, hash_data
+from itsdangerous import TimedJSONWebSignatureSerializer
+from werkzeug.datastructures import MultiDict
+
+from faraday.server.models import User
+from flask_security.utils import (
+    _datastore,
+)
+
+from faraday.server.utils.invalid_chars import remove_null_caracters
+
+auth = Blueprint('auth', __name__)
+
+@auth.route('/auth/forgot_password', methods= ['GET', 'POST'])
+@anonymous_user_required
+def forgot_password():
+    if 'email' not in request.json:
+        return flask.abort(status=400)
+    email = request.json.get('email')
+
+    user = User.query.filter_by(email=email).first()
+    if not user:
+        # TODO: avoid user enumeration
+        return flask.abort(status=200)
+    result_send = send_reset_password_instructions(user)
+    return flask.jsonify(response=dict(email=email), success=True, code=200)
+
+forgot_password.is_public = True




@@ -27,6 +27,9 @@ from flask_security.utils import (
     get_message,
     verify_and_update_password,
     verify_hash)
+
+from flask_mail import Mail
+
 from flask_kvsession import KVSessionExtension
 from simplekv.fs import FilesystemStore
 from simplekv.decorator import PrefixDecorator
@@ -90,6 +93,9 @@ def register_blueprints(app):
     from faraday.server.api.modules.preferences import preferences_api  # pylint:disable=import-outside-toplevel
     from faraday.server.api.modules.export_data import export_data_api  # pylint:disable=import-outside-toplevel
 
+    #Custom reset password
+    from faraday.server.api.modules.auth import auth
+
     app.register_blueprint(commandsrun_api)
     app.register_blueprint(activityfeed_api)
     app.register_blueprint(credentials_api)
@@ -114,6 +120,7 @@ def register_blueprints(app):
     app.register_blueprint(searchfilter_api)
     app.register_blueprint(preferences_api)
     app.register_blueprint(export_data_api)
+    app.register_blueprint(auth)
 
 
 def check_testing_configuration(testing, app):
@@ -298,11 +305,18 @@ def create_app(db_connection_string=None, testing=None):
         'SECURITY_POST_LOGIN_VIEW': '/_api/session',
         'SECURITY_POST_LOGOUT_VIEW': '/_api/login',
         'SECURITY_POST_CHANGE_VIEW': '/_api/change',
+        'SECURITY_RESET_PASSWORD_TEMPLATE': '/security/reset.html',
+        'SECURITY_POST_RESET_VIEW': '/',
+        'SECURITY_SEND_PASSWORD_RESET_EMAIL':True,
+        #For testing porpouse
+        'SECURITY_EMAIL_SENDER': "noreply@infobytesec.com",
         'SECURITY_CHANGEABLE': True,
+        'SECURITY_RECOVERABLE': True,
         'SECURITY_SEND_PASSWORD_CHANGE_EMAIL': False,
         'SECURITY_MSG_USER_DOES_NOT_EXIST': login_failed_message,
         'SECURITY_TOKEN_AUTHENTICATION_HEADER': 'Authorization',
 
+
         # The line bellow should not be necessary because of the
         # CustomLoginForm, but i'll include it anyway.
         'SECURITY_MSG_INVALID_PASSWORD': login_failed_message,
@@ -372,6 +386,11 @@ def create_app(db_connection_string=None, testing=None):
     app.view_functions['security.login'].is_public = True
     app.view_functions['security.logout'].is_public = True
 
+    # For password recovery of Flask Security
+    app.view_functions['security.forgot_password'].is_public = True
+    app.view_functions['security.reset_pass