@@ -1002,16 +1002,13 @@ static pyc_object *get_object(RBuffer *buffer) {
 		return NULL;
 	}
 
+	bool doref = false;
 	if (flag) {
 		ret = get_none_object ();
 		if (!ret) {
 			return NULL;
 		}
-		ref_idx = r_list_append (refs, ret);
-		if (!ref_idx) {
-			free_object (ret);
-			return NULL;
-		}
+		doref = true;
 	}
 
 	switch (type) {
@@ -1119,6 +1116,13 @@ static pyc_object *get_object(RBuffer *buffer) {
 		free_object (ret);
 		return NULL;
 	}
+	if (doref) {
+		ref_idx = r_list_append (refs, ret);
+		if (!ref_idx) {
+			free_object (ret);
+			return NULL;
+		}
+	}
 
 	if (flag && ref_idx) {
 		if (ref_idx->data != ret) {