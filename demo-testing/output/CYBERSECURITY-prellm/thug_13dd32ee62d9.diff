@@ -618,3 +618,11 @@ class TestMiscSamplesChrome(object):
                     'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testIFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testIFrame.html")
+        expected = ['[iframe redirection]',
+                    'width: 3',
+                    'height: 4']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -583,3 +583,11 @@ class TestMiscSamplesFirefox(object):
                     'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testIFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testIFrame.html")
+        expected = ['[iframe redirection]',
+                    'width: 3',
+                    'height: 4']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -800,3 +800,11 @@ class TestMiscSamplesIE(object):
                     'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testIFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testIFrame.html")
+        expected = ['[iframe redirection]',
+                    'width: 3',
+                    'height: 4']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -768,3 +768,11 @@ class TestMiscSamplesIE(object):
                     'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testIFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testIFrame.html")
+        expected = ['[iframe redirection]',
+                    'width: 3',
+                    'height: 4']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -802,3 +802,11 @@ class TestMiscSamplesIE(object):
                     'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testIFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testIFrame.html")
+        expected = ['[iframe redirection]',
+                    'width: 3',
+                    'height: 4']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -733,3 +733,11 @@ class TestMiscSamplesIE(object):
                     'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testIFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testIFrame.html")
+        expected = ['[iframe redirection]',
+                    'width: 3',
+                    'height: 4']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -941,3 +941,11 @@ class TestMiscSamplesIE(object):
                     'paused: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testIFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testIFrame.html")
+        expected = ['[iframe redirection]',
+                    'width: 3',
+                    'height: 4']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -599,3 +599,11 @@ class TestMiscSamplesChrome(object):
                     'Alert Text: data:text/html,<script>alert(\'Hello world\');</script>']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testIFrame(self, caplog):
+        sample   = os.path.join(self.misc_path, "testIFrame.html")
+        expected = ['[iframe redirection]',
+                    'width: 3',
+                    'height: 4']
+
+        self.do_perform_test(caplog, sample, expected)