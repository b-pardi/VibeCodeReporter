@@ -668,3 +668,10 @@ class TestMiscSamplesChrome(object):
                     'cssText: foo: bar;']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFormProperty(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFormProperty.html")
+        expected = ['[object HTMLFormElement]',
+                    'formA']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -640,3 +640,10 @@ class TestMiscSamplesFirefox(object):
                     'cssText: foo: bar;']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFormProperty(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFormProperty.html")
+        expected = ['[object HTMLFormElement]',
+                    'formA']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -838,3 +838,10 @@ class TestMiscSamplesIE(object):
                     'cssText: foo: bar;']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFormProperty(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFormProperty.html")
+        expected = ['[object HTMLFormElement]',
+                    'formA']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -806,3 +806,10 @@ class TestMiscSamplesIE(object):
                     'cssText: foo: bar;']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFormProperty(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFormProperty.html")
+        expected = ['[object HTMLFormElement]',
+                    'formA']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -847,3 +847,10 @@ class TestMiscSamplesIE(object):
                     'cssText: foo: bar;']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFormProperty(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFormProperty.html")
+        expected = ['[object HTMLFormElement]',
+                    'formA']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -771,3 +771,10 @@ class TestMiscSamplesIE(object):
                     'cssText: foo: bar;']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFormProperty(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFormProperty.html")
+        expected = ['[object HTMLFormElement]',
+                    'formA']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -1003,3 +1003,10 @@ class TestMiscSamplesIE(object):
                     'cssText: foo: bar;']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFormProperty(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFormProperty.html")
+        expected = ['[object HTMLFormElement]',
+                    'formA']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -649,3 +649,10 @@ class TestMiscSamplesChrome(object):
                     'cssText: foo: bar;']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testFormProperty(self, caplog):
+        sample   = os.path.join(self.misc_path, "testFormProperty.html")
+        expected = ['[object HTMLFormElement]',
+                    'formA']
+
+        self.do_perform_test(caplog, sample, expected)