@@ -48,6 +48,7 @@ static const char *help_msg_pc[] = {
 	"pc*", "", "print 'wx' r2 commands",
 	"pcA", "", ".bytes with instructions in comments",
 	"pca", "", "GAS .byte blob",
+	"pcc", "", "C char * multiline string",
 	"pcd", "", "C dwords (8 byte)",
 	"pch", "", "C half-words (2 byte)",
 	"pci", "", "C array of bytes with instructions",




@@ -1,4 +1,4 @@
-/* radare - LGPL - Copyright 2007-2020 - pancake */
+/* radare - LGPL - Copyright 2007-2022 - pancake */
 
 #include <r_util.h>
 #include <r_util/r_print.h>
@@ -119,6 +119,27 @@ R_API void r_print_code(RPrint *p, ut64 addr, const ut8 *buf, int len, char lang
 	case 'A': // "pcA"
 		/* implemented in core because of disasm :( */
 		break;
+	case 'c': // "pcc"
+		p->cb_printf ("const char cstr[%d] = \"", len);
+		{
+		int col = 0;
+		const int max_cols = 60;
+		for (i = 0; !r_print_is_interrupted () && i < len; i++) {
+			if (col == 0 || col > max_cols) {
+				p->cb_printf ("\"\\\n  \"");
+				col = 0;
+			}
+			if (IS_PRINTABLE (buf[i])) {
+				p->cb_printf ("%c", buf[i]);
+				col ++;
+			} else {
+				p->cb_printf ("\\x%02x", buf[i]);
+				col += 4;
+			}
+		}
+		}
+		p->cb_printf ("\";\n", len);
+		break;
 	case 'a': // "pca"
 		p->cb_printf ("shellcode:");
 		for (i = 0; !r_print_is_interrupted () && i < len; i++) {