@@ -604,3 +604,9 @@ class TestMiscSamplesChrome(object):
                     'f.target: ']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testApplet(self, caplog):
+        sample   = os.path.join(self.misc_path, "testApplet.html")
+        expected = ['[applet redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -569,3 +569,9 @@ class TestMiscSamplesFirefox(object):
                     'f.target: ']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testApplet(self, caplog):
+        sample   = os.path.join(self.misc_path, "testApplet.html")
+        expected = ['[applet redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -786,3 +786,9 @@ class TestMiscSamplesIE(object):
         expected = ['[VsmIDE.DTE ActiveX] CreateObject (WScript.Network)']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testApplet(self, caplog):
+        sample   = os.path.join(self.misc_path, "testApplet.html")
+        expected = ['[applet redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -754,3 +754,9 @@ class TestMiscSamplesIE(object):
         expected = ['[VsmIDE.DTE ActiveX] CreateObject (WScript.Network)']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testApplet(self, caplog):
+        sample   = os.path.join(self.misc_path, "testApplet.html")
+        expected = ['[applet redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -788,3 +788,9 @@ class TestMiscSamplesIE(object):
         expected = ['[VsmIDE.DTE ActiveX] CreateObject (WScript.Network)']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testApplet(self, caplog):
+        sample   = os.path.join(self.misc_path, "testApplet.html")
+        expected = ['[applet redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -719,3 +719,9 @@ class TestMiscSamplesIE(object):
         expected = ['[VsmIDE.DTE ActiveX] CreateObject (WScript.Network)']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testApplet(self, caplog):
+        sample   = os.path.join(self.misc_path, "testApplet.html")
+        expected = ['[applet redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -901,3 +901,9 @@ class TestMiscSamplesIE(object):
         expected = ['[VsmIDE.DTE ActiveX] CreateObject (WScript.Network)']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testApplet(self, caplog):
+        sample   = os.path.join(self.misc_path, "testApplet.html")
+        expected = ['[applet redirection]']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -585,3 +585,9 @@ class TestMiscSamplesChrome(object):
                     'f.target: ']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testApplet(self, caplog):
+        sample   = os.path.join(self.misc_path, "testApplet.html")
+        expected = ['[applet redirection]']
+
+        self.do_perform_test(caplog, sample, expected)