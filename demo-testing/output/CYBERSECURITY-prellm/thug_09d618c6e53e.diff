@@ -386,3 +386,13 @@ class TestMiscSamplesChrome(object):
                     'Final attributes length: 1']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testEntityReference(self, caplog):
+        sample   = os.path.join(self.misc_path, "testEntityReference.html")
+        expected = ['node: [object EntityReference]',
+                    'name: &',
+                    'nodeName: &',
+                    'nodeType: 5',
+                    'nodeValue: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -375,3 +375,13 @@ class TestMiscSamplesFirefox(object):
                     'Final attributes length: 1']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testEntityReference(self, caplog):
+        sample   = os.path.join(self.misc_path, "testEntityReference.html")
+        expected = ['node: [object EntityReference]',
+                    'name: &',
+                    'nodeName: &',
+                    'nodeType: 5',
+                    'nodeValue: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -467,3 +467,13 @@ class TestMiscSamplesIE(object):
                     'Final attributes length: 1']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testEntityReference(self, caplog):
+        sample   = os.path.join(self.misc_path, "testEntityReference.html")
+        expected = ['node: [object EntityReference]',
+                    'name: &',
+                    'nodeName: &',
+                    'nodeType: 5',
+                    'nodeValue: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -443,3 +443,13 @@ class TestMiscSamplesIE(object):
                     'Final attributes length: 1']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testEntityReference(self, caplog):
+        sample   = os.path.join(self.misc_path, "testEntityReference.html")
+        expected = ['node: [object EntityReference]',
+                    'name: &',
+                    'nodeName: &',
+                    'nodeType: 5',
+                    'nodeValue: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -413,3 +413,13 @@ class TestMiscSamplesIE(object):
                     'Final attributes length: 1']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testEntityReference(self, caplog):
+        sample   = os.path.join(self.misc_path, "testEntityReference.html")
+        expected = ['node: [object EntityReference]',
+                    'name: &',
+                    'nodeName: &',
+                    'nodeType: 5',
+                    'nodeValue: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -413,3 +413,13 @@ class TestMiscSamplesIE(object):
                     'Final attributes length: 1']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testEntityReference(self, caplog):
+        sample   = os.path.join(self.misc_path, "testEntityReference.html")
+        expected = ['node: [object EntityReference]',
+                    'name: &',
+                    'nodeName: &',
+                    'nodeType: 5',
+                    'nodeValue: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -472,3 +472,13 @@ class TestMiscSamplesIE(object):
                     'Final attributes length: 1']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testEntityReference(self, caplog):
+        sample   = os.path.join(self.misc_path, "testEntityReference.html")
+        expected = ['node: [object EntityReference]',
+                    'name: &',
+                    'nodeName: &',
+                    'nodeType: 5',
+                    'nodeValue: null']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -376,3 +376,13 @@ class TestMiscSamplesChrome(object):
                     'Final attributes length: 1']
 
         self.do