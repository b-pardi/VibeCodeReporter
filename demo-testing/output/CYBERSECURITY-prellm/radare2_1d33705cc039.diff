@@ -1,21 +1,20 @@
-/* radare - LGPL - Copyright 2006-2011 pancake<nopcode.org> */
+/* radare - LGPL - Copyright 2006-2019 - pancake */
 
 #include "r_config.h"
 
-// XXX spaguetti code lives here
-static int r_config_setter_q(void *data) {
+static bool r_config_setter_q(void *data) {
 	RConfigNode *node = data;
 	*(node->cb_ptr_q) = node->i_value;
 	return true;
 }
 
-static int r_config_setter_i(void *data) {
+static bool r_config_setter_i(void *data) {
 	RConfigNode *node = data;
 	*(node->cb_ptr_i) = node->i_value;
 	return true;
 }
 
-static int r_config_setter_s(void *data) {
+static bool r_config_setter_s(void *data) {
 	RConfigNode *node = data;
 	if (!node->value || !*node->value) {
 		free (*node->cb_ptr_s);
@@ -26,7 +25,7 @@ static int r_config_setter_s(void *data) {
 	return true;
 }
 
-R_API int r_config_set_setter_q(RConfig *cfg, const char *name, ut64 *ptr) {
+R_API bool r_config_set_setter_q(RConfig *cfg, const char *name, ut64 *ptr) {
 	RConfigNode *node = r_config_node_get (cfg, name);
 	if (node) {
 		node->cb_ptr_q = ptr;
@@ -36,7 +35,7 @@ R_API int r_config_set_setter_q(RConfig *cfg, const char *name, ut64 *ptr) {
 	return false;
 }
 
-R_API int r_config_set_setter_i(RConfig *cfg, const char *name, int *ptr) {
+R_API bool r_config_set_setter_i(RConfig *cfg, const char *name, int *ptr) {
 	RConfigNode *node = r_config_node_get (cfg, name);
 	if (node) {
 		node->cb_ptr_i = ptr;
@@ -46,7 +45,7 @@ R_API int r_config_set_setter_i(RConfig *cfg, const char *name, int *ptr) {
 	return false;
 }
 
-R_API int r_config_set_setter_s(RConfig *cfg, const char *name, char * *ptr) {
+R_API bool r_config_set_setter_s(RConfig *cfg, const char *name, char * *ptr) {
 	RConfigNode *node = r_config_node_get (cfg, name);
 	if (node) {
 		node->cb_ptr_s = ptr;




@@ -45,19 +45,6 @@ R_API void r_config_node_free(void *n) {
 	free (node);
 }
 
-static bool isBoolean(const char *val) {
-	if (!r_str_casecmp (val, "true") || !r_str_casecmp (val, "false")) {
-		return true;
-	}
-	if (!r_str_casecmp (val, "on") || !r_str_casecmp (val, "off")) {
-		return true;
-	}
-	if (!r_str_casecmp (val, "yes") || !r_str_casecmp (val, "no")) {
-		return true;
-	}
-	return false;
-}
-
 static void config_print_value_json(RConfig *cfg, RConfigNode *node) {
 	const char *val = node->value;
 	if (!val) {
@@ -68,7 +55,7 @@ static void config_print_value_json(RConfig *cfg, RConfigNode *node) {
 		if (!strncmp (val, "0x", 2)) {
 			ut64 n = r_num_get (NULL, val);
 			cfg->cb_printf ("%"PFMT64d, n);
-		} else if (r_str_isnumber (val) || isBoolean (val)) {
+		} else if (r_str_isnumber (val) || r_str_is_bool (val)) {
 			cfg->cb_printf ("%s", val);
 		} else {
 			cfg->cb_printf ("\"%s\"", sval);
@@ -133,11 +120,11 @@ static void config_print_node(RConfig *cfg, RConfigNode *node, const char *pfx,
 					if (isFirst) {
 						isFirst = false;
 					} else {
-						cfg->cb_printf(", ");
+						cfg->cb_printf (", ");
 					}
-					cfg->cb_printf("%s", option);
+					cfg->cb_printf ("%s", option);
 				}
-				cfg->cb_printf("]");
+				cfg->cb_printf ("]");
 			}
 			cfg->cb_printf ("\n");
 		} else {
@@ -251,7 +238,6 @@ R_API void r_config_list(RConfig *cfg, const char *str, int rad) {
 		verbose = true;
 	/* fallthrou */
 	case 'j':
-		json = true;
 		isFirst = true;
 		if (verbose) {
 			cfg->cb_printf ("[");
@@ -266,7 +252,7 @@ R_API void r_config_list(RConfig *cfg, const char *str, int rad) {
 					} else {
 						cfg->cb_printf (",");
 					}
-					config_print_node (cfg, node, pfx, sfx, verbose, json);
+					config_print_node (cfg, node, pfx, sfx, verbose, true);
 				}
 			}
 		}
@@ -280,42 +266,35 @@ R_API void r_config_list(RConfig *cfg, const char *str, int rad) {
 }
 
 R_API RConfigNode* r_config_node_get(RConfig *cfg, const char *name) {
-	if (!cfg || IS_NULLSTR (name)) {
-		return NULL;
-	}
+	r_return_val_if_fail (cfg && name && *name, NULL);
 	return ht_pp_find (cfg->ht, name, NULL);
 }
 
-R_API int r_config_set_getter(RConfig *cfg, const char *key, 