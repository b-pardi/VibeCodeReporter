@@ -1687,3 +1687,8 @@ R_API const char *r_bin_get_meth_flag_string(ut64 flag, bool compact) {
 		return NULL;
 	}
 }
+
+R_API void r_bin_section_free(RBinSection *bs) {
+	free (bs->format);
+	free (bs);
+}




@@ -119,7 +119,7 @@ static RList* sections(RBinFile *bf) {
 	RBinObject *obj = bf ? bf->o : NULL;
 	int i;
 
-	if (!obj || !obj->bin_obj || !(ret = r_list_newf ((RListFree)free))) {
+	if (!obj || !obj->bin_obj || !(ret = r_list_newf ((RListFree)r_bin_section_free))) {
 		return NULL;
 	}
 	if (!(sections = MACH0_(get_sections) (obj->bin_obj))) {




@@ -675,6 +675,9 @@ R_API RBinFile *r_bin_file_xtr_load_bytes(RBin *bin, RBinXtrPlugin *xtr, const c
 R_API bool r_bin_file_set_bytes(RBinFile *binfile, const ut8 *bytes, ut64 sz, bool steal_ptr);
 R_API RBinPlugin *r_bin_file_cur_plugin(RBinFile *binfile);
 
+/* section */
+R_API void r_bin_section_free(RBinSection *bs);
+
 /* obj.c */
 R_API void r_bin_object_free(void /*RBinObject*/ *o_);
 R_API ut64 r_bin_object_get_baddr(RBinObject *o);