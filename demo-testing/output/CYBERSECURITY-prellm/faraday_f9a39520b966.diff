@@ -844,28 +844,42 @@ class TestListVulnerabilityView(ReadWriteAPITests):
         assert set(vuln['_id'] for vuln in res.json['data']) == expected_ids
 
     @pytest.mark.usefixtures('ignore_nplusone')
-    def test_filter_restless_by_target__(self, test_client, session, workspace, host_factory, vulnerability_factory):
+    @pytest.mark.parametrize('filter_params', [
+        {
+            'test_name': 'filter_by_target',
+            'filter_field_name': 'target',
+            'filter_operation': 'eq',
+            'filter_value': '"192.168.0.1"',
+            'res_status_code': 200,
+            'count': 10
+        },
+        {
+            'test_name': 'filter_by_target_host_ip',  # Habria que sacar esto ya que esta por target :|
+            'filter_field_name': 'target',
+            'filter_operation': 'eq',
+            'filter_value': '"192.168.0.1"',
+            'res_status_code': 200,
+            'count': 10
+        },
+        {
+            'test_name': 'test_filter_restless_by_service_port',  # Habria que sacar esto ya que esta por target :|
+            'filter_field_name': 'service',
+            'filter_operation': 'has',
+            'filter_value': '{"name": "port", "op": "eq", "val": "8956"}',
+            'res_status_code': 200,
+            'count': 10
+        },
+        {
+            'test_name': 'test_filter_restless_by_service_name',  # Habria que sacar esto ya que esta por target :|
+            'filter_field_name': 'service',
+            'filter_operation': 'has',
+            'filter_value': '{"name": "name", "op": "eq", "val": "ssh"}',
+            'res_status_code': 200,
+            'count': 1
+        },
 
-        Vulnerability.query.delete()
-        host = host_factory.create(workspace=workspace, ip="192.168.0.2")
-        host_vulns = vulnerability_factory.create_batch(
-            1, workspace=self.workspace, host=host, service=None)
-
-        host2 = host_factory.create(workspace=workspace, ip="192.168.0.1")
-        host_vulns2 = vulnerability_factory.create_batch(
-            10, workspace=self.workspace, host=host2, service=None)
-
-        session.commit()
-        res = test_client.get(urljoin(
-            self.url(), 'filter?q={"filters":[{"name": "target", "op":"eq", "val":"192.168.0.1"}]}'
-        ))
-
-        assert res.status_code == 200
-        assert len(res.json['vulnerabilities']) == 10
-
-    @pytest.mark.usefixtures('ignore_nplusone')
-    def test_filter_restless_by_target_host_ip(self, test_client, session, workspace,
-                                               host_factory, vulnerability_factory):
+    ])
+    def test_filter_restless(self, test_client, session, workspace, host_factory, vulnerability_web_factory, vulnerability_factory, service_factory, filter_params):
 
         Vulnerability.query.delete()
         host = host_factory.create(workspace=workspace, ip="192.168.0.2")
@@ -876,20 +890,6 @@ class TestListVulnerabilityView(ReadWriteAPITests):
         host_vulns2 = vulnerability_factory.create_batch(
             10, workspace=self.workspace, host=host2, service=None)
 
-        session.commit()
-        res = test_client.get(urljoin(
-            self.url(),
-            'filter?q={"filters":[{"name": "target_host_ip", "op":"eq", "val":"192.168.0.2"}]}'
-        ))
-        assert res.status_code == 200
-        assert len(res.json['vulnerabilities']) == 1
-        assert res.json['vulnerabilities'][0]['value']['target'] == '192.168.0.2'
-
-    @pytest.mark.usefixtures('ignore_nplusone')
-    def test_filter_restless_by_service_port(self, test_client, session, workspace,
-                                             host_factory, vulnerability_factory,
-                                             vulnerability_web_factory, service_factory):
-
         service = service_factory.create(port=9098, name="ssh", workspace=self.workspace)
         vulns = vulnerability_factory.create_batch(
             1, workspace=self.workspace, service=servi