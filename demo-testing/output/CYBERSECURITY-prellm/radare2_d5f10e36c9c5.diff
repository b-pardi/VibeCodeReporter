@@ -81,8 +81,8 @@ static int dalvik_op(RAnal *anal, RAnalOp *op, ut64 addr, const ut8 *data, int l
 	case 0x17: // const
 	case 0x42: // const
 	case 0x12: // const/4
-		op->type = R_ANAL_OP_TYPE_MOV;
 		{
+			op->type = R_ANAL_OP_TYPE_MOV;
 			ut32 vB = (data[1] & 0x0f);
 			ut32 vA = (data[1] & 0xf0) >> 4;
 			ut32 vC = (len > 4)? r_read_le32 (data + 2): 0x22;
@@ -92,7 +92,9 @@ static int dalvik_op(RAnal *anal, RAnalOp *op, ut64 addr, const ut8 *data, int l
 			op->val = val;
 	//		op->reg = vB;
 			op->nopcode = 2;
-			esilprintf (op, "0x%"PFMT64x",v%d,=", val, vB);
+			if (mask & R_ANAL_OP_MASK_ESIL) {
+				esilprintf (op, "0x%"PFMT64x",v%d,=", val, vB);
+			}
 		}
 		break;
 	case 0x01: // move
@@ -103,12 +105,16 @@ static int dalvik_op(RAnal *anal, RAnalOp *op, ut64 addr, const ut8 *data, int l
 			ut32 vA = (data[1] & 0xf0) >> 4;
 			if (vA == vB) {
 				op->type = R_ANAL_OP_TYPE_NOP;
-				esilprintf (op, ",");
+				if (mask & R_ANAL_OP_MASK_ESIL) {
+					esilprintf (op, ",");
+				}
 			} else {
 				op->type = R_ANAL_OP_TYPE_MOV;
 				op->stackop = R_ANAL_STACK_SET;
 				op->ptr = -vA;
-				esilprintf (op, "v%d,v%d,=", vA, vB);
+				if (mask & R_ANAL_OP_MASK_ESIL) {
+					esilprintf (op, "v%d,v%d,=", vA, vB);
+				}
 			}
 		}
 		break;
@@ -123,7 +129,9 @@ static int dalvik_op(RAnal *anal, RAnalOp *op, ut64 addr, const ut8 *data, int l
 		if (len > 2) {
 			int vA = (int) data[1];
 			ut32 vB = (data[3] << 8) | data[2];
-			esilprintf (op, "v%d,v%d,=", vA, vB);
+			if (mask & R_ANAL_OP_MASK_ESIL) {
+				esilprintf (op, "v%d,v%d,=", vA, vB);
+			}
 			op->val = vB;
 		}
 		break;
@@ -138,7 +146,7 @@ static int dalvik_op(RAnal *anal, RAnalOp *op, ut64 addr, const ut8 *data, int l
 	case 0x0b: // move-result-wide
 	 	// TODO: add MOVRET OP TYPE ??
 		op->type = R_ANAL_OP_TYPE_MOV;
-		{
+		if (mask & R_ANAL_OP_MASK_ESIL) {
 			ut32 vA = data[1];
 			esilprintf (op, "sp,v%d,=[8],8,sp,+=,8", vA);
 		}
@@ -151,8 +159,10 @@ static int dalvik_op(RAnal *anal, RAnalOp *op, ut64 addr, const ut8 *data, int l
 			ut32 vB = (data[3]<<8) | data[2];
 			ut64 offset = R_ANAL_GET_OFFSET (anal, 's', vB);
 			op->ptr = offset;
-			// op->refptr = 0;
-			esilprintf (op, "0x%"PFMT64x",v%d,=", offset, vA);
+			if (mask & R_ANAL_OP_MASK_ESIL) {
+				// op->refptr = 0;
+				esilprintf (op, "0x%"PFMT64x",v%d,=", offset, vA);
+			}
 		}
 		break;
 	case 0x1c: // const-class
@@ -175,33 +185,33 @@ static int dalvik_op(RAnal *anal, RAnalOp *op, ut64 addr, const ut8 *data, int l
 	case 0x8d: // int-to-byte
 	case 0x8e: // int-to-char
 		op->type = R_ANAL_OP_TYPE_CAST;
-		{
-		ut32 vA = (data[1] & 0x0f);
-		ut32 vB = (data[1] & 0xf0) >> 4;
-		esilprintf (op, "v%d,0xff,&,v%d,=", vB, vA);
+		if (mask & R_ANAL_OP_MASK_ESIL) {
+			ut32 vA = (data[1] & 0x0f);
+			ut32 vB = (data[1] & 0xf0) >> 4;
+			esilprintf (op, "v%d,0xff,&,v%d,=", vB, vA);
 		}
 		break;
 	case 0x8f: // int-to-short
 		op->type = R_ANAL_OP_TYPE_CAST;
 		// op->datatype = R_ANAL_DATATYPE_INT32 | R_ANAL_DATATYPE_INT16;
-		{
-		ut32 vA = (data[1] & 0x0f);
-		ut32 vB = (data[1] & 0xf0) >> 4;
-		esilprintf (op, "v%d,0xffff,&,v%d,=", vB, vA);
+		if (mask & R_ANAL_OP_MASK_ESIL) {
+			ut32 vA = (data[1] & 0x0f);
+			ut32 vB = (data[1] & 0xf0) >> 4;
+			esilprintf (op, "v%d,0xffff,&,v%d,=", vB, vA);
 		}
 		break;
 	case 0x84: // long-to-int
 		op->type = R_ANAL_OP_TYPE_CAST;
-		{
-		ut32 vA = (data[1] & 0x0f);
-		ut32 vB = (data[1] & 0xf0) >> 4;
-		esilprintf (op, "v%d,0xffffffff,&,v%d,=", vB, vA);
+		if (mask & R_ANAL_OP_MASK_ESIL) {
+			ut32 vA = (data[1] & 0x0f);
+			ut32 vB = (data[1] & 0xf0) >> 4;
+			esilprintf (op, "v%d,0xffffffff,&,v%d,=", vB, vA);
 		}
 		break;
 	case 0x20: // instance-of
-		{
 		op->type = R_ANAL_OP_TYPE_CMP;
-		esilprintf (op, "%d,instanceof,%d,-,!,v%d,=", vC, vB, vA);
+		if (mask & R_ANAL_OP_MASK_ESIL) {
+			esilprintf (op, "%d,instanceof,%d,-,!,v%d,=", vC, vB, vA);
 		}
 		break;
 	case 0x21: // array-length
@@ -241,34 +251,34 @@ static int dalvik_op(RAnal *anal