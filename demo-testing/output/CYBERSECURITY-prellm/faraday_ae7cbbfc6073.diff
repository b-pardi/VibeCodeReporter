@@ -853,6 +853,52 @@ class TestListVulnerabilityView(ReadOnlyAPITests):  # TODO migration: use read w
         assert len(res.json['vulnerabilities']) == 1
         assert res.json['vulnerabilities'][0]['value']['target'] == '192.168.0.2'
 
+
+    @pytest.mark.usefixtures('ignore_nplusone')
+    def test_filter_restless_by_service_port(self, test_client, session, workspace,
+                                        host_factory, vulnerability_factory,
+                                        vulnerability_web_factory, service_factory):
+
+        service = service_factory.create(port=9098, name="ssh", workspace=self.workspace)
+        vulns = vulnerability_factory.create_batch(
+            1, workspace=self.workspace, service=service, host=None)
+
+
+        service = service_factory.create(port=8956, name="443", workspace=self.workspace)
+
+        vulns_web = vulnerability_web_factory.create_batch(
+            10, workspace=self.workspace, host=None, service=service)
+
+        session.commit()
+        res = test_client.get(f'{self.url()}'
+                              f'filter?q={{"filters":[{{"name": "service", "op":"has", "val":{{"name": "port", "op":"eq", "val":"8956"}} }}]}}')
+        assert res.status_code == 200
+        assert len(res.json['vulnerabilities']) == 10
+        assert res.json['count'] == 10
+
+
+    @pytest.mark.usefixtures('ignore_nplusone')
+    def test_filter_restless_by_service_name(self, test_client, session, workspace,
+                                        host_factory, vulnerability_factory,
+                                        vulnerability_web_factory, service_factory):
+
+        service = service_factory.create(port=9098, name="ssh", workspace=self.workspace)
+        vulns = vulnerability_factory.create_batch(
+            1, workspace=self.workspace, service=service, host=None)
+
+
+        service = service_factory.create(port=8956, name="443", workspace=self.workspace)
+
+        vulns_web = vulnerability_web_factory.create_batch(
+            10, workspace=self.workspace, host=None, service=service)
+
+        session.commit()
+        res = test_client.get(f'{self.url()}'
+                              f'filter?q={{"filters":[{{"name": "service", "op":"has", "val":{{"name": "name", "op":"eq", "val":"ssh"}} }}]}}')
+        assert res.status_code == 200
+        assert len(res.json['vulnerabilities']) == 1
+        assert res.json['count'] == 1
+
     @pytest.mark.usefixtures('mock_envelope_list')
     def test_sort_by_method(self, session, test_client, second_workspace,
                             vulnerability_factory, vulnerability_web_factory):