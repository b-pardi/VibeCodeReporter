@@ -142,13 +142,6 @@ def make_parser(argv):
         help="minimum string length",
     )
 
-    parser.add_argument(
-        "-L",
-        "--large-file",
-        action="store_true",
-        help="allow processing files larger than {} MB".format(int(MAX_FILE_SIZE / MEGABYTE)),
-    )
-
     parser.add_argument(
         "sample",
         type=argparse.FileType("rb"),
@@ -218,6 +211,14 @@ def make_parser(argv):
         if show_all_options
         else argparse.SUPPRESS,
     )
+    advanced_group.add_argument(
+        "-L",
+        "--large-file",
+        action="store_true",
+        help="allow processing files larger than {} MB".format(int(MAX_FILE_SIZE / MEGABYTE))
+        if show_all_options
+        else argparse.SUPPRESS,
+    )
     advanced_group.add_argument(
         "--version",
         action="version",