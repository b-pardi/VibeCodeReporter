@@ -25,25 +25,36 @@ from pkg_resources import DistributionNotFound, VersionConflict
 from lib.core.data import options
 from lib.core.exceptions import FailedDependenciesInstallation
 from lib.core.installation import check_dependencies, install_dependencies
+from lib.core.settings import OPTIONS_FILE
+from lib.parse.config import ConfigParser
 
 if sys.version_info < (3, 7):
     sys.stdout.write("Sorry, dirsearch requires Python 3.7 or higher\n")
     sys.exit(1)
 
-try:
-    check_dependencies()
-except (DistributionNotFound, VersionConflict):
-    option = input("Missing required dependencies to run.\n"
-                   "Do you want dirsearch to automatically install them? [Y/n] ")
+config = ConfigParser()
+config.read(OPTIONS_FILE)
 
-    if option.lower() == 'y':
-        print("Installing required dependencies...")
+if config.safe_getboolean("options", "check-dependencies", False):
+    try:
+        check_dependencies()
+    except (DistributionNotFound, VersionConflict):
+        option = input("Missing required dependencies to run.\n"
+                       "Do you want dirsearch to automatically install them? [Y/n] ")
 
-        try:
-            install_dependencies()
-        except FailedDependenciesInstallation:
-            print("Failed to install dirsearch dependencies, try doing it manually.")
-            exit(1)
+        if option.lower() == 'y':
+            print("Installing required dependencies...")
+
+            try:
+                install_dependencies()
+            except FailedDependenciesInstallation:
+                print("Failed to install dirsearch dependencies, try doing it manually.")
+                exit(1)
+        else:
+            config.set("options", "check-dependencies", "False")
+
+            with open(OPTIONS_FILE, "w") as fh:
+                config.write(fh)
 
 
 def main():




@@ -32,6 +32,8 @@ BANNER = f"""
 
 SCRIPT_PATH = FileUtils.parent(__file__, 3)
 
+OPTIONS_FILE = "options.ini"
+
 IS_WINDOWS = sys.platform in ("win32", "msys")
 
 DEFAULT_ENCODING = "utf-8"




@@ -20,12 +20,9 @@ import configparser
 
 
 class ConfigParser(configparser.ConfigParser):
-    def __init__(self):
-        configparser.ConfigParser.__init__(self)
-
     def safe_get(self, section, option, default=None, allowed=None):
         try:
-            result = configparser.ConfigParser.get(self, section, option)
+            result = super().get(section, option)
 
             if allowed is not None:
                 return result if result in allowed else default
@@ -36,7 +33,7 @@ class ConfigParser(configparser.ConfigParser):
 
     def safe_getfloat(self, section, option, default=0, allowed=None):
         try:
-            result = configparser.ConfigParser.getfloat(self, section, option)
+            result = super().getfloat(section, option)
 
             if allowed is not None:
                 return result if result in allowed else default
@@ -47,7 +44,7 @@ class ConfigParser(configparser.ConfigParser):
 
     def safe_getboolean(self, section, option, default=False, allowed=None):
         try:
-            result = configparser.ConfigParser.getboolean(self, section, option)
+            result = super().getboolean(section, option)
 
             if allowed is not None:
                 return result if result in allowed else default
@@ -58,7 +55,7 @@ class ConfigParser(configparser.ConfigParser):
 
     def safe_getint(self, section, option, default=0, allowed=None):
         try:
-            result = configparser.ConfigParser.getint(self, section, option)
+            result = super().getint(section, option)
 
             if allowed is not None:
                 return result if result in allowed else default