@@ -496,3 +496,9 @@ class TestMiscSamplesChrome(object):
                     'confirm: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testObject1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testObject1.html")
+        expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -475,3 +475,9 @@ class TestMiscSamplesFirefox(object):
                     'confirm: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testObject1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testObject1.html")
+        expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -568,3 +568,9 @@ class TestMiscSamplesIE(object):
                     'confirm: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testObject1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testObject1.html")
+        expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -537,3 +537,9 @@ class TestMiscSamplesIE(object):
                     'confirm: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testObject1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testObject1.html")
+        expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -571,3 +571,9 @@ class TestMiscSamplesIE(object):
                     'confirm: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testObject1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testObject1.html")
+        expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -502,3 +502,9 @@ class TestMiscSamplesIE(object):
                     'confirm: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testObject1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testObject1.html")
+        expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -649,3 +649,9 @@ class TestMiscSamplesIE(object):
                     'confirm: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testObject1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testObject1.html")
+        expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -477,3 +477,9 @@ class TestMiscSamplesChrome(object):
                     'confirm: true']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testObject1(self, caplog):
+        sample   = os.path.join(self.misc_path, "testObject1.html")
+        expected = ['[object data redirection] about:blank -> https://github.com/buffer/thug/raw/master/tests/test_files/sample.swf']
+
+        self.do_perform_test(caplog, sample, expected)