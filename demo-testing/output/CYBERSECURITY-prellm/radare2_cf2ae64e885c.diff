@@ -1,4 +1,4 @@
-/* radare2 - LGPL - Copyright 2008-2021 - pancake, Jody Frankowski */
+/* radare2 - LGPL - Copyright 2008-2022 - pancake, Jody Frankowski */
 
 #include <r_cons.h>
 #include <r_util.h>
@@ -13,10 +13,20 @@
 
 R_LIB_VERSION (r_cons);
 
-static RConsContext r_cons_context_default = {{{{0}}}};
-static RCons r_cons_instance = {0};
-#define I r_cons_instance
-#define C r_cons_instance.context
+static R_TH_LOCAL RConsContext r_cons_context_default = {{{{0}}}};
+static R_TH_LOCAL RCons g_cons_instance = {0};
+static R_TH_LOCAL RCons *r_cons_instance = NULL;
+static R_TH_LOCAL RThreadLock *r_cons_lock = NULL;
+#define I (r_cons_instance)
+#define C (getctx())
+
+static RConsContext *getctx(void) {
+	if (!r_cons_instance) {
+		r_cons_instance = &g_cons_instance;
+		r_cons_instance->context = &r_cons_context_default;
+	}
+	return r_cons_instance->context;
+}
 
 //this structure goes into cons_stack when r_cons_push/pop
 typedef struct {
@@ -42,7 +52,6 @@ static void cons_stack_free(void *ptr) {
 	free (s->buf);
 	if (s->grep) {
 		R_FREE (s->grep->str);
-		C->grep.str = NULL;
 	}
 	free (s->grep);
 	free (s);
@@ -80,6 +89,7 @@ static RConsStack *cons_stack_dump(bool recreate) {
 static void cons_stack_load(RConsStack *data, bool free_current) {
 	r_return_if_fail (data);
 	if (free_current) {
+		// double free
 		free (C->buffer);
 	}
 	C->buffer = data->buf;
@@ -101,6 +111,7 @@ static void cons_grep_reset(RConsGrep *grep) {
 }
 
 static void cons_context_init(RConsContext *context, R_NULLABLE RConsContext *parent) {
+	r_cons_lock = r_th_lock_new (false);
 	context->breaked = false;
 	context->cmd_depth = R_CONS_CMD_DEPTH + 1;
 	context->error = r_strbuf_new ("");
@@ -145,20 +156,20 @@ static void __break_signal(int sig) {
 
 static inline void __cons_write_ll(const char *buf, int len) {
 #if __WINDOWS__
-	if (I.vtmode) {
-		(void) write (I.fdout, buf, len);
+	if (I->vtmode) {
+		(void) write (I->fdout, buf, len);
 	} else {
-		if (I.fdout == 1) {
+		if (I->fdout == 1) {
 			r_cons_w32_print (buf, len, false);
 		} else {
-			(void) write (I.fdout, buf, len);
+			(void) write (I->fdout, buf, len);
 		}
 	}
 #else
-	if (I.fdout < 1) {
-		I.fdout = 1;
+	if (I->fdout < 1) {
+		I->fdout = 1;
 	}
-	R_IGNORE_RETURN (write (I.fdout, buf, len));
+	R_IGNORE_RETURN (write (I->fdout, buf, len));
 #endif
 }
 
@@ -311,7 +322,7 @@ R_API RConsContext *r_cons_context(void) {
 }
 
 R_API RCons *r_cons_singleton(void) {
-	return &I;
+	return I;
 }
 
 R_API void r_cons_break_clear(void) {
@@ -393,14 +404,14 @@ R_API bool r_cons_was_breaked(void) {
 }
 
 R_API bool r_cons_is_breaked(void) {
-	if (I.cb_break) {
-		I.cb_break (I.user);
+	if (I->cb_break) {
+		I->cb_break (I->user);
 	}
-	if (I.timeout) {
-		if (r_time_now_mono () > I.timeout) {
+	if (I->timeout) {
+		if (r_time_now_mono () > I->timeout) {
 			C->breaked = true;
 			eprintf ("\nTimeout!\n");
-			I.timeout = 0;
+			I->timeout = 0;
 		}
 	}
 	if (!C->was_breaked) {
@@ -453,13 +464,13 @@ R_API int r_cons_get_cur_line(void) {
 }
 
 R_API void r_cons_break_timeout(int timeout) {
-	I.timeout = (timeout && !I.timeout)
+	I->timeout = (timeout && !I->timeout)
 		? r_time_now_mono () + ((ut64) timeout << 20) : 0;
 }
 
 R_API void r_cons_break_end(void) {
 	C->breaked = false;
-	I.timeout = 0;
+	I->timeout = 0;
 #if __UNIX__ && !__wasi__
 	r_sys_signal (SIGINT, SIG_IGN);
 #endif
@@ -474,15 +485,15 @@ R_API void r_cons_break_end(void) {
 }
 
 R_API void *r_cons_sleep_begin(void) {
-	if (!I.cb_sleep_begin) {
+	if (!I->cb_sleep_begin) {
 		return NULL;
 	}
-	return I.cb_sleep_begin (I.user);
+	return I->cb_sleep_begin (I->user);
 }
 
 R_API void r_cons_sleep_end(void *user) {
-	if (I.cb_sleep_end) {
-		I.cb_sleep_end (I.user, user);
+	if (I->cb_sleep_end) {
+		I->cb_sleep_end (I->user, user);
 	}
 }
 
@@ -503,40 +514,40 @@ static void resize(int sig) {
 }
 #endif
 void resizeWin(void) {
-	if (I.event_resize) {
-		I.event_resize (I.event_data);
+	if (I->event_resize) {
+		I->event_resize (I-