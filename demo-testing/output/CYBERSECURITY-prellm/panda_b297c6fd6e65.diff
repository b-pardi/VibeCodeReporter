@@ -35,7 +35,7 @@ PANDAENDCOMMENT */
 #include "panda/plugin_plugin.h"
 #include "panda/tcg-llvm.h"
 
-#include "fast_shad.h"
+#include "shad.h"
 #include "llvm_taint_lib.h"
 #include "taint_ops.h"
 #include "taint2.h"
@@ -97,7 +97,8 @@ static inline Constant *const_struct_ptr(LLVMContext &C, llvm::Type *ptrT, void
     return ConstantExpr::getIntToPtr(const_uint64_ptr(C, ptr), ptrT);
 }
 
-static void taint_branch_run(FastShad *shad, uint64_t src, uint64_t size) {
+static void taint_branch_run(Shad *shad, uint64_t src, uint64_t size)
+{
     // this arg should be the register number
     Addr a = make_laddr(src / MAXREGSIZE, 0);
     PPP_RUN_CB(on_branch2, a, size);
@@ -114,7 +115,8 @@ void taint_pointer_run(uint64_t src, uint64_t ptr, uint64_t dest, bool is_store,
     }
 }
 
-static void taint_copyRegToPc_run(FastShad *shad, uint64_t src, uint64_t size) {
+static void taint_copyRegToPc_run(Shad *shad, uint64_t src, uint64_t size)
+{
     // this arg should be the register number
     Addr a = make_laddr(src / MAXREGSIZE, 0);
     PPP_RUN_CB(on_indirect_jump, a, size);
@@ -173,7 +175,7 @@ bool PandaTaintFunctionPass::doInitialization(Module &M) {
     PTV.resetFrameF = M.getFunction("taint_reset_frame");
     PTV.breadcrumbF = M.getFunction("taint_breadcrumb");
 
-    llvm::Type *shadT = M.getTypeByName("class.FastShad");
+    llvm::Type *shadT = M.getTypeByName("class.Shad");
     assert(shadT);
     llvm::Type *shadP = PointerType::getUnqual(shadT);
 

similarity index 87%
rename from panda/plugins/taint2/fast_shad.cpp
rename to panda/plugins/taint2/shad.cpp



@@ -25,14 +25,18 @@ PANDAENDCOMMENT */
 #include <sys/mman.h>
 
 #include "taint_defines.h"
-#include "fast_shad.h"
+#include "shad.h"
 
 #include <set>
 #include <string>
 
+Shad::~Shad() = default;
+
 typedef const std::set<uint32_t> *LabelSetP;
 
-FastShad::FastShad(std::string name, uint64_t labelsets) : _name(name) {
+FastShad::FastShad(std::string name, uint64_t labelsets)
+{
+    this->_name = name;
     uint64_t bytes = sizeof(TaintData) * labelsets;
 
     TaintData *array;
@@ -64,3 +68,12 @@ FastShad::~FastShad() {
         munmap(orig_labels, sizeof(TaintData) * size);
     }
 }
+
+LazyShad::LazyShad(std::string name, uint64_t max_size)
+{
+    assert(max_size > 0);
+}
+
+LazyShad::~LazyShad()
+{
+}

similarity index 54%
rename from panda/plugins/taint2/fast_shad.h
rename to panda/plugins/taint2/shad.h



@@ -15,9 +15,12 @@ PANDAENDCOMMENT */
 #ifndef __FAST_SHAD_H
 #define __FAST_SHAD_H
 
+#include <algorithm>
 #include <cassert>
 #include <cstdint>
+#include <cstring>
 #include <string>
+#include <map>
 
 #ifdef TAINT2_DEBUG
 #include "qemu/osdep.h"
@@ -27,11 +30,11 @@ PANDAENDCOMMENT */
 #include "taint_defines.h"
 #include "label_set.h"
 
-class FastShad;
+class Shad;
 
 extern "C" {
 extern bool track_taint_state;
-extern void taint_state_changed(FastShad *fast_shad, uint64_t addr, uint64_t size);
+extern void taint_state_changed(Shad *shad, uint64_t addr, uint64_t size);
 }
 
 #define CPU_LOG_TAINT_OPS (1 << 28)
@@ -107,38 +110,28 @@ struct TaintData {
     }
 };
 
-class FastShad {
-private:
-    TaintData *labels;
-    TaintData *orig_labels;
+class Shad
+{
+  protected:
     uint64_t size; // Number of labelsets contained.
     std::string _name;
 
-    inline TaintData *get_td_p(uint64_t guest_addr) {
-        tassert(guest_addr < size);
-        return &labels[guest_addr];
-    }
+    // Determines if any of the memory locations in the range [addr ..
+    // addr+size-1] are tainted.
+    virtual bool range_tainted(uint64_t addr, uint64_t size) = 0;
 
-    inline bool range_tainted(uint64_t addr, uint64_t size) {
-        for (unsigned i = addr; i < addr+size; i++) {
-            if (get_td_p(i)->ls) return true;
-        }
-        return false;
-    }
-
-public:
-    FastShad(std::string name, uint64_t size);
-    ~FastShad();
+  public:
+    virtual ~Shad() = 0;
 
-    uint64_t get_size() { return size; }
+    uint64_t get_size()
+    {
+   