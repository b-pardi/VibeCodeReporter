@@ -648,3 +648,9 @@ class TestMiscSamplesChrome(object):
                     'onerror handler fired']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTitle(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTitle.html")
+        expected = ['New title: Foobar']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -613,3 +613,9 @@ class TestMiscSamplesFirefox(object):
                     'onerror handler fired']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTitle(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTitle.html")
+        expected = ['New title: Foobar']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -830,3 +830,9 @@ class TestMiscSamplesIE(object):
                     'onerror handler fired']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTitle(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTitle.html")
+        expected = ['New title: Foobar']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -798,3 +798,9 @@ class TestMiscSamplesIE(object):
                     'onerror handler fired']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTitle(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTitle.html")
+        expected = ['New title: Foobar']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -832,3 +832,9 @@ class TestMiscSamplesIE(object):
                     'onerror handler fired']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTitle(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTitle.html")
+        expected = ['New title: Foobar']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -763,3 +763,9 @@ class TestMiscSamplesIE(object):
                     'onerror handler fired']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTitle(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTitle.html")
+        expected = ['New title: Foobar']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -971,3 +971,9 @@ class TestMiscSamplesIE(object):
                     'onerror handler fired']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTitle(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTitle.html")
+        expected = ['New title: Foobar']
+
+        self.do_perform_test(caplog, sample, expected)




@@ -629,3 +629,9 @@ class TestMiscSamplesChrome(object):
                     'onerror handler fired']
 
         self.do_perform_test(caplog, sample, expected)
+
+    def test_testTitle(self, caplog):
+        sample   = os.path.join(self.misc_path, "testTitle.html")
+        expected = ['New title: Foobar']
+
+        self.do_perform_test(caplog, sample, expected)