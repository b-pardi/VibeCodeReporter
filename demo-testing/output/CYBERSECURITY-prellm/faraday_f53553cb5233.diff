@@ -354,7 +354,6 @@ def _create_vuln(ws, vuln_data, command=None, **kwargs):
 
     vuln_data.pop('_attachments', {})
     references = vuln_data.pop('references', [])
-
     cves = vuln_data.pop('cves', [])
 
     policyviolations = vuln_data.pop('policy_violations', [])
@@ -401,12 +400,6 @@ def _create_vuln(ws, vuln_data, command=None, **kwargs):
             # Updates Custom Fields
             vuln.custom_fields = vuln_data.pop('custom_fields', {})
             db.session.commit()
-        try:
-            vuln.cves = cves
-        except ValueError:
-            flask.abort(400)
-
-        db.session.commit()
 
     if command is not None:
         _create_command_object_for(ws, created, vuln, command)




@@ -175,7 +175,7 @@ def test_create_host_vuln(session, host):
     assert not vuln.impact_availability
     assert not vuln.impact_confidentiality
     assert set(vuln.references) == set(vuln_data['refs'])
-    assert set(vuln.cve) == set(vuln_data['refs'])
+    assert set(vuln.cves) == set(vuln_data['cve'])
     assert vuln.tool == "some_tool"
 
 
@@ -193,7 +193,7 @@ def test_create_service_vuln(session, service):
     assert not vuln.impact_availability
     assert not vuln.impact_confidentiality
     assert set(vuln.references) == set(vuln_data['refs'])
-    assert set(vuln.cve) == set(vuln_data['refs'])
+    assert set(vuln.cves) == set(vuln_data['cve'])
     assert vuln.tool == "some_tool"
 
 
@@ -275,32 +275,6 @@ def test_create_existing_host_vuln(session, host, vulnerability_factory):
     assert 'old' in vuln.references  # it must preserve the old references
 
 
-def test_create_existing_host_vuln_with_cve(session, host, vulnerability_factory):
-    vuln = vulnerability_factory.create(
-        workspace=host.workspace, host=host, service=None)
-    session.add(vuln)
-    session.commit()
-    vuln.references = ['old']
-    vuln.cve = ["CVE-2021-0001"]
-    session.add(vuln)
-    session.commit()
-    data = {
-        'name': vuln.name,
-        'desc': vuln.description,
-        'severity': vuln.severity,
-        'type': 'Vulnerability',
-        'refs': ['new'],
-        'cve': ['CVE-2021-0002']
-    }
-    data = bc.VulnerabilitySchema().load(data)
-    bc._create_hostvuln(host.workspace, host, data)
-    session.commit()
-    assert count(Vulnerability, host.workspace) == 1
-    vuln = Vulnerability.query.get(vuln.id)  # just in case it isn't refreshed
-    assert 'old' in vuln.references  # it must preserve the old references
-    assert 'CVE-2021-0002' in vuln.cve
-
-
 @pytest.mark.skip(reason="unique constraing on credential isn't working")
 def test_create_existing_host_cred(session, host, credential_factory):
     cred = credential_factory.create(




@@ -1020,6 +1020,26 @@ class TestListVulnerabilityView(ReadWriteAPITests):
         assert res.status_code == 201
         assert session.query(CVE).count() == 2
 
+    def test_patch_vuln_with_cves(self, host_with_hostnames, test_client, session):
+        session.commit()  # flush host_with_hostnames
+        raw_data = _create_post_data_vulnerability(
+            name='New vulns',
+            vuln_type='Vulnerability',
+            parent_id=host_with_hostnames.id,
+            parent_type='Host',
+            refs=[],
+            cves=['CVE-2017-0002', 'CVE-2017-0012', 'CVE-2017-0012'],
+            policyviolations=[]
+        )
+        ws = host_with_hostnames.workspace
+        res = test_client.post(self.url(workspace=ws), data=raw_data)
+        assert res.status_code == 201
+
+        new_cves = ['CVE-2017-0001']
+        res = test_client.patch(f'{self.url(res.json["_id"], workspace=ws)}', data={'cve': new_cves})
+        assert res.status_code == 200
+        assert set(res.json['cve']) == set(new_cves)
+
     def test_create_vuln_and_get_cves(self, host_with_hostnames, test_client, session):
         session.commit()  # flush host_with_hostnames
         raw_data = _create_post_data_vulnerability(